---
phase: 03-room-sessions-queue-control
plan: 02
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - apps/api/src/modules/rooms/repositories/pairing-token-repository.ts
  - apps/api/src/modules/rooms/pairing-token-service.ts
  - apps/api/src/modules/controller/repositories/control-session-repository.ts
  - apps/api/src/modules/controller/control-session-service.ts
  - apps/api/src/modules/rooms/build-control-snapshot.ts
  - apps/api/src/routes/control-sessions.ts
  - apps/api/src/routes/room-snapshots.ts
  - apps/api/src/modules/player/register-player.ts
  - apps/api/src/routes/admin-rooms.ts
  - apps/api/src/server.ts
  - apps/api/src/test/control-sessions.test.ts
autonomous: true
requirements:
  - PAIR-01
  - PAIR-02
  - PAIR-03
  - PAIR-04
  - ADMN-03
must_haves:
  truths:
    - "TV snapshots expose one stable persistent living-room token with a 15-minute expiry."
    - "Mobile controllers exchange a valid pairing token plus deviceId for a server-side control session and httpOnly cookie."
    - "Restore works after token expiry or rotation when the control session itself is still active."
    - "Admin token refresh invalidates old scan tokens but does not revoke active control sessions."
    - "Restore and command-support services touch activity and renew the 2-hour idle timeout."
  artifacts:
    - path: "apps/api/src/modules/rooms/pairing-token-service.ts"
      provides: "15-minute stable pairing token lifecycle"
    - path: "apps/api/src/modules/controller/control-session-service.ts"
      provides: "cookie + device id session creation, restore, touch, and renewal"
    - path: "apps/api/src/routes/control-sessions.ts"
      provides: "mobile control session HTTP routes"
    - path: "apps/api/src/routes/admin-rooms.ts"
      provides: "admin pairing refresh route"
  key_links:
    - from: "apps/api/src/routes/room-snapshots.ts"
      to: "apps/api/src/modules/rooms/pairing-token-service.ts"
      via: "TV QR snapshots use persistent pairing info"
      pattern: "getOrCreatePairingInfo"
    - from: "apps/api/src/routes/control-sessions.ts"
      to: "apps/api/src/modules/controller/control-session-service.ts"
      via: "cookie + device id restore and activity renewal"
      pattern: "touchControlSession"
    - from: "apps/api/src/routes/admin-rooms.ts"
      to: "apps/api/src/modules/rooms/pairing-token-service.ts"
      via: "admin refresh replaces scan token without revoking sessions"
      pattern: "refreshPairingToken"
---

<objective>
Implement persistent pairing token lifecycle and server-side mobile control sessions.

Purpose: controllers need a reliable entry and restore path before they can safely send queue commands.
Output: repositories, services, control session routes, persistent pairing in TV snapshots, admin token refresh API, and tests.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-room-sessions-queue-control/03-CONTEXT.md
@.planning/phases/03-room-sessions-queue-control/03-RESEARCH.md
@.planning/phases/03-room-sessions-queue-control/03-01-SUMMARY.md
@apps/api/src/routes/room-snapshots.ts
@apps/api/src/modules/player/register-player.ts
@apps/api/src/server.ts
@apps/api/src/db/schema.ts

<interfaces>
Routes created or updated:
```typescript
POST /rooms/:roomSlug/control-sessions
GET /rooms/:roomSlug/control-session?deviceId=...
POST /admin/rooms/:roomSlug/pairing-token/refresh
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement pairing token repository and service</name>
  <files>apps/api/src/modules/rooms/repositories/pairing-token-repository.ts, apps/api/src/modules/rooms/pairing-token-service.ts, apps/api/src/routes/room-snapshots.ts, apps/api/src/modules/player/register-player.ts, apps/api/src/server.ts, apps/api/src/test/control-sessions.test.ts</files>
  <read_first>
- apps/api/src/modules/player/register-player.ts
- apps/api/src/routes/room-snapshots.ts
- apps/api/src/server.ts
- apps/api/src/db/schema.ts
- packages/player-contracts/src/index.ts
- .planning/phases/03-room-sessions-queue-control/03-01-SUMMARY.md
  </read_first>
  <behavior>
- Test 1: repeated room snapshot requests return the same `PairingInfo.token` until expiry.
- Test 2: a repository-only reload still returns the same `PairingInfo.token` until expiry, proving the token is displayable after reading from persistence.
- Test 3: `PAIRING_TOKEN_TTL_MS` is `15 * 60 * 1000`.
- Test 4: `verifyPairingToken` accepts the current token and rejects an expired or rotated token.
  </behavior>
  <action>
Create `pairing-token-repository.ts`:
- `RoomPairingTokenRepository.findByRoomId(roomId)`
- `RoomPairingTokenRepository.upsert(input)`
- `PgRoomPairingTokenRepository`
- `InMemoryRoomPairingTokenRepository`

Create `pairing-token-service.ts`:
- Export `PAIRING_TOKEN_TTL_MS = 15 * 60 * 1000`.
- Export `createOpaquePairingToken()` using `randomUUID().replaceAll("-", "")`.
- Export `hashPairingToken(token)` using SHA-256.
- Export `getOrCreatePairingInfo({ room, publicBaseUrl, repository, now })`:
  - if existing `token_expires_at > now`, return `toPairingInfo` using persisted `token_value`;
  - otherwise generate a new opaque token, store both `token_value` and `token_hash`, and return it.
- Export `refreshPairingToken({ room, publicBaseUrl, repository, now })` that always replaces `token_value`, `token_hash`, and expiry.
- Export `verifyPairingToken({ roomId, pairingToken, repository, now })` that compares `hashPairingToken(pairingToken)` with persisted `token_hash`.
- Export `toPairingInfo({ roomSlug, publicBaseUrl, token, tokenExpiresAt })` that builds `/controller?room=${roomSlug}&token=${token}`.

Update `room-snapshots.ts` and `register-player.ts` to use `getOrCreatePairingInfo`; remove any `5 * 60 * 1000` pairing TTL.

Update `server.ts` repositories so PostgreSQL and in-memory paths expose `pairingTokens`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- control-sessions</automated>
  </verify>
  <acceptance_criteria>
- `rg "PAIRING_TOKEN_TTL_MS = 15 \\* 60 \\* 1000" apps/api/src/modules/rooms/pairing-token-service.ts` returns a match.
- `rg "tokenValue|token_value" apps/api/src/modules/rooms/repositories/pairing-token-repository.ts apps/api/src/modules/rooms/pairing-token-service.ts apps/api/src/test/control-sessions.test.ts` returns matches.
- `rg "getOrCreatePairingInfo" apps/api/src/routes/room-snapshots.ts apps/api/src/modules/player/register-player.ts` returns matches.
- `rg "5 \\* 60 \\* 1000" apps/api/src/modules/player/register-player.ts apps/api/src/routes/room-snapshots.ts apps/api/src/modules/rooms/pairing-token-service.ts` returns no matches.
- `rg "repository-only reload|reload" apps/api/src/test/control-sessions.test.ts` returns a match.
- `pnpm -F @home-ktv/api test -- control-sessions` exits 0.
  </acceptance_criteria>
  <done>Pairing tokens are stable, re-displayable, 15-minute entry credentials rather than generated-per-request values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement control session service and routes</name>
  <files>apps/api/src/modules/controller/repositories/control-session-repository.ts, apps/api/src/modules/controller/control-session-service.ts, apps/api/src/modules/rooms/build-control-snapshot.ts, apps/api/src/routes/control-sessions.ts, apps/api/src/server.ts, apps/api/src/test/control-sessions.test.ts</files>
  <read_first>
- apps/api/src/modules/rooms/pairing-token-service.ts
- apps/api/src/modules/rooms/repositories/pairing-token-repository.ts
- apps/api/src/routes/room-snapshots.ts
- apps/api/src/server.ts
- packages/domain/src/index.ts
- packages/player-contracts/src/index.ts
  </read_first>
  <behavior>
- Test 1: `POST /rooms/living-room/control-sessions` sets `Set-Cookie: ktv_control_session=...; HttpOnly; SameSite=Lax; Path=/; Max-Age=7200`.
- Test 2: `GET /rooms/living-room/control-session?deviceId=phone-a` restores from cookie plus device id.
- Test 3: restore touches `last_seen_at`, extends `expires_at` to now + 2 hours, and refreshes cookie `Max-Age=7200`.
- Test 4: expired or revoked sessions return `401 { code: "CONTROL_SESSION_REQUIRED" }`.
  </behavior>
  <action>
Create `control-session-repository.ts`:
- `upsertForDevice(input)`
- `findActiveByIdAndDevice(input)`
- `touch(input)`
- `countActiveByRoom(roomId, activeAfter)`
- PostgreSQL and in-memory implementations.

Create `control-session-service.ts`:
- Export `CONTROL_SESSION_IDLE_TTL_MS = 2 * 60 * 60 * 1000`.
- Export `CONTROL_SESSION_COOKIE = "ktv_control_session"`.
- Export `createControlSession({ room, pairingToken, deviceId, deviceName, pairingTokens, controlSessions, now })`.
- Export `restoreControlSession({ room, cookieHeader, deviceId, controlSessions, now })`.
- Export `touchControlSession({ session, controlSessions, now })` that extends `expiresAt` to `now + CONTROL_SESSION_IDLE_TTL_MS`.
- Export `serializeControlSessionCookie(session)` returning `ktv_control_session=${session.id}; HttpOnly; SameSite=Lax; Path=/; Max-Age=7200`.
- Reject invalid tokens with `INVALID_PAIRING_TOKEN`.
- Require both cookie id and matching `deviceId`; cookie-only restore is invalid.

Create `build-control-snapshot.ts`:
- Export `buildRoomControlSnapshot(input)` returning `RoomControlSnapshot`.
- Reuse room snapshot fields for state/current/switch/pairing/notice.
- Include `controllers.onlineCount`.
- Return `queue: []`; queue previews are filled in Plan 03-04.

Create `routes/control-sessions.ts`:
- `POST /rooms/:roomSlug/control-sessions` accepts `{ pairingToken, deviceId, deviceName }`.
- `GET /rooms/:roomSlug/control-session?deviceId=...` restores from cookie.
- Both success paths set/refresh the cookie and return `{ controlSession, snapshot }`.

Register route dependencies from `server.ts`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- control-sessions && pnpm -F @home-ktv/api build</automated>
  </verify>
  <acceptance_criteria>
- `rg "CONTROL_SESSION_IDLE_TTL_MS = 2 \\* 60 \\* 60 \\* 1000" apps/api/src/modules/controller/control-session-service.ts` returns a match.
- `rg "CONTROL_SESSION_COOKIE = \"ktv_control_session\"" apps/api/src/modules/controller/control-session-service.ts` returns a match.
- `rg "HttpOnly; SameSite=Lax; Path=/; Max-Age=7200" apps/api/src/modules/controller/control-session-service.ts apps/api/src/routes/control-sessions.ts` returns a match.
- `rg "touchControlSession" apps/api/src/modules/controller/control-session-service.ts apps/api/src/routes/control-sessions.ts apps/api/src/test/control-sessions.test.ts` returns matches.
- `rg "CONTROL_SESSION_REQUIRED" apps/api/src/routes/control-sessions.ts apps/api/src/test/control-sessions.test.ts` returns matches.
- `pnpm -F @home-ktv/api test -- control-sessions` exits 0.
  </acceptance_criteria>
  <done>Mobile controllers can create and restore server-side sessions, and restore activity renews the 2-hour idle timeout.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add admin token refresh API that preserves active sessions</name>
  <files>apps/api/src/routes/admin-rooms.ts, apps/api/src/server.ts, apps/api/src/test/control-sessions.test.ts</files>
  <read_first>
- apps/api/src/routes/admin-imports.ts
- apps/api/src/routes/admin-catalog.ts
- apps/api/src/modules/rooms/pairing-token-service.ts
- apps/api/src/modules/controller/control-session-service.ts
- apps/api/src/server.ts
  </read_first>
  <behavior>
- Test 1: `POST /admin/rooms/living-room/pairing-token/refresh` returns a different token with a 15-minute expiry.
- Test 2: the old token fails for new control-session creation after refresh.
- Test 3: a control session created before refresh still restores successfully after refresh.
  </behavior>
  <action>
Create `routes/admin-rooms.ts` with:
```typescript
POST /admin/rooms/:roomSlug/pairing-token/refresh
```

The route must:
- return `404 { error: "ROOM_NOT_FOUND" }` if the slug is unknown;
- call `refreshPairingToken`;
- return `{ pairing }`;
- never update `control_sessions.revoked_at`;
- never delete from `control_sessions`;
- remain auth-free like existing Phase 2 admin routes.

Register the route from `server.ts`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- control-sessions && pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
- `rg "POST /admin/rooms/:roomSlug/pairing-token/refresh" apps/api/src/routes/admin-rooms.ts apps/api/src/test/control-sessions.test.ts` returns a match.
- `rg "refreshPairingToken" apps/api/src/routes/admin-rooms.ts` returns a match.
- `rg "ROOM_NOT_FOUND" apps/api/src/routes/admin-rooms.ts` returns a match.
- `rg "revoked_at|DELETE FROM control_sessions" apps/api/src/routes/admin-rooms.ts` returns no matches.
- `rg "INVALID_PAIRING_TOKEN" apps/api/src/test/control-sessions.test.ts` returns a match.
- `pnpm -F @home-ktv/api test -- control-sessions` exits 0.
  </acceptance_criteria>
  <done>Admin can rotate scan tokens without killing active mobile sessions.</done>
</task>

</tasks>

<verification>
Run `pnpm -F @home-ktv/api test -- control-sessions`, `pnpm -F @home-ktv/api build`, and `pnpm typecheck`.
</verification>

<success_criteria>
- `pairingToken` TTL is 15 minutes.
- Control sessions use server persistence, httpOnly cookie, and device id.
- Pairing token expiry and rotation do not invalidate active control sessions.
- Restore activity renews session expiry and cookie Max-Age.
- Admin can refresh the room pairing token.
- This plan modifies fewer than 15 files.
</success_criteria>

<output>
After completion, create `.planning/phases/03-room-sessions-queue-control/03-02-SUMMARY.md`.
</output>
