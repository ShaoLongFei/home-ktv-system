---
phase: 03-room-sessions-queue-control
plan: 08
type: execute
wave: 7
depends_on:
  - "03-05"
files_modified:
  - apps/tv-player/src/runtime/player-client.ts
  - apps/tv-player/src/runtime/use-room-snapshot.ts
  - apps/tv-player/src/test/use-room-snapshot.test.ts
autonomous: true
requirements:
  - PAIR-02
  - PLAY-04
must_haves:
  truths:
    - "TV consumes the same server snapshot stream as mobile controllers through WebSocket first."
    - "TV falls back to existing 1500ms polling when WebSocket is unavailable."
    - "Server-refreshed pairing tokens replace old TV QR state."
    - "Conflict bootstrap behavior remains intact."
  artifacts:
    - path: "apps/tv-player/src/runtime/use-room-snapshot.ts"
      provides: "WebSocket-first TV snapshot sync with polling fallback"
  key_links:
    - from: "apps/tv-player/src/runtime/use-room-snapshot.ts"
      to: "apps/tv-player/src/runtime/player-client.ts"
      via: "TV hook opens realtime URL before polling fallback"
      pattern: "createSnapshotSocketUrl"
    - from: "apps/tv-player/src/runtime/use-room-snapshot.ts"
      to: "packages/protocol/src/index.ts"
      via: "TV applies room.control.snapshot.updated envelopes"
      pattern: "room.control.snapshot.updated"
    - from: "apps/tv-player/src/runtime/use-room-snapshot.ts"
      to: "apps/api/src/routes/realtime.ts"
      via: "TV subscribes with client=tv to shared server snapshot stream"
      pattern: "client=tv"
---

<objective>
Close the TV runtime loop by moving snapshot sync to WebSocket-first behavior.

Purpose: Phase 3 requires TV and phones to observe the same server-authored state without local drift.
Output: TV player client WebSocket helper, updated snapshot hook, and tests.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-room-sessions-queue-control/03-CONTEXT.md
@.planning/phases/03-room-sessions-queue-control/03-RESEARCH.md
@.planning/phases/03-room-sessions-queue-control/03-05-SUMMARY.md
@apps/tv-player/src/runtime/player-client.ts
@apps/tv-player/src/runtime/use-room-snapshot.ts
@apps/tv-player/src/test/use-room-snapshot.test.ts
@packages/player-contracts/src/index.ts
@packages/protocol/src/index.ts

<interfaces>
TV realtime route:
```typescript
GET /rooms/:roomSlug/realtime?deviceId=...&client=tv
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add TV WebSocket snapshot helper</name>
  <files>apps/tv-player/src/runtime/player-client.ts, apps/tv-player/src/test/use-room-snapshot.test.ts</files>
  <read_first>
- apps/tv-player/src/runtime/player-client.ts
- apps/tv-player/src/test/player-client.test.ts
- packages/protocol/src/index.ts
  </read_first>
  <behavior>
- Test 1: helper builds `/rooms/living-room/realtime?deviceId=...&client=tv`.
- Test 2: helper converts http/https API base to ws/wss URLs.
  </behavior>
  <action>
Extend `PlayerClient`:
- Add `createSnapshotSocketUrl()` returning a WebSocket URL for `/rooms/${roomSlug}/realtime?deviceId=${deviceId}&client=tv`.
- Convert `http://` to `ws://` and `https://` to `wss://`.
- Keep existing `fetchSnapshot()` for polling fallback.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/tv-player test -- use-room-snapshot</automated>
  </verify>
  <acceptance_criteria>
- `rg "client=tv" apps/tv-player/src/runtime/player-client.ts apps/tv-player/src/test/use-room-snapshot.test.ts` returns matches.
- `rg "wss://|ws://" apps/tv-player/src/runtime/player-client.ts apps/tv-player/src/test/use-room-snapshot.test.ts` returns matches.
- `pnpm -F @home-ktv/tv-player test -- use-room-snapshot` exits 0.
  </acceptance_criteria>
  <done>TV runtime can open the Phase 3 realtime snapshot endpoint while retaining polling API support.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make TV snapshot hook WebSocket-first with polling fallback</name>
  <files>apps/tv-player/src/runtime/use-room-snapshot.ts, apps/tv-player/src/test/use-room-snapshot.test.ts</files>
  <read_first>
- apps/tv-player/src/runtime/use-room-snapshot.ts
- apps/tv-player/src/runtime/player-client.ts
- apps/tv-player/src/test/use-room-snapshot.test.ts
- packages/player-contracts/src/index.ts
- packages/protocol/src/index.ts
  </read_first>
  <behavior>
- Test 1: hook applies incoming `room.control.snapshot.updated` payloads.
- Test 2: WebSocket error/close starts existing 1500ms polling fallback.
- Test 3: refreshed server pairing replaces old token; old `stabilizeSnapshotPairing` behavior is removed or bypassed.
- Test 4: conflict bootstrap still stops polling and keeps playback disabled.
  </behavior>
  <action>
Update `use-room-snapshot.ts`:
- Bootstrap first as today.
- If bootstrap is not conflict, open WebSocket using `PlayerClient.createSnapshotSocketUrl()`.
- On `room.control.snapshot.updated`, read `payload` and map compatible fields into `RoomSnapshot`.
- On WebSocket error/close, start polling via existing `fetchSnapshot()` every 1500ms.
- Stop polling when WebSocket reconnect succeeds.
- Remove or bypass `stabilizeSnapshotPairing` so server-refreshed persistent pairing reaches TV QR state.
- Preserve `shouldContinueSnapshotPolling` conflict behavior.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/tv-player test -- use-room-snapshot && pnpm -F @home-ktv/tv-player build</automated>
  </verify>
  <acceptance_criteria>
- `rg "room.control.snapshot.updated" apps/tv-player/src/runtime/use-room-snapshot.ts apps/tv-player/src/test/use-room-snapshot.test.ts` returns matches.
- `rg "setInterval\\(updateSnapshot, pollingIntervalMs\\)" apps/tv-player/src/runtime/use-room-snapshot.ts` returns a match.
- `rg "stabilizeSnapshotPairing" apps/tv-player/src/runtime/use-room-snapshot.ts apps/tv-player/src/test/use-room-snapshot.test.ts` returns no matches, or tests assert refreshed server pairing replaces the old token.
- `rg "shouldContinueSnapshotPolling" apps/tv-player/src/runtime/use-room-snapshot.ts apps/tv-player/src/test/use-room-snapshot.test.ts` returns matches.
- `pnpm -F @home-ktv/tv-player build` exits 0.
  </acceptance_criteria>
  <done>TV uses WebSocket-first server snapshots, updates QR pairing after admin refresh, and retains polling/conflict safety.</done>
</task>

</tasks>

<verification>
Run `pnpm -F @home-ktv/tv-player test -- use-room-snapshot`, `pnpm -F @home-ktv/tv-player build`, and `pnpm typecheck`.
</verification>

<success_criteria>
- TV consumes the same backend snapshot source as mobile controllers.
- Polling fallback remains available.
- Admin token refresh can update the TV QR token.
- Conflict behavior from Phase 1 remains intact.
- This plan modifies fewer than 15 files.
</success_criteria>

<output>
After completion, create `.planning/phases/03-room-sessions-queue-control/03-08-SUMMARY.md`.
</output>
