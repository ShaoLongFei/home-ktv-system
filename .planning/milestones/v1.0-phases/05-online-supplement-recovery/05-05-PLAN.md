---
phase: 05-online-supplement-recovery
plan: 05
type: execute
gap_closure: true
wave: 5
depends_on:
  - 05-04
files_modified:
  - apps/mobile-controller/src/App.tsx
  - apps/mobile-controller/src/test/controller.test.tsx
autonomous: true
requirements:
  - ONLN-01
must_haves:
  truths:
    - "When local results and online supplement candidates both exist, the phone renders local results before the online supplement panel"
    - "When local results are empty, online candidates and the request-supplement entry still render"
    - "Online candidate cards preserve title, artist, source, duration, candidate type, risk/reliability labels, task state, and explicit request-supplement behavior"
  artifacts:
    - path: "apps/mobile-controller/src/App.tsx"
      provides: "local-first mobile search rendering order"
      contains: "online-panel"
    - path: "apps/mobile-controller/src/test/controller.test.tsx"
      provides: "mixed local+online ordering regression coverage"
      contains: "localIndex"
  key_links:
    - from: "apps/mobile-controller/src/App.tsx"
      to: "apps/mobile-controller/src/runtime/use-room-controller.ts"
      via: "controller.songSearch.local and controller.songSearch.online render order"
      pattern: "songSearch\\?\\.local\\.map"
    - from: "apps/mobile-controller/src/test/controller.test.tsx"
      to: "apps/mobile-controller/src/App.tsx"
      via: "DOM text index assertion inside Song search panel"
      pattern: "localIndex.*toBeLessThan\\(onlineIndex\\)"
---

<objective>
Close the remaining Phase 5 mobile local-first presentation gap by proving and fixing the render order of mixed local and online search results.

Purpose: Phase 5 decision D-02 requires online candidates to remain visible below local search results when both exist. Runtime wiring is already closed by 05-04; this plan only corrects the mobile controller presentation and regression coverage.
Output: A focused mobile controller ordering test and a small `App.tsx` render-order change.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-online-supplement-recovery/05-CONTEXT.md
@.planning/phases/05-online-supplement-recovery/05-RESEARCH.md
@.planning/phases/05-online-supplement-recovery/05-VERIFICATION.md
@.planning/phases/05-online-supplement-recovery/05-04-SUMMARY.md
@apps/mobile-controller/src/App.tsx
@apps/mobile-controller/src/test/controller.test.tsx
@apps/mobile-controller/src/runtime/use-room-controller.ts
@apps/mobile-controller/src/api/client.ts

<interfaces>
Existing mobile search contracts to preserve:

```typescript
export interface RoomControllerState {
  songSearch: SongSearchResponse | null;
  songSearchQuery: string;
  songSearchStatus: "idle" | "loading" | "success" | "error";
  requestSupplement(provider: string, providerCandidateId: string): Promise<void>;
}

export async function searchSongs(input: {
  roomSlug: string;
  query: string;
  limit?: number;
  signal?: AbortSignal;
}): Promise<SongSearchResponse>;

export async function requestSupplement(input: {
  roomSlug: string;
  deviceId: string;
  sessionVersion: number;
  provider: string;
  providerCandidateId: string;
}): Promise<SupplementCommandAcceptedResponse>;
```

Current verifier gap:

```text
truth: When local results exist, online candidates remain visible below them
reason: Runtime search can now produce online candidates, but the phone UI renders the online supplement section before local search results.
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add RED mixed local-plus-online ordering coverage</name>
  <files>apps/mobile-controller/src/test/controller.test.tsx</files>
  <read_first>
    - apps/mobile-controller/src/App.tsx
    - apps/mobile-controller/src/test/controller.test.tsx
    - apps/mobile-controller/src/runtime/use-room-controller.ts
    - apps/mobile-controller/src/api/client.ts
    - .planning/phases/05-online-supplement-recovery/05-VERIFICATION.md
  </read_first>
  <behavior>
    - A mobile controller test renders a search response containing at least one local result and at least one online candidate.
    - The test scopes its order check to the `Song search` panel and records the text index for one local result label and one online candidate label.
    - The test asserts the local text index is less than the online text index, so the current pre-fix UI fails RED.
    - The existing empty-local online supplement test remains in place and still requires online candidates plus the request-supplement entry when `local: []`.
  </behavior>
  <action>
    In `apps/mobile-controller/src/test/controller.test.tsx`, add a new test near the existing online supplement tests named `renders online supplement candidates below local results when both exist`.

    Use `installControllerFetchMock({ songSearchResponse })` to return a mixed response with:
    - `local` containing a distinctive local song such as title `晴天` and artist `周杰伦`
    - `online.status: "available"`
    - `online.message: "找到在线补歌候选"`
    - `online.requestSupplement: { visible: true, label: "请求补歌" }`
    - `online.candidates` containing a distinctive online title such as `远端七里香` with the existing candidate fields: `provider`, `providerCandidateId`, `title`, `artistName`, `sourceLabel`, `durationMs`, `candidateType`, `reliabilityLabel`, `riskLabel`, `taskState`, and `taskId`

    After rendering `<App />`, wait for both local and online text to appear. Then read `screen.getByLabelText("Song search").textContent ?? ""`, compute:
    - `const localIndex = searchPanelText.indexOf("晴天");`
    - `const onlineIndex = searchPanelText.indexOf("远端七里香");`

    Assert:
    - `expect(localIndex).toBeGreaterThanOrEqual(0);`
    - `expect(onlineIndex).toBeGreaterThanOrEqual(0);`
    - `expect(localIndex).toBeLessThan(onlineIndex);`

    Do not change production files in this task. This is the RED step for the verifier gap and should fail before Task 2 because `App.tsx` currently renders the online panel before the local map.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/mobile-controller test -- controller</automated>
  </verify>
  <acceptance_criteria>
    - `rg "renders online supplement candidates below local results when both exist" apps/mobile-controller/src/test/controller.test.tsx` finds the new regression test.
    - `rg "localIndex|onlineIndex|toBeLessThan\\(onlineIndex\\)" apps/mobile-controller/src/test/controller.test.tsx` finds the explicit ordering assertion.
    - `pnpm -F @home-ktv/mobile-controller test -- controller` fails before Task 2 with the new mixed local+online order assertion, proving RED coverage.
    - The existing empty-local test still contains `local: []`, `requestSupplement: { visible: true`, and `getAllByRole("button", { name: "请求补歌" })`.
  </acceptance_criteria>
  <done>The mobile controller has a failing regression test that directly proves local result text must appear before online supplement candidate text when both exist.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render local results before the online supplement panel</name>
  <files>apps/mobile-controller/src/App.tsx, apps/mobile-controller/src/test/controller.test.tsx</files>
  <read_first>
    - apps/mobile-controller/src/App.tsx
    - apps/mobile-controller/src/test/controller.test.tsx
    - apps/mobile-controller/src/runtime/use-room-controller.ts
    - apps/mobile-controller/src/api/client.ts
    - .planning/phases/05-online-supplement-recovery/05-VERIFICATION.md
  </read_first>
  <behavior>
    - `App.tsx` renders `controller.songSearch?.local.map(...)` before the online supplement `<section className="online-panel" aria-label="Online supplement">`.
    - Mixed local+online results show local result cards first and online candidate cards below them, per D-02.
    - Empty-local behavior is preserved: the local empty message, online candidates, disabled request-supplement entry, and candidate request buttons all still render.
    - Online candidate card fields and explicit `controller.requestSupplement(candidate.provider, candidate.providerCandidateId)` behavior remain unchanged.
  </behavior>
  <action>
    In `apps/mobile-controller/src/App.tsx`, reorder the JSX inside `<div className="song-list">` so the local result rendering block comes before the online supplement panel.

    Concrete target state:
    - The `controller.songSearch?.local.map((result) => { ... })` block must appear before the `<section className="online-panel" aria-label="Online supplement">` block.
    - The online supplement panel must remain below local results and above or near the local empty-state text only if the resulting UI still satisfies both cases:
      1. mixed local+online: local text index is less than online candidate text index
      2. empty-local: `本地未找到`, online candidates, and both `请求补歌` controls still render
    - Preserve all online candidate fields currently displayed: title, artist, `sourceLabel`, formatted duration, `candidateType`, `reliabilityLabel`, `riskLabel`, and `taskState`.
    - Preserve the request button call shape exactly: `controller.requestSupplement(candidate.provider, candidate.providerCandidateId)`.

    Keep the fix scoped to mobile UI ordering and tests. Do not replan or modify backend runtime wiring, admin recovery, playback recovery, provider config, or API contracts unless TypeScript exposes a small type-only issue directly caused by this UI move.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/mobile-controller test -- controller</automated>
    <automated>pnpm -F @home-ktv/mobile-controller typecheck</automated>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `rg -n "songSearch\\?\\.local\\.map|online-panel" apps/mobile-controller/src/App.tsx` shows the local result map line before the `online-panel` line.
    - `rg "localIndex|onlineIndex|toBeLessThan\\(onlineIndex\\)" apps/mobile-controller/src/test/controller.test.tsx` finds the mixed ordering assertion.
    - `rg "sourceLabel|candidateType|reliabilityLabel|riskLabel|taskState|requestSupplement\\(candidate\\.provider, candidate\\.providerCandidateId\\)" apps/mobile-controller/src/App.tsx` confirms online card fields and request behavior remain present.
    - `pnpm -F @home-ktv/mobile-controller test -- controller` passes, including the mixed local+online test and the empty-local online supplement test.
    - `pnpm -F @home-ktv/mobile-controller typecheck` passes.
    - `pnpm typecheck` passes at workspace scope.
  </acceptance_criteria>
  <done>Mobile search is local-first when both result types exist, while empty-local online supplement and explicit request-supplement behavior continue to work.</done>
</task>

</tasks>

<verification>
Run:
- `pnpm -F @home-ktv/mobile-controller test -- controller`
- `pnpm -F @home-ktv/mobile-controller typecheck`
- `pnpm typecheck`

Then confirm:
- `rg -n "songSearch\\?\\.local\\.map|online-panel" apps/mobile-controller/src/App.tsx` reports the local-result line before the online-panel line.
- `rg "localIndex|onlineIndex|toBeLessThan\\(onlineIndex\\)" apps/mobile-controller/src/test/controller.test.tsx` reports the mixed ordering assertion.
</verification>

<success_criteria>
- The verifier truth `When local results exist, online candidates remain visible below them` is satisfied by both code order and an automated mixed local+online test.
- Existing empty-local behavior from D-01 remains intact.
- Candidate card field rendering and request-supplement behavior from D-03 and D-04 remain intact.
- No backend runtime, admin recovery, playback recovery, or provider config files are modified.
</success_criteria>

<output>
After completion, create `.planning/phases/05-online-supplement-recovery/05-05-SUMMARY.md`
</output>
