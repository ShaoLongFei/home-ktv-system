---
phase: 05-online-supplement-recovery
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - apps/api/src/modules/online/candidate-task-service.ts
  - apps/api/src/modules/online/candidate-task-repository.ts
  - apps/api/src/modules/online/candidate-cache-worker.ts
  - apps/api/src/modules/playback/session-command-service.ts
  - apps/api/src/modules/catalog/admission-service.ts
  - apps/api/src/routes/admin-rooms.ts
  - apps/api/src/routes/player.ts
  - apps/api/src/modules/player/telemetry-service.ts
  - apps/api/src/modules/realtime/room-snapshot-broadcaster.ts
  - apps/api/src/test/online-candidate-task.test.ts
  - apps/api/src/test/room-queue-commands.test.ts
  - apps/api/src/test/admin-online-task-actions.test.ts
  - apps/api/src/test/player-failure-recovery.test.ts
autonomous: true
requirements:
  - ONLN-02
  - ONLN-03
  - PLAY-05
must_haves:
  truths:
    - "Candidate tasks advance through selected, fetching, fetched, ready, failed, stale, promoted, and purged as cache flow progresses"
    - "Ready cached assets stay supplement-sourced and only enter queue when explicitly selected"
    - "Playback failure skips directly to the next song and the controller sees both the reason and the fallback result"
    - "Failed and stale tasks can be retried or cleaned without auto-enqueueing or auto-promoting"
  artifacts:
    - path: "apps/api/src/modules/online/candidate-task-service.ts"
      provides: "candidate lifecycle transitions and retry/cleanup operations"
      contains: "review_required"
    - path: "apps/api/src/modules/playback/session-command-service.ts"
      provides: "queue ready-gate and playback advance logic"
      contains: "SONG_NOT_QUEUEABLE"
    - path: "apps/api/src/routes/player.ts"
      provides: "failed playback telemetry branch and recovery notice broadcast"
      contains: "switch_failed"
    - path: "apps/api/src/routes/admin-rooms.ts"
      provides: "room-scoped retry/cleanup/promotion endpoints"
      contains: "pairing-token/refresh"
    - path: "apps/api/src/modules/realtime/room-snapshot-broadcaster.ts"
      provides: "updated snapshot fanout after failure recovery"
      contains: "room.control.snapshot.updated"
  key_links:
    - from: "apps/api/src/routes/player.ts"
      to: "apps/api/src/modules/playback/session-command-service.ts"
      via: "failed telemetry resolves through server-side skip advancement"
      pattern: "handlePlayerEnded"
    - from: "apps/api/src/modules/playback/session-command-service.ts"
      to: "apps/api/src/modules/online/candidate-task-service.ts"
      via: "ready-gate and promotion checks keep supplement resources separate from the formal local truth layer"
      pattern: "online_cached"
    - from: "apps/api/src/routes/admin-rooms.ts"
      to: "apps/api/src/modules/online/candidate-task-service.ts"
      via: "retry, clean, and promote actions stay task-scoped"
      pattern: "candidate_tasks"
---

<objective>
Complete the server-side online cache lifecycle and playback recovery behavior so online supplement resources can become ready, be retried or cleaned, and recover from playback failure without changing the project's skip-first recovery rule.

Purpose: Phase 5 must keep cache-before-play and server-authoritative failure handling aligned while still allowing explicit queueing of ready supplement resources.
Output: Candidate lifecycle transitions, retry and cleanup operations, ready-gate enforcement, explicit promotion handling, and failure-skip recovery notices.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-online-supplement-recovery/05-CONTEXT.md
@.planning/phases/05-online-supplement-recovery/05-RESEARCH.md
@apps/api/src/modules/playback/session-command-service.ts
@apps/api/src/routes/player.ts
@apps/api/src/modules/player/telemetry-service.ts
@apps/api/src/routes/admin-rooms.ts
@apps/api/src/modules/catalog/admission-service.ts
@apps/api/src/modules/realtime/room-snapshot-broadcaster.ts
@docs/KTV-ARCHITECTURE.md

<interfaces>
Current playback and telemetry hooks the executor should keep server-owned:

```typescript
export type PlayerTelemetryKind =
  | "loading"
  | "playing"
  | "ended"
  | "failed"
  | "switch_failed"
  | "recovery_fallback_start_over";

export interface CommandExecutionResult {
  status: "accepted" | "duplicate" | "conflict" | "rejected";
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement candidate lifecycle transitions and cache orchestration</name>
  <files>apps/api/src/modules/online/candidate-task-service.ts, apps/api/src/modules/online/candidate-task-repository.ts, apps/api/src/modules/online/candidate-cache-worker.ts, apps/api/src/test/online-candidate-task.test.ts</files>
  <read_first>
    - apps/api/src/modules/online/candidate-task-service.ts
    - apps/api/src/modules/online/candidate-task-repository.ts
    - docs/KTV-ARCHITECTURE.md
    - .planning/phases/05-online-supplement-recovery/05-CONTEXT.md
    - .planning/phases/05-online-supplement-recovery/05-RESEARCH.md
  </read_first>
  <behavior>
    - Selected candidates move through `selected -> fetching -> fetched -> ready` when cache and verification succeed.
    - Risky or failed branches go through `review_required` or `failed` instead of a universal pre-review gate.
    - Failed and stale tasks can be retried or cleaned while preserving the audit trail and room scope.
    - Provider kill-switch logic blocks disabled providers from fetching while still allowing discovered candidates to remain visible.
  </behavior>
  <action>
    Build the candidate lifecycle service around the repository from Phase 5-01. Keep it in-process and DB-backed for this phase, with explicit transition methods for select, fetch, verify, ready, fail, stale, promote, and purge. Use the phase decisions as the source of truth: no global pre-review gate, and `review_required` only for failed or risky branches.

    Add the cache orchestration path in `apps/api/src/modules/online/candidate-cache-worker.ts` so provider `prepareFetch` / `verify` work moves through the lifecycle and records the latest event metadata on each transition. Keep the worker reusable from the route layer, but do not introduce a separate queue system yet.

    Expose retry and cleanup helpers on `apps/api/src/modules/online/candidate-task-service.ts` so later admin actions can retry failed tasks or purge stale/failed rows without mutating queue state.

    Extend `apps/api/src/test/online-candidate-task.test.ts` to cover the lifecycle transitions, room scoping, and retry/cleanup behavior.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- online-candidate-task</automated>
    <automated>pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Candidate tasks can move through the full lifecycle without hand-editing rows.
    - Retry and cleanup operations are room-scoped and preserve the audit trail.
    - Provider kill-switches are enforced before fetch/verification begins.
  </acceptance_criteria>
  <done>Online supplement tasks have a complete lifecycle engine with explicit retry and cleanup paths.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce ready-gate, promotion, and room-scoped repair actions</name>
  <files>apps/api/src/modules/playback/session-command-service.ts, apps/api/src/modules/catalog/admission-service.ts, apps/api/src/routes/admin-rooms.ts, apps/api/src/test/room-queue-commands.test.ts, apps/api/src/test/admin-online-task-actions.test.ts</files>
  <read_first>
    - apps/api/src/modules/playback/session-command-service.ts
    - apps/api/src/modules/catalog/admission-service.ts
    - apps/api/src/routes/admin-rooms.ts
    - apps/api/src/test/room-queue-commands.test.ts
    - docs/KTV-ARCHITECTURE.md
    - .planning/phases/05-online-supplement-recovery/05-CONTEXT.md
  </read_first>
  <behavior>
    - Only `ready` cached assets can pass the queue gate, and `online_ephemeral` remains rejected.
    - Ready supplement resources keep supplement semantics and do not become the local-library truth layer.
    - Resource promotion is explicit and task-scoped; it is not exposed as a prominent Rooms-page button.
    - Retry and clean actions operate on failed tasks and do not auto-enqueue playback.
  </behavior>
  <action>
    Keep `apps/api/src/modules/playback/session-command-service.ts` strict about queue admission: accept ready cached assets only when the selected asset is queueable, verified, and supplement-safe, while preserving the current rejection of ephemeral online resources.

    Add the task-scoped recovery endpoints to `apps/api/src/routes/admin-rooms.ts` so admins can retry failed tasks, clean failed tasks, refresh room state, refresh pairing tokens, and promote resources from the task flow. Route promotion through the admission service or task service, not through a Rooms-page quick action.

    Update `apps/api/src/modules/catalog/admission-service.ts` only where it needs to cooperate with the new promotion flow, and keep the formal catalog boundary intact so supplement resources do not silently merge into the local song truth layer.

    Extend `apps/api/src/test/room-queue-commands.test.ts` and add `apps/api/src/test/admin-online-task-actions.test.ts` so the ready gate, retry/clean actions, and task-scoped promotion behavior are exercised end to end.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- room-queue-commands admin-online-task-actions</automated>
    <automated>pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Queue admission still rejects ephemeral online assets and accepts only ready verified cached assets.
    - Admin repair actions can retry or clean failed tasks and promote resources through the task flow.
    - Rooms-page affordances stay task-scoped rather than exposing a prominent ready promote button.
  </acceptance_criteria>
  <done>Ready-gate enforcement and task-scoped repair/promotion actions are wired on the server side.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Skip failed playback directly to the next song and broadcast recovery notices</name>
  <files>apps/api/src/routes/player.ts, apps/api/src/modules/player/telemetry-service.ts, apps/api/src/modules/playback/session-command-service.ts, apps/api/src/modules/realtime/room-snapshot-broadcaster.ts, apps/api/src/test/player-failure-recovery.test.ts</files>
  <read_first>
    - apps/api/src/routes/player.ts
    - apps/api/src/modules/player/telemetry-service.ts
    - apps/api/src/modules/playback/session-command-service.ts
    - apps/api/src/modules/realtime/room-snapshot-broadcaster.ts
    - .planning/phases/05-online-supplement-recovery/05-CONTEXT.md
    - .planning/phases/05-online-supplement-recovery/05-RESEARCH.md
  </read_first>
  <behavior>
    - A `failed` telemetry event advances to the next song directly rather than trying same-song alternate assets.
    - The controller notice includes both the failure cause and the fallback result, such as a skip confirmation.
    - The failure path stays server-authoritative and does not add client-side retry logic.
    - Existing switch-failed and reconnect recovery notices stay intact.
  </behavior>
  <action>
    Add a failure branch in the player route that records the failure event, advances the session to the next queue entry, and broadcasts the resulting snapshot/notice through the existing server path. Keep the recovery rule simple: skip first, then surface the reason and result, instead of attempting alternate same-song assets.

    Update `apps/api/src/modules/player/telemetry-service.ts` and `apps/api/src/modules/playback/session-command-service.ts` so the failure event is recorded consistently and the session state remains authoritative after the skip.

    Keep `apps/api/src/modules/realtime/room-snapshot-broadcaster.ts` on the same snapshot envelope so mobile and admin clients see the resolved failure result immediately.

    Add `apps/api/src/test/player-failure-recovery.test.ts` to cover the direct-skip behavior and the emitted notice payload.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- player-failure-recovery</automated>
    <automated>pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Playback failure moves the room directly to the next song.
    - The returned notice includes the failure cause and the fallback result text.
    - No same-song alternate retry path is introduced on the client side.
  </acceptance_criteria>
  <done>Playback failure is resolved server-side with a direct skip and a broadcast recovery notice.</done>
</task>

</tasks>

<verification>
Run:
- `pnpm -F @home-ktv/api test -- online-candidate-task room-queue-commands admin-online-task-actions player-failure-recovery`
- `pnpm -F @home-ktv/api typecheck`
</verification>

<success_criteria>
- The online task lifecycle reaches ready, failed, stale, promoted, and purged through explicit server transitions.
- Queue admission only accepts ready verified cached assets.
- Playback failure skips directly to the next song and surfaces the server-side reason/result notice.
</success_criteria>

<output>
After completion, create `.planning/phases/05-online-supplement-recovery/05-02-SUMMARY.md`
</output>
