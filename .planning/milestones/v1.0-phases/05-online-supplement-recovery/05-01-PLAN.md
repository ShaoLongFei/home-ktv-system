---
phase: 05-online-supplement-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/domain/src/index.ts
  - apps/api/src/db/migrations/0006_online_candidates.sql
  - apps/api/src/db/schema.ts
  - apps/api/src/modules/online/repositories/candidate-task-repository.ts
  - apps/api/src/modules/online/provider-registry.ts
  - apps/api/src/routes/song-search.ts
  - apps/api/src/routes/control-commands.ts
  - apps/api/src/modules/online/candidate-task-service.ts
  - apps/api/src/test/online-candidate-task.test.ts
  - apps/api/src/test/song-search-routes.test.ts
  - apps/mobile-controller/src/api/client.ts
  - apps/mobile-controller/src/runtime/use-room-controller.ts
  - apps/mobile-controller/src/App.tsx
  - apps/mobile-controller/src/test/controller.test.tsx
autonomous: true
requirements:
  - ONLN-01
  - ONLN-02
must_haves:
  truths:
    - "When local search has no matches, the phone shows online candidates plus a request-supplement entry"
    - "When local results exist, online candidates remain visible below them"
    - "Online candidate cards show title, artist, source, duration, candidate type, and risk/reliability labels"
    - "Submitting a supplement request creates a persisted task flow instead of auto-enqueueing playback"
    - "The task record preserves discovered, selected, review_required, fetching, fetched, ready, failed, stale, promoted, and purged so later cache and recovery plans do not lose the supplement state machine"
  artifacts:
    - path: "packages/domain/src/index.ts"
      provides: "shared online-candidate search/task contracts"
      contains: "export interface SongSearchResponse"
    - path: "apps/api/src/db/migrations/0006_online_candidates.sql"
      provides: "candidate_tasks persistence and indexes"
      contains: "CREATE TABLE IF NOT EXISTS candidate_tasks"
    - path: "apps/api/src/modules/online/repositories/candidate-task-repository.ts"
      provides: "online task persistence API"
      contains: "export interface CandidateTaskRepository"
    - path: "apps/api/src/routes/song-search.ts"
      provides: "local-first search response with online candidates"
      contains: "online:"
    - path: "apps/mobile-controller/src/App.tsx"
      provides: "mobile online candidate and supplement entry UI"
      contains: "搜索歌曲"
  key_links:
    - from: "apps/api/src/routes/song-search.ts"
      to: "apps/api/src/modules/online/candidate-task-service.ts"
      via: "discovered online candidates are upserted into candidate_tasks before response"
      pattern: "candidate_tasks"
    - from: "apps/mobile-controller/src/runtime/use-room-controller.ts"
      to: "apps/mobile-controller/src/api/client.ts"
      via: "searchSongs plus supplement request helper"
      pattern: "searchSongs"
    - from: "apps/api/src/modules/online/repositories/candidate-task-repository.ts"
      to: "apps/api/src/db/migrations/0006_online_candidates.sql"
      via: "candidate task columns, status transitions, and room-scoped indexes"
      pattern: "candidate_tasks"
---

<objective>
Establish the online candidate contract, persistence model, and mobile entry wiring so search can surface supplement candidates and persist the request flow instead of treating online results as a separate playback path.

Purpose: Phase 5 needs a visible online supplement entry that stays local-first and audit-friendly before later lifecycle and recovery work can complete the cache flow.
Output: Shared online candidate/task types, candidate task persistence, search-route discovery wiring, supplement command plumbing, and mobile candidate display wiring.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-online-supplement-recovery/05-CONTEXT.md
@.planning/phases/05-online-supplement-recovery/05-RESEARCH.md
@packages/domain/src/index.ts
@apps/api/src/routes/song-search.ts
@apps/api/src/routes/control-commands.ts
@apps/mobile-controller/src/api/client.ts
@apps/mobile-controller/src/runtime/use-room-controller.ts
@apps/mobile-controller/src/App.tsx

<interfaces>
Current search and controller entry points the executor should extend:

```typescript
export interface SongSearchResponse {
  query: string;
  local: SongSearchLocalResult[];
  online: SongSearchOnlinePlaceholder;
}

export interface RoomControllerState {
  songSearch: SongSearchResponse | null;
  songSearchQuery: string;
  songSearchStatus: "idle" | "loading" | "success" | "error";
  duplicateConfirm: { songId: string; assetId: string; title: string } | null;
  addSongVersion(songId: string, assetId: string): Promise<void>;
  requestAddSongVersion(songId: string, assetId: string, title: string, queueState: SongSearchQueueState): void;
  submitSongSearch(): void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add online candidate contracts and persistence</name>
  <files>packages/domain/src/index.ts, apps/api/src/db/migrations/0006_online_candidates.sql, apps/api/src/db/schema.ts, apps/api/src/modules/online/repositories/candidate-task-repository.ts, apps/api/src/test/online-candidate-task.test.ts</files>
  <read_first>
    - packages/domain/src/index.ts
    - apps/api/src/db/schema.ts
    - docs/KTV-ARCHITECTURE.md
    - .planning/phases/05-online-supplement-recovery/05-CONTEXT.md
    - .planning/phases/05-online-supplement-recovery/05-RESEARCH.md
  </read_first>
  <behavior>
    - `SongSearchResponse.online` can carry explicit candidate cards instead of only a disabled placeholder.
    - Candidate task rows persist room/provider/candidate identity, lifecycle status, risk and reliability labels, failure reason, and ready/promoted/purged timestamps.
    - New tasks start in `discovered`; later work can move them to `selected`, `review_required`, `fetching`, `fetched`, `ready`, `failed`, `stale`, `promoted`, or `purged`.
    - Provider kill-switch logic stays server-side and provider capability flags decide whether a provider may enter cache flow.
  </behavior>
  <action>
    Extend `packages/domain/src/index.ts` with shared online supplement contracts for candidate cards, task states, and a richer `SongSearchResponse.online` payload that can hold discoverable candidates plus a request-supplement entry. Keep the local result shape intact and keep `ready` as supplement-sourced, not local-truth data.

    Add `apps/api/src/db/migrations/0006_online_candidates.sql` and mirror it in `apps/api/src/db/schema.ts` for a `candidate_tasks` persistence model with room-scoped indexes, provider metadata, candidate identity, lifecycle status, failure reason, recent event metadata, and task timestamps.

    Create `apps/api/src/modules/online/repositories/candidate-task-repository.ts` as the typed persistence boundary for discovering, upserting, listing, and transitioning candidate tasks. Keep the API narrow so later cache and admin work can reuse it without re-reading schema internals.

    Add `apps/api/src/test/online-candidate-task.test.ts` to cover the shared contracts and repository behavior, including the required lifecycle values and room/provider scoping.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- online-candidate-task</automated>
    <automated>pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/domain/src/index.ts` contains the online candidate/task contracts.
    - `apps/api/src/db/migrations/0006_online_candidates.sql` creates `candidate_tasks`.
    - `apps/api/src/modules/online/repositories/candidate-task-repository.ts` exposes a typed persistence API for discovery and transitions.
    - The new test covers the lifecycle states and room-scoped persistence behavior.
  </acceptance_criteria>
  <done>Online supplement candidates have a concrete shared contract and a persisted task model ready for discovery and later cache transitions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Discover online candidates and persist supplement requests</name>
  <files>apps/api/src/routes/song-search.ts, apps/api/src/routes/control-commands.ts, apps/api/src/modules/online/provider-registry.ts, apps/api/src/modules/online/candidate-task-service.ts, apps/api/src/test/online-candidate-discovery.test.ts, apps/api/src/test/song-search-routes.test.ts</files>
  <read_first>
    - apps/api/src/routes/song-search.ts
    - apps/api/src/routes/control-commands.ts
    - apps/api/src/modules/catalog/admission-service.ts
    - docs/KTV-ARCHITECTURE.md
    - .planning/phases/05-online-supplement-recovery/05-CONTEXT.md
    - .planning/phases/05-online-supplement-recovery/05-RESEARCH.md
  </read_first>
  <behavior>
    - Local results stay first, and online candidates remain visible beneath them when local matches exist.
    - When local results are empty, the response still shows online candidates and a request-supplement entry.
    - Each candidate card carries title, artist, source, duration, candidate type, and risk/reliability labels.
    - Requesting supplement creates or updates a persisted task in `selected` or `review_required` instead of auto-enqueueing playback.
  </behavior>
  <action>
    Wire `apps/api/src/routes/song-search.ts` to a provider registry that honors enabled-provider configuration and the server-side kill-switch. Discover online candidates, dedupe them, upsert them into `candidate_tasks` as `discovered`, and return them in the search response so they appear below local results when local matches exist.

    Extend `apps/api/src/routes/control-commands.ts` with the supplement-request command that selects an online candidate without creating a queue entry. Route that command through `apps/api/src/modules/online/candidate-task-service.ts` so the selection path can move directly into cache flow or into `review_required` when the provider branch is risky or failed.

    Keep the search response and command payloads aligned with the new shared contracts from Task 1, including the request-supplement entry that appears when local search has no matches.

    Add `apps/api/src/test/online-candidate-discovery.test.ts` and extend `apps/api/src/test/song-search-routes.test.ts` so the local-first ordering, supplement entry, candidate labels, and persisted task selection are all covered.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- song-search-routes online-candidate-discovery</automated>
    <automated>pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/routes/song-search.ts` returns online candidates below local results and still exposes them when local search is empty.
    - Supplement submission writes candidate task state and does not enqueue playback.
    - Provider discovery respects the enabled-provider list and server-side kill-switch.
    - Search tests cover the local-first and empty-local cases.
  </acceptance_criteria>
  <done>Search can discover online candidates, persist supplement requests, and expose the request flow without turning supplement submission into playback enqueueing.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire mobile search for online candidates and supplement requests</name>
  <files>apps/mobile-controller/src/api/client.ts, apps/mobile-controller/src/runtime/use-room-controller.ts, apps/mobile-controller/src/App.tsx, apps/mobile-controller/src/test/controller.test.tsx</files>
  <read_first>
    - apps/mobile-controller/src/api/client.ts
    - apps/mobile-controller/src/runtime/use-room-controller.ts
    - apps/mobile-controller/src/App.tsx
    - apps/mobile-controller/src/test/controller.test.tsx
    - packages/domain/src/index.ts
    - .planning/phases/05-online-supplement-recovery/05-CONTEXT.md
  </read_first>
  <behavior>
    - Empty-query search after session restore still fetches the online supplement view so the phone never lands on a blank search screen.
    - When local results exist, online candidates remain visible below them.
    - Candidate cards expose task-state text for supplement flow, not just a playback action.
    - Supplement submission is explicit and does not auto-enqueue a song.
  </behavior>
  <action>
    Replace the mobile controller's old search-client assumptions with the richer online candidate response from Task 2. Keep `searchSongs` as the runtime entry point, add a supplement-request command helper, and preserve the stale-response guards so slower requests do not overwrite the latest query result.

    Extend `apps/mobile-controller/src/runtime/use-room-controller.ts` with the supplement/task state needed to surface online candidate cards, request supplementation, and keep the flow separate from explicit queueing of ready resources.

    Rebuild `apps/mobile-controller/src/App.tsx` so the search panel renders local results first, online candidates underneath, and a request-supplement entry when local search is empty. Keep the result cards dense and functional, not explanatory.

    Update `apps/mobile-controller/src/test/controller.test.tsx` so it covers local-empty and local-present rendering, supplement submission, and the fact that the phone does not auto-enqueue online candidates.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/mobile-controller test -- controller</automated>
    <automated>pnpm -F @home-ktv/mobile-controller typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The mobile controller shows online candidate cards and a request-supplement entry.
    - Online candidates remain visible below local search results.
    - Supplement submission updates task state instead of pushing a queue entry.
    - The mobile controller tests cover the new search and supplement flow.
  </acceptance_criteria>
  <done>Mobile search can show supplement candidates, preserve local-first ordering, and submit a task without auto-queuing playback.</done>
</task>

</tasks>

<verification>
Run:
- `pnpm -F @home-ktv/api test -- online-candidate-task song-search-routes online-candidate-discovery`
- `pnpm -F @home-ktv/api typecheck`
- `pnpm -F @home-ktv/mobile-controller test -- controller`
- `pnpm -F @home-ktv/mobile-controller typecheck`
</verification>

<success_criteria>
- Search can surface online supplement candidates with the required metadata and room-scoped persistence.
- Supplement submission persists task state instead of enqueueing playback.
- Mobile search keeps local-first ordering while still showing online candidates and a request-supplement entry.
</success_criteria>

<output>
After completion, create `.planning/phases/05-online-supplement-recovery/05-01-SUMMARY.md`
</output>
