---
phase: 05-online-supplement-recovery
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - apps/api/src/modules/playback/repositories/playback-event-repository.ts
  - apps/api/src/modules/rooms/build-control-snapshot.ts
  - apps/api/src/routes/admin-rooms.ts
  - apps/api/src/test/admin-room-status.test.ts
  - apps/admin/src/rooms/types.ts
  - apps/admin/src/api/client.ts
  - apps/admin/src/rooms/RoomStatusView.tsx
  - apps/admin/src/App.css
  - apps/admin/src/test/room-status.test.tsx
autonomous: true
requirements:
  - ONLN-04
  - ADMN-02
must_haves:
  truths:
    - "Admin Rooms defaults to room state, current song, queue, TV online, controller count, recent events, and online task summary"
    - "Operators can refresh room state, refresh pairing token, retry or clean failed tasks, and promote resources from task detail"
    - "The Rooms page does not show a prominent ready-resource promote button"
    - "The recovery view makes the task and event relationship visible enough to diagnose failures quickly"
  artifacts:
    - path: "apps/api/src/modules/playback/repositories/playback-event-repository.ts"
      provides: "recent playback event query"
      contains: "export interface PlaybackEventRepository"
    - path: "apps/api/src/modules/rooms/build-control-snapshot.ts"
      provides: "room snapshot and task summary assembly"
      contains: "export async function buildRoomControlSnapshot"
    - path: "apps/api/src/routes/admin-rooms.ts"
      provides: "admin room recovery endpoints and summary response"
      contains: "pairing-token/refresh"
    - path: "apps/admin/src/rooms/types.ts"
      provides: "admin room recovery view contracts"
      contains: "export interface RoomStatusResponse"
    - path: "apps/admin/src/rooms/RoomStatusView.tsx"
      provides: "operational recovery UI"
      contains: "Room status"
  key_links:
    - from: "apps/api/src/modules/rooms/build-control-snapshot.ts"
      to: "apps/admin/src/rooms/types.ts"
      via: "recentEvents and onlineTasks payload shape"
      pattern: "RoomStatusResponse"
    - from: "apps/api/src/routes/admin-rooms.ts"
      to: "apps/admin/src/api/client.ts"
      via: "refresh, retry, cleanup, and promote routes"
      pattern: "pairing-token/refresh"
    - from: "apps/admin/src/rooms/RoomStatusView.tsx"
      to: "apps/admin/src/App.css"
      via: "dense recovery layout and task-action grouping"
      pattern: "room-status-grid"
---

<objective>
Expose the room recovery surface that operators need to diagnose and repair online supplement activity: room state, live queue, TV presence, controller count, recent events, online task summary, and task-scoped actions.

Purpose: The final phase surface must make recovery legible without turning the Rooms page into a generic debug console or a promote-everything control panel.
Output: Extended room status payloads, admin action helpers, and a recovery-focused Rooms view.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-online-supplement-recovery/05-CONTEXT.md
@.planning/phases/05-online-supplement-recovery/05-RESEARCH.md
@apps/api/src/modules/playback/repositories/playback-event-repository.ts
@apps/api/src/modules/rooms/build-control-snapshot.ts
@apps/api/src/routes/admin-rooms.ts
@apps/admin/src/rooms/types.ts
@apps/admin/src/api/client.ts
@apps/admin/src/rooms/RoomStatusView.tsx
@apps/admin/src/App.css

<interfaces>
Current admin room response shape the executor should extend:

```typescript
export interface RoomStatusResponse {
  room: { roomId: string; roomSlug: string; status: string };
  pairing: { tokenExpiresAt: string; controllerUrl: string; qrPayload: string };
  tvPresence: { online: boolean; deviceName: string | null; lastSeenAt: string | null; conflict: unknown };
  controllers: { onlineCount: number };
  sessionVersion: number;
  current: { queueEntryId: string; songTitle: string; artistName: string; vocalMode: string } | null;
  queue: RoomQueueEntryPreview[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the admin room snapshot with recent events and task summary</name>
  <files>apps/api/src/modules/playback/repositories/playback-event-repository.ts, apps/api/src/modules/rooms/build-control-snapshot.ts, apps/api/src/routes/admin-rooms.ts, apps/admin/src/rooms/types.ts, apps/api/src/test/admin-room-status.test.ts</files>
  <read_first>
    - apps/api/src/modules/playback/repositories/playback-event-repository.ts
    - apps/api/src/modules/rooms/build-control-snapshot.ts
    - apps/api/src/routes/admin-rooms.ts
    - apps/admin/src/rooms/types.ts
    - apps/api/src/test/admin-room-status.test.ts
    - .planning/phases/05-online-supplement-recovery/05-CONTEXT.md
    - .planning/phases/05-online-supplement-recovery/05-RESEARCH.md
  </read_first>
  <behavior>
    - Room status responses include room state, current song, queue, TV online, controller count, recent playback events, and online task summary.
    - Task rows carry room, candidate, status, failure reason, and recent event timestamps so the UI can explain why a task is ready, failed, or stale.
    - Refresh pairing token remains on the same admin room surface while the new summary fields stay additive.
    - The response shape keeps promotion task-scoped, not Rooms-page-prominent.
  </behavior>
  <action>
    Add a recent-room-event query to `apps/api/src/modules/playback/repositories/playback-event-repository.ts` and use it from `apps/api/src/modules/rooms/build-control-snapshot.ts` and `apps/api/src/routes/admin-rooms.ts` so the admin room status response can include recent playback events alongside the live queue summary.

    Extend `apps/admin/src/rooms/types.ts` so the room status contract can carry the new recent event list and online task summary while preserving the existing pairing, TV presence, controllers, current song, and queue fields.

    Update `apps/api/src/test/admin-room-status.test.ts` to prove the admin response now includes the room recovery data the UI will rely on.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- admin-room-status</automated>
    <automated>pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - Admin room status responses include recent events and online task summary data.
    - The response contract still carries the live room and pairing state needed for recovery.
    - Tests cover the new summary fields.
  </acceptance_criteria>
  <done>Room recovery data is available from the backend in the shape the admin UI will consume.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add admin API helpers for room recovery and task actions</name>
  <files>apps/admin/src/api/client.ts</files>
  <read_first>
    - apps/admin/src/api/client.ts
    - apps/admin/src/rooms/types.ts
    - apps/api/src/routes/admin-rooms.ts
    - .planning/phases/05-online-supplement-recovery/05-CONTEXT.md
  </read_first>
  <behavior>
    - The client exposes explicit helpers for refresh room state, refresh pairing token, retry failed task, clean failed task, and promote resource actions.
    - Payloads stay room-scoped and task-scoped; they do not imply a prominent Rooms-page promote shortcut.
    - Response decoding stays aligned with the richer room status contract from Task 1.
  </behavior>
  <action>
    Add the admin API helper methods needed by the recovery view: room refresh, pairing refresh, retry failed task, clean failed task, and resource promotion. Keep the helpers thin and explicit so the UI can call them directly without inventing extra client-side state.

    Keep the helper return types aligned with `apps/admin/src/rooms/types.ts`, including any refreshed room status or action-response payloads the view needs after a mutation.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/admin typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The admin API client can call every room recovery action the view needs.
    - The helper signatures line up with the extended room status types.
  </acceptance_criteria>
  <done>Admin code has typed helpers for the recovery operations the view will render.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Rebuild the Rooms page as a recovery console</name>
  <files>apps/admin/src/rooms/RoomStatusView.tsx, apps/admin/src/App.css, apps/admin/src/test/room-status.test.tsx</files>
  <read_first>
    - apps/admin/src/rooms/RoomStatusView.tsx
    - apps/admin/src/App.css
    - apps/admin/src/test/room-status.test.tsx
    - apps/admin/src/api/client.ts
    - apps/admin/src/rooms/types.ts
    - .planning/phases/05-online-supplement-recovery/05-CONTEXT.md
  </read_first>
  <behavior>
    - The Rooms page shows room state, current song, queue, TV online, controller count, recent events, and online task summary together.
    - Retry, clean, refresh-room, refresh-pairing, and promote actions live on task rows or task detail, not as a prominent ready-resource promote button.
    - The layout stays dense and operational rather than card-heavy or explanatory.
    - Refresh actions remain visible and straightforward so operators can recover the room without leaving the page.
  </behavior>
  <action>
    Rework `apps/admin/src/rooms/RoomStatusView.tsx` into a recovery-focused surface that uses the new client helpers and the richer room status payload to show room state, current playback, queue, live TV presence, controller count, recent events, and the online task summary in one place.

    Update `apps/admin/src/App.css` so the layout reads like an ops console: full-width sections, compact task rows, clear state chips, and no prominent ready-resource promote button on the Rooms page itself.

    Extend `apps/admin/src/test/room-status.test.tsx` so the new recovery fields, action buttons, and no-prominent-promote constraint are all covered.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/admin test -- room-status</automated>
    <automated>pnpm -F @home-ktv/admin typecheck</automated>
  </verify>
  <acceptance_criteria>
    - The Rooms page presents the room recovery information in a single operational view.
    - Task actions are task-scoped, not a top-level promote button.
    - The room status test covers the new surface and the no-prominent-promote constraint.
  </acceptance_criteria>
  <done>Admin operators can inspect room recovery state and act on tasks from a focused recovery console.</done>
</task>

</tasks>

<verification>
Run:
- `pnpm -F @home-ktv/api test -- admin-room-status`
- `pnpm -F @home-ktv/api typecheck`
- `pnpm -F @home-ktv/admin test -- room-status`
- `pnpm -F @home-ktv/admin typecheck`
</verification>

<success_criteria>
- Admin rooms show room state, current song, queue, TV online, controller count, recent events, and online task summary.
- Room recovery actions are available without exposing a prominent ready promote button on the Rooms page.
- The admin UI can refresh, repair, and inspect the room and task lifecycle from one recovery view.
</success_criteria>

<output>
After completion, create `.planning/phases/05-online-supplement-recovery/05-03-SUMMARY.md`
</output>
