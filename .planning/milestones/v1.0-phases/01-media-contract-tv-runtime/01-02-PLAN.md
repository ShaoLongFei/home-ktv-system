---
phase: 01-media-contract-tv-runtime
plan: 02
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - apps/api/src/db/schema.ts
  - "apps/api/src/db/migrations/*"
  - apps/api/src/modules/catalog/repositories/song-repository.ts
  - apps/api/src/modules/catalog/repositories/asset-repository.ts
  - apps/api/src/modules/rooms/repositories/room-repository.ts
  - apps/api/src/modules/playback/repositories/playback-session-repository.ts
  - apps/api/src/modules/playback/repositories/playback-event-repository.ts
  - apps/api/src/modules/assets/asset-gateway.ts
  - apps/api/src/modules/assets/media-path-resolver.ts
  - apps/api/src/modules/playback/build-playback-target.ts
  - apps/api/src/modules/playback/build-switch-target.ts
  - apps/api/src/routes/media.ts
  - apps/api/src/test/build-playback-target.test.ts
  - apps/api/src/test/build-switch-target.test.ts
  - packages/domain/src/index.ts
  - packages/player-contracts/src/index.ts
autonomous: true
requirements:
  - LIBR-03
  - PLAY-07
user_setup: []
must_haves:
  truths:
    - "Backend persists canonical `Song`/`Asset`/`Room`/`QueueEntry`/`PlaybackSession` records with explicit switch metadata instead of raw file-path truth."
    - "Playback and switch target builders expose controlled media URLs and never leak NAS paths directly to TV clients."
    - "A switch target only exists when the current asset has a same-family counterpart that is explicitly marked ready and verified."
  artifacts:
    - "Database schema exists for `songs`, `assets`, `rooms`, `queue_entries`, `playback_sessions`, `device_sessions`, and `playback_events`."
    - "API media route exists for controlled asset serving."
    - "Tests exist for playback target construction and switch target rejection cases."
  key_links:
    - "`build-playback-target.ts` reads persisted room/session state and emits `PlaybackTarget` from `@home-ktv/player-contracts`."
    - "`build-switch-target.ts` requires exact `switchFamily`, `vocalMode`, and verified readiness state from the `assets` table."
    - "`media.ts` maps `assetId` to controlled responses through `asset-gateway.ts` instead of returning raw filesystem locations."
---

<objective>
落地 Phase 1 的核心媒体模型、受控资源访问层和播放/切换目标构造器。

Purpose: 把“用户点的是歌、播放器播的是资源、切换靠同族资源对”这件事写进数据库和后端代码，而不是只停留在文档。
Output: 数据库 schema、受控媒体路由、播放目标构造器、切换目标构造器和相应测试。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-media-contract-tv-runtime/01-CONTEXT.md
@.planning/phases/01-media-contract-tv-runtime/01-RESEARCH.md
@.planning/phases/01-media-contract-tv-runtime/01-01-PLAN.md
@docs/KTV-ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define the canonical Postgres schema for playback and switchable assets</name>
  <files>apps/api/src/db/schema.ts, apps/api/src/db/migrations/*, packages/domain/src/index.ts</files>
  <read_first>packages/domain/src/index.ts, .planning/phases/01-media-contract-tv-runtime/01-CONTEXT.md, docs/KTV-ARCHITECTURE.md</read_first>
  <action>Create a concrete database schema containing exact tables `songs`, `assets`, `rooms`, `queue_entries`, `device_sessions`, `playback_sessions`, and `playback_events`. Ensure `assets` has exact columns for `song_id`, `source_type`, `asset_kind`, `file_path`, `duration_ms`, `lyric_mode`, `vocal_mode`, `status`, `switch_family`, and `switch_quality_status`; ensure `rooms` seeds a default row with `slug = "living-room"`; ensure `playback_sessions` has exact columns `room_id`, `current_queue_entry_id`, `active_asset_id`, `target_vocal_mode`, `player_state`, `player_position_ms`, `next_queue_entry_id`, and `version`. Update shared domain types so the TypeScript contract matches the persisted schema exactly.</action>
  <verify>`pnpm --filter @home-ktv/api typecheck`</verify>
  <acceptance_criteria>
    - `apps/api/src/db/schema.ts` contains table names `songs`, `assets`, `rooms`, `queue_entries`, `device_sessions`, `playback_sessions`, and `playback_events`
    - `apps/api/src/db/schema.ts` contains the strings `switch_family` and `switch_quality_status`
    - `apps/api/src/db/schema.ts` seeds or references `living-room`
  </acceptance_criteria>
  <done>The canonical media/playback schema exists and carries the exact switch metadata Phase 1 runtime needs.</done>
</task>

<task type="auto">
  <name>Task 2: Build the controlled media gateway and playback target services</name>
  <files>apps/api/src/modules/assets/asset-gateway.ts, apps/api/src/modules/assets/media-path-resolver.ts, apps/api/src/modules/playback/build-playback-target.ts, apps/api/src/modules/playback/build-switch-target.ts, apps/api/src/routes/media.ts, packages/player-contracts/src/index.ts</files>
  <read_first>apps/api/src/db/schema.ts, packages/player-contracts/src/index.ts, .planning/phases/01-media-contract-tv-runtime/01-RESEARCH.md, docs/KTV-ARCHITECTURE.md</read_first>
  <action>Implement a controlled media route pattern `GET /media/:assetId` that resolves `assetId` to a backend-served file stream and never exposes a raw NAS path to the client. In `build-playback-target.ts`, construct `PlaybackTarget` with exact fields `roomId`, `sessionVersion`, `queueEntryId`, `assetId`, `playbackUrl`, `resumePositionMs`, `vocalMode`, `switchFamily`, and `nextQueueEntryPreview`. In `build-switch-target.ts`, return a `SwitchTarget` only when the current asset has exactly one counterpart with the same `switch_family`, a different `vocal_mode`, `status = ready`, and `switch_quality_status = verified`; include `rollbackAssetId` and `resumePositionMs` in the returned target.</action>
  <verify>`pnpm --filter @home-ktv/api build`</verify>
  <acceptance_criteria>
    - `apps/api/src/routes/media.ts` contains the route path `/media/:assetId`
    - `apps/api/src/modules/playback/build-playback-target.ts` contains `resumePositionMs` and `nextQueueEntryPreview`
    - `apps/api/src/modules/playback/build-switch-target.ts` contains `switch_quality_status = verified` or equivalent exact verified-state check
  </acceptance_criteria>
  <done>TV clients can fetch assets through a controlled gateway and receive deterministic playback/switch targets from backend code.</done>
</task>

<task type="auto">
  <name>Task 3: Prove switch target correctness with explicit tests</name>
  <files>apps/api/src/test/build-playback-target.test.ts, apps/api/src/test/build-switch-target.test.ts</files>
  <read_first>apps/api/src/modules/playback/build-playback-target.ts, apps/api/src/modules/playback/build-switch-target.ts, .planning/phases/01-media-contract-tv-runtime/01-CONTEXT.md</read_first>
  <action>Create tests covering exact cases: a verified original/instrumental pair in the same `switch_family` produces a switch target; a missing counterpart returns no switch target; a wrong-family counterpart returns no switch target; a `switch_quality_status = review_required` asset returns no switch target; and a playback target for `living-room` includes the current asset URL plus the next queue preview when present.</action>
  <verify>`pnpm --filter @home-ktv/api test -- build-playback-target build-switch-target`</verify>
  <acceptance_criteria>
    - `apps/api/src/test/build-switch-target.test.ts` contains test cases for `review_required` and wrong-family rejection
    - `apps/api/src/test/build-playback-target.test.ts` contains `living-room`
    - test command exits 0
  </acceptance_criteria>
  <done>The backend media contract is test-proven and rejects invalid switch candidates instead of letting runtime code guess.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck`
- [ ] `pnpm --filter @home-ktv/api test -- build-playback-target build-switch-target`
- [ ] `pnpm --filter @home-ktv/api build`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Controlled media URLs exist for playback
- Switch targets are derived from explicit verified asset metadata rather than filename heuristics
</success_criteria>

<output>
After completion, create `.planning/phases/01-media-contract-tv-runtime/01-02-SUMMARY.md`
</output>
