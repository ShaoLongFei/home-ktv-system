---
phase: 16-policy-seam-android-reservation-and-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/test/seed-demo-song.test.ts
  - apps/api/src/test/online-candidate-task.test.ts
  - apps/api/src/test/admin-online-task-actions.test.ts
  - apps/api/src/test/room-queue-commands.test.ts
  - apps/admin/src/test/song-catalog.test.tsx
autonomous: true
requirements: [HARD-03]
must_haves:
  truths:
    - "Demo song generation still creates, approves, and exposes two distinct demo songs after the real-MV schema exists."
    - "Online supplement tasks still transition room-scoped lifecycle states without being affected by real-MV catalog changes."
    - "Legacy queue commands and Admin catalog maintenance remain usable alongside real-MV assets."
  artifacts:
    - path: "apps/api/src/test/seed-demo-song.test.ts"
      provides: "demo song generation regression"
      contains: "Demo Song Sunrise"
    - path: "apps/api/src/test/room-queue-commands.test.ts"
      provides: "legacy queue command regression"
      contains: "switch-vocal-mode"
    - path: "apps/admin/src/test/song-catalog.test.tsx"
      provides: "Admin song maintenance regression"
      contains: "切换质量: verified"
  key_links:
    - from: "seed-demo-song flow"
      to: "available songs after approval"
      via: "scan -> approve -> available songs"
      pattern: "availableSongsCalls"
    - from: "room queue command service"
      to: "legacy add/delete/undo/promote/skip commands"
      via: "existing song assets and queue entries"
      pattern: "switch-vocal-mode"
---

<objective>
Keep legacy demo/local/online/Admin flows stable after the real-MV schema is in place.

Purpose: Phase 16 must harden the new real-MV work without regressing the original product flows that already ship to users. This plan adds targeted compatibility assertions to the existing test suites rather than widening the runtime surface.
Output: the demo song pipeline, online supplement task lifecycle, room queue commands, and Admin song maintenance all continue to pass after the schema hardening work.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-policy-seam-android-reservation-and-hardening/16-CONTEXT.md
@.planning/phases/16-policy-seam-android-reservation-and-hardening/16-RESEARCH.md
@apps/api/src/test/seed-demo-song.test.ts
@apps/api/src/test/online-candidate-task.test.ts
@apps/api/src/test/admin-online-task-actions.test.ts
@apps/api/src/test/room-queue-commands.test.ts
@apps/admin/src/test/song-catalog.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Keep demo/local/online queue compatibility green after real-MV schema changes</name>
  <files>apps/api/src/test/seed-demo-song.test.ts, apps/api/src/test/online-candidate-task.test.ts, apps/api/src/test/admin-online-task-actions.test.ts, apps/api/src/test/room-queue-commands.test.ts</files>
  <read_first>
    - apps/api/src/test/seed-demo-song.test.ts
    - apps/api/src/test/online-candidate-task.test.ts
    - apps/api/src/test/admin-online-task-actions.test.ts
    - apps/api/src/test/room-queue-commands.test.ts
    - apps/api/src/modules/playback/session-command-service.ts
    - apps/api/src/modules/online/candidate-task-service.ts
  </read_first>
  <behavior>
    - Demo song generation still waits for scan, approves both demo songs, and verifies availability.
    - Online supplement tasks still expose the request-supplement entry and normal lifecycle transitions.
    - Legacy queue commands still accept add/delete/undo/promote/skip and keep `switch-vocal-mode` working for non-real-MV assets.
    - Admin online task actions stay room-scoped and do not append queue entries.
  </behavior>
  <action>
    In `apps/api/src/test/seed-demo-song.test.ts`, keep the current regression and add one assertion that the generated songs remain exactly `["Demo Song Sunrise", "Demo Song Night Drive"]` after scan/approval, and that `state.availableSongsCalls` is at least `2`.

    In `apps/api/src/test/online-candidate-task.test.ts`, add one assertion that the request-supplement entry stays visible with `label: "请求补歌"` and a candidate card still reports `taskState: "discovered"` after the real-MV schema exists.

    In `apps/api/src/test/admin-online-task-actions.test.ts`, keep the room-scoped retry/purge/promote checks and add one assertion that `promoteTask` does not append any queue entry mutation when the task is promoted.

    In `apps/api/src/test/room-queue-commands.test.ts`, add one regression case showing a legacy song can still:
    - be queued from `add-queue-entry`,
    - be deleted and undone,
    - be promoted,
    - use `switch-vocal-mode` through the existing accepted/rejected validation paths,
    while the harness also contains a real-MV asset. Do not change the command semantics or introduce any real-MV-specific branch in the legacy flow.
  </action>
  <acceptance_criteria>
    - `pnpm -F @home-ktv/api test -- src/test/seed-demo-song.test.ts src/test/online-candidate-task.test.ts src/test/admin-online-task-actions.test.ts src/test/room-queue-commands.test.ts` exits 0.
    - `rg -n "Demo Song Sunrise|Demo Song Night Drive|请求补歌|switch-vocal-mode" apps/api/src/test/seed-demo-song.test.ts apps/api/src/test/online-candidate-task.test.ts apps/api/src/test/admin-online-task-actions.test.ts apps/api/src/test/room-queue-commands.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/seed-demo-song.test.ts src/test/online-candidate-task.test.ts src/test/admin-online-task-actions.test.ts src/test/room-queue-commands.test.ts</automated>
  </verify>
  <done>Legacy demo/local/online queue behavior remains stable after real-MV schema hardening.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep Admin song maintenance regression green alongside real-MV assets</name>
  <files>apps/admin/src/test/song-catalog.test.tsx</files>
  <read_first>
    - apps/admin/src/test/song-catalog.test.tsx
    - apps/admin/src/songs/SongCatalogView.tsx
    - apps/admin/src/songs/use-song-catalog-runtime.ts
    - apps/api/src/test/admin-catalog-routes.test.ts
  </read_first>
  <behavior>
    - The Admin catalog still renders legacy song metadata and resource controls.
    - Real-MV schema fields do not remove or rename the existing song maintenance labels.
    - Admin still shows `切换质量: verified`, `默认资源`, and the legacy song edit controls.
  </behavior>
  <action>
    Extend `apps/admin/src/test/song-catalog.test.tsx` with one compatibility regression that renders the catalog after the real-MV schema exists and still finds:
    - the `歌曲目录` view;
    - the legacy song row for `七里香`;
    - the `默认资源` selector;
    - `切换质量: verified`;
    - the existing metadata edit and default-asset controls.

    Keep the assertions product-focused and do not add any real-MV-specific admin controls to this legacy catalog suite.
  </action>
  <acceptance_criteria>
    - `pnpm -F @home-ktv/admin test -- src/test/song-catalog.test.tsx` exits 0.
    - `rg -n "切换质量: verified|默认资源|歌曲目录" apps/admin/src/test/song-catalog.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/admin test -- src/test/song-catalog.test.tsx</automated>
  </verify>
  <done>Admin song maintenance keeps working unchanged after the real-MV schema lands.</done>
</task>

</tasks>

<verification>
Run:
- `pnpm -F @home-ktv/api test -- src/test/seed-demo-song.test.ts src/test/online-candidate-task.test.ts src/test/admin-online-task-actions.test.ts src/test/room-queue-commands.test.ts`
- `pnpm -F @home-ktv/admin test -- src/test/song-catalog.test.tsx`
</verification>

<success_criteria>
- HARD-03 is covered by unchanged legacy demo/local/online/Admin flows.
- Real-MV schema changes do not change queue command semantics or task lifecycle semantics.
- Admin maintenance still exposes the legacy song editing and default-asset surface.
</success_criteria>

<output>
After completion, create `.planning/phases/16-policy-seam-android-reservation-and-hardening/16-02-SUMMARY.md`.
</output>
