---
phase: 12-contract-schema-and-playback-risk-spike
plan: 04
type: execute
wave: 3
depends_on: [12-02, 12-03]
files_modified:
  - packages/player-contracts/src/index.ts
  - apps/api/src/modules/playback/build-playback-target.ts
  - apps/api/src/test/build-playback-target.test.ts
autonomous: true
requirements: [MEDIA-04, MEDIA-02]
must_haves:
  truths:
    - "Backend-authored playback targets expose playbackProfile and selectedTrackRef for real-MV assets."
    - "The TV runtime can receive selected audio-track intent explicitly instead of inferring it from asset metadata."
    - "Legacy separate-asset playback targets remain valid with a separate_asset_pair playback profile and selectedTrackRef null."
  artifacts:
    - path: "packages/player-contracts/src/index.ts"
      provides: "PlaybackTarget contract extension"
      contains: "selectedTrackRef"
    - path: "apps/api/src/modules/playback/build-playback-target.ts"
      provides: "Backend construction of playbackProfile and selectedTrackRef"
      contains: "buildPlaybackProfileForAsset"
    - path: "apps/api/src/test/build-playback-target.test.ts"
      provides: "Playback target regression coverage for real-MV track selection"
      contains: "single_file_audio_tracks"
  key_links:
    - from: "apps/api/src/modules/playback/build-playback-target.ts"
      to: "packages/player-contracts/src/index.ts"
      via: "PlaybackTarget fields"
      pattern: "playbackProfile|selectedTrackRef"
    - from: "apps/api/src/modules/playback/build-playback-target.ts"
      to: "packages/domain/src/index.ts"
      via: "Asset.trackRoles and Asset.playbackProfile"
      pattern: "trackRoles|PlaybackProfile"
---

<objective>
Extend the backend-authored player payload with platform-neutral playback profile and explicit selected track reference.

Purpose: satisfy the Phase 12 playback boundary without implementing full TV audio-track switching.
Output: player contract fields and backend target construction for both legacy pair assets and single-file real-MV assets.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-contract-schema-and-playback-risk-spike/12-CONTEXT.md
@.planning/phases/12-contract-schema-and-playback-risk-spike/12-RESEARCH.md
@packages/domain/src/index.ts
@packages/player-contracts/src/index.ts
@apps/api/src/modules/playback/build-playback-target.ts
@apps/api/src/test/build-playback-target.test.ts
</context>

<interfaces>
Existing PlaybackTarget before this plan:

```typescript
export interface PlaybackTarget {
  roomId: RoomId;
  sessionVersion: number;
  queueEntryId: QueueEntryId;
  assetId: AssetId;
  currentQueueEntryPreview: QueueEntryPreview;
  playbackUrl: string;
  resumePositionMs: number;
  vocalMode: VocalMode;
  switchFamily: SwitchFamily | null;
  nextQueueEntryPreview: QueueEntryPreview | null;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend PlaybackTarget with platform-neutral fields</name>
  <files>packages/player-contracts/src/index.ts, apps/api/src/test/build-playback-target.test.ts</files>
  <read_first>
    - packages/player-contracts/src/index.ts
    - packages/domain/src/index.ts
    - apps/api/src/test/build-playback-target.test.ts
    - .planning/phases/12-contract-schema-and-playback-risk-spike/12-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: a playback target can carry a PlaybackProfile.
    - Test 2: a playback target can carry a selected TrackRef.
    - Test 3: no Android-specific contract field appears in player contracts.
  </behavior>
  <action>
    In `packages/player-contracts/src/index.ts`, import `PlaybackProfile` and `TrackRef` from `@home-ktv/domain`.

    Extend `PlaybackTarget` with:
    - `playbackProfile?: PlaybackProfile;`
    - `selectedTrackRef?: TrackRef | null;`

    These fields are optional at the TypeScript boundary to preserve older snapshot fixtures during Phase 12, but every backend-authored target in `build-playback-target.ts` must emit both fields after Task 2. Do not add `android`, `media3`, `exo`, package names, device names, or native-app fields.
  </action>
  <acceptance_criteria>
    - `rg -n 'PlaybackProfile' packages/player-contracts/src/index.ts` exits 0.
    - `rg -n 'TrackRef' packages/player-contracts/src/index.ts` exits 0.
    - `rg -n 'playbackProfile\\?: PlaybackProfile' packages/player-contracts/src/index.ts` exits 0.
    - `rg -n 'selectedTrackRef\\?: TrackRef \\| null' packages/player-contracts/src/index.ts` exits 0.
    - `rg -n 'android|media3|exo' packages/player-contracts/src/index.ts` exits 1.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/player-contracts typecheck</automated>
  </verify>
  <done>`PlaybackTarget` exposes platform-neutral playback profile and selected audio-track intent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build playbackProfile and selectedTrackRef from persisted asset fields</name>
  <files>apps/api/src/modules/playback/build-playback-target.ts, apps/api/src/test/build-playback-target.test.ts</files>
  <read_first>
    - apps/api/src/modules/playback/build-playback-target.ts
    - apps/api/src/test/build-playback-target.test.ts
    - apps/api/src/modules/catalog/repositories/asset-repository.ts
    - packages/domain/src/index.ts
    - packages/player-contracts/src/index.ts
  </read_first>
  <behavior>
    - Test 1: legacy video asset target has playbackProfile.kind `separate_asset_pair` and selectedTrackRef null.
    - Test 2: real-MV dual-track asset with `vocalMode: "instrumental"` returns playbackProfile.kind `single_file_audio_tracks` and selectedTrackRef equal to `trackRoles.instrumental`.
    - Test 3: real-MV dual-track asset with `vocalMode: "original"` returns selectedTrackRef equal to `trackRoles.original`.
  </behavior>
  <action>
    In `apps/api/src/modules/playback/build-playback-target.ts`:
    - Import `PlaybackProfile` and `TrackRef` from `@home-ktv/domain`.
    - Add helper `buildPlaybackProfileForAsset(asset: Asset): PlaybackProfile`.
    - If `asset.playbackProfile` exists, return it.
    - Otherwise return:
      `{ kind: "separate_asset_pair", container: asset.mediaInfoSummary?.container ?? null, videoCodec: asset.mediaInfoSummary?.videoCodec ?? null, audioCodecs: asset.mediaInfoSummary?.audioTracks.map((track) => track.codec).filter((codec): codec is string => Boolean(codec)) ?? [], requiresAudioTrackSelection: false }`
    - Add helper `selectedTrackRefForAsset(asset: Asset): TrackRef | null`.
    - For `asset.vocalMode === "original"`, return `asset.trackRoles?.original ?? null`.
    - For `asset.vocalMode === "instrumental"`, return `asset.trackRoles?.instrumental ?? null`.
    - For all other vocal modes, return null.
    - In both `buildPlaybackTarget` and `buildPlaybackTargetFromResolvedState`, set `playbackProfile: buildPlaybackProfileForAsset(asset)` and `selectedTrackRef: selectedTrackRefForAsset(asset)`.

    Update `apps/api/src/test/build-playback-target.test.ts`:
    - Existing test expected object must include `playbackProfile: { kind: "separate_asset_pair", container: null, videoCodec: null, audioCodecs: [], requiresAudioTrackSelection: false }` and `selectedTrackRef: null`.
    - Add a new test named `selects instrumental track ref for single-file real MV playback` with one asset: `assetKind: "dual-track-video"`, `vocalMode: "instrumental"`, `trackRoles.original.index = 0`, `trackRoles.instrumental.index = 1`, `playbackProfile.kind = "single_file_audio_tracks"`. Assert target.selectedTrackRef equals the instrumental TrackRef and target.assetId is the same single asset id.
  </action>
  <acceptance_criteria>
    - `rg -n 'function buildPlaybackProfileForAsset' apps/api/src/modules/playback/build-playback-target.ts` exits 0.
    - `rg -n 'function selectedTrackRefForAsset' apps/api/src/modules/playback/build-playback-target.ts` exits 0.
    - `rg -n 'playbackProfile: buildPlaybackProfileForAsset\\(asset\\)' apps/api/src/modules/playback/build-playback-target.ts` exits 0.
    - `rg -n 'selectedTrackRef: selectedTrackRefForAsset\\(asset\\)' apps/api/src/modules/playback/build-playback-target.ts` exits 0.
    - `rg -n 'selects instrumental track ref for single-file real MV playback' apps/api/src/test/build-playback-target.test.ts` exits 0.
    - `rg -n 'single_file_audio_tracks' apps/api/src/test/build-playback-target.test.ts` exits 0.
    - `rg -n 'asset-original.*asset-instrumental' apps/api/src/test/build-playback-target.test.ts` exits 1 for the new real-MV test block.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/build-playback-target.test.ts && pnpm -F @home-ktv/api typecheck && pnpm -F @home-ktv/player-contracts typecheck</automated>
  </verify>
  <done>Backend playback targets always include playbackProfile and selectedTrackRef, with real-MV track intent carried explicitly to consumers.</done>
</task>

</tasks>

<verification>
Run `pnpm -F @home-ktv/api test -- src/test/build-playback-target.test.ts`, `pnpm -F @home-ktv/api typecheck`, and `pnpm -F @home-ktv/player-contracts typecheck`.
</verification>

<success_criteria>
- `PlaybackTarget` exposes `playbackProfile` and `selectedTrackRef`.
- Current backend target construction emits both fields for legacy and real-MV assets.
- No Android-specific player contract fields are introduced.
- Full TV switching integration remains out of scope.
</success_criteria>

<output>
After completion, create `.planning/phases/12-contract-schema-and-playback-risk-spike/12-04-SUMMARY.md`.
</output>
