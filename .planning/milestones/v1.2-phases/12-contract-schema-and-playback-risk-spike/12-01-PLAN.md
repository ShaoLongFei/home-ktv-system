---
phase: 12-contract-schema-and-playback-risk-spike
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/domain/src/index.ts
  - apps/api/src/test/real-mv-domain-contracts.test.ts
autonomous: true
requirements: [MEDIA-01, MEDIA-02, MEDIA-03, MEDIA-04]
must_haves:
  truths:
    - "One physical MKV/MPG/MPEG file can be represented by one Asset identity, not separate original and instrumental Asset rows."
    - "Compatibility readiness uses exactly one compatibilityStatus value from unknown, review_required, playable, unsupported."
    - "Original and instrumental roles are represented by trackRoles.original and trackRoles.instrumental TrackRef objects."
    - "TrackRef carries index, id, and label for runtime selection plus human review."
  artifacts:
    - path: "packages/domain/src/index.ts"
      provides: "Real-MV domain contract types and Asset/ImportCandidateFile fields"
      contains: "export type CompatibilityStatus"
    - path: "apps/api/src/test/real-mv-domain-contracts.test.ts"
      provides: "Compile-time and runtime contract assertions for Phase 12 decisions"
      contains: "single real-MV asset"
  key_links:
    - from: "packages/domain/src/index.ts"
      to: "apps/api/src/test/real-mv-domain-contracts.test.ts"
      via: "imported shared domain types"
      pattern: "TrackRoles|CompatibilityStatus|PlaybackProfile"
---

<objective>
Define the shared real-MV domain vocabulary that every later Phase 12 plan implements against.

Purpose: lock the Phase 12 decisions into TypeScript contracts before schema, probe, or player code consumes them.
Output: exported domain types for compatibility status, reasons, MediaInfo summary/provenance, track refs, track roles, and platform-neutral playback profiles.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-contract-schema-and-playback-risk-spike/12-CONTEXT.md
@.planning/phases/12-contract-schema-and-playback-risk-spike/12-RESEARCH.md
@packages/domain/src/index.ts
@apps/api/src/test/library-ingest-schema.test.ts
</context>

<interfaces>
Current source of truth before this plan:

```typescript
export type AssetKind = "video" | "audio+lyrics" | "dual-track-video";
export type VocalMode = "original" | "instrumental" | "dual" | "unknown";
export type SwitchQualityStatus = "verified" | "review_required" | "rejected" | "unknown";
export interface Asset {
  id: AssetId;
  songId: SongId;
  sourceType: AssetSourceType;
  assetKind: AssetKind;
  displayName: string;
  filePath: string;
  durationMs: number;
  lyricMode: LyricMode;
  vocalMode: VocalMode;
  status: AssetStatus;
  switchFamily: SwitchFamily | null;
  switchQualityStatus: SwitchQualityStatus;
  createdAt: string;
  updatedAt: string;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Phase 12 real-MV domain contract types</name>
  <files>packages/domain/src/index.ts, apps/api/src/test/real-mv-domain-contracts.test.ts</files>
  <read_first>
    - packages/domain/src/index.ts
    - apps/api/src/test/library-ingest-schema.test.ts
    - .planning/phases/12-contract-schema-and-playback-risk-spike/12-CONTEXT.md
    - .planning/phases/12-contract-schema-and-playback-risk-spike/12-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: compatibilityStatuses is exactly ["unknown", "review_required", "playable", "unsupported"].
    - Test 2: TrackRef requires index, id, and label.
    - Test 3: MediaInfoSummary includes container, durationMs, videoCodec, resolution, fileSizeBytes, and audioTracks.
    - Test 4: PlaybackProfile contains only platform-neutral fields and no Android-specific names.
  </behavior>
  <action>
    In `packages/domain/src/index.ts`, export these exact contracts:
    - `export const compatibilityStatuses = ["unknown", "review_required", "playable", "unsupported"] as const;`
    - `export type CompatibilityStatus = (typeof compatibilityStatuses)[number];`
    - `export type CompatibilityReasonSeverity = "warning" | "error";`
    - `export type CompatibilityReasonSource = "probe" | "runtime_spike" | "review" | "scanner";`
    - `export interface CompatibilityReason { code: string; severity: CompatibilityReasonSeverity; message: string; source: CompatibilityReasonSource; }`
    - `export interface TrackRef { index: number; id: string; label: string; }`
    - `export interface TrackRoles { original: TrackRef | null; instrumental: TrackRef | null; }`
    - `export interface VideoResolution { width: number; height: number; }`
    - `export interface AudioTrackSummary extends TrackRef { language: string | null; codec: string | null; channels: number | null; }`
    - `export interface MediaInfoSummary { container: string | null; durationMs: number | null; videoCodec: string | null; resolution: VideoResolution | null; fileSizeBytes: number; audioTracks: readonly AudioTrackSummary[]; }`
    - `export interface MediaInfoProvenance { source: "ffprobe" | "mediainfo" | "manual" | "unknown"; sourceVersion: string | null; probedAt: string | null; importedFrom: string | null; }`
    - `export interface PlaybackProfile { kind: "separate_asset_pair" | "single_file_audio_tracks"; container: string | null; videoCodec: string | null; audioCodecs: readonly string[]; requiresAudioTrackSelection: boolean; }`

    Add `apps/api/src/test/real-mv-domain-contracts.test.ts` with Vitest tests that import these names from `@home-ktv/domain`, assert the exact status array, create a `TrackRoles` object with `original` and `instrumental` refs, create a `MediaInfoSummary` with two audio tracks, and assert `JSON.stringify(playbackProfile).toLowerCase()` does not contain `android`, `exo`, or `media3`.

    This implements D-01 through D-03, D-05, D-06, D-08, D-09, and D-13. Do not add a fifth compatibility value for "ingestable"; ingest visibility remains derived from candidate/probe state.
  </action>
  <acceptance_criteria>
    - `rg -n 'export const compatibilityStatuses = \\["unknown", "review_required", "playable", "unsupported"\\] as const' packages/domain/src/index.ts` exits 0.
    - `rg -n 'export interface TrackRef' packages/domain/src/index.ts` exits 0.
    - `rg -n 'index: number' packages/domain/src/index.ts` exits 0.
    - `rg -n 'id: string' packages/domain/src/index.ts` exits 0.
    - `rg -n 'label: string' packages/domain/src/index.ts` exits 0.
    - `rg -n 'export interface MediaInfoSummary' packages/domain/src/index.ts` exits 0.
    - `rg -n 'fileSizeBytes: number' packages/domain/src/index.ts` exits 0.
    - `rg -n 'kind: "separate_asset_pair" \\| "single_file_audio_tracks"' packages/domain/src/index.ts` exits 0.
    - `rg -n 'android|exo|media3' packages/domain/src/index.ts` exits 1.
    - `rg -n 'compatibilityStatuses' apps/api/src/test/real-mv-domain-contracts.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/domain typecheck && pnpm -F @home-ktv/api test -- src/test/real-mv-domain-contracts.test.ts</automated>
  </verify>
  <done>Shared domain exports contain the exact Phase 12 status values and platform-neutral media/track contracts, with tests preventing two-track refs from collapsing to raw indexes only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Attach real-MV metadata fields to Asset and candidate file contracts</name>
  <files>packages/domain/src/index.ts, apps/api/src/test/real-mv-domain-contracts.test.ts</files>
  <read_first>
    - packages/domain/src/index.ts
    - apps/api/src/test/real-mv-domain-contracts.test.ts
    - .planning/phases/12-contract-schema-and-playback-risk-spike/12-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: a single `Asset` can carry `trackRoles.original` and `trackRoles.instrumental` while `assetKind` remains `"dual-track-video"`.
    - Test 2: an `ImportCandidateFile` can carry the same normalized compatibility/media/track fields before admission.
    - Test 3: neither contract requires two Asset rows for one real-MV file.
  </behavior>
  <action>
    Extend `Asset` in `packages/domain/src/index.ts` with optional Phase 12 fields so legacy test fixtures can omit them while repository-backed real-MV rows can expose them:
    - `compatibilityStatus?: CompatibilityStatus;`
    - `compatibilityReasons?: readonly CompatibilityReason[];`
    - `mediaInfoSummary?: MediaInfoSummary | null;`
    - `mediaInfoProvenance?: MediaInfoProvenance | null;`
    - `trackRoles?: TrackRoles;`
    - `playbackProfile?: PlaybackProfile;`

    Extend `ImportCandidateFile` with the same optional fields:
    - `compatibilityStatus?: CompatibilityStatus;`
    - `compatibilityReasons?: readonly CompatibilityReason[];`
    - `mediaInfoSummary?: MediaInfoSummary | null;`
    - `mediaInfoProvenance?: MediaInfoProvenance | null;`
    - `trackRoles?: TrackRoles;`
    - `playbackProfile?: PlaybackProfile;`

    In `apps/api/src/test/real-mv-domain-contracts.test.ts`, add a test named `models one physical real-MV file as one asset with trackRoles` that creates exactly one Asset object for `jay/qilixiang.mkv`, sets `assetKind: "dual-track-video"`, `vocalMode: "dual"`, and `trackRoles: { original: { index: 0, id: "0x1100", label: "Original vocal" }, instrumental: { index: 1, id: "0x1101", label: "Instrumental" } }`. Assert `assets` has length 1 and the instrumental index is 1.

    This implements D-04, D-05, D-06, and D-07. Do not create any `originalAssetId`, `instrumentalAssetId`, `androidProfile`, or Android-specific fields.
  </action>
  <acceptance_criteria>
    - `rg -n 'compatibilityStatus\\?: CompatibilityStatus' packages/domain/src/index.ts` exits 0.
    - `rg -n 'trackRoles\\?: TrackRoles' packages/domain/src/index.ts` exits 0.
    - `rg -n 'playbackProfile\\?: PlaybackProfile' packages/domain/src/index.ts` exits 0.
    - `rg -n 'models one physical real-MV file as one asset with trackRoles' apps/api/src/test/real-mv-domain-contracts.test.ts` exits 0.
    - `rg -n 'assets\\)\\.toHaveLength\\(1\\)' apps/api/src/test/real-mv-domain-contracts.test.ts` exits 0.
    - `rg -n 'originalAssetId|instrumentalAssetId|androidProfile|media3|exo' packages/domain/src/index.ts apps/api/src/test/real-mv-domain-contracts.test.ts` exits 1.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/domain typecheck && pnpm -F @home-ktv/api test -- src/test/real-mv-domain-contracts.test.ts</automated>
  </verify>
  <done>`Asset` and `ImportCandidateFile` can represent a real MV as one physical file with internal track roles and platform-neutral compatibility metadata.</done>
</task>

</tasks>

<verification>
Run `pnpm -F @home-ktv/domain typecheck` and `pnpm -F @home-ktv/api test -- src/test/real-mv-domain-contracts.test.ts`.
</verification>

<success_criteria>
- `compatibilityStatus` values are exactly `unknown`, `review_required`, `playable`, `unsupported`.
- `trackRoles` is the only original/accompaniment mapping added for real-MV assets.
- `TrackRef` includes `index`, `id`, and `label`.
- Media summary/provenance and playback profile contracts are platform-neutral.
</success_criteria>

<output>
After completion, create `.planning/phases/12-contract-schema-and-playback-risk-spike/12-01-SUMMARY.md`.
</output>
