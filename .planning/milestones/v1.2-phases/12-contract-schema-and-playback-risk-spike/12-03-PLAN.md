---
phase: 12-contract-schema-and-playback-risk-spike
plan: 03
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - apps/api/src/modules/ingest/media-probe.ts
  - apps/api/src/modules/ingest/candidate-builder.ts
  - apps/api/src/modules/assets/asset-gateway.ts
  - apps/api/src/modules/media/real-mv-compatibility.ts
  - apps/api/src/test/import-scanner.test.ts
  - apps/api/src/test/real-mv-media-contracts.test.ts
autonomous: true
requirements: [MEDIA-02, MEDIA-03]
must_haves:
  truths:
    - "Probe output is normalized into a durable MediaInfo-style summary and provenance."
    - "Candidate probe summaries do not expose raw ffprobe streams or format payloads as the durable contract."
    - "MKV, MPG, and MPEG assets are served with explicit video/x-matroska or video/mpeg content types."
    - "Compatibility status is derived by one pure evaluator using the four locked status values and structured reasons."
  artifacts:
    - path: "apps/api/src/modules/ingest/media-probe.ts"
      provides: "MediaInfo-style summary/provenance extraction from ffprobe"
      contains: "mediaInfoSummary"
    - path: "apps/api/src/modules/media/real-mv-compatibility.ts"
      provides: "Pure compatibility evaluator"
      contains: "evaluateRealMvCompatibility"
    - path: "apps/api/src/modules/assets/asset-gateway.ts"
      provides: "MKV/MPG/MPEG content type inference"
      contains: "video/x-matroska"
    - path: "apps/api/src/test/real-mv-media-contracts.test.ts"
      provides: "Probe, MIME, and compatibility regression tests"
      contains: "browser-cannot-play-type"
  key_links:
    - from: "apps/api/src/modules/ingest/media-probe.ts"
      to: "packages/domain/src/index.ts"
      via: "MediaInfoSummary and MediaInfoProvenance return fields"
      pattern: "MediaInfoSummary|MediaInfoProvenance"
    - from: "apps/api/src/modules/media/real-mv-compatibility.ts"
      to: "packages/domain/src/index.ts"
      via: "CompatibilityStatus and CompatibilityReason"
      pattern: "CompatibilityStatus|CompatibilityReason"
---

<objective>
Normalize media probe facts and derive compatibility risk without turning raw probe payloads into catalog/player contracts.

Purpose: make playback risk explicit and testable before scanner sidecars, Admin review, or full playback switching are implemented.
Output: probe summary/provenance helpers, MIME inference for real MV containers, pure compatibility evaluator, and focused tests.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-contract-schema-and-playback-risk-spike/12-CONTEXT.md
@.planning/phases/12-contract-schema-and-playback-risk-spike/12-RESEARCH.md
@packages/domain/src/index.ts
@apps/api/src/modules/ingest/media-probe.ts
@apps/api/src/modules/ingest/candidate-builder.ts
@apps/api/src/modules/assets/asset-gateway.ts
@apps/api/src/test/import-scanner.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Normalize ffprobe facts into standard summary and provenance</name>
  <files>apps/api/src/modules/ingest/media-probe.ts, apps/api/src/modules/ingest/candidate-builder.ts, apps/api/src/test/import-scanner.test.ts, apps/api/src/test/real-mv-media-contracts.test.ts</files>
  <read_first>
    - apps/api/src/modules/ingest/media-probe.ts
    - apps/api/src/modules/ingest/candidate-builder.ts
    - apps/api/src/test/import-scanner.test.ts
    - packages/domain/src/index.ts
    - .planning/phases/12-contract-schema-and-playback-risk-spike/12-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: a fake ffprobe payload with two audio streams becomes a MediaInfoSummary with two AudioTrackSummary entries.
    - Test 2: each audio track has index, id, label, language, codec, and channels.
    - Test 3: CandidateBuilder probeSummary contains mediaInfoSummary and mediaInfoProvenance but not raw `streams` or `format`.
  </behavior>
  <action>
    In `apps/api/src/modules/ingest/media-probe.ts`:
    - Import `MediaInfoProvenance` and `MediaInfoSummary`.
    - Extend `FfprobeStream` with `index?: number`, `id?: string`, `channels?: number`, and `tags?: Record<string, string>`.
    - Extend `MediaProbeSummary` with `mediaInfoSummary: MediaInfoSummary` and `mediaInfoProvenance: MediaInfoProvenance`; keep `raw: Record<string, unknown>` only as an implementation/debug field for `import_files.probe_payload`.
    - Add exported pure function `buildMediaInfoSummaryFromFfprobe(input)` that accepts `{ payload: FfprobePayload; fileSizeBytes: number; filePath: string; probedAt: string; sourceVersion?: string | null }` and returns `{ mediaInfoSummary, mediaInfoProvenance }`.
    - TrackRef id rule: use `stream.id` when it is a non-empty string; otherwise use `stream-${stream.index ?? fallbackIndex}`. TrackRef label rule: use `stream.tags?.title`, then `stream.tags?.handler_name`, then `Audio ${fallbackIndex + 1}`.

    In `apps/api/src/modules/ingest/candidate-builder.ts`, update `buildProbeSummary(file)` to return only:
    - `durationMs`
    - `probeStatus`
    - `mediaInfoSummary: file.probePayload.mediaInfoSummary ?? null`
    - `mediaInfoProvenance: file.probePayload.mediaInfoProvenance ?? null`

    Do not spread raw `file.probePayload` into candidate `probeSummary`.
  </action>
  <acceptance_criteria>
    - `rg -n 'mediaInfoSummary: MediaInfoSummary' apps/api/src/modules/ingest/media-probe.ts` exits 0.
    - `rg -n 'mediaInfoProvenance: MediaInfoProvenance' apps/api/src/modules/ingest/media-probe.ts` exits 0.
    - `rg -n 'buildMediaInfoSummaryFromFfprobe' apps/api/src/modules/ingest/media-probe.ts apps/api/src/test/real-mv-media-contracts.test.ts` exits 0.
    - `rg -n 'stream-\\$\\{stream.index \\?\\? fallbackIndex\\}' apps/api/src/modules/ingest/media-probe.ts` exits 0.
    - `rg -n 'mediaInfoSummary: .*probePayload' apps/api/src/modules/ingest/candidate-builder.ts` exits 0.
    - `rg -n '\\.\\.\\.file\\.probePayload' apps/api/src/modules/ingest/candidate-builder.ts` exits 1.
    - `rg -n 'streams|format' apps/api/src/test/real-mv-media-contracts.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/real-mv-media-contracts.test.ts src/test/import-scanner.test.ts</automated>
  </verify>
  <done>Probe facts are normalized into standard summary/provenance fields while raw probe payload remains outside candidate/catalog/player contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add container MIME inference and pure compatibility evaluator</name>
  <files>apps/api/src/modules/assets/asset-gateway.ts, apps/api/src/modules/media/real-mv-compatibility.ts, apps/api/src/test/real-mv-media-contracts.test.ts</files>
  <read_first>
    - apps/api/src/modules/assets/asset-gateway.ts
    - apps/api/src/routes/media.ts
    - packages/domain/src/index.ts
    - .planning/phases/12-contract-schema-and-playback-risk-spike/12-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: `.mkv` maps to `video/x-matroska`.
    - Test 2: `.mpg` and `.mpeg` map to `video/mpeg`.
    - Test 3: evaluator returns `unsupported` with `browser-cannot-play-type` when browser support is empty.
    - Test 4: evaluator returns `review_required` when instrumental track is missing.
    - Test 5: evaluator returns `playable` only when no error or warning reasons remain and canPlayType is `probably`.
  </behavior>
  <action>
    In `apps/api/src/modules/assets/asset-gateway.ts`, export `inferVideoContentType(filePath: string)` and add exact mappings:
    - `.mkv` -> `video/x-matroska`
    - `.mpg` -> `video/mpeg`
    - `.mpeg` -> `video/mpeg`
    Keep existing `.webm`, `.m4v`, and default `video/mp4` behavior.

    Create `apps/api/src/modules/media/real-mv-compatibility.ts` exporting:
    - `export interface EvaluateRealMvCompatibilityInput { summary: MediaInfoSummary; trackRoles: TrackRoles; currentWebCanPlayType: "" | "maybe" | "probably" | "unknown"; }`
    - `export interface EvaluateRealMvCompatibilityResult { compatibilityStatus: CompatibilityStatus; compatibilityReasons: CompatibilityReason[]; }`
    - `export function evaluateRealMvCompatibility(input: EvaluateRealMvCompatibilityInput): EvaluateRealMvCompatibilityResult`

    Evaluator rules:
    - If `summary.videoCodec` is null, add `{ code: "missing-video-codec", severity: "error", message: "Video codec is missing", source: "probe" }`.
    - If `summary.audioTracks.length === 0`, add `{ code: "missing-audio-tracks", severity: "error", message: "No audio tracks were detected", source: "probe" }`.
    - If `trackRoles.instrumental` is null, add `{ code: "instrumental-track-unmapped", severity: "warning", message: "Instrumental track needs review", source: "review" }`.
    - If `currentWebCanPlayType === ""`, add `{ code: "browser-cannot-play-type", severity: "error", message: "Current TV browser reports the media type unsupported", source: "runtime_spike" }`.
    - Return `unsupported` if any reason severity is `error`.
    - Return `review_required` if there are warning reasons or `currentWebCanPlayType !== "probably"`.
    - Return `playable` otherwise.
    - Never return any status outside `unknown`, `review_required`, `playable`, `unsupported`.
  </action>
  <acceptance_criteria>
    - `rg -n 'export function inferVideoContentType' apps/api/src/modules/assets/asset-gateway.ts` exits 0.
    - `rg -n 'endsWith\\("\\.mkv"\\).*video/x-matroska' apps/api/src/modules/assets/asset-gateway.ts` exits 0.
    - `rg -n 'endsWith\\("\\.mpg"\\).*endsWith\\("\\.mpeg"\\).*video/mpeg' apps/api/src/modules/assets/asset-gateway.ts` exits 0.
    - `test -f apps/api/src/modules/media/real-mv-compatibility.ts` exits 0.
    - `rg -n 'evaluateRealMvCompatibility' apps/api/src/modules/media/real-mv-compatibility.ts apps/api/src/test/real-mv-media-contracts.test.ts` exits 0.
    - `rg -n 'browser-cannot-play-type' apps/api/src/modules/media/real-mv-compatibility.ts apps/api/src/test/real-mv-media-contracts.test.ts` exits 0.
    - `rg -n 'ingestable|android|media3|exo' apps/api/src/modules/media/real-mv-compatibility.ts` exits 1.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/real-mv-media-contracts.test.ts && pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <done>Real-MV MIME and compatibility behavior is explicit, pure, and constrained to the Phase 12 status values.</done>
</task>

</tasks>

<verification>
Run `pnpm -F @home-ktv/api test -- src/test/real-mv-media-contracts.test.ts src/test/import-scanner.test.ts` and `pnpm -F @home-ktv/api typecheck`.
</verification>

<success_criteria>
- Standard media facts include container, duration, video codec, resolution, file size, and audio track details.
- Candidate summaries do not promote raw ffprobe payload as durable contract.
- MKV/MPG/MPEG content types are explicit.
- Compatibility risk is derived by one tested evaluator with structured reasons.
</success_criteria>

<output>
After completion, create `.planning/phases/12-contract-schema-and-playback-risk-spike/12-03-SUMMARY.md`.
</output>
