---
phase: 12-contract-schema-and-playback-risk-spike
plan: 06
type: execute
wave: 5
depends_on: [12-05]
files_modified:
  - .planning/phases/12-contract-schema-and-playback-risk-spike/12-PLAYBACK-RISK-SAMPLES.md
autonomous: false
requirements: [MEDIA-02, MEDIA-04]
user_setup:
  - service: real_mv_samples
    why: "Phase 12 decisions D-14 and D-15 require at least one real MKV and one real MPG/MPEG sample, preferably both with two audio tracks."
    env_vars:
      - name: REAL_MV_MKV_SAMPLE
        source: "Absolute path to a user-provided .mkv sample file"
      - name: REAL_MV_MPEG_SAMPLE
        source: "Absolute path to a user-provided .mpg or .mpeg sample file"
must_haves:
  truths:
    - "Playback-risk evidence includes one real MKV sample and one real MPG/MPEG sample."
    - "Evidence records probe facts, browser capability result, compatibility status, and switching risk message."
    - "A runtime switching failure in Phase 12 is treated as review/unsupported evidence, not as a permanent catalog ban."
  artifacts:
    - path: ".planning/phases/12-contract-schema-and-playback-risk-spike/12-PLAYBACK-RISK-SAMPLES.md"
      provides: "User-sample playback-risk evidence"
      contains: "MKV sample"
  key_links:
    - from: "scripts/real-mv-playback-risk-spike.mjs"
      to: ".planning/phases/12-contract-schema-and-playback-risk-spike/12-PLAYBACK-RISK-SAMPLES.md"
      via: "real sample spike command"
      pattern: "REAL_MV_MKV_SAMPLE|REAL_MV_MPEG_SAMPLE"
---

<objective>
Run the playback-risk spike against user-provided real MV samples and record Phase 12 evidence.

Purpose: close the empirical part of the Phase 12 spike with one MKV and one MPG/MPEG file without building full TV switching integration.
Output: `.planning/phases/12-contract-schema-and-playback-risk-spike/12-PLAYBACK-RISK-SAMPLES.md` with probe and browser capability findings.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-contract-schema-and-playback-risk-spike/12-CONTEXT.md
@.planning/phases/12-contract-schema-and-playback-risk-spike/12-RESEARCH.md
@.planning/phases/12-contract-schema-and-playback-risk-spike/12-05-SUMMARY.md
@scripts/real-mv-playback-risk-spike.mjs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Record real MKV and MPG/MPEG playback-risk sample evidence</name>
  <files>.planning/phases/12-contract-schema-and-playback-risk-spike/12-PLAYBACK-RISK-SAMPLES.md</files>
  <read_first>
    - scripts/real-mv-playback-risk-spike.mjs
    - .planning/phases/12-contract-schema-and-playback-risk-spike/12-CONTEXT.md
    - .planning/phases/12-contract-schema-and-playback-risk-spike/12-RESEARCH.md
    - .planning/phases/12-contract-schema-and-playback-risk-spike/12-05-SUMMARY.md
  </read_first>
  <action>
    Before running the spike, require both sample paths:
    - `REAL_MV_MKV_SAMPLE` must point to an existing `.mkv` file.
    - `REAL_MV_MPEG_SAMPLE` must point to an existing `.mpg` or `.mpeg` file.

    If either environment variable is empty or points to a missing file, stop this plan and ask the user for the two paths. Do not synthesize or download copyrighted samples.

    Once both paths exist, run:
    `pnpm real-mv:risk-spike -- --mkv "$REAL_MV_MKV_SAMPLE" --mpeg "$REAL_MV_MPEG_SAMPLE" --output .planning/phases/12-contract-schema-and-playback-risk-spike/12-PLAYBACK-RISK-SAMPLES.md`

    The report must include these headings and facts:
    - `# Phase 12 Playback Risk Sample Evidence`
    - `## MKV sample`
    - `## MPEG sample`
    - `## Browser capability`
    - `## Compatibility decision`
    - absolute or repo-relative sample paths
    - probe summary fields for container, durationMs, videoCodec, resolution, fileSizeBytes, and audioTracks
    - `canPlayType`
    - `hasAudioTracksApi`
    - `compatibilityStatus`
    - the exact message `current device does not support audio-track switching` when switching cannot be verified

    If a switch attempt or capability inspection fails, record the failure as `review_required` or `unsupported` evidence with structured reasons. Per D-16, do not mark the whole song permanently unusable from one switch failure and do not add code that reloads playback aggressively.
  </action>
  <acceptance_criteria>
    - `test -n "$REAL_MV_MKV_SAMPLE" && test -f "$REAL_MV_MKV_SAMPLE"` exits 0.
    - `test -n "$REAL_MV_MPEG_SAMPLE" && test -f "$REAL_MV_MPEG_SAMPLE"` exits 0.
    - `test -f .planning/phases/12-contract-schema-and-playback-risk-spike/12-PLAYBACK-RISK-SAMPLES.md` exits 0.
    - `rg -n '# Phase 12 Playback Risk Sample Evidence|## MKV sample|## MPEG sample|## Browser capability|## Compatibility decision' .planning/phases/12-contract-schema-and-playback-risk-spike/12-PLAYBACK-RISK-SAMPLES.md` exits 0.
    - `rg -n 'container|durationMs|videoCodec|resolution|fileSizeBytes|audioTracks' .planning/phases/12-contract-schema-and-playback-risk-spike/12-PLAYBACK-RISK-SAMPLES.md` exits 0.
    - `rg -n 'canPlayType|hasAudioTracksApi|compatibilityStatus' .planning/phases/12-contract-schema-and-playback-risk-spike/12-PLAYBACK-RISK-SAMPLES.md` exits 0.
    - `rg -n 'current device does not support audio-track switching|audio track switching verified' .planning/phases/12-contract-schema-and-playback-risk-spike/12-PLAYBACK-RISK-SAMPLES.md` exits 0.
    - `rg -n 'permanently unusable|aggressive reload|android|media3|exo' .planning/phases/12-contract-schema-and-playback-risk-spike/12-PLAYBACK-RISK-SAMPLES.md` exits 1.
  </acceptance_criteria>
  <verify>
    <automated>test -n "$REAL_MV_MKV_SAMPLE" && test -f "$REAL_MV_MKV_SAMPLE" && test -n "$REAL_MV_MPEG_SAMPLE" && test -f "$REAL_MV_MPEG_SAMPLE" && pnpm real-mv:risk-spike -- --mkv "$REAL_MV_MKV_SAMPLE" --mpeg "$REAL_MV_MPEG_SAMPLE" --output .planning/phases/12-contract-schema-and-playback-risk-spike/12-PLAYBACK-RISK-SAMPLES.md && rg -n '# Phase 12 Playback Risk Sample Evidence|## MKV sample|## MPEG sample|canPlayType|hasAudioTracksApi|compatibilityStatus' .planning/phases/12-contract-schema-and-playback-risk-spike/12-PLAYBACK-RISK-SAMPLES.md</automated>
  </verify>
  <done>Real-sample playback-risk evidence exists for one MKV and one MPG/MPEG file, with probe facts, browser capability findings, compatibility decision, and switching risk message.</done>
</task>

</tasks>

<verification>
Run the automated verification command in Task 1 after `REAL_MV_MKV_SAMPLE` and `REAL_MV_MPEG_SAMPLE` are set.
</verification>

<success_criteria>
- User-provided MKV and MPG/MPEG sample paths are verified.
- Sample evidence file exists and records normalized probe facts plus browser capability results.
- Switching risk is documented without adding full TV switching integration or Android-specific fields.
</success_criteria>

<output>
After completion, create `.planning/phases/12-contract-schema-and-playback-risk-spike/12-06-SUMMARY.md`.
</output>
