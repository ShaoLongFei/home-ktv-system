---
phase: 13-mediainfo-probe-scanner-and-sidecars
plan: 01
type: execute
wave: 1
depends_on: ["12-06"]
files_modified:
  - apps/api/src/modules/ingest/import-scanner.ts
  - apps/api/src/modules/ingest/real-mv-sidecars.ts
  - apps/api/src/test/import-scanner.test.ts
autonomous: true
requirements:
  - SCAN-01
  - SCAN-02
  - SCAN-03
  - SCAN-05
requirements_addressed: [SCAN-01, SCAN-02, SCAN-03, SCAN-05]
must_haves:
  truths:
    - "Scanner discovers .mkv, .mpg, and .mpeg files under existing MEDIA_ROOT import roots."
    - "Same-stem cover and song.json artifacts are attached to the media file and do not create standalone candidates."
    - "Sidecar or cover changes are included in the import file change signature so rescans reconcile metadata."
    - "Unstable files are persisted as retryable pending files with scanner reasons instead of crashing or blocking the scan."
  key_links:
    - "ImportScanner media discovery uses isRealMvMediaPath so .mkv/.mpg/.mpeg are media inputs while same-stem cover/song.json files are artifacts only."
    - "findRealMvSidecars feeds probePayload.realMv.sidecars for every changed real MV media file."
    - "buildRealMvArtifactSignature is included in quickHash so changing cover or song.json triggers candidate reconciliation."
    - "Unstable real MV files still call upsertDiscoveredFile with probePayload.realMv.scannerReasons for later CandidateBuilder visibility."
  artifacts:
    - path: "apps/api/src/modules/ingest/import-scanner.ts"
      provides: "Real MV extension support, artifact-aware quick hash, and stability guard"
      contains: ".mpeg"
    - path: "apps/api/src/modules/ingest/real-mv-sidecars.ts"
      provides: "Pure sidecar and cover discovery helpers"
      contains: "findRealMvSidecars"
---

<objective>
Extend import scanning so real MV files and their adjacent artifacts are discovered safely and retryably.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-mediainfo-probe-scanner-and-sidecars/13-CONTEXT.md
@.planning/phases/13-mediainfo-probe-scanner-and-sidecars/13-RESEARCH.md
@apps/api/src/modules/ingest/import-scanner.ts
@apps/api/src/modules/ingest/media-probe.ts
@apps/api/src/test/import-scanner.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add real MV sidecar discovery helpers</name>
  <files>apps/api/src/modules/ingest/real-mv-sidecars.ts, apps/api/src/test/import-scanner.test.ts</files>
  <read_first>
    - apps/api/src/modules/ingest/import-scanner.ts
    - apps/api/src/test/import-scanner.test.ts
    - .planning/phases/13-mediainfo-probe-scanner-and-sidecars/13-CONTEXT.md
  </read_first>
  <action>
    First add failing tests in `apps/api/src/test/import-scanner.test.ts` for a helper that will discover artifacts for `/root/imports/pending/周杰伦-七里香.mkv`.

    Required test cases:
    - when `周杰伦-七里香.jpg` exists beside the media file, the helper returns a cover artifact with `relativePath: "周杰伦-七里香.jpg"` and `contentType: "image/jpeg"`;
    - when `周杰伦-七里香.song.json` exists beside the media file, the helper returns a `songJson` artifact with `relativePath: "周杰伦-七里香.song.json"`;
    - sidecar helpers do not classify `.jpg` or `.song.json` as media files.

    Then create `apps/api/src/modules/ingest/real-mv-sidecars.ts` exporting:
    - `REAL_MV_MEDIA_EXTENSIONS = new Set([".mkv", ".mpg", ".mpeg"])`
    - `REAL_MV_COVER_EXTENSIONS = new Map([[".jpg","image/jpeg"],[".jpeg","image/jpeg"],[".png","image/png"],[".webp","image/webp"]])`
    - `isRealMvMediaPath(filePath: string): boolean`
    - `findRealMvSidecars(input: { mediaAbsolutePath: string; rootPath: string }): Promise<RealMvSidecars>`
    - `buildRealMvArtifactSignature(sidecars: RealMvSidecars): string`

    `findRealMvSidecars` must check same-stem candidates in this order:
    - `stem.song.json`
    - `stem.json`
    - `song.json` only when the containing directory has exactly one real-MV media file
    - cover candidates `stem.jpg`, `stem.jpeg`, `stem.png`, `stem.webp`

    Return only root-relative POSIX paths and file stat data. Do not return absolute paths in the serialized artifact object.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/import-scanner.test.ts</automated>
    <automated>pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/modules/ingest/real-mv-sidecars.ts` contains `REAL_MV_MEDIA_EXTENSIONS`.
    - `apps/api/src/modules/ingest/real-mv-sidecars.ts` contains `.mpeg`.
    - `apps/api/src/modules/ingest/real-mv-sidecars.ts` contains `findRealMvSidecars`.
    - `apps/api/src/modules/ingest/real-mv-sidecars.ts` contains `buildRealMvArtifactSignature`.
    - `apps/api/src/test/import-scanner.test.ts` contains `周杰伦-七里香.song.json`.
    - `pnpm -F @home-ktv/api test -- src/test/import-scanner.test.ts` exits 0.
  </acceptance_criteria>
  <done>Real MV sidecars can be discovered without becoming standalone media inputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend scanner discovery and change signatures</name>
  <files>apps/api/src/modules/ingest/import-scanner.ts, apps/api/src/test/import-scanner.test.ts</files>
  <read_first>
    - apps/api/src/modules/ingest/import-scanner.ts
    - apps/api/src/modules/ingest/real-mv-sidecars.ts
    - apps/api/src/test/import-scanner.test.ts
  </read_first>
  <action>
    Add failing tests proving:
    - `.mpg` and `.mpeg` files under `imports/pending` are discovered and passed to `probeMedia`;
    - same-stem cover or sidecar changes alter the persisted `quickHash`;
    - sidecar files themselves are not passed into `CandidateBuilder`.

    Modify `ImportScanner`:
    - import `isRealMvMediaPath`, `findRealMvSidecars`, and `buildRealMvArtifactSignature`;
    - include `.mpg` and `.mpeg` in supported media discovery;
    - when persisting a real MV media file, discover sidecars before quick-hash comparison;
    - append artifact signature to the existing quick-hash string, using the format `media:${existingQuickHash}|artifacts:${artifactSignature}`;
    - merge sidecar artifact facts into `probePayload.realMv.sidecars` for probed or pending files.

    Keep existing behavior for `.mp4`, `.mov`, `.webm`, and `song.json` formal catalog scans unless the file is a real-MV sidecar attached to a real-MV media file.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/import-scanner.test.ts</automated>
    <automated>pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/modules/ingest/import-scanner.ts` contains `.mpg`.
    - `apps/api/src/modules/ingest/import-scanner.ts` contains `buildRealMvArtifactSignature`.
    - `apps/api/src/modules/ingest/import-scanner.ts` contains `artifacts:`.
    - `apps/api/src/test/import-scanner.test.ts` contains `.mpeg`.
    - `apps/api/src/test/import-scanner.test.ts` contains `quickHash`.
    - `pnpm -F @home-ktv/api test -- src/test/import-scanner.test.ts` exits 0.
  </acceptance_criteria>
  <done>Real MV files and sidecar changes flow through existing scan change detection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add lightweight unstable-file guard</name>
  <files>apps/api/src/modules/ingest/import-scanner.ts, apps/api/src/test/import-scanner.test.ts</files>
  <read_first>
    - apps/api/src/modules/ingest/import-scanner.ts
    - apps/api/src/modules/ingest/repositories/import-file-repository.ts
    - apps/api/src/test/import-scanner.test.ts
  </read_first>
  <action>
    Add a failing test where `stat` data changes between two stability checks for a real MV file. The expected behavior:
    - `probeMedia` is not called;
    - `upsertDiscoveredFile` receives `probeStatus: "pending"`;
    - `durationMs` is `null`;
    - `probePayload.realMv.scannerReasons` contains an object with `code: "file-unstable"`, `severity: "warning"`, and `source: "scanner"`.

    Modify `ImportScanner`:
    - add a small helper `waitForStableFile(absolutePath, delayMs = 500)` or an injectable stability checker for tests;
    - run the check before quick hash/probe for real MV media only;
    - if unstable, persist a pending import file and return `{ added, changed: true }` so `CandidateBuilder` can create or update a visible review candidate;
    - keep the scan running for other files.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/import-scanner.test.ts</automated>
    <automated>pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/modules/ingest/import-scanner.ts` contains `file-unstable`.
    - `apps/api/src/modules/ingest/import-scanner.ts` contains `probeStatus: \"pending\"` or `probeStatus: "pending"`.
    - `apps/api/src/test/import-scanner.test.ts` contains `file-unstable`.
    - `apps/api/src/test/import-scanner.test.ts` contains `not.toHaveBeenCalled`.
    - `pnpm -F @home-ktv/api test -- src/test/import-scanner.test.ts` exits 0.
  </acceptance_criteria>
  <done>Partial large files are visible and retryable without crashing scans.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `pnpm -F @home-ktv/api test -- src/test/import-scanner.test.ts`
- [ ] `pnpm -F @home-ktv/api typecheck`
</verification>

<success_criteria>
- SCAN-01 real MV extension discovery is covered.
- SCAN-02 and SCAN-03 sidecar discovery is covered at scanner level.
- SCAN-05 unstable-file retry visibility is covered.
- Existing import scanning for non-real-MV media remains compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/13-mediainfo-probe-scanner-and-sidecars/13-01-SUMMARY.md`.
</output>
