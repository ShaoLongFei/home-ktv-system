---
phase: 14-admin-review-and-catalog-admission
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/routes/admin-imports.ts
  - apps/api/src/modules/ingest/repositories/import-candidate-repository.ts
  - apps/api/src/test/admin-imports-routes.test.ts
autonomous: true
requirements: [REVIEW-02]
must_haves:
  truths:
    - "Admin can submit reviewed original and accompaniment track-role refs for a real-MV candidate file."
    - "Invalid reviewed track-role refs are rejected before they can overwrite candidate file JSONB."
    - "Raw audio-track facts remain the validation source for candidate file track roles."
  artifacts:
    - path: "apps/api/src/routes/admin-imports.ts"
      provides: "PATCH parser and route error response for reviewed trackRoles"
      contains: "parseTrackRolesPatch"
    - path: "apps/api/src/modules/ingest/repositories/import-candidate-repository.ts"
      provides: "metadata update persistence and audio-track validation for trackRoles"
      contains: "validateTrackRolesAgainstAudioTracks"
    - path: "apps/api/src/test/admin-imports-routes.test.ts"
      provides: "route-level regression coverage for reviewed trackRoles"
      contains: "trackRoles"
  key_links:
    - from: "apps/api/src/routes/admin-imports.ts"
      to: "apps/api/src/modules/ingest/repositories/import-candidate-repository.ts"
      via: "UpdateImportCandidateMetadataInput.files[].trackRoles"
      pattern: "trackRoles"
    - from: "apps/api/src/modules/ingest/repositories/import-candidate-repository.ts"
      to: "import_candidate_files.track_roles"
      via: "UPDATE import_candidate_files"
      pattern: "track_roles"
---

<objective>
Add the API and repository contract for reviewed real-MV `trackRoles`.

Purpose: Admin corrections must persist before approval so Phase 14 admits one real-MV asset with reviewed original/accompaniment refs instead of scanner guesses only.
Output: `PATCH /admin/import-candidates/:candidateId` accepts typed `files[].trackRoles`, validates refs against `mediaInfoSummary.audioTracks`, and writes `import_candidate_files.track_roles`.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-admin-review-and-catalog-admission/14-CONTEXT.md
@.planning/phases/14-admin-review-and-catalog-admission/14-RESEARCH.md
@apps/api/src/routes/admin-imports.ts
@apps/api/src/modules/ingest/repositories/import-candidate-repository.ts
@apps/api/src/modules/catalog/repositories/asset-repository.ts
@apps/api/src/test/admin-imports-routes.test.ts
@packages/domain/src/index.ts

<interfaces>
From `packages/domain/src/index.ts`:
```ts
export interface TrackRef {
  index: number;
  id: string;
  label: string;
}

export interface TrackRoles {
  original: TrackRef | null;
  instrumental: TrackRef | null;
}

export interface MediaInfoSummary {
  audioTracks: readonly AudioTrackSummary[];
}
```

Current PATCH body shape in `apps/api/src/routes/admin-imports.ts`:
```ts
files: [{
  candidateFileId: string;
  selected?: boolean;
  proposedVocalMode?: VocalMode;
  proposedAssetKind?: AssetKind;
}]
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Parse reviewed trackRoles in the Admin metadata PATCH route</name>
  <files>apps/api/src/routes/admin-imports.ts, apps/api/src/test/admin-imports-routes.test.ts</files>
  <read_first>
    - apps/api/src/routes/admin-imports.ts
    - apps/api/src/test/admin-imports-routes.test.ts
    - packages/domain/src/index.ts
    - .planning/phases/14-admin-review-and-catalog-admission/14-CONTEXT.md
  </read_first>
  <behavior>
    - PATCH accepts `files[].trackRoles` with exactly `{ original: TrackRef | null, instrumental: TrackRef | null }`.
    - A `TrackRef` is valid only when `index` is a finite number and `id` and `label` are strings.
    - Malformed `trackRoles` returns HTTP 400 with `{ error: "INVALID_IMPORT_CANDIDATE_METADATA" }`.
  </behavior>
  <action>
    Implement the parser portion only. In `apps/api/src/routes/admin-imports.ts`, add type imports for `TrackRef` and `TrackRoles` from `@home-ktv/domain`. Add helper functions named `parseTrackRolesPatch(value: unknown): TrackRoles | null`, `parseTrackRoleSlot(value: unknown): TrackRef | null | undefined`, and `isTrackRefPatch(value: unknown): value is TrackRef`.

    In `parseMetadataPatch`, when a file patch contains `trackRoles`, call `parseTrackRolesPatch(file.trackRoles)`. If it returns `null`, return `null` from `parseMetadataPatch`. If it returns a `TrackRoles` object, assign `patch.trackRoles = parsedTrackRoles`.

    Parser rules per D-04, D-05, and D-06:
    - `trackRoles` may be omitted.
    - If `trackRoles` is present, it must be an object with both `original` and `instrumental` keys.
    - Each key may be `null` or a full `{ index, id, label }` object.
    - Do not require both roles to be non-null; missing role guesses are review warnings, not approval blockers per D-02 and D-05.

    Update `apps/api/src/test/admin-imports-routes.test.ts` by extending the existing `PATCH /admin/import-candidates/:candidateId saves D-07 metadata without moving files` payload with:
    ```ts
    trackRoles: {
      original: { index: 0, id: "0x1100", label: "Original vocal" },
      instrumental: { index: 1, id: "0x1101", label: "Instrumental" }
    }
    ```
    Assert `importCandidates.updateCandidateMetadata` receives that same `trackRoles` object in `files[0]`. Add a new route test where `trackRoles.original.id` is a number and assert status `400` and body `{ error: "INVALID_IMPORT_CANDIDATE_METADATA" }`.
  </action>
  <acceptance_criteria>
    - `rg -n "parseTrackRolesPatch|parseTrackRoleSlot|isTrackRefPatch" apps/api/src/routes/admin-imports.ts` finds all three helper names.
    - `rg -n "trackRoles" apps/api/src/test/admin-imports-routes.test.ts` finds assertions in the PATCH test.
    - `pnpm -F @home-ktv/api test -- src/test/admin-imports-routes.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/admin-imports-routes.test.ts</automated>
  </verify>
  <done>PATCH request parsing accepts valid reviewed `trackRoles` and rejects malformed shape without changing import file locations.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist reviewed trackRoles after validating against raw audio tracks</name>
  <files>apps/api/src/routes/admin-imports.ts, apps/api/src/modules/ingest/repositories/import-candidate-repository.ts, apps/api/src/test/admin-imports-routes.test.ts</files>
  <read_first>
    - apps/api/src/modules/ingest/repositories/import-candidate-repository.ts
    - apps/api/src/routes/admin-imports.ts
    - apps/api/src/modules/catalog/repositories/asset-repository.ts
    - apps/api/src/test/admin-imports-routes.test.ts
    - .planning/phases/14-admin-review-and-catalog-admission/14-RESEARCH.md
  </read_first>
  <behavior>
    - Repository metadata updates write `import_candidate_files.track_roles`.
    - Non-null role refs must match the current candidate file's `mediaInfoSummary.audioTracks` by both `id` and `index`.
    - Invalid refs return HTTP 400 with `{ error: "INVALID_TRACK_ROLE_REF" }` and do not persist.
  </behavior>
  <action>
    In `UpdateImportCandidateMetadataInput.files[]`, add `trackRoles?: TrackRoles`.

    In `PgImportCandidateRepository`, add an exported error class:
    ```ts
    export class ImportCandidateMetadataError extends Error {
      constructor(readonly code: "INVALID_TRACK_ROLE_REF", message: string, readonly details: Record<string, unknown> = {}) {
        super(message);
      }
    }
    ```

    Inside `updateCandidateMetadata`, before running file updates, load the current file details for the candidate inside the same transaction. Add a private helper `listCandidateFileDetailsForDb(db: QueryExecutor, candidateId: ImportCandidateId): Promise<ImportCandidateFileDetail[]>` by moving the existing SELECT body used by `listCandidateFileDetails` into the helper; keep `listCandidateFileDetails(candidateId)` as a wrapper that calls the helper with `this.db`.

    Add helper `validateTrackRolesAgainstAudioTracks(file: ImportCandidateFileDetail, trackRoles: TrackRoles): void`. For each non-null role in `trackRoles.original` and `trackRoles.instrumental`, find a raw track in `file.mediaInfoSummary?.audioTracks ?? []` where both `track.id === role.id` and `track.index === role.index`. If no match exists, throw `new ImportCandidateMetadataError("INVALID_TRACK_ROLE_REF", "Track role ref does not match candidate audio tracks", { candidateFileId: file.id, role: "original" | "instrumental", trackRef: role })`.

    Extend the `UPDATE import_candidate_files` statement to set `track_roles = COALESCE($5::jsonb, track_roles)` and shift `candidate_id` to `$6`. Pass `file.trackRoles ?? null` as the fifth SQL value.

    In `apps/api/src/routes/admin-imports.ts`, import `ImportCandidateMetadataError`. Wrap `updateCandidateMetadata` in a try/catch; if the caught error is `ImportCandidateMetadataError` with code `INVALID_TRACK_ROLE_REF`, return `reply.code(400).send({ error: "INVALID_TRACK_ROLE_REF" })`. Re-throw all other errors.

    Add a route test where `importCandidates.updateCandidateMetadata` rejects with `new ImportCandidateMetadataError("INVALID_TRACK_ROLE_REF", "bad ref")`; assert HTTP 400 and body `{ error: "INVALID_TRACK_ROLE_REF" }`.
  </action>
  <acceptance_criteria>
    - `rg -n "ImportCandidateMetadataError|validateTrackRolesAgainstAudioTracks|track_roles = COALESCE" apps/api/src/modules/ingest/repositories/import-candidate-repository.ts` finds all three patterns.
    - `rg -n "INVALID_TRACK_ROLE_REF" apps/api/src/routes/admin-imports.ts apps/api/src/test/admin-imports-routes.test.ts` finds route handling and test coverage.
    - `pnpm -F @home-ktv/api test -- src/test/admin-imports-routes.test.ts` exits 0.
    - `pnpm -F @home-ktv/api typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/admin-imports-routes.test.ts &amp;&amp; pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <done>Reviewed real-MV track roles are durably stored only when they reference actual raw audio tracks on the candidate file.</done>
</task>

</tasks>

<verification>
Run `pnpm -F @home-ktv/api test -- src/test/admin-imports-routes.test.ts` and `pnpm -F @home-ktv/api typecheck`.
</verification>

<success_criteria>
- `PATCH /admin/import-candidates/:candidateId` can persist reviewed `trackRoles`.
- Invalid `trackRoles` shape returns `INVALID_IMPORT_CANDIDATE_METADATA`.
- Invalid role refs return `INVALID_TRACK_ROLE_REF`.
- No approval, search, queue, playback, runtime switching, transcoding, or Android TV work is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/14-admin-review-and-catalog-admission/14-01-SUMMARY.md`.
</output>
