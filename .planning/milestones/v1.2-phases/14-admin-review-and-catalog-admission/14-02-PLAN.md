---
phase: 14-admin-review-and-catalog-admission
plan: 02
type: execute
wave: 2
depends_on: ["14-01"]
files_modified:
  - apps/admin/src/imports/types.ts
  - apps/admin/src/imports/CandidateEditor.tsx
  - apps/admin/src/i18n.tsx
  - apps/admin/src/App.css
  - apps/admin/src/test/import-workbench.test.tsx
autonomous: true
requirements: [REVIEW-01, REVIEW-02, REVIEW-05]
must_haves:
  truths:
    - "Admin can review title, artist, language, cover, MediaInfo facts, filename/sidecar provenance, and conflicts in one candidate editor."
    - "Admin can see scanner-guessed original/accompaniment roles and raw audio-track facts before saving corrections."
    - "Unsupported or incomplete real-MV candidates stay visible with repair and retry guidance."
  artifacts:
    - path: "apps/admin/src/imports/CandidateEditor.tsx"
      provides: "real-MV review controls, raw audio track facts, conflict/provenance rendering, guidance copy"
      contains: "RealMvTrackRoleReview"
    - path: "apps/admin/src/imports/types.ts"
      provides: "Admin MetadataUpdateInput trackRoles contract"
      contains: "trackRoles?: TrackRoles"
    - path: "apps/admin/src/test/import-workbench.test.tsx"
      provides: "DOM and PATCH payload coverage for reviewed track roles and guidance"
      contains: "原唱声轨"
  key_links:
    - from: "apps/admin/src/imports/CandidateEditor.tsx"
      to: "PATCH /admin/import-candidates/:candidateId"
      via: "toMetadataInput(form).files[].trackRoles"
      pattern: "trackRoles"
    - from: "apps/admin/src/imports/CandidateEditor.tsx"
      to: "mediaInfoSummary.audioTracks"
      via: "track-role select options"
      pattern: "audioTracks"
---

<objective>
Upgrade the Admin import editor from preview-only real-MV evidence to an actionable review surface.

Purpose: The user needs to resolve metadata, conflicts, compatibility, and audio-track roles before formal admission, while keeping Phase 15 playback/search boundaries out of scope.
Output: Admin UI shows raw MediaInfo audio facts, guessed roles, editable original/accompaniment selectors, metadata conflict/provenance facts, and repair/retry guidance.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-admin-review-and-catalog-admission/14-CONTEXT.md
@.planning/phases/14-admin-review-and-catalog-admission/14-RESEARCH.md
@.planning/phases/14-admin-review-and-catalog-admission/14-01-SUMMARY.md
@apps/admin/src/imports/types.ts
@apps/admin/src/imports/CandidateEditor.tsx
@apps/admin/src/imports/use-import-workbench-runtime.ts
@apps/admin/src/i18n.tsx
@apps/admin/src/App.css
@apps/admin/src/test/import-workbench.test.tsx

<interfaces>
Plan 14-01 provides this PATCH contract:
```ts
files: Array<{
  candidateFileId: string;
  selected: boolean;
  proposedVocalMode: VocalMode;
  proposedAssetKind: AssetKind;
  trackRoles?: TrackRoles;
}>;
```

Existing Admin types already define:
```ts
export interface MediaInfoSummary {
  audioTracks: ReadonlyArray<TrackRef & {
    language: string | null;
    codec: string | null;
    channels: number | null;
  }>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add editable original/accompaniment track-role controls</name>
  <files>apps/admin/src/imports/types.ts, apps/admin/src/imports/CandidateEditor.tsx, apps/admin/src/test/import-workbench.test.tsx</files>
  <read_first>
    - apps/admin/src/imports/types.ts
    - apps/admin/src/imports/CandidateEditor.tsx
    - apps/admin/src/test/import-workbench.test.tsx
    - .planning/phases/14-admin-review-and-catalog-admission/14-01-SUMMARY.md
    - .planning/phases/14-admin-review-and-catalog-admission/14-CONTEXT.md
  </read_first>
  <behavior>
    - Real-MV candidate files initialize form state from `file.trackRoles`.
    - Original and accompaniment selectors list every `mediaInfoSummary.audioTracks` entry.
    - Selecting an audio track writes the full `{ index, id, label }` ref into the PATCH payload.
    - Selecting the blank option writes `null` for that role and does not block saving.
  </behavior>
  <action>
    In `apps/admin/src/imports/types.ts`, add `trackRoles?: TrackRoles` to `MetadataUpdateInput.files[]`.

    In `CandidateEditor.tsx`, extend `FileFormState` with:
    ```ts
    trackRoles: TrackRoles;
    ```
    Initialize it in `toFormState` with `file.trackRoles ?? { original: null, instrumental: null }`.

    Add helper `updateTrackRole(candidateFileId: string, role: keyof TrackRoles, trackId: string)` inside `CandidateEditor`. It must find the selected candidate file by `candidateFileId`, find the raw track by `track.id === trackId`, and update the form file with `trackRoles[role] = track ? { index: track.index, id: track.id, label: track.label } : null`.

    Add a component/function named `RealMvTrackRoleReview` in `CandidateEditor.tsx`. Render it for any selected file where `file.proposedAssetKind === "dual-track-video"` or `file.playbackProfile?.kind === "single_file_audio_tracks"`. It must show:
    - A select labelled `原唱声轨` / `Original vocal track` bound to `trackRoles.original?.id ?? ""`.
    - A select labelled `伴唱声轨` / `Accompaniment track` bound to `trackRoles.instrumental?.id ?? ""`.
    - A blank option labelled `需要确认` / `Needs review`.
    - One option per raw audio track with visible text `#${track.index} ${track.label} ${track.codec ?? ""}`.

    In `toMetadataInput`, include `trackRoles: file.trackRoles` for each file patch. This is D-04 through D-06: guesses are surfaced and editable, but manual mapping is not mandatory.

    Update `apps/admin/src/test/import-workbench.test.tsx` so the real-MV fixture changes the instrumental selector from `0x1101` to `0x1100`, clicks `保存元数据`, and asserts the PATCH body for `/admin/import-candidates/real-mv-candidate` includes:
    ```ts
    trackRoles: {
      original: { index: 0, id: "0x1100", label: "Original vocal" },
      instrumental: { index: 0, id: "0x1100", label: "Original vocal" }
    }
    ```
  </action>
  <acceptance_criteria>
    - `rg -n "RealMvTrackRoleReview|updateTrackRole|trackRoles: file.trackRoles" apps/admin/src/imports/CandidateEditor.tsx` finds all three patterns.
    - `rg -n "trackRoles\\?: TrackRoles" apps/admin/src/imports/types.ts` finds the metadata payload field.
    - `rg -n "原唱声轨|伴唱声轨|0x1100" apps/admin/src/test/import-workbench.test.tsx` finds DOM and payload assertions.
    - `pnpm -F @home-ktv/admin test -- src/test/import-workbench.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/admin test -- src/test/import-workbench.test.tsx</automated>
  </verify>
  <done>Admin can save reviewed real-MV original/accompaniment track refs through the existing metadata PATCH flow.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Show raw track facts, provenance conflicts, and repair guidance</name>
  <files>apps/admin/src/imports/CandidateEditor.tsx, apps/admin/src/i18n.tsx, apps/admin/src/App.css, apps/admin/src/test/import-workbench.test.tsx</files>
  <read_first>
    - apps/admin/src/imports/CandidateEditor.tsx
    - apps/admin/src/i18n.tsx
    - apps/admin/src/App.css
    - apps/admin/src/test/import-workbench.test.tsx
    - .planning/phases/14-admin-review-and-catalog-admission/14-CONTEXT.md
    - .planning/phases/14-admin-review-and-catalog-admission/14-RESEARCH.md
  </read_first>
  <behavior>
    - Raw audio track rows remain visible with index, label, codec, language, and channel count.
    - Metadata source/conflict facts show filename and sidecar values when present.
    - `unsupported` compatibility shows preprocess guidance and does not hide the candidate.
    - `review_required` and `unknown` compatibility show formal-catalog review guidance, not a hard blocker.
    - The approve button is disabled only when busy, title is blank, or artist is blank.
  </behavior>
  <action>
    In `RealMvPreviewPanel`, add sections with these localized labels:
    - `candidate.rawAudioTracks`: `Raw audio tracks` / `原始音轨`
    - `candidate.trackCodec`: `Codec` / `编码`
    - `candidate.trackLanguage`: `Language` / `语言`
    - `candidate.trackChannels`: `Channels` / `声道`
    - `candidate.metadataConflicts`: `Metadata conflicts` / `元数据冲突`
    - `candidate.compatibilityGuidance`: `Compatibility guidance` / `兼容性建议`
    - `candidate.retryScanGuidance`: `Fix sidecars or preprocess media, then scan imports again.` / `修复 song.json/封面或预处理媒体后，点击扫描导入目录重试。`
    - `candidate.unsupportedGuidance`: `Unsupported in v1.2; preprocess outside the system before approval.` / `v1.2 不自动转码，请先在系统外预处理后再入库。`
    - `candidate.reviewRequiredGuidance`: `Can enter the catalog as review_required after title and artist are complete.` / `歌名和歌手完整后可入库为需复核。`
    - `candidate.playableGuidance`: `Scanner marked this candidate playable; approval can create ready catalog status.` / `扫描器判断可播放，批准后可进入已准备状态。`
    - `candidate.titleArtistRequired`: `Title and artist are required to approve.` / `批准入库前必须填写歌名和歌手。`

    Render raw audio tracks from `mediaInfoSummary.audioTracks` in a compact table/list with visible strings like `#0`, `Original vocal`, `aac`, `zh`, and `2`.

    Render metadata conflicts from `realMv.metadataConflicts`. For each conflict, show its `field`; for each `values[]` entry, show `${source}: ${value}` when both keys are strings.

    Add a `compatibilityGuidance(file)` helper:
    - `unsupported` returns `candidate.unsupportedGuidance` plus `candidate.retryScanGuidance`.
    - `review_required` or `unknown` returns `candidate.reviewRequiredGuidance`.
    - `playable` returns `candidate.playableGuidance`.

    Change the approve button disabled expression to:
    ```tsx
    disabled={isBusy || !form.title.trim() || !form.artistName.trim()}
    ```
    Render the `candidate.titleArtistRequired` note only when either field is blank. This implements D-01 through D-03 exactly: title and artist are hard blockers; language, cover, sidecar completeness, and manual track confirmation are not.

    Add CSS classes `.track-role-review`, `.audio-track-table`, `.metadata-conflict-list`, and `.compatibility-guidance` using the existing dense Admin style: no nested cards, 8px or smaller radius, stable table/list columns, and no large decorative hero treatment.

    Update `import-workbench.test.tsx` assertions to cover visible Chinese strings `原始音轨`, `原唱声轨`, `伴唱声轨`, `元数据冲突`, `filename: 想你的夜`, `sidecar: 想你的夜 Live`, and `歌名和歌手完整后可入库为需复核。`.
  </action>
  <acceptance_criteria>
    - `rg -n "candidate.rawAudioTracks|candidate.unsupportedGuidance|candidate.reviewRequiredGuidance|candidate.titleArtistRequired" apps/admin/src/i18n.tsx` finds all four keys in both language dictionaries.
    - `rg -n "audio-track-table|metadata-conflict-list|compatibility-guidance|track-role-review" apps/admin/src/App.css` finds all four classes.
    - `rg -n "disabled=\\{isBusy \\|\\| !form.title.trim\\(\\) \\|\\| !form.artistName.trim\\(\\)\\}" apps/admin/src/imports/CandidateEditor.tsx` finds the approval gate.
    - `pnpm -F @home-ktv/admin test -- src/test/import-workbench.test.tsx` exits 0.
    - `pnpm -F @home-ktv/admin typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/admin test -- src/test/import-workbench.test.tsx &amp;&amp; pnpm -F @home-ktv/admin typecheck</automated>
  </verify>
  <done>The Admin review UI exposes real-MV facts and guidance without adding Phase 15 playback/search behavior or new hard blockers beyond title and artist.</done>
</task>

</tasks>

<verification>
Run `pnpm -F @home-ktv/admin test -- src/test/import-workbench.test.tsx` and `pnpm -F @home-ktv/admin typecheck`.
</verification>

<success_criteria>
- Admin sees cover, MediaInfo, raw audio tracks, provenance, conflicts, compatibility, and retry/preprocess guidance.
- Admin can edit and save original/accompaniment track-role refs.
- Approval controls block only blank title or blank artist.
- No mobile search, queue, TV playback, runtime switching, transcoding, or Android TV work is added.
</success_criteria>

<output>
After completion, create `.planning/phases/14-admin-review-and-catalog-admission/14-02-SUMMARY.md`.
</output>
