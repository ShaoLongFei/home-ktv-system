---
phase: 14-admin-review-and-catalog-admission
plan: 03
type: execute
wave: 2
depends_on: ["14-01", "14-04"]
files_modified:
  - apps/api/src/modules/catalog/admission-service.ts
  - apps/api/src/test/catalog-admission.test.ts
autonomous: true
requirements: [REVIEW-03, REVIEW-04, REVIEW-05]
must_haves:
  truths:
    - "Approving a supported real-MV candidate creates one formal Song and one real-MV Asset."
    - "The formal real-MV Asset stores reviewed `trackRoles`, MediaInfo, playbackProfile, and compatibility facts."
    - "When a real-MV candidate has a cover sidecar, approval carries that cover into the formal song directory and writes `coverPath` into `song.json`."
    - "Scanner compatibility drives formal readiness without claiming runtime switching verification."
    - "Unsupported real-MV candidates remain visible and are not force-approved."
  artifacts:
    - path: "apps/api/src/modules/catalog/admission-service.ts"
      provides: "real-MV admission branch, readiness mapping, one-asset promotion writer"
      contains: "approveRealMvCandidate"
    - path: "apps/api/src/test/catalog-admission.test.ts"
      provides: "service and writer regression coverage for one-asset real-MV admission"
      contains: "single real MV"
  key_links:
    - from: "CatalogAdmissionService.approve"
      to: "CatalogAdmissionService.approveRealMvCandidate"
      via: "selected dual-track-video or single_file_audio_tracks file"
      pattern: "approveRealMvCandidate"
    - from: "CatalogAdmissionService.approveRealMvCandidate"
      to: "writeSongJson"
      via: "one SongJsonAsset plus coverPath with trackRoles and compatibility fields"
      pattern: "coverPath"
    - from: "candidate realMv.sidecars.cover"
      to: "formal song directory"
      via: "copy cover sidecar beside approved media"
      pattern: "copyRealMvCoverSidecar"
    - from: "PgCatalogAdmissionWriter.promoteApprovedCandidate"
      to: "assets.track_roles"
      via: "INSERT INTO assets JSONB columns"
      pattern: "track_roles"
---

<objective>
Implement formal catalog admission for real-MV candidates.

Purpose: Phase 13 can scan one physical MV as one candidate; Phase 14 must admit it as one formal Song with one real-MV Asset instead of the legacy original/instrumental asset pair.
Output: A real-MV branch in admission service and writer that preserves `trackRoles`, compatibility, MediaInfo, playback profile, and full `song.json` fields.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-admin-review-and-catalog-admission/14-CONTEXT.md
@.planning/phases/14-admin-review-and-catalog-admission/14-RESEARCH.md
@.planning/phases/14-admin-review-and-catalog-admission/14-01-SUMMARY.md
@.planning/phases/14-admin-review-and-catalog-admission/14-04-SUMMARY.md
@apps/api/src/modules/catalog/admission-service.ts
@apps/api/src/modules/catalog/song-json.ts
@apps/api/src/modules/catalog/repositories/asset-repository.ts
@apps/api/src/modules/ingest/repositories/import-candidate-repository.ts
@apps/api/src/test/catalog-admission.test.ts
@packages/domain/src/index.ts

<interfaces>
Readiness mapping locked for this plan:
```ts
playable -> song.status = "ready", asset.status = "ready", switchQualityStatus = "review_required"
review_required | unknown -> song.status = "review_required", asset.status = "promoted", switchQualityStatus = "review_required"
unsupported -> do not promote; candidate.status = "review_required"; reviewNotes = "real-mv-unsupported"
```

Real-MV file detection:
```ts
file.selected &&
(file.proposedAssetKind === "dual-track-video" ||
 file.playbackProfile?.kind === "single_file_audio_tracks")
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a real-MV approval branch with compatibility-driven readiness</name>
  <files>apps/api/src/modules/catalog/admission-service.ts, apps/api/src/test/catalog-admission.test.ts</files>
  <read_first>
    - apps/api/src/modules/catalog/admission-service.ts
    - apps/api/src/modules/catalog/song-json.ts
    - apps/api/src/modules/ingest/repositories/import-candidate-repository.ts
    - apps/api/src/test/catalog-admission.test.ts
    - .planning/phases/14-admin-review-and-catalog-admission/14-CONTEXT.md
    - .planning/phases/14-admin-review-and-catalog-admission/14-04-SUMMARY.md
  </read_first>
  <behavior>
    - Real-MV approval runs before legacy `evaluatePair`.
    - Blank title returns review_required reason `missing-title`; blank artist returns `missing-artist`.
    - `unsupported` real-MV candidates return review_required reason `real-mv-unsupported` and do not move files or call the catalog writer.
    - `review_required` or `unknown` candidates are admitted as formal `review_required` songs with a non-ready `promoted` asset.
    - `playable` candidates are admitted as formal `ready` songs with a `ready` asset.
    - Every real-MV admitted asset keeps `switchQualityStatus: "review_required"`; never write `"verified"` in this branch.
    - If the candidate has `realMv.sidecars.cover.relativePath`, approval copies that cover beside the formal media and writes `coverPath` as the copied cover filename in `song.json`.
  </behavior>
  <action>
    In `CatalogAdmissionService.approve`, after `const record = await this.requireCandidate(candidateId);` and before `const evaluation = this.evaluatePair(record);`, find a real-MV file:
    ```ts
    const realMvFile = record.files.find(isSelectedRealMvFile);
    if (realMvFile) {
      return this.approveRealMvCandidate(record, realMvFile, resolution);
    }
    ```

    Add helpers in `admission-service.ts`:
    - `isSelectedRealMvFile(file: ImportCandidateFileDetail): boolean`
    - `deriveRealMvReadiness(file: ImportCandidateFileDetail): { promotable: boolean; reason?: string; songStatus: SongStatus; assetStatus: AssetStatus; switchQualityStatus: SwitchQualityStatus }`
    - `defaultSingleFilePlaybackProfile(file: ImportCandidateFileDetail): PlaybackProfile`
    - `defaultTrackRoles(): TrackRoles`
    - `readRealMvCoverSidecar(record: ImportCandidateWithFiles, file: ImportCandidateFileDetail): { relativePath: string } | null`
    - `copyRealMvCoverSidecar(record: ImportCandidateWithFiles, file: ImportCandidateFileDetail, targetDirectory: string): Promise<string | null>`

    Implement readiness exactly:
    - `file.compatibilityStatus === "unsupported"` -> `promotable: false`, `reason: "real-mv-unsupported"`.
    - `file.compatibilityStatus === "playable"` -> `songStatus: "ready"`, `assetStatus: "ready"`, `switchQualityStatus: "review_required"`.
    - all other values -> `songStatus: "review_required"`, `assetStatus: "promoted"`, `switchQualityStatus: "review_required"`.

    Add private method `approveRealMvCandidate(record, file, resolution?)`. It must:
    - Enforce only title and artist as approval blockers per D-01: blank title -> update candidate `review_required` with reviewNotes `missing-title`; blank artist -> update candidate `review_required` with reviewNotes `missing-artist`.
    - For unsupported readiness, update candidate `review_required` with reviewNotes `real-mv-unsupported` and `candidateMeta: { repairReason: "real-mv-unsupported" }`; return status `review_required`.
    - Reuse the existing target-directory conflict behavior and `approval_failed` repair behavior.
    - Move exactly the selected real-MV media file into the target directory and update its import file location to `rootKind: "songs"` with the `songsRoot` relative path.
    - Before calling `writeSongJson`, call `copyRealMvCoverSidecar(record, file, targetDirectory)`. This helper must:
      - Read the cover sidecar from `file.probeSummary.realMv.sidecars.cover.relativePath` first, then `record.candidate.candidateMeta.realMv.sidecars.cover.relativePath` as fallback.
      - Resolve the source under `this.absolutePathFor(file.rootKind, path.dirname(file.relativePath))` only when the stored cover path is relative to the same import root; reject `..` and absolute paths by returning `null`.
      - Copy the cover into `targetDirectory` using the source basename, for example `cover.jpg` or `想你的夜.jpg`, without deleting the original sidecar.
      - Return the copied basename as `coverPath`; return `null` if no safe cover sidecar exists.
    - Use asset id `asset-${record.candidate.id}-real-mv`, song id `song-${record.candidate.id}` unless merging, `vocalMode: "dual"`, `assetKind: "dual-track-video"`, `lyricMode: "none"`, and `switchFamily: null`.
    - Call `writeSongJson` with one asset only. Include `status`, `defaultAssetId`, `defaultAssetPath`, `coverPath`, `defaultVocalMode: "instrumental"`, `sameVersionConfirmed: record.candidate.sameVersionConfirmed`, `genre`, `tags`, `aliases`, `searchHints`, `releaseYear`, `source: { importCandidateId: record.candidate.id }`, and asset fields from Plan 14-04: `compatibilityStatus`, `compatibilityReasons`, `mediaInfoSummary`, `mediaInfoProvenance`, `trackRoles`, `playbackProfile`, `container`, `videoCodec`, and `audioCodecs`.
    - Do not create original/instrumental asset ids or a legacy switch pair.

    Add tests in `catalog-admission.test.ts`:
    - `"approves a single real MV candidate as one formal asset with reviewed trackRoles"`: create one selected `dual-track-video` file with `compatibilityStatus: "review_required"`, `trackRoles`, `mediaInfoSummary.audioTracks`, `playbackProfile.kind: "single_file_audio_tracks"`, and `probeSummary.realMv.sidecars.cover.relativePath = "周杰伦/七里香/cover.jpg"`; write that cover file under the import root before approval; assert result `approved`, writer receives exactly one asset with `assetKind: "dual-track-video"`, `vocalMode: "dual"`, `status: "promoted"`, `switchQualityStatus: "review_required"`, and `trackRoles.instrumental.id === "0x1101"`; assert written `song.json` has `coverPath: "cover.jpg"`, one asset, and no `asset-candidate-1-original`; assert `targetDirectory/cover.jpg` exists.
    - `"maps playable real MV approval to ready without verifying switching"`: assert song status `ready`, asset status `ready`, and `switchQualityStatus` remains `review_required`.
    - `"keeps unsupported real MV candidates visible for repair"`: assert result `review_required`, reason `real-mv-unsupported`, no file move, and catalog writer not called.
  </action>
  <acceptance_criteria>
    - `rg -n "approveRealMvCandidate|isSelectedRealMvFile|deriveRealMvReadiness|copyRealMvCoverSidecar|real-mv-unsupported" apps/api/src/modules/catalog/admission-service.ts` finds all five patterns.
    - `rg -n "asset-\\$\\{record.candidate.id\\}-real-mv|dual-track-video|switchQualityStatus: \"review_required\"" apps/api/src/modules/catalog/admission-service.ts` finds real-MV asset construction.
    - `rg -n "single real MV|coverPath|cover.jpg|real-mv-unsupported|0x1101" apps/api/src/test/catalog-admission.test.ts` finds the new tests.
    - `pnpm -F @home-ktv/api test -- src/test/catalog-admission.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/catalog-admission.test.ts</automated>
  </verify>
  <done>Real-MV approval creates one formal song and one real-MV asset for promotable candidates, and keeps unsupported candidates visible without promotion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the catalog admission writer for real-MV asset fields</name>
  <files>apps/api/src/modules/catalog/admission-service.ts, apps/api/src/test/catalog-admission.test.ts</files>
  <read_first>
    - apps/api/src/modules/catalog/admission-service.ts
    - apps/api/src/modules/catalog/repositories/asset-repository.ts
    - apps/api/src/db/schema.ts
    - apps/api/src/test/catalog-admission.test.ts
    - packages/domain/src/index.ts
  </read_first>
  <behavior>
    - `PgCatalogAdmissionWriter.promoteApprovedCandidate` can insert legacy pair assets unchanged.
    - For real-MV assets, writer inserts `asset_kind`, `status`, `switch_quality_status`, `compatibility_status`, `compatibility_reasons`, `media_info_summary`, `media_info_provenance`, `track_roles`, and `playback_profile`.
    - Songs are inserted/updated with caller-provided `songStatus` instead of hard-coded `ready`.
  </behavior>
  <action>
    Extend `PromoteApprovedCandidateInput`:
    ```ts
    songStatus?: SongStatus;
    assets: Array<{
      assetId: string;
      importFileId: string;
      filePath: string;
      vocalMode: VocalMode;
      durationMs: number;
      assetKind?: AssetKind;
      status?: AssetStatus;
      switchFamily?: string | null;
      switchQualityStatus?: SwitchQualityStatus;
      compatibilityStatus?: CompatibilityStatus;
      compatibilityReasons?: readonly CompatibilityReason[];
      mediaInfoSummary?: MediaInfoSummary | null;
      mediaInfoProvenance?: MediaInfoProvenance | null;
      trackRoles?: TrackRoles;
      playbackProfile?: PlaybackProfile;
    }>;
    ```
    Keep existing legacy callers valid by defaulting omitted values to `songStatus: "ready"`, `assetKind: "video"`, `status: "ready"`, `switchFamily: input.switchFamily`, `switchQualityStatus: "verified"`, `compatibilityStatus: "playable"`, empty compatibility reasons, default MediaInfo/provenance, default track roles, and default playback profile.

    Update the `INSERT INTO songs` query to use a `$status` parameter instead of literal `'ready'` in both `VALUES` and `DO UPDATE SET status = EXCLUDED.status`.

    Update the `INSERT INTO assets` query to include columns:
    ```sql
    asset_kind, status, switch_family, switch_quality_status,
    compatibility_status, compatibility_reasons, media_info_summary,
    media_info_provenance, track_roles, playback_profile
    ```
    Pass real-MV values as JSONB for the JSON columns and use `ON CONFLICT(id) DO UPDATE` to update the same columns.

    Extend the existing `"persists generated title and artist search keys when promoting formal songs"` writer test or add a new one to assert the recorded SQL contains `compatibility_status`, `track_roles`, and `playback_profile`, and values contain `"dual-track-video"`, `"review_required"`, and a `trackRoles` object with `instrumental.id === "0x1101"`.
  </action>
  <acceptance_criteria>
    - `rg -n "songStatus\\?: SongStatus|compatibilityStatus\\?: CompatibilityStatus|playbackProfile\\?: PlaybackProfile" apps/api/src/modules/catalog/admission-service.ts` finds extended input fields.
    - `rg -n "compatibility_status|media_info_summary|media_info_provenance|track_roles|playback_profile" apps/api/src/modules/catalog/admission-service.ts` finds writer SQL columns.
    - `rg -n "dual-track-video|trackRoles|playback_profile" apps/api/src/test/catalog-admission.test.ts` finds writer assertions.
    - `pnpm -F @home-ktv/api test -- src/test/catalog-admission.test.ts` exits 0.
    - `pnpm -F @home-ktv/api typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/catalog-admission.test.ts &amp;&amp; pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <done>Catalog promotion persists one real-MV asset with the real-MV JSONB fields while preserving legacy pair admission defaults.</done>
</task>

</tasks>

<verification>
Run `pnpm -F @home-ktv/api test -- src/test/catalog-admission.test.ts` and `pnpm -F @home-ktv/api typecheck`.
</verification>

<success_criteria>
- Real-MV candidates no longer go through the legacy missing-original-instrumental-pair rejection.
- Formal catalog admission creates one Song and one real-MV Asset with reviewed `trackRoles`.
- `playable` may become `ready`; `review_required` or `unknown` become formal `review_required`; `unsupported` is not force-approved.
- Runtime switch verification remains Phase 15 and no asset gets `switchQualityStatus: "verified"` from the real-MV branch.
</success_criteria>

<output>
After completion, create `.planning/phases/14-admin-review-and-catalog-admission/14-03-SUMMARY.md`.
</output>
