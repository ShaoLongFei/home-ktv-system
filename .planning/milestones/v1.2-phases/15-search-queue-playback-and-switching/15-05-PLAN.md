---
phase: 15-search-queue-playback-and-switching
plan: 05
type: execute
wave: 5
depends_on: [15-01, 15-02, 15-03, 15-04]
files_modified:
  - apps/mobile-controller/src/App.tsx
  - apps/mobile-controller/src/runtime/use-room-controller-runtime.ts
  - apps/mobile-controller/src/api/client.ts
  - apps/mobile-controller/src/test/controller.test.tsx
  - apps/tv-player/src/test/app-runtime.test.tsx
  - apps/api/src/test/real-mv-playback-flow.test.ts
autonomous: true
requirements: [PLAY-01, PLAY-02, PLAY-03, PLAY-04, PLAY-05]
must_haves:
  truths:
    - "Mobile users can queue queueable real-MV search results and cannot queue disabled real-MV results."
    - "The current-song switch button stays visible/clickable for eligible real-MV playback and surfaces failures."
    - "Cross-surface tests cover search -> queue -> playback target -> switch target -> TV commit/failure behavior."
  artifacts:
    - path: "apps/mobile-controller/src/App.tsx"
      provides: "Chinese disabled result UI and queue button behavior"
      contains: "需预处理"
    - path: "apps/mobile-controller/src/test/controller.test.tsx"
      provides: "Mobile regression coverage for disabled real-MV search results and switch failures"
      contains: "暂不可播放"
    - path: "apps/api/src/test/real-mv-playback-flow.test.ts"
      provides: "API regression coverage for end-to-end real-MV search/queue/playback/switch contracts"
      contains: "real MV playback flow"
  key_links:
    - from: "apps/mobile-controller/src/App.tsx"
      to: "packages/domain/src/index.ts"
      via: "SongSearchVersionOption.canQueue"
      pattern: "canQueue"
    - from: "apps/api/src/test/real-mv-playback-flow.test.ts"
      to: "apps/api/src/modules/playback/build-playback-target.ts"
      via: "selectedTrackRef"
      pattern: "selectedTrackRef"
---

<objective>
Finish Mobile UI behavior and cross-surface regression coverage for real-MV playback.

Purpose: the backend and TV runtime plans create the mechanics; this plan makes the user-facing Mobile surface obvious and adds one high-signal regression suite that proves the Phase 15 flow works end to end without relying only on isolated unit tests.
Output: Mobile search cards disable nonqueueable real-MV versions with Chinese labels, queueable real MV can be added normally, switch failures surface in Mobile/TV notices, and API/TV/Mobile tests cover PLAY-01 through PLAY-05.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-search-queue-playback-and-switching/15-CONTEXT.md
@.planning/phases/15-search-queue-playback-and-switching/15-RESEARCH.md
@apps/mobile-controller/src/App.tsx
@apps/mobile-controller/src/runtime/use-room-controller-runtime.ts
@apps/mobile-controller/src/api/client.ts
@apps/mobile-controller/src/test/controller.test.tsx
@apps/tv-player/src/test/app-runtime.test.tsx
@apps/api/src/test/song-search-routes.test.ts
@apps/api/src/test/room-queue-commands.test.ts
@apps/api/src/test/build-playback-target.test.ts
@apps/api/src/test/build-switch-target.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Render disabled real-MV search versions in Mobile</name>
  <files>apps/mobile-controller/src/App.tsx, apps/mobile-controller/src/test/controller.test.tsx</files>
  <read_first>
    - apps/mobile-controller/src/App.tsx
    - apps/mobile-controller/src/runtime/use-room-controller-runtime.ts
    - apps/mobile-controller/src/test/controller.test.tsx
    - packages/domain/src/index.ts
    - .planning/phases/15-search-queue-playback-and-switching/15-CONTEXT.md
  </read_first>
  <behavior>
    - Queueable versions keep the existing click-to-queue behavior.
    - Disabled versions remain visible but cannot call `add-queue-entry`.
    - Mobile labels are Chinese and short: `"需预处理"`, `"暂不可播放"`, or `"缺少伴唱声轨"`.
  </behavior>
  <action>
    In `apps/mobile-controller/src/App.tsx`, update the search result version rendering:
    - Read `version.canQueue`, `version.queueState`, and `version.disabledLabel`.
    - The queue button is disabled when `version.canQueue === false`.
    - Disabled button text should be `version.disabledLabel ?? "暂不可播放"`.
    - Queueable button text should remain the existing Chinese queue action text.
    - Add a compact status badge near the version metadata when disabled. Use the exact class name `version-option__status` if the component uses plain CSS; otherwise use the existing local styling pattern and include a queryable text node.

    In `requestAddSongVersion`, do not add a new vocal-mode parameter and do not branch on original/accompaniment. Mobile still sends only `songId` and `assetId`.

    Add Mobile tests:
    - a search result with `canQueue: false`, `disabledLabel: "需预处理"` renders that text and clicking it does not send `/commands/add-queue-entry`;
    - a search result with `canQueue: true` still sends `/commands/add-queue-entry` with `{ songId, assetId }` and no `vocalMode` field;
    - a missing disabled label falls back to `"暂不可播放"`.
  </action>
  <acceptance_criteria>
    - `rg -n "canQueue|disabledLabel|version-option__status|需预处理|暂不可播放" apps/mobile-controller/src/App.tsx apps/mobile-controller/src/test/controller.test.tsx` exits 0.
    - `rg -n "vocalMode" apps/mobile-controller/src/api/client.ts apps/mobile-controller/src/runtime/use-room-controller-runtime.ts` does not show a new add-queue payload field.
    - `pnpm -F @home-ktv/mobile-controller test -- src/test/controller.test.tsx` exits 0.
    - `pnpm -F @home-ktv/mobile-controller typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/mobile-controller test -- src/test/controller.test.tsx &amp;&amp; pnpm -F @home-ktv/mobile-controller typecheck</automated>
  </verify>
  <done>Mobile search communicates disabled real-MV states without hiding songs or adding a queue-time vocal picker.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Keep Mobile switch action visible and surface switch failures</name>
  <files>apps/mobile-controller/src/App.tsx, apps/mobile-controller/src/runtime/use-room-controller-runtime.ts, apps/mobile-controller/src/test/controller.test.tsx</files>
  <read_first>
    - apps/mobile-controller/src/App.tsx
    - apps/mobile-controller/src/runtime/use-room-controller-runtime.ts
    - apps/mobile-controller/src/test/controller.test.tsx
    - packages/player-contracts/src/index.ts
    - .planning/phases/15-search-queue-playback-and-switching/15-CONTEXT.md
  </read_first>
  <behavior>
    - The switch button remains clickable when there is a current target and the server exposes a pending/available switch flow.
    - Mobile errors for `SWITCH_TARGET_NOT_AVAILABLE` or runtime switch failure are Chinese.
    - Mobile does not permanently disable switching after one failure.
  </behavior>
  <action>
    In `apps/mobile-controller/src/runtime/use-room-controller-runtime.ts`, update `switchVocalMode` error handling:
    - If command fails with `SWITCH_TARGET_NOT_AVAILABLE`, set `errorMessage` to `"当前歌曲暂不支持切换原唱/伴唱"` and keep the UI interactive.
    - If snapshot notice kind is `switch_failed_reverted`, allow App to render the notice through the existing notice/banner path.

    In `apps/mobile-controller/src/App.tsx`, ensure the current-song switch button is visible when `snapshot.currentTarget` exists and not hidden only because `currentTarget.switchFamily` is null. Real-MV same-asset switching can have `switchFamily` null on the source asset; availability is decided by the server command and TV runtime.

    Add tests:
    - a current real-MV target with `switchFamily: null`, `playbackProfile.kind: "single_file_audio_tracks"`, and `selectedTrackRef` still shows the switch button;
    - clicking switch sends `/commands/switch-vocal-mode`;
    - an API rejection code `SWITCH_TARGET_NOT_AVAILABLE` renders `"当前歌曲暂不支持切换原唱/伴唱"`.
  </action>
  <acceptance_criteria>
    - `rg -n "SWITCH_TARGET_NOT_AVAILABLE|当前歌曲暂不支持切换原唱/伴唱" apps/mobile-controller/src/runtime/use-room-controller-runtime.ts apps/mobile-controller/src/test/controller.test.tsx` exits 0.
    - `rg -n "single_file_audio_tracks" apps/mobile-controller/src/test/controller.test.tsx` exits 0.
    - `pnpm -F @home-ktv/mobile-controller test -- src/test/controller.test.tsx` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/mobile-controller test -- src/test/controller.test.tsx</automated>
  </verify>
  <done>Mobile lets users attempt real-MV switching and reports unavailable attempts clearly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add API cross-surface real-MV playback flow regression suite</name>
  <files>apps/api/src/test/real-mv-playback-flow.test.ts</files>
  <read_first>
    - apps/api/src/test/song-search-routes.test.ts
    - apps/api/src/test/room-queue-commands.test.ts
    - apps/api/src/test/build-playback-target.test.ts
    - apps/api/src/test/build-switch-target.test.ts
    - apps/api/src/modules/catalog/repositories/song-repository.ts
    - apps/api/src/modules/playback/session-command-service.ts
    - apps/api/src/modules/playback/build-playback-target.ts
    - apps/api/src/modules/playback/build-switch-target.ts
  </read_first>
  <behavior>
    - One regression file proves the formal real-MV flow across search, queue, playback target, and switch target.
    - The test uses repository/service fakes already present in the API test suite.
    - It does not require real media files, transcoding, Android TV, or external providers.
  </behavior>
  <action>
    Create `apps/api/src/test/real-mv-playback-flow.test.ts`. Reuse in-memory repository/fake harness patterns from existing API tests instead of introducing a live DB dependency.

    Add tests:
    - `"real MV playback flow searches queues and builds selected accompaniment target"`:
      - create one ready song with one ready local real-MV asset;
      - asset has `assetKind: "dual-track-video"`, `vocalMode: "dual"`, `compatibilityStatus: "playable"`, `playbackProfile.kind: "single_file_audio_tracks"`, `requiresAudioTrackSelection: true`, and track roles `0x1100` original, `0x1101` instrumental;
      - assert search returns a local version with `canQueue: true`;
      - execute `add-queue-entry`;
      - assert queue entry `playbackOptions.preferredVocalMode === "instrumental"`;
      - build playback target and assert `vocalMode === "instrumental"` and `selectedTrackRef.id === "0x1101"`.
    - `"real MV playback flow builds same-asset switch target and commits original mode"`:
      - start with the queue entry above;
      - build switch target and assert `switchKind === "audio_track"`, `fromAssetId === toAssetId`, `vocalMode === "original"`, `selectedTrackRef.id === "0x1100"`;
      - ingest `playing` telemetry with `stage: "switch_committed"` and `vocalMode: "original"`;
      - assert the queue entry committed `preferredVocalMode === "original"`.
    - `"real MV playback flow leaves unsupported results visible but disabled"`:
      - create an unsupported real-MV asset;
      - assert search returns `canQueue: false`, `queueState: "needs_preprocess"`, `disabledLabel: "需预处理"`;
      - assert `add-queue-entry` returns `SONG_NOT_QUEUEABLE`.

    Add a source guard proving out-of-scope items are absent:
    ```ts
    expect(source).not.toMatch(/Android TV|transcod|OpenList|download/i);
    ```
  </action>
  <acceptance_criteria>
    - `test -f apps/api/src/test/real-mv-playback-flow.test.ts` exits 0.
    - `rg -n "real MV playback flow|selectedTrackRef|switch_committed|需预处理|SONG_NOT_QUEUEABLE" apps/api/src/test/real-mv-playback-flow.test.ts` exits 0.
    - `rg -n "Android TV|transcod|OpenList|download" apps/api/src/test/real-mv-playback-flow.test.ts` exits 0 only inside the source guard.
    - `pnpm -F @home-ktv/api test -- src/test/real-mv-playback-flow.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/real-mv-playback-flow.test.ts</automated>
  </verify>
  <done>One regression suite proves the Phase 15 contract from search through queue, playback target, switch target, and switch commit.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Run final Phase 15 regression set</name>
  <files>apps/api/src/test/real-mv-playback-flow.test.ts, apps/mobile-controller/src/test/controller.test.tsx, apps/tv-player/src/test/app-runtime.test.tsx</files>
  <read_first>
    - apps/api/src/test/real-mv-playback-flow.test.ts
    - apps/mobile-controller/src/test/controller.test.tsx
    - apps/tv-player/src/test/app-runtime.test.tsx
    - .planning/phases/15-search-queue-playback-and-switching/15-CONTEXT.md
    - .planning/phases/15-search-queue-playback-and-switching/15-RESEARCH.md
  </read_first>
  <behavior>
    - Final verification covers API, Mobile, TV, and shared contracts.
    - Test names and grep evidence map back to PLAY-01 through PLAY-05.
  </behavior>
  <action>
    Run and fix only Phase 15-related failures in:
    ```bash
    pnpm -F @home-ktv/api test -- src/test/song-search-routes.test.ts src/test/room-queue-commands.test.ts src/test/build-playback-target.test.ts src/test/build-switch-target.test.ts src/test/player-runtime-contract.test.ts src/test/real-mv-playback-flow.test.ts
    pnpm -F @home-ktv/mobile-controller test -- src/test/controller.test.tsx
    pnpm -F @home-ktv/tv-player test -- src/test/active-playback-controller.test.tsx src/test/app-runtime.test.tsx src/test/tv-screen-states.test.tsx
    pnpm -F @home-ktv/player-contracts typecheck
    pnpm -F @home-ktv/api typecheck
    pnpm -F @home-ktv/mobile-controller typecheck
    pnpm -F @home-ktv/tv-player typecheck
    ```

    Do not fix unrelated dirty worktree files unless the failing output proves they are part of Phase 15 behavior.
  </action>
  <acceptance_criteria>
    - API regression command above exits 0.
    - Mobile regression command above exits 0.
    - TV regression command above exits 0.
    - All four typecheck commands above exit 0.
    - `rg -n "PLAY-01|PLAY-02|PLAY-03|PLAY-04|PLAY-05" .planning/phases/15-search-queue-playback-and-switching/15-*-PLAN.md` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>All commands listed in the action block exit 0.</automated>
  </verify>
  <done>Phase 15 has direct automated evidence across search, queue, playback target, switching, UI, and failure states.</done>
</task>

</tasks>

<verification>
Run:
- `pnpm -F @home-ktv/api test -- src/test/song-search-routes.test.ts src/test/room-queue-commands.test.ts src/test/build-playback-target.test.ts src/test/build-switch-target.test.ts src/test/player-runtime-contract.test.ts src/test/real-mv-playback-flow.test.ts`
- `pnpm -F @home-ktv/mobile-controller test -- src/test/controller.test.tsx`
- `pnpm -F @home-ktv/tv-player test -- src/test/active-playback-controller.test.tsx src/test/app-runtime.test.tsx src/test/tv-screen-states.test.tsx`
- `pnpm -F @home-ktv/player-contracts typecheck`
- `pnpm -F @home-ktv/api typecheck`
- `pnpm -F @home-ktv/mobile-controller typecheck`
- `pnpm -F @home-ktv/tv-player typecheck`
</verification>

<success_criteria>
- PLAY-01 through PLAY-05 each have UI or automated regression evidence.
- Mobile queueing remains simple and does not ask for vocal mode.
- Switch attempts remain server-authoritative and failure-tolerant.
- Unsupported/preprocess states are visible and disabled, not hidden.
</success_criteria>

<output>
After completion, create `.planning/phases/15-search-queue-playback-and-switching/15-05-SUMMARY.md`.
</output>
