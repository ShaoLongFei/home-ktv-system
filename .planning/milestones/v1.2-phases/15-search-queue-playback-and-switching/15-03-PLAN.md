---
phase: 15-search-queue-playback-and-switching
plan: 03
type: execute
wave: 3
depends_on: [15-02]
files_modified:
  - packages/player-contracts/src/index.ts
  - apps/api/src/modules/playback/build-switch-target.ts
  - apps/api/src/modules/playback/apply-switch-transition.ts
  - apps/api/src/modules/player/telemetry-service.ts
  - apps/api/src/modules/playback/repositories/queue-entry-repository.ts
  - apps/api/src/test/build-switch-target.test.ts
  - apps/api/src/test/player-runtime-contract.test.ts
autonomous: true
requirements: [PLAY-03, PLAY-04, PLAY-05]
must_haves:
  truths:
    - "The existing switch-vocal-mode command path supports same-asset real-MV audio-track switching."
    - "SwitchTarget explicitly carries selectedTrackRef and playbackProfile for audio-track switches."
    - "Successful TV switch telemetry commits the current real-MV vocal mode so snapshots stop requesting the same switch."
  artifacts:
    - path: "packages/player-contracts/src/index.ts"
      provides: "SwitchTarget contract for asset switches and audio-track switches"
      contains: "switchKind"
    - path: "apps/api/src/modules/playback/build-switch-target.ts"
      provides: "Same-asset real-MV switch-target builder"
      contains: "buildRealMvSwitchTarget"
    - path: "apps/api/src/modules/player/telemetry-service.ts"
      provides: "Switch commit persistence hook"
      contains: "updatePreferredVocalMode"
  key_links:
    - from: "apps/api/src/modules/playback/build-switch-target.ts"
      to: "packages/player-contracts/src/index.ts"
      via: "SwitchTarget.selectedTrackRef"
      pattern: "selectedTrackRef"
    - from: "apps/api/src/modules/player/telemetry-service.ts"
      to: "apps/api/src/modules/playback/repositories/queue-entry-repository.ts"
      via: "queue entry playbackOptions preferredVocalMode"
      pattern: "preferredVocalMode"
---

<objective>
Extend the existing switch transition contract for same-asset real-MV audio-track switching.

Purpose: existing switching assumes a verified counterpart Asset. Real MV must reuse `switch-vocal-mode`, `/player/switch-transition`, realtime snapshots, telemetry, rollback, and conflict semantics while changing only the selected source audio track inside the same Asset.
Output: `SwitchTarget` can represent `switchKind: "audio_track"` with `fromAssetId === toAssetId`, explicit `selectedTrackRef`, and successful TV `playing` telemetry commits queue `playbackOptions.preferredVocalMode`.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-search-queue-playback-and-switching/15-CONTEXT.md
@.planning/phases/15-search-queue-playback-and-switching/15-RESEARCH.md
@packages/player-contracts/src/index.ts
@apps/api/src/modules/playback/build-switch-target.ts
@apps/api/src/modules/playback/apply-switch-transition.ts
@apps/api/src/modules/player/telemetry-service.ts
@apps/api/src/modules/playback/repositories/queue-entry-repository.ts
@apps/api/src/test/build-switch-target.test.ts
@apps/api/src/test/player-runtime-contract.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend SwitchTarget with explicit switch kind and selected track fields</name>
  <files>packages/player-contracts/src/index.ts, apps/api/src/test/build-switch-target.test.ts</files>
  <read_first>
    - packages/player-contracts/src/index.ts
    - packages/domain/src/index.ts
    - apps/api/src/test/build-switch-target.test.ts
    - .planning/phases/15-search-queue-playback-and-switching/15-RESEARCH.md
  </read_first>
  <behavior>
    - Legacy counterpart-asset switch targets are explicitly `switchKind: "asset"`.
    - Real-MV same-asset switch targets can be represented as `switchKind: "audio_track"`.
    - `selectedTrackRef` and `playbackProfile` are available to TV at switch time.
  </behavior>
  <action>
    In `packages/player-contracts/src/index.ts`, extend `SwitchTarget` with:
    ```ts
    switchKind: "asset" | "audio_track";
    playbackProfile?: PlaybackProfile;
    selectedTrackRef?: TrackRef | null;
    ```

    Import `PlaybackProfile` and `TrackRef` from `@home-ktv/domain` if not already imported.

    Update all existing API tests/builders that construct `SwitchTarget` to include `switchKind: "asset"`. Do not remove `fromAssetId`, `toAssetId`, `rollbackAssetId`, `switchFamily`, or `playbackUrl`; existing TV rollback code still needs them.
  </action>
  <acceptance_criteria>
    - `rg -n "switchKind: \"asset\" \\| \"audio_track\"" packages/player-contracts/src/index.ts` exits 0.
    - `rg -n "selectedTrackRef\\?: TrackRef \\| null" packages/player-contracts/src/index.ts` exits 0.
    - `rg -n "playbackProfile\\?: PlaybackProfile" packages/player-contracts/src/index.ts` exits 0.
    - `rg -n "switchKind: \"asset\"" apps/api/src/test apps/tv-player/src/test packages/player-contracts/src/index.ts` exits 0.
    - `pnpm -F @home-ktv/player-contracts typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/player-contracts typecheck</automated>
  </verify>
  <done>`SwitchTarget` can explicitly describe both legacy asset switches and real-MV audio-track switches.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build same-asset real-MV switch targets from reviewed trackRoles</name>
  <files>apps/api/src/modules/playback/build-switch-target.ts, apps/api/src/modules/playback/apply-switch-transition.ts, apps/api/src/test/build-switch-target.test.ts</files>
  <read_first>
    - apps/api/src/modules/playback/build-switch-target.ts
    - apps/api/src/modules/playback/apply-switch-transition.ts
    - apps/api/src/test/build-switch-target.test.ts
    - apps/api/src/modules/playback/build-playback-target.ts
    - apps/api/src/modules/playback/repositories/queue-entry-repository.ts
    - packages/domain/src/index.ts
    - packages/player-contracts/src/index.ts
  </read_first>
  <behavior>
    - Legacy counterpart-asset switch behavior remains available.
    - Real-MV switch target uses the same asset id and same playback URL.
    - Target vocal mode toggles between `original` and `instrumental` based on committed queue `playbackOptions.preferredVocalMode`.
    - If the target role is missing, no switch target is available.
  </behavior>
  <action>
    In `apps/api/src/modules/playback/build-switch-target.ts`, add helper functions:
    ```ts
    function isSingleFileRealMvAsset(asset: Asset): boolean
    function committedVocalModeForQueueEntry(queueEntry: QueueEntry): "original" | "instrumental"
    function nextRealMvVocalMode(current: "original" | "instrumental"): "original" | "instrumental"
    function selectedTrackRefForRealMvSwitch(asset: Asset, vocalMode: "original" | "instrumental"): TrackRef | null
    function buildRealMvSwitchTarget(...): SwitchTarget | null
    ```

    Exact semantics:
    - Current mode is `queueEntry.playbackOptions.preferredVocalMode` when it is `"original"` or `"instrumental"`, otherwise `"instrumental"`.
    - Target mode is the opposite mode.
    - `selectedTrackRef` is `asset.trackRoles[targetMode]`; if missing, return null.
    - Return `switchKind: "audio_track"`, `fromAssetId: currentAsset.id`, `toAssetId: currentAsset.id`, `rollbackAssetId: currentAsset.id`, `playbackUrl: assetGateway.createPlaybackUrl(currentAsset.id)`, `switchFamily: currentAsset.switchFamily ?? "real-mv-audio-track"`, `playbackProfile: currentAsset.playbackProfile`, and `selectedTrackRef`.

    Keep the existing counterpart branch and mark its returned target as `switchKind: "asset"` with `selectedTrackRef` and `playbackProfile` for the target asset when available.

    In `apply-switch-transition.ts`, preserve current behavior but ensure the returned `switchTarget` keeps `selectedTrackRef`, `playbackProfile`, and `switchKind` after overriding `resumePositionMs`.

    Add tests to `apps/api/src/test/build-switch-target.test.ts`:
    - `"builds audio-track switch target for formal real MV from accompaniment to original"`: one real-MV asset, queue entry `preferredVocalMode: "instrumental"`, assert `switchKind === "audio_track"`, `fromAssetId === toAssetId`, target `vocalMode === "original"`, and `selectedTrackRef.id === "0x1100"`.
    - `"builds audio-track switch target for formal real MV from original to accompaniment"`: queue entry `preferredVocalMode: "original"`, assert target `selectedTrackRef.id === "0x1101"`.
    - `"returns null when real MV target role is missing"`: remove the target role and assert null.
    - Update legacy switch test expected object with `switchKind: "asset"`.
  </action>
  <acceptance_criteria>
    - `rg -n "buildRealMvSwitchTarget|nextRealMvVocalMode|selectedTrackRefForRealMvSwitch" apps/api/src/modules/playback/build-switch-target.ts` exits 0.
    - `rg -n "switchKind: \"audio_track\"" apps/api/src/modules/playback/build-switch-target.ts apps/api/src/test/build-switch-target.test.ts` exits 0.
    - `rg -n "fromAssetId: currentAsset.id" apps/api/src/modules/playback/build-switch-target.ts` exits 0.
    - `rg -n "selectedTrackRef.id.*0x1100|selectedTrackRef.id.*0x1101" apps/api/src/test/build-switch-target.test.ts` exits 0.
    - `pnpm -F @home-ktv/api test -- src/test/build-switch-target.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/build-switch-target.test.ts</automated>
  </verify>
  <done>The existing switch transition path can return same-asset audio-track switch targets for real MV.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Commit real-MV switch success to queue playbackOptions</name>
  <files>apps/api/src/modules/player/telemetry-service.ts, apps/api/src/modules/playback/repositories/queue-entry-repository.ts, apps/api/src/test/player-runtime-contract.test.ts</files>
  <read_first>
    - apps/api/src/modules/player/telemetry-service.ts
    - apps/api/src/modules/playback/repositories/queue-entry-repository.ts
    - apps/api/src/test/player-runtime-contract.test.ts
    - apps/api/src/test/realtime-room-sync.test.ts
    - packages/domain/src/index.ts
  </read_first>
  <behavior>
    - TV switch success is committed only after TV sends `eventType: "playing"` with `stage: "switch_committed"`.
    - Queue `playbackOptions.preferredVocalMode` changes to telemetry `vocalMode` only for `"original"` or `"instrumental"`.
    - `switch_failed` does not commit the attempted target mode.
  </behavior>
  <action>
    In `QueueEntryRepository`, add:
    ```ts
    updatePreferredVocalMode(input: {
      roomId: RoomId;
      queueEntryId: QueueEntryId;
      preferredVocalMode: Extract<VocalMode, "original" | "instrumental">;
    }): Promise<QueueEntry | null>;
    ```

    Implement it in `PgQueueEntryRepository` by updating `playback_options` JSONB with:
    ```sql
    jsonb_set(COALESCE(playback_options, '{}'::jsonb), '{preferredVocalMode}', to_jsonb($3::text), true)
    ```

    Implement it in `InMemoryQueueEntryRepository` by replacing `entry.playbackOptions.preferredVocalMode`.

    In `IngestTelemetryInput`, widen `queueEntries` to include `"markPlaybackState" | "updatePreferredVocalMode"`.

    In `ingestPlayerTelemetry`, after the current queue status update, add:
    ```ts
    if (
      telemetry.eventType === "playing" &&
      telemetry.stage === "switch_committed" &&
      (telemetry.vocalMode === "original" || telemetry.vocalMode === "instrumental")
    ) {
      await input.queueEntries?.updatePreferredVocalMode?.({
        roomId: telemetry.room.id,
        queueEntryId: telemetry.queueEntryId,
        preferredVocalMode: telemetry.vocalMode
      });
    }
    ```

    Add tests in `apps/api/src/test/player-runtime-contract.test.ts` or the existing telemetry-focused suite:
    - `"commits real MV current vocal mode after switch_committed telemetry"`: call telemetry ingest with `eventType: "playing"`, `stage: "switch_committed"`, `vocalMode: "original"` and assert queue entry preferred mode changes to original.
    - `"does not commit preferred vocal mode on switch_failed telemetry"`: assert queue entry remains instrumental after `eventType: "switch_failed"`.
  </action>
  <acceptance_criteria>
    - `rg -n "updatePreferredVocalMode" apps/api/src/modules/playback/repositories/queue-entry-repository.ts apps/api/src/modules/player/telemetry-service.ts` exits 0.
    - `rg -n "switch_committed" apps/api/src/modules/player/telemetry-service.ts apps/api/src/test/player-runtime-contract.test.ts` exits 0.
    - `rg -n "jsonb_set\\(COALESCE\\(playback_options" apps/api/src/modules/playback/repositories/queue-entry-repository.ts` exits 0.
    - `pnpm -F @home-ktv/api test -- src/test/build-switch-target.test.ts src/test/player-runtime-contract.test.ts` exits 0.
    - `pnpm -F @home-ktv/api typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/build-switch-target.test.ts src/test/player-runtime-contract.test.ts &amp;&amp; pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <done>Successful same-asset TV switches become committed server state without committing failed attempts.</done>
</task>

</tasks>

<verification>
Run:
- `pnpm -F @home-ktv/player-contracts typecheck`
- `pnpm -F @home-ktv/api test -- src/test/build-switch-target.test.ts`
- `pnpm -F @home-ktv/api test -- src/test/player-runtime-contract.test.ts`
- `pnpm -F @home-ktv/api typecheck`
</verification>

<success_criteria>
- PLAY-03 switch payloads carry selected track intent.
- PLAY-04 same-asset real-MV switching reuses the existing switch command and transition endpoint.
- PLAY-05 failed switch attempts leave committed mode unchanged.
- No separate real-MV-only command is introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/15-search-queue-playback-and-switching/15-03-SUMMARY.md`.
</output>
