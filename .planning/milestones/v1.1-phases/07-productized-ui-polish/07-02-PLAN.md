---
phase: 07-productized-ui-polish
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - apps/mobile-controller/src/App.css
  - apps/mobile-controller/src/App.tsx
  - apps/mobile-controller/src/runtime/use-room-controller.ts
  - apps/mobile-controller/src/test/controller.test.tsx
  - apps/admin/src/App.css
  - apps/admin/src/rooms/RoomStatusView.tsx
  - apps/admin/src/imports/ImportWorkbench.tsx
  - apps/admin/src/songs/SongCatalogView.tsx
  - apps/admin/src/test/room-status.test.tsx
  - apps/admin/src/test/import-workbench.test.tsx
  - apps/admin/src/test/song-catalog.test.tsx
autonomous: true
requirements:
  - PROD-02
  - PROD-03
  - PROD-04
must_haves:
  truths:
    - "Mobile search, queue, current playback, online supplement, dialogs, and buttons remain stable at phone width"
    - "Mobile command buttons keep centered text and at least 44px height"
    - "Admin import, songs, rooms, and online task actions show disabled/busy states and visible refreshed results"
    - "Admin task promote/retry/clean results are visible without browser reload"
  artifacts:
    - path: "apps/mobile-controller/src/App.css"
      provides: "phone-width layout constraints and centered button text"
      contains: "@media (max-width: 520px)"
    - path: "apps/mobile-controller/src/App.tsx"
      provides: "stable online candidate and current playback row structure"
      contains: "online-candidate-row"
    - path: "apps/admin/src/rooms/RoomStatusView.tsx"
      provides: "task action busy state and post-action refresh feedback"
      contains: "busyTaskAction"
    - path: "apps/admin/src/App.css"
      provides: "Admin narrow/desktop layout constraints and task row stability"
      contains: ".room-task-row"
  key_links:
    - from: "apps/mobile-controller/src/runtime/use-room-controller.ts"
      to: "apps/mobile-controller/src/App.tsx"
      via: "command/search state drives button busy and feedback labels"
      pattern: "pendingSupplementKeys"
    - from: "apps/admin/src/rooms/RoomStatusView.tsx"
      to: "apps/admin/src/test/room-status.test.tsx"
      via: "task actions refresh room state without page reload"
      pattern: "handleTaskAction"
---

<objective>
Polish Mobile and Admin layouts, button states, action feedback, and visible refresh behavior.

Purpose: After copy is controlled, the most visible product issues are phone-width stability, centered button text, online supplement row readability, and Admin actions that must reflect their result without manual browser refresh.
Output: refined CSS/layouts, stable busy states, better command feedback, and interaction regression tests.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-productized-ui-polish/07-CONTEXT.md
@.planning/phases/07-productized-ui-polish/07-RESEARCH.md
@.planning/phases/07-productized-ui-polish/07-UI-SPEC.md
@.planning/phases/07-productized-ui-polish/07-01-SUMMARY.md
@apps/mobile-controller/src/App.css
@apps/mobile-controller/src/App.tsx
@apps/mobile-controller/src/runtime/use-room-controller.ts
@apps/admin/src/App.css
@apps/admin/src/rooms/RoomStatusView.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add interaction tests for busy states and immediate feedback</name>
  <files>apps/mobile-controller/src/test/controller.test.tsx, apps/admin/src/test/room-status.test.tsx, apps/admin/src/test/import-workbench.test.tsx, apps/admin/src/test/song-catalog.test.tsx</files>
  <read_first>
    - apps/mobile-controller/src/test/controller.test.tsx
    - apps/admin/src/test/room-status.test.tsx
    - apps/admin/src/test/import-workbench.test.tsx
    - apps/admin/src/test/song-catalog.test.tsx
    - .planning/phases/07-productized-ui-polish/07-UI-SPEC.md
  </read_first>
  <action>
    Update Mobile tests:
    - Add a test named `keeps request supplement disabled while submission is pending and then shows ready state`.
    - In that test, make `/commands/request-supplement` resolve after a controlled promise. Click `请求补歌`, assert the same button shows `提交中` and is disabled, resolve the promise with task status `ready`, then assert the candidate shows `已准备` and the button text is `已准备`.
    - Add assertions to existing queue/control tests that `切歌`, `顶歌`, `删除`, `撤销`, `点歌`, and `请求补歌` buttons are enabled only when their existing domain flags allow it.

    Update Admin Rooms tests:
    - Assert clicking `入库任务 task-ready` disables task action buttons while the request is pending.
    - Assert the promoted task disappears or changes to `已入库` after the command/refresh completes without calling `window.location.reload`.
    - Add `vi.spyOn(window.location, "reload")` only if supported by happy-dom; otherwise assert no test helper triggers reload and document that in the test comment.

    Update Admin import/catalog tests:
    - Assert `扫描导入目录` shows a busy/disabled state while scan mutation is pending.
    - Assert catalog revalidate/validate/set-default actions have disabled state while a mutation is pending where current component structure supports this.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/mobile-controller test -- controller</automated>
    <automated>pnpm -F @home-ktv/admin test -- room-status import-workbench song-catalog</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile-controller/src/test/controller.test.tsx` contains `keeps request supplement disabled while submission is pending`.
    - `apps/admin/src/test/room-status.test.tsx` contains `window.location.reload` or `without browser reload`.
    - `apps/admin/src/test/import-workbench.test.tsx` contains `扫描导入目录`.
    - `pnpm -F @home-ktv/mobile-controller test -- controller` fails before implementation if busy feedback is missing.
  </acceptance_criteria>
  <done>Tests define action feedback and no-reload behavior for Mobile/Admin interactions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Polish Mobile controller layout and button state behavior</name>
  <files>apps/mobile-controller/src/App.css, apps/mobile-controller/src/App.tsx, apps/mobile-controller/src/runtime/use-room-controller.ts</files>
  <read_first>
    - apps/mobile-controller/src/App.css
    - apps/mobile-controller/src/App.tsx
    - apps/mobile-controller/src/runtime/use-room-controller.ts
    - apps/mobile-controller/src/i18n.tsx
    - .planning/phases/07-productized-ui-polish/07-UI-SPEC.md
  </read_first>
  <action>
    Update `apps/mobile-controller/src/App.css`:
    - Keep global `button` as `display: inline-flex`, `align-items: center`, `justify-content: center`, `min-height: 44px`, and `text-align: center`.
    - Add `min-width: 0` to `.top-bar`, `.top-actions`, `.panel`, `.current-panel`, `.queue-row`, `.song-row`, `.version-row`, `.result-main`, `.online-candidate-row`, and `.online-candidate-list`.
    - Add `overflow-wrap: anywhere` to song title, artist, provider, and metadata text selectors in `.queue-row`, `.song-row`, `.version-row`, and `.online-candidate-row`.
    - Ensure `.row-actions > button`, `.command-row > button`, `.song-row > button`, and `.version-row > button` have `min-width: 0` and centered text.
    - Under `@media (max-width: 520px)`, set `.command-row`, `.row-actions`, `.song-row > button`, `.version-row > button`, `.online-candidate-row > button`, and `.search-form > button` to `width: 100%`.
    - Keep cards/panels at `border-radius: 8px` or less.

    Update `apps/mobile-controller/src/App.tsx`:
    - Add a stable class to current state chip, for example `playback-state-chip`.
    - For online candidates, group productized metadata into short chips and keep source/provider as secondary text.
    - Ensure the request supplement button is disabled when `isPending || isReady` and uses `提交中` / `已准备` / `请求补歌` consistently.

    Update `apps/mobile-controller/src/runtime/use-room-controller.ts` only if needed:
    - Keep `pendingSupplementKeys`.
    - If a supplement request fails, remove the pending key in `finally` and set a productized error message fallback such as `请求补歌失败`.
    - Do not change command semantics or auto-enqueue behavior.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/mobile-controller test -- controller</automated>
    <automated>pnpm -F @home-ktv/mobile-controller typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile-controller/src/App.css` contains `.online-candidate-row`.
    - `apps/mobile-controller/src/App.css` contains `overflow-wrap: anywhere`.
    - `apps/mobile-controller/src/App.css` contains `.command-row > button`.
    - `apps/mobile-controller/src/App.tsx` contains `playback-state-chip`.
    - `apps/mobile-controller/src/App.tsx` contains `isPending ? t("button.submitting") : isReady ? t("button.ready") : t("button.requestSupplement")`.
    - `pnpm -F @home-ktv/mobile-controller test -- controller` exits 0.
    - `pnpm -F @home-ktv/mobile-controller typecheck` exits 0.
  </acceptance_criteria>
  <done>Mobile controller is phone-width stable with centered buttons and clear online supplement states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Polish Admin action feedback, task rows, and density</name>
  <files>apps/admin/src/App.css, apps/admin/src/rooms/RoomStatusView.tsx, apps/admin/src/imports/ImportWorkbench.tsx, apps/admin/src/songs/SongCatalogView.tsx</files>
  <read_first>
    - apps/admin/src/App.css
    - apps/admin/src/rooms/RoomStatusView.tsx
    - apps/admin/src/imports/ImportWorkbench.tsx
    - apps/admin/src/songs/SongCatalogView.tsx
    - apps/admin/src/i18n.tsx
    - .planning/phases/07-productized-ui-polish/07-UI-SPEC.md
  </read_first>
  <action>
    Update `apps/admin/src/rooms/RoomStatusView.tsx`:
    - For `handleTaskAction`, keep `busyTaskAction = "${action}:${task.taskId}"`.
    - Disable only task action buttons while `busyTaskAction !== null`, as current code does, but show localized busy text on the active button:
      - retry active: `重试中...`
      - clean active: `清理中...`
      - promote active: `入库中...`
    - After action success, keep `const refreshed = await refreshRoomStatus(roomSlug); setRoomStatus(refreshed);` so no browser reload is needed.
    - Add a short success status area only if the refreshed row result is not otherwise visible. If added, use text `任务状态已更新`.

    Update `apps/admin/src/imports/ImportWorkbench.tsx`:
    - Change the scan button label while pending from `扫描导入目录` to `扫描中...`.
    - Preserve `disabled={scanMutation.isPending}`.
    - If any queue query is error, show a localized error line `候选加载失败，请稍后重试。` instead of silently showing empty groups.

    Update `apps/admin/src/songs/SongCatalogView.tsx`:
    - If song query errors, show `歌曲加载失败，请稍后重试。`.
    - Keep list/detail structure; do not redesign as cards.

    Update `apps/admin/src/App.css`:
    - Add or refine constraints so `.admin-header-actions`, `.room-section-header`, `.room-task-row`, `.task-action-group`, `.catalog-filters`, `.status-strip`, `.candidate-row`, and `.song-row` wrap/truncate without horizontal overflow.
    - Keep Admin panels at 6px to 8px radius.
    - Add `overflow-wrap: anywhere` to task/event diagnostic text where raw IDs can be long.
    - Add disabled visual treatment for `.primary-button:disabled`, `.secondary-button:disabled`, `.danger-button:disabled`, and `.compact-button:disabled`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/admin test -- room-status import-workbench song-catalog</automated>
    <automated>pnpm -F @home-ktv/admin typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/admin/src/rooms/RoomStatusView.tsx` contains `入库中...`.
    - `apps/admin/src/imports/ImportWorkbench.tsx` contains `扫描中...`.
    - `apps/admin/src/imports/ImportWorkbench.tsx` contains `候选加载失败，请稍后重试。`.
    - `apps/admin/src/songs/SongCatalogView.tsx` contains `歌曲加载失败，请稍后重试。`.
    - `apps/admin/src/App.css` contains `.compact-button:disabled`.
    - `apps/admin/src/App.css` contains `overflow-wrap: anywhere`.
    - `pnpm -F @home-ktv/admin test -- room-status import-workbench song-catalog` exits 0.
    - `pnpm -F @home-ktv/admin typecheck` exits 0.
  </acceptance_criteria>
  <done>Admin key actions have clear busy/error feedback and operational rows remain scannable.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `pnpm -F @home-ktv/mobile-controller test -- controller`
- [ ] `pnpm -F @home-ktv/admin test -- room-status import-workbench song-catalog`
- [ ] `pnpm -F @home-ktv/mobile-controller typecheck`
- [ ] `pnpm -F @home-ktv/admin typecheck`
- [ ] `pnpm typecheck`
</verification>

<success_criteria>
- PROD-02: Empty/loading/error/success feedback is productized across Admin/Mobile.
- PROD-03: Mobile controller remains stable and centered on phone widths.
- PROD-04: Admin import/song/room/task actions have consistent disabled/busy/refresh feedback.
</success_criteria>

<output>
After completion, create `.planning/phases/07-productized-ui-polish/07-02-SUMMARY.md`.
</output>
