---
phase: 11-admin-runtime-boundary-completion
plan: 02
type: execute
wave: 1
depends_on: ["08-code-structure-hardening"]
gap_closure: true
files_modified:
  - apps/admin/src/songs/use-song-catalog-runtime.ts
  - apps/admin/src/songs/SongCatalogView.tsx
  - apps/admin/src/test/song-catalog-runtime.test.tsx
  - apps/admin/src/test/song-catalog.test.tsx
autonomous: true
must_haves:
  truths:
    - "Song Catalog query and mutation orchestration lives in useSongCatalogRuntime()"
    - "SongCatalogView.tsx renders state and callbacks instead of owning TanStack Query orchestration"
    - "SongDetailEditor visible behavior, Chinese copy, filters, busy states, load-error, evaluation, and validation behavior remain unchanged"
    - "Song runtime/helper tests prove the extracted boundary owns selection, cache updates, mutation side effects, and validation/evaluation state"
  artifacts:
    - path: "apps/admin/src/songs/use-song-catalog-runtime.ts"
      provides: "Feature-local runtime hook for Song Catalog"
      contains: "useSongCatalogRuntime"
    - path: "apps/admin/src/songs/SongCatalogView.tsx"
      provides: "Render-focused Song Catalog shell"
      contains: "useSongCatalogRuntime"
    - path: "apps/admin/src/test/song-catalog-runtime.test.tsx"
      provides: "Runtime boundary regression tests for Song Catalog"
      contains: "renderHook"
  key_links:
    - from: "apps/admin/src/songs/SongCatalogView.tsx"
      to: "apps/admin/src/songs/use-song-catalog-runtime.ts"
      via: "Song Catalog uses feature-local runtime hook"
      pattern: "useSongCatalogRuntime"
    - from: "apps/admin/src/test/song-catalog-runtime.test.tsx"
      to: "apps/admin/src/songs/use-song-catalog-runtime.ts"
      via: "Runtime tests import the extracted hook"
      pattern: "useSongCatalogRuntime"
---

<objective>
Extract Song Catalog runtime orchestration into a focused app-local hook while preserving the existing Admin catalog maintenance UI behavior.

Purpose: Close the Songs half of the `QUAL-01` Admin runtime boundary gap by moving catalog filters, song loading, selection repair, metadata/default-asset/asset/revalidate/validate mutations, cache updates, and evaluation/validation state out of `SongCatalogView.tsx`.
Output: `useSongCatalogRuntime()`, a thinner `SongCatalogView.tsx`, and focused runtime tests.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-admin-runtime-boundary-completion/11-CONTEXT.md
@.planning/phases/11-admin-runtime-boundary-completion/11-RESEARCH.md
@.planning/phases/08-code-structure-hardening/08-CONTEXT.md
@apps/admin/src/songs/SongCatalogView.tsx
@apps/admin/src/songs/SongDetailEditor.tsx
@apps/admin/src/songs/types.ts
@apps/admin/src/rooms/use-room-status.ts
@apps/admin/src/api/client.ts
@apps/admin/src/test/song-catalog.test.tsx
@apps/admin/package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Song Catalog runtime tests</name>
  <files>apps/admin/src/test/song-catalog-runtime.test.tsx</files>
  <read_first>
    - apps/admin/src/songs/SongCatalogView.tsx
    - apps/admin/src/songs/types.ts
    - apps/admin/src/test/song-catalog.test.tsx
    - apps/admin/src/rooms/use-room-status.ts
    - apps/admin/package.json
  </read_first>
  <action>
    Create `apps/admin/src/test/song-catalog-runtime.test.tsx` using Vitest, `renderHook`, `waitFor`, and a local `QueryClientProvider` wrapper.

    The tests should target the planned `useSongCatalogRuntime()` hook and may initially fail until Task 2 implements the hook.

    Include at least these behaviors:
    - The hook loads `/admin/catalog/songs`, exposes `songs`, selects the first song when no selected song exists, and clears selected song when the filtered list is empty.
    - Changing `status` to `review_required` requests `/admin/catalog/songs?status=review_required`.
    - `setDefaultAsset(songId, assetId)` PATCHes `/admin/catalog/songs/:songId/default-asset`, stores `evaluation`, and updates the matching song cache so `selectedSong.defaultAssetId` reflects the response.
    - `validateSong(songId)` requests `/admin/catalog/songs/:songId/validate` and stores `validation`.

    Use small fixtures shaped like `song-catalog.test.tsx`: one `ready` song with two assets and one `review_required` song is enough.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/admin test -- src/test/song-catalog-runtime.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `apps/admin/src/test/song-catalog-runtime.test.tsx` contains `renderHook`.
    - `apps/admin/src/test/song-catalog-runtime.test.tsx` contains `QueryClientProvider`.
    - `apps/admin/src/test/song-catalog-runtime.test.tsx` contains `useSongCatalogRuntime`.
    - `apps/admin/src/test/song-catalog-runtime.test.tsx` contains `/admin/catalog/songs?status=review_required`.
    - `apps/admin/src/test/song-catalog-runtime.test.tsx` contains `/admin/catalog/songs/song-1/default-asset`.
    - `apps/admin/src/test/song-catalog-runtime.test.tsx` contains `/admin/catalog/songs/song-1/validate`.
    - `pnpm -F @home-ktv/admin test -- src/test/song-catalog-runtime.test.tsx` exits 0 after Task 2.
  </acceptance_criteria>
  <done>Song Catalog runtime boundary behavior is specified before extraction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract useSongCatalogRuntime</name>
  <files>apps/admin/src/songs/use-song-catalog-runtime.ts, apps/admin/src/songs/SongCatalogView.tsx</files>
  <read_first>
    - apps/admin/src/songs/SongCatalogView.tsx
    - apps/admin/src/songs/SongDetailEditor.tsx
    - apps/admin/src/songs/types.ts
    - apps/admin/src/rooms/use-room-status.ts
    - apps/admin/src/api/client.ts
    - apps/admin/src/test/song-catalog-runtime.test.tsx
  </read_first>
  <action>
    Create `apps/admin/src/songs/use-song-catalog-runtime.ts`.

    Export:
    - `songStatusOptions: Array<SongStatus | "">` with values `""`, `ready`, `review_required`, `unavailable`.
    - `languageOptions: Array<Language | "">` with values `""`, `mandarin`, `cantonese`, `other`.
    - `useSongCatalogRuntime()`.

    `useSongCatalogRuntime()` must own the runtime orchestration currently in `SongCatalogView.tsx`:
    - `status` and `setStatus`.
    - `language` and `setLanguage`.
    - Catalog `useQuery` for `fetchCatalogSongs({ status, language })`.
    - `songs`.
    - `selectedSongId` and `setSelectedSongId`.
    - Selection repair: clear selection when `songs.length === 0`, otherwise select the first song if current selection is absent.
    - `selectedSong`.
    - `evaluation` state.
    - `validation` state.
    - Metadata save mutation exposed as `saveMetadata(songId, input)`.
    - Default asset mutation exposed as `setDefaultAsset(songId, assetId)`.
    - Asset mutation exposed as `updateAsset(assetId, patch)`.
    - Revalidate mutation exposed as `revalidateSong(songId)`.
    - Validate mutation exposed as `validateSong(songId)`.
    - Busy aggregation exposed as `isBusy`.
    - Query status flags exposed as `queryIsLoading` and `queryIsError`.
    - Cache helper equivalent to the current `cacheSong()` behavior.

    Refactor `SongCatalogView.tsx` so it imports `useSongCatalogRuntime`, `songStatusOptions`, and `languageOptions`, calls the hook once, and uses returned state/callbacks for rendering.

    Keep these visible behaviors unchanged:
    - Header title/description.
    - Status/language filters and labels.
    - Song list row copy and selected row behavior.
    - Catalog loading/error/empty-detail behavior.
    - `SongDetailEditor` props and callback behavior.
    - Evaluation and song.json validation display behavior.
    - Busy/disabled behavior for editor actions.

    If you discover a small stale-selection/cache bug during extraction, fix it only if a test covers it and document it in the eventual summary as a small bug fix.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/admin test -- src/test/song-catalog-runtime.test.tsx</automated>
    <automated>pnpm -F @home-ktv/admin test -- src/test/song-catalog.test.tsx</automated>
    <automated>pnpm -F @home-ktv/admin typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/admin/src/songs/use-song-catalog-runtime.ts` contains `export function useSongCatalogRuntime`.
    - `apps/admin/src/songs/use-song-catalog-runtime.ts` contains `songStatusOptions`.
    - `apps/admin/src/songs/use-song-catalog-runtime.ts` contains `languageOptions`.
    - `apps/admin/src/songs/use-song-catalog-runtime.ts` contains `useQuery`.
    - `apps/admin/src/songs/use-song-catalog-runtime.ts` contains `useMutation`.
    - `apps/admin/src/songs/use-song-catalog-runtime.ts` contains `cacheSong`.
    - `apps/admin/src/songs/SongCatalogView.tsx` contains `useSongCatalogRuntime`.
    - `apps/admin/src/songs/SongCatalogView.tsx` does not contain `useMutation(`.
    - `apps/admin/src/songs/SongCatalogView.tsx` does not contain `useQuery(`.
    - `pnpm -F @home-ktv/admin test -- src/test/song-catalog-runtime.test.tsx` exits 0.
    - `pnpm -F @home-ktv/admin test -- src/test/song-catalog.test.tsx` exits 0.
    - `pnpm -F @home-ktv/admin typecheck` exits 0.
  </acceptance_criteria>
  <done>Song Catalog is render-focused and catalog runtime orchestration lives behind a feature-local hook.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `pnpm -F @home-ktv/admin test -- src/test/song-catalog-runtime.test.tsx`
- [ ] `pnpm -F @home-ktv/admin test -- src/test/song-catalog.test.tsx`
- [ ] `pnpm -F @home-ktv/admin typecheck`
</verification>

<success_criteria>
- Song Catalog filters, song loading, selection, metadata save, default asset changes, asset updates, revalidation, song.json validation, cache updates, evaluation/validation state, and busy state are behind `useSongCatalogRuntime()`.
- Existing Song Catalog UI behavior remains unchanged.
- Focused runtime tests prove the new boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/11-admin-runtime-boundary-completion/11-02-SUMMARY.md`.
</output>

