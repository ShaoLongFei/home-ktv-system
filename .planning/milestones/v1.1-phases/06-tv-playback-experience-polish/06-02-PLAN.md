---
phase: 06-tv-playback-experience-polish
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - apps/tv-player/src/App.tsx
  - apps/tv-player/src/screens/IdleScreen.tsx
  - apps/tv-player/src/screens/PlayingScreen.tsx
  - apps/tv-player/src/screens/ConflictScreen.tsx
  - apps/tv-player/src/components/PlaybackStatusBanner.tsx
  - apps/tv-player/src/components/PairingQr.tsx
  - apps/tv-player/src/screens/tv-display-model.ts
  - apps/tv-player/src/test/app-runtime.test.tsx
  - apps/tv-player/src/test/playing-screen.test.tsx
  - apps/tv-player/src/test/playback-status-banner.test.tsx
autonomous: true
requirements:
  - TVUX-01
  - TVUX-02
  - TVUX-03
  - TVUX-04
  - TVUX-05
must_haves:
  truths:
    - "TV idle, loading, playing, recovering, offline/error, and conflict states render Chinese product copy"
    - "First-play blocked renders a couch-visible actionable prompt and clears after playback starts"
    - "Playing view persistently shows current song, artist, next song, vocal mode, playback state, and mm:ss / mm:ss text"
    - "Corner QR, status banner, song title, time text, and next-song text have bounded layout rules to prevent overlap"
    - "Transient playback notices are short and non-blocking; persistent conflict/offline/error states stay until state changes"
  artifacts:
    - path: "apps/tv-player/src/App.tsx"
      provides: "first-play blocked state routing and display model consumption"
      contains: "firstPlayBlocked"
    - path: "apps/tv-player/src/screens/PlayingScreen.tsx"
      provides: "polished video-dominant playing overlay"
      contains: "formatPlaybackClock"
    - path: "apps/tv-player/src/components/PlaybackStatusBanner.tsx"
      provides: "Chinese notice banner with stable bounds"
      contains: "noticeCopyFor"
    - path: "apps/tv-player/src/screens/IdleScreen.tsx"
      provides: "Chinese idle large-QR entry screen"
      contains: "扫码点歌"
  key_links:
    - from: "apps/tv-player/src/App.tsx"
      to: "apps/tv-player/src/screens/tv-display-model.ts"
      via: "deriveTvDisplayState creates state labels and first-play prompt input"
      pattern: "deriveTvDisplayState"
    - from: "apps/tv-player/src/App.tsx"
      to: "apps/tv-player/src/screens/PlayingScreen.tsx"
      via: "PlayingScreen receives displayState and firstPlayPrompt"
      pattern: "displayState"
    - from: "apps/tv-player/src/components/PlaybackStatusBanner.tsx"
      to: "apps/tv-player/src/screens/tv-display-model.ts"
      via: "noticeCopyFor provides Chinese fallback copy"
      pattern: "noticeCopyFor"
---

<objective>
Implement the polished TV screens, first-play prompt, Chinese state copy, bounded playing overlay, and non-blocking feedback surface.

Purpose: Phase 6 must make the TV screen understandable and readable during real singing without changing queue or playback command semantics.
Output: Updated TV components and runtime glue that consume the Plan 06-01 display model.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-tv-playback-experience-polish/06-CONTEXT.md
@.planning/phases/06-tv-playback-experience-polish/06-RESEARCH.md
@.planning/phases/06-tv-playback-experience-polish/06-UI-SPEC.md
@.planning/phases/06-tv-playback-experience-polish/06-01-SUMMARY.md
@apps/tv-player/src/App.tsx
@apps/tv-player/src/screens/tv-display-model.ts
@apps/tv-player/src/screens/IdleScreen.tsx
@apps/tv-player/src/screens/PlayingScreen.tsx
@apps/tv-player/src/screens/ConflictScreen.tsx
@apps/tv-player/src/components/PlaybackStatusBanner.tsx
@apps/tv-player/src/components/PairingQr.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend component tests for Chinese TV states and first-play prompt</name>
  <files>apps/tv-player/src/test/playing-screen.test.tsx, apps/tv-player/src/test/playback-status-banner.test.tsx, apps/tv-player/src/test/app-runtime.test.tsx</files>
  <read_first>
    - apps/tv-player/src/test/playing-screen.test.tsx
    - apps/tv-player/src/test/playback-status-banner.test.tsx
    - apps/tv-player/src/test/app-runtime.test.tsx
    - apps/tv-player/src/screens/tv-display-model.ts
    - .planning/phases/06-tv-playback-experience-polish/06-UI-SPEC.md
  </read_first>
  <action>
    Update `apps/tv-player/src/test/playing-screen.test.tsx` to assert the polished playing view contains:
    - `播放中`
    - `伴唱`
    - `00:12 / 03:00`
    - `下一首 - 歌手`
    - a first-play prompt heading `点击电视开始播放` when the `firstPlayPrompt.visible` prop is true.

    Update `apps/tv-player/src/test/playback-status-banner.test.tsx` so fallback copy for `{ kind: "playback_failed_skipped", message: "" }` is `播放失败，已跳到下一首`, and add a test for `{ kind: "switch_failed_reverted", message: "" }` rendering `切换失败，已恢复到原模式`.

    Update `apps/tv-player/src/test/app-runtime.test.tsx` so the autoplay-blocked test also waits for visible text `点击电视开始播放`, then simulates `pointerDown` and expects the prompt to clear after `ActivePlaybackController.ensurePlaying()` returns `playing` on retry. Preserve the existing telemetry expectation for `stage: "autoplay_blocked"`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/tv-player test -- playing-screen playback-status-banner app-runtime</automated>
  </verify>
  <acceptance_criteria>
    - `apps/tv-player/src/test/playing-screen.test.tsx` contains `点击电视开始播放`.
    - `apps/tv-player/src/test/playing-screen.test.tsx` contains `播放中`.
    - `apps/tv-player/src/test/playback-status-banner.test.tsx` contains `播放失败，已跳到下一首`.
    - `apps/tv-player/src/test/app-runtime.test.tsx` contains `autoplay_blocked`.
    - `pnpm -F @home-ktv/tv-player test -- playing-screen playback-status-banner app-runtime` fails before implementation because current components still use English copy/prompt wiring.
  </acceptance_criteria>
  <done>Tests define the expected polished TV copy, first-play prompt, and notice behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire first-play blocked state through App</name>
  <files>apps/tv-player/src/App.tsx, apps/tv-player/src/screens/tv-display-model.ts, apps/tv-player/src/test/app-runtime.test.tsx</files>
  <read_first>
    - apps/tv-player/src/App.tsx
    - apps/tv-player/src/runtime/active-playback-controller.ts
    - apps/tv-player/src/runtime/use-room-snapshot.ts
    - apps/tv-player/src/screens/tv-display-model.ts
    - apps/tv-player/src/test/app-runtime.test.tsx
  </read_first>
  <action>
    In `apps/tv-player/src/App.tsx`, add local state `const [firstPlayBlocked, setFirstPlayBlocked] = useState(false);`.

    Pass `setFirstPlayBlocked` into `synchronizePlayback()` and `ensureCurrentPlayback()`.

    When `ActivePlaybackController.ensurePlaying()` returns `blocked`, call `setFirstPlayBlocked(true)`, keep sending the existing `loading` telemetry with `stage: "autoplay_blocked"`, and set a local notice with Chinese message `点击电视开始播放`.

    When playback starts successfully, call `setFirstPlayBlocked(false)` before clearing loading notice. Also clear `firstPlayBlocked` when `snapshot.currentTarget` becomes null, `snapshot.conflict` is present, or `roomState.status === "error"`.

    Replace the current `renderScreen(status, snapshot, errorMessage, playbackPositionMs, durationMs)` call with a call that also passes `firstPlayBlocked`. Inside `renderScreen`, call `deriveTvDisplayState({ status, snapshot, errorMessage, firstPlayBlocked })` and pass the resulting `displayState` to TV screen components.

    Keep the existing pointerdown retry listener and telemetry behavior. Do not add a fake autoplay success path.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/tv-player test -- app-runtime tv-display-model</automated>
    <automated>pnpm -F @home-ktv/tv-player typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/tv-player/src/App.tsx` contains `firstPlayBlocked`.
    - `apps/tv-player/src/App.tsx` contains `setFirstPlayBlocked(true)`.
    - `apps/tv-player/src/App.tsx` contains `deriveTvDisplayState`.
    - `apps/tv-player/src/App.tsx` still contains `stage: "autoplay_blocked"`.
    - `pnpm -F @home-ktv/tv-player test -- app-runtime tv-display-model` exits 0.
  </acceptance_criteria>
  <done>Browser autoplay block is represented as visible TV UI state while telemetry remains intact.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Polish TV screens and feedback components</name>
  <files>apps/tv-player/src/screens/IdleScreen.tsx, apps/tv-player/src/screens/PlayingScreen.tsx, apps/tv-player/src/screens/ConflictScreen.tsx, apps/tv-player/src/components/PlaybackStatusBanner.tsx, apps/tv-player/src/components/PairingQr.tsx, apps/tv-player/src/App.tsx</files>
  <read_first>
    - apps/tv-player/src/screens/IdleScreen.tsx
    - apps/tv-player/src/screens/PlayingScreen.tsx
    - apps/tv-player/src/screens/ConflictScreen.tsx
    - apps/tv-player/src/components/PlaybackStatusBanner.tsx
    - apps/tv-player/src/components/PairingQr.tsx
    - apps/tv-player/src/screens/tv-display-model.ts
    - .planning/phases/06-tv-playback-experience-polish/06-UI-SPEC.md
  </read_first>
  <action>
    Update component props and rendering so:
    - `IdleScreen` accepts `displayState: TvDisplayState` and renders `displayState.heading` and `displayState.detail`; expected heading is `扫码点歌`.
    - `ConflictScreen` accepts `displayState: TvDisplayState` plus conflict details, renders heading `已有电视端在线`, and keeps `conflict.activeDeviceName` as secondary detail.
    - `PlayingScreen` accepts `displayState: TvDisplayState`, `playbackPositionMs`, `durationMs`, and renders `displayState.stateLabel`, current song, artist, vocal mode, `formatPlaybackClock(playbackPositionMs, durationMs)`, next song, `PlaybackStatusBanner`, corner QR, and first-play prompt when `displayState.firstPlayPrompt.visible` is true.
    - `PlaybackStatusBanner` uses `noticeCopyFor(notice)` from `tv-display-model.ts` and no longer exposes raw notice kind text like `playback_failed_skipped` as visible primary text.
    - `PairingQr` large caption becomes `手机扫码点歌`; corner caption remains compact but must not use uppercase transformation.

    Apply the UI-SPEC layout constraints:
    - Top rail uses bounded left status area and right QR area, with `minWidth: 0`.
    - Footer uses stable columns for mode, state, time, and next song; next song has `minWidth: 0` and can truncate before pushing time off-screen.
    - First-play prompt is a prominent overlay/panel with heading `点击电视开始播放` and body from `firstPlayPromptCopy`.
    - Long title and artist text have `maxWidth`, `overflowWrap: "anywhere"`, and no negative letter spacing.
    - Reduce decorative background intensity if it competes with video; do not add gradient orbs or decorative SVGs.

    Preserve the existing full-viewport video elements and `objectFit: "contain"`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/tv-player test -- playing-screen playback-status-banner app-runtime</automated>
    <automated>pnpm -F @home-ktv/tv-player typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/tv-player/src/screens/IdleScreen.tsx` contains `扫码点歌`.
    - `apps/tv-player/src/screens/PlayingScreen.tsx` contains `formatPlaybackClock`.
    - `apps/tv-player/src/screens/PlayingScreen.tsx` contains `点击电视开始播放`.
    - `apps/tv-player/src/screens/PlayingScreen.tsx` contains `minWidth: 0`.
    - `apps/tv-player/src/components/PlaybackStatusBanner.tsx` contains `noticeCopyFor`.
    - `apps/tv-player/src/components/PlaybackStatusBanner.tsx` does not render `notice.kind.replaceAll`.
    - `apps/tv-player/src/components/PairingQr.tsx` contains `手机扫码点歌`.
    - `pnpm -F @home-ktv/tv-player test -- playing-screen playback-status-banner app-runtime` exits 0.
    - `pnpm -F @home-ktv/tv-player typecheck` exits 0.
  </acceptance_criteria>
  <done>TV screens render polished Chinese state copy, first-play prompt, stable time text, bounded QR/status/title layout, and non-blocking playback feedback.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `pnpm -F @home-ktv/tv-player test -- playing-screen playback-status-banner app-runtime tv-display-model`
- [ ] `pnpm -F @home-ktv/tv-player typecheck`
</verification>

<success_criteria>
- TV idle, loading, playing, recovering, offline/error, and conflict states are clear and Chinese-first.
- First-play blocked state is visually actionable and clears on successful playback.
- Playing screen keeps current song, next song, vocal mode, playback state, QR, and time visible without adding a progress bar.
- Transient notices no longer expose raw technical notice kinds.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tv-playback-experience-polish/06-02-SUMMARY.md`.
</output>
