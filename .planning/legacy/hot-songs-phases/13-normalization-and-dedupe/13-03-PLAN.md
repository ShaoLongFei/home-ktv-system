---
phase: 13-normalization-and-dedupe
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - package.json
  - packages/hot-songs/package.json
  - packages/hot-songs/src/normalize-cli.ts
  - packages/hot-songs/src/test/normalize-cli.test.ts
  - .planning/reports/hot-songs/phase-13-fixture-candidates/candidate-snapshot.json
autonomous: true
requirements:
  - NORM-01
  - NORM-02
  - NORM-03
  - NORM-04
requirements_addressed: [NORM-01, NORM-02, NORM-03, NORM-04]
must_haves:
  truths:
    - "User can run a single normalization command against Phase 12 source-row artifacts"
    - "The command writes `candidate-snapshot.json` with schema version, source statuses, candidate identities, canonical keys, and raw evidence"
    - "The fixture candidate snapshot contains six candidates from fifteen source rows"
    - "The command does not score, rank, export CSV/Markdown, match OpenList files, download files, schedule jobs, mutate DB/catalog, or touch runtime code"
  artifacts:
    - path: "packages/hot-songs/src/normalize-cli.ts"
      provides: "Candidate normalization CLI"
      contains: "runNormalizeSourcesCli"
    - path: ".planning/reports/hot-songs/phase-13-fixture-candidates/candidate-snapshot.json"
      provides: "Fixture candidate identity snapshot"
      contains: "hot-songs.candidate-snapshot.v1"
---

<objective>
Wire a normalization CLI and produce the Phase 13 fixture candidate snapshot.

Purpose: Make the conservative identity layer usable from the command line and generate the artifact Phase 14 will consume for scoring/export planning.
Output: `pnpm hot-songs:normalize`, `candidate-snapshot.json`, CLI tests, and final fixture verification.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-normalization-and-dedupe/13-RESEARCH.md
@.planning/phases/13-normalization-and-dedupe/13-02-PLAN.md
@packages/hot-songs/src/cli.ts
@packages/hot-songs/src/manifest.ts
@packages/hot-songs/src/report/source-health.ts
@packages/hot-songs/src/normalize/candidates.ts
@.planning/reports/hot-songs/phase-12-fixture-all/source-rows.json
@.planning/reports/hot-songs/phase-12-fixture-all/source-report.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add normalization CLI</name>
  <files>package.json, packages/hot-songs/package.json, packages/hot-songs/src/normalize-cli.ts, packages/hot-songs/src/test/normalize-cli.test.ts</files>
  <read_first>
    - package.json
    - packages/hot-songs/package.json
    - packages/hot-songs/src/cli.ts
    - packages/hot-songs/src/manifest.ts
    - packages/hot-songs/src/normalize/candidates.ts
  </read_first>
  <action>
    Add root script:
    - `"hot-songs:normalize": "pnpm -F @home-ktv/hot-songs normalize"`

    Add package script:
    - `"normalize": "tsx src/normalize-cli.ts"`

    Create `packages/hot-songs/src/normalize-cli.ts` exporting:
    - `parseNormalizeSourcesArgs(argv: string[])`
    - `runNormalizeSourcesCli(argv: string[]): Promise<number>`

    CLI options:
    - `--source-rows` string required
    - `--source-report` string optional
    - `--out` string required
    - `--help` boolean with `-h`

    Behavior:
    - handle leading `--` from pnpm scripts, matching `parseCollectSourcesArgs`
    - resolve paths against `process.env.INIT_CWD ?? process.cwd()` using `resolveRunPath`
    - read and validate source rows JSON using `SourceRowSchema.array()`
    - read source report JSON when provided and pass source statuses into `buildCandidateSnapshot`
    - write `<outDir>/candidate-snapshot.json`
    - print `Candidate normalization complete: <candidateCount> candidates from <sourceRowCount> source rows`
    - return exit code `1` for invalid inputs

    Add CLI tests proving the Phase 12 fixture source rows produce a candidate snapshot and the help text contains `hot-songs:normalize`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/hot-songs test -- src/test/normalize-cli.test.ts</automated>
    <automated>pnpm -F @home-ktv/hot-songs typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains `hot-songs:normalize`.
    - `packages/hot-songs/package.json` contains `"normalize": "tsx src/normalize-cli.ts"`.
    - `packages/hot-songs/src/normalize-cli.ts` contains `runNormalizeSourcesCli`.
    - `packages/hot-songs/src/normalize-cli.ts` contains `candidate-snapshot.json`.
    - `packages/hot-songs/src/test/normalize-cli.test.ts` contains `hot-songs:normalize`.
    - `pnpm -F @home-ktv/hot-songs test -- src/test/normalize-cli.test.ts` exits 0.
  </acceptance_criteria>
  <done>User can run a normalization command that writes a schema-versioned candidate snapshot.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Generate and verify fixture candidate snapshot</name>
  <files>.planning/reports/hot-songs/phase-13-fixture-candidates/candidate-snapshot.json, packages/hot-songs/src/test/normalize-cli.test.ts</files>
  <read_first>
    - packages/hot-songs/src/normalize-cli.ts
    - .planning/reports/hot-songs/phase-12-fixture-all/source-rows.json
    - .planning/reports/hot-songs/phase-12-fixture-all/source-report.json
  </read_first>
  <action>
    Run:
    `pnpm hot-songs:normalize -- --source-rows .planning/reports/hot-songs/phase-12-fixture-all/source-rows.json --source-report .planning/reports/hot-songs/phase-12-fixture-all/source-report.json --out .planning/reports/hot-songs/phase-13-fixture-candidates`

    Commit the generated `candidate-snapshot.json`.

    Verify the snapshot:
    - schema version is `hot-songs.candidate-snapshot.v1`
    - `sourceRowCount` is `15`
    - `candidateCount` is `6`
    - `后来` / `刘若英` has evidence from at least five source rows
    - `同手同脚 (Live)` carries `variant-live`
    - candidate IDs start with `song_`
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/hot-songs test</automated>
    <automated>pnpm -F @home-ktv/hot-songs typecheck</automated>
    <automated>pnpm hot-songs:normalize -- --source-rows .planning/reports/hot-songs/phase-12-fixture-all/source-rows.json --source-report .planning/reports/hot-songs/phase-12-fixture-all/source-report.json --out .planning/reports/hot-songs/phase-13-fixture-candidates</automated>
    <automated>node -e "const fs=require('fs'); const s=JSON.parse(fs.readFileSync('.planning/reports/hot-songs/phase-13-fixture-candidates/candidate-snapshot.json','utf8')); if (s.schemaVersion !== 'hot-songs.candidate-snapshot.v1') throw new Error('bad schema'); if (s.sourceRowCount !== 15) throw new Error('bad row count'); if (s.candidateCount !== 6) throw new Error('bad candidate count'); const live=s.candidates.find(c => c.displayTitle.includes('同手同脚')); if (!live || !live.warnings.includes('variant-live')) throw new Error('missing live warning');"</automated>
    <automated>bash -lc 'if rg -n "score|ranking|tier|OpenList|download|scheduler|cron|database|postgres|Fastify|Admin UI|OCR" packages/hot-songs/src/normalize packages/hot-songs/src/normalize-cli.ts packages/hot-songs/package.json package.json; then exit 1; fi'</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/reports/hot-songs/phase-13-fixture-candidates/candidate-snapshot.json` contains `hot-songs.candidate-snapshot.v1`.
    - `.planning/reports/hot-songs/phase-13-fixture-candidates/candidate-snapshot.json` contains `candidateCount`.
    - `.planning/reports/hot-songs/phase-13-fixture-candidates/candidate-snapshot.json` contains `variant-live`.
    - `pnpm hot-songs:normalize -- --source-rows .planning/reports/hot-songs/phase-12-fixture-all/source-rows.json --source-report .planning/reports/hot-songs/phase-12-fixture-all/source-report.json --out .planning/reports/hot-songs/phase-13-fixture-candidates` exits 0.
  </acceptance_criteria>
  <done>Phase 13 produces a reviewable candidate identity snapshot from Phase 12 source rows.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `pnpm -F @home-ktv/hot-songs test`
- [ ] `pnpm -F @home-ktv/hot-songs typecheck`
- [ ] `pnpm hot-songs:normalize -- --source-rows .planning/reports/hot-songs/phase-12-fixture-all/source-rows.json --source-report .planning/reports/hot-songs/phase-12-fixture-all/source-report.json --out .planning/reports/hot-songs/phase-13-fixture-candidates`
- [ ] `node -e "const fs=require('fs'); const s=JSON.parse(fs.readFileSync('.planning/reports/hot-songs/phase-13-fixture-candidates/candidate-snapshot.json','utf8')); if (s.schemaVersion !== 'hot-songs.candidate-snapshot.v1') throw new Error('bad schema'); if (s.sourceRowCount !== 15) throw new Error('bad row count'); if (s.candidateCount !== 6) throw new Error('bad candidate count'); const live=s.candidates.find(c => c.displayTitle.includes('同手同脚')); if (!live || !live.warnings.includes('variant-live')) throw new Error('missing live warning');"`
- [ ] `bash -lc 'if rg -n "score|ranking|tier|OpenList|download|scheduler|cron|database|postgres|Fastify|Admin UI|OCR" packages/hot-songs/src/normalize packages/hot-songs/src/normalize-cli.ts packages/hot-songs/package.json package.json; then exit 1; fi'`
</verification>

<success_criteria>
- User can run a normalization command against Phase 12 source rows.
- Candidate snapshot preserves raw evidence, canonical keys, source statuses, candidate IDs, and variant warnings.
- Fixture snapshot contains 6 candidates from 15 source rows.
- No scoring, ranking tiers, Markdown/CSV final exports, OpenList matching, downloads, scheduler, Admin UI, OCR, DB/catalog mutation, or runtime mutation enters this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/13-normalization-and-dedupe/13-03-SUMMARY.md`.
</output>
