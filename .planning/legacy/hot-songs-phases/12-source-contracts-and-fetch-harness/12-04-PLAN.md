---
phase: 12-source-contracts-and-fetch-harness
plan: 04
type: execute
wave: 4
depends_on: ["12-03"]
files_modified:
  - packages/hot-songs/package.json
  - pnpm-lock.yaml
  - packages/hot-songs/src/fetch/http.ts
  - packages/hot-songs/src/adapters/qq-toplist.ts
  - packages/hot-songs/src/cli.ts
  - packages/hot-songs/src/test/adapters.test.ts
  - packages/hot-songs/fixtures/html/qq-kge-toplist.fixture.html
autonomous: true
requirements:
  - SRC-01
  - SRC-02
  - SRC-03
  - SRC-04
  - SRC-05
requirements_addressed: [SRC-01, SRC-02, SRC-03, SRC-04, SRC-05]
must_haves:
  truths:
    - "QQ Music `K歌金曲榜` is implemented as public KTV-first source `qq-kge-toplist` with adapter `qq_toplist`"
    - "QQ toplist parsing is fixture-backed and does not require live network in tests"
    - "The HTTP helper uses native fetch, timeout, and public metadata user-agent"
    - "QQ adapter failures surface through the existing source health runner"
  artifacts:
    - path: "packages/hot-songs/src/adapters/qq-toplist.ts"
      provides: "QQ Music embedded toplist parser"
      contains: "collectQqToplistSource"
    - path: "packages/hot-songs/src/fetch/http.ts"
      provides: "Public metadata fetch helper"
      contains: "home-ktv-hot-songs/0.1 (+public metadata only)"
---

<objective>
Add the QQ Music public toplist adapter, including `K歌金曲榜`.

Purpose: Implement the strongest automated K歌 signal required by SRC-02 and establish the reusable public HTML/data fetch pattern for support sources.
Output: HTTP helper, QQ adapter, fixture, adapter tests, and CLI registry mapping for `qq_toplist`.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-source-contracts-and-fetch-harness/12-RESEARCH.md
@.planning/phases/12-source-contracts-and-fetch-harness/12-03-PLAN.md
@packages/hot-songs/package.json
@packages/hot-songs/src/contracts.ts
@packages/hot-songs/src/runner.ts
@packages/hot-songs/src/cli.ts
@packages/hot-songs/config/sources.example.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add public fetch helper</name>
  <files>packages/hot-songs/package.json, pnpm-lock.yaml, packages/hot-songs/src/fetch/http.ts</files>
  <read_first>
    - packages/hot-songs/package.json
    - packages/hot-songs/src/runner.ts
    - .planning/phases/12-source-contracts-and-fetch-harness/12-RESEARCH.md
  </read_first>
  <action>
    Add dependency `"cheerio": "1.2.0"` to `@home-ktv/hot-songs` and update `pnpm-lock.yaml`.

    Create `packages/hot-songs/src/fetch/http.ts` exporting `FetchText` and `fetchText(url, timeoutMs)`.
    `fetchText` must call native `fetch`, use `AbortSignal.timeout(timeoutMs)`, send user agent `home-ktv-hot-songs/0.1 (+public metadata only)`, throw `GET <url> -> <status>` for non-OK responses, and return `response.text()`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/hot-songs typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/hot-songs/package.json` contains `"cheerio": "1.2.0"`.
    - `packages/hot-songs/src/fetch/http.ts` contains `AbortSignal.timeout`.
    - `packages/hot-songs/src/fetch/http.ts` contains `home-ktv-hot-songs/0.1 (+public metadata only)`.
    - `packages/hot-songs/src/fetch/http.ts` contains `GET`.
    - `pnpm -F @home-ktv/hot-songs typecheck` exits 0.
  </acceptance_criteria>
  <done>Public chart adapters have a timeout-bound HTTP helper.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Parse QQ K歌 toplist rows</name>
  <files>packages/hot-songs/src/adapters/qq-toplist.ts, packages/hot-songs/src/cli.ts, packages/hot-songs/src/test/adapters.test.ts, packages/hot-songs/fixtures/html/qq-kge-toplist.fixture.html</files>
  <read_first>
    - packages/hot-songs/src/contracts.ts
    - packages/hot-songs/src/fetch/http.ts
    - packages/hot-songs/src/runner.ts
    - packages/hot-songs/src/cli.ts
    - packages/hot-songs/config/sources.example.json
  </read_first>
  <action>
    Create `packages/hot-songs/src/adapters/qq-toplist.ts` exporting `collectQqToplistSource` and `parseQqToplistRows`.

    Parser requirements:
    - read embedded `window.__INITIAL_DATA__`
    - extract rank, song title, and singer names
    - preserve raw title and raw artists
    - set `sourceUrl` from `source.url`
    - set `sourcePublishedAt` when embedded data contains a date/period, otherwise return warning `missing-source-published-at`
    - add warning `missing-artist` for rows with no artist
    - throw `QQ toplist data not found` when no embedded data can be parsed

    Add fixture `packages/hot-songs/fixtures/html/qq-kge-toplist.fixture.html` containing `window.__INITIAL_DATA__`, `topId:36` or `"topId":36`, and rows for `后来` / `刘若英`, `小幸运` / `田馥甄`, and `Run Wild（向风而野）` / `周深`.

    Update `packages/hot-songs/src/cli.ts` so adapter registry maps `qq_toplist` to `collectQqToplistSource`.

    Add `packages/hot-songs/src/test/adapters.test.ts` proving fixture parsing returns three rows and first row has `sourceId: "qq-kge-toplist"`, `sourceType: "ktv_first"`, rank `1`, `rawTitle: "后来"`, and artist `刘若英`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/hot-songs test -- src/test/adapters.test.ts</automated>
    <automated>pnpm -F @home-ktv/hot-songs typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/hot-songs/src/adapters/qq-toplist.ts` contains `collectQqToplistSource`.
    - `packages/hot-songs/src/adapters/qq-toplist.ts` contains `parseQqToplistRows`.
    - `packages/hot-songs/src/adapters/qq-toplist.ts` contains `QQ toplist data not found`.
    - `packages/hot-songs/fixtures/html/qq-kge-toplist.fixture.html` contains `window.__INITIAL_DATA__`.
    - `packages/hot-songs/fixtures/html/qq-kge-toplist.fixture.html` contains `Run Wild（向风而野）`.
    - `packages/hot-songs/src/cli.ts` contains `collectQqToplistSource`.
    - `packages/hot-songs/src/test/adapters.test.ts` contains `qq-kge-toplist`.
    - `pnpm -F @home-ktv/hot-songs test -- src/test/adapters.test.ts` exits 0.
  </acceptance_criteria>
  <done>QQ Music K歌金曲榜 can be collected as a public KTV-first source.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `pnpm -F @home-ktv/hot-songs test -- src/test/adapters.test.ts`
- [ ] `pnpm -F @home-ktv/hot-songs typecheck`
</verification>

<success_criteria>
- QQ K歌金曲榜 fixture rows parse into source rows.
- `qq_toplist` is wired into the CLI adapter registry.
- Public fetch uses timeout and public metadata user agent.
- No private cookies, login scraping, downloads, normalization, dedupe, scoring, DB, or runtime changes are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/12-source-contracts-and-fetch-harness/12-04-SUMMARY.md`.
</output>
