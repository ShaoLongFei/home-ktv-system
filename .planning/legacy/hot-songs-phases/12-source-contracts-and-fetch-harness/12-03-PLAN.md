---
phase: 12-source-contracts-and-fetch-harness
plan: 03
type: execute
wave: 3
depends_on: ["12-02"]
files_modified:
  - packages/hot-songs/src/runner.ts
  - packages/hot-songs/src/report/source-health.ts
  - packages/hot-songs/src/cli.ts
  - packages/hot-songs/src/test/runner.test.ts
autonomous: true
requirements:
  - SRC-01
  - SRC-04
  - SRC-05
requirements_addressed: [SRC-01, SRC-04, SRC-05]
must_haves:
  truths:
    - "The collector records a status for every configured source"
    - "Status values are exactly `succeeded`, `failed`, `stale`, or `skipped`"
    - "Source health reports include row counts, usable flags, warnings, and failure reasons"
    - "Partial failures remain visible while the run continues if at least one usable source has rows"
    - "The command fails when no usable source rows remain or required configuration is invalid"
  artifacts:
    - path: "packages/hot-songs/src/runner.ts"
      provides: "Source execution and health gate"
      contains: "collectSources"
    - path: "packages/hot-songs/src/report/source-health.ts"
      provides: "Source report JSON writer"
      contains: "hot-songs.source-report.v1"
    - path: "packages/hot-songs/src/cli.ts"
      provides: "Manual-source-capable collect-sources execution"
      contains: "source-report.json"
---

<objective>
Wire the runner, source health report, and executable CLI for manual sources.

Purpose: Make the Phase 12 harness runnable with the CAVCA manual source and prove partial failures, stale/skipped statuses, and no-usable-source failure behavior before public adapters are added.
Output: `collectSources`, `source-report.json`, `source-rows.json`, manual adapter CLI wiring, and runner tests.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-source-contracts-and-fetch-harness/12-RESEARCH.md
@.planning/phases/12-source-contracts-and-fetch-harness/12-01-PLAN.md
@.planning/phases/12-source-contracts-and-fetch-harness/12-02-PLAN.md
@packages/hot-songs/src/contracts.ts
@packages/hot-songs/src/manifest.ts
@packages/hot-songs/src/adapters/manual-json.ts
@packages/hot-songs/src/cli.ts
@packages/hot-songs/config/sources.example.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement runner and source health report</name>
  <files>packages/hot-songs/src/runner.ts, packages/hot-songs/src/report/source-health.ts, packages/hot-songs/src/test/runner.test.ts</files>
  <read_first>
    - packages/hot-songs/src/contracts.ts
    - packages/hot-songs/src/manifest.ts
    - packages/hot-songs/src/adapters/manual-json.ts
    - packages/hot-songs/config/sources.example.json
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Create `packages/hot-songs/src/runner.ts` exporting `SourceAdapter`, `CollectContext`, `collectOneSource`, and `collectSources`.

    Runner behavior:
    - disabled sources become `skipped`, `usable: false`, `rowCount: 0`, warning `source-disabled`
    - `--source` filters become `skipped`, `usable: false`, `rowCount: 0`, warning `source-filtered`
    - adapter exceptions become `failed`, `usable: false`, `rowCount: 0`, and `error`
    - low row counts add warning `row-count-below-expected`
    - stale sources become `stale`, with `usable: true` only when `usableWhenStale` is true and `rowCount > 0`
    - successful non-stale sources with rows become `succeeded`, `usable: true`
    - after all sources settle, throw `No usable hot-song sources remain. Check source-report.json for failed, stale, and skipped sources.` when no usable row-bearing source remains

    Create `packages/hot-songs/src/report/source-health.ts` exporting `buildSourceHealthReport` and `writeSourceReport`.
    The report file must be `<outDir>/source-report.json` and include `schemaVersion: "hot-songs.source-report.v1"`, `generatedAt`, `totalRows`, `usableSourceCount`, `statusCounts`, and `sources`.

    Add `runner.test.ts` covering partial failure, disabled, filtered, stale, no usable source, and status count reporting.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/hot-songs test -- src/test/runner.test.ts</automated>
    <automated>pnpm -F @home-ktv/hot-songs typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/hot-songs/src/runner.ts` contains `collectSources`.
    - `packages/hot-songs/src/runner.ts` contains `source-disabled`.
    - `packages/hot-songs/src/runner.ts` contains `source-filtered`.
    - `packages/hot-songs/src/runner.ts` contains `row-count-below-expected`.
    - `packages/hot-songs/src/runner.ts` contains `No usable hot-song sources remain`.
    - `packages/hot-songs/src/report/source-health.ts` contains `hot-songs.source-report.v1`.
    - `packages/hot-songs/src/test/runner.test.ts` contains `succeeded`.
    - `packages/hot-songs/src/test/runner.test.ts` contains `failed`.
    - `packages/hot-songs/src/test/runner.test.ts` contains `stale`.
    - `packages/hot-songs/src/test/runner.test.ts` contains `skipped`.
    - `pnpm -F @home-ktv/hot-songs test -- src/test/runner.test.ts` exits 0.
  </acceptance_criteria>
  <done>Source execution produces complete visible health statuses and enforces the no-usable-source gate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute CLI with manual adapter</name>
  <files>packages/hot-songs/src/cli.ts, packages/hot-songs/src/test/runner.test.ts</files>
  <read_first>
    - packages/hot-songs/src/cli.ts
    - packages/hot-songs/src/manifest.ts
    - packages/hot-songs/src/runner.ts
    - packages/hot-songs/src/report/source-health.ts
    - packages/hot-songs/src/adapters/manual-json.ts
    - packages/hot-songs/config/sources.example.json
  </read_first>
  <action>
    Update `packages/hot-songs/src/cli.ts` so it exports `runCollectSourcesCli(argv: string[]): Promise<number>` and executes it when run as the package script.

    Define `const runRoot = process.env.INIT_CWD ?? process.cwd()` inside CLI execution. Resolve `--manifest` and `--out` through `resolveRunPath(value, runRoot)` before loading or writing. Pass `runRoot` into `collectSources` so manual snapshot `file` paths in the manifest are resolved from the same root. This is required because the user-facing root script uses `pnpm -F @home-ktv/hot-songs collect-sources`, which runs inside `packages/hot-songs` while the command arguments and example manifest paths are root-relative.

    CLI behavior:
    - resolves root-relative CLI paths using `INIT_CWD`
    - loads manifest with `loadSourceManifest`
    - maps `manual_json` to `collectManualJsonSource`
    - maps public adapters to a temporary failure adapter that throws `Adapter <name> is not implemented in this plan`; Plan 12-04 and 12-05 replace those mappings
    - calls `collectSources`
    - writes `<outDir>/source-rows.json`
    - writes `<outDir>/source-report.json`
    - prints `Source collection complete: <rowCount> rows from <usableSourceCount> usable sources`
    - returns exit code `1` for invalid manifest or no usable source rows

    Extend tests or add runner/CLI tests proving the root command can run with `--source cavca-golden-mic-manual`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/hot-songs test -- src/test/runner.test.ts</automated>
    <automated>pnpm -F @home-ktv/hot-songs typecheck</automated>
    <automated>pnpm hot-songs:sources -- --manifest packages/hot-songs/config/sources.example.json --out .planning/reports/hot-songs/phase-12-fixture --source cavca-golden-mic-manual</automated>
  </verify>
  <acceptance_criteria>
    - `packages/hot-songs/src/cli.ts` contains `runCollectSourcesCli`.
    - `packages/hot-songs/src/cli.ts` contains `collectManualJsonSource`.
    - `packages/hot-songs/src/cli.ts` contains `INIT_CWD`.
    - `packages/hot-songs/src/cli.ts` contains `resolveRunPath`.
    - `packages/hot-songs/src/cli.ts` contains `source-rows.json`.
    - `packages/hot-songs/src/cli.ts` contains `source-report.json`.
    - `pnpm hot-songs:sources -- --manifest packages/hot-songs/config/sources.example.json --out .planning/reports/hot-songs/phase-12-fixture --source cavca-golden-mic-manual` exits 0 and creates `.planning/reports/hot-songs/phase-12-fixture/source-report.json`.
  </acceptance_criteria>
  <done>The user can run one command against a configured manual KTV source and receive source health artifacts.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `pnpm -F @home-ktv/hot-songs test -- src/test/runner.test.ts`
- [ ] `pnpm -F @home-ktv/hot-songs typecheck`
- [ ] `pnpm hot-songs:sources -- --manifest packages/hot-songs/config/sources.example.json --out .planning/reports/hot-songs/phase-12-fixture --source cavca-golden-mic-manual`
- [ ] `node -e "const fs=require('fs'); const r=JSON.parse(fs.readFileSync('.planning/reports/hot-songs/phase-12-fixture/source-report.json','utf8')); if (r.schemaVersion !== 'hot-songs.source-report.v1') throw new Error('bad source report schema'); if (!r.sources.some(s => s.sourceId === 'cavca-golden-mic-manual' && s.status === 'succeeded')) throw new Error('missing CAVCA success');"`
</verification>

<success_criteria>
- Source health report includes row counts, warnings, usable flags, and all Phase 12 status values.
- Partial failures remain visible while at least one usable source exists.
- No usable sources fail the command.
- Manual-source CLI run succeeds and writes `source-report.json` and `source-rows.json`.
</success_criteria>

<output>
After completion, create `.planning/phases/12-source-contracts-and-fetch-harness/12-03-SUMMARY.md`.
</output>
