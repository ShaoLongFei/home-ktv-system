---
phase: 06-tv-playback-experience-polish
plan: 03
type: execute
wave: 3
depends_on: ["06-02"]
files_modified:
  - apps/tv-player/src/test/tv-screen-states.test.tsx
  - apps/tv-player/src/test/playing-screen.test.tsx
  - apps/tv-player/src/test/app-runtime.test.tsx
  - scripts/tv-visual-check.mjs
  - package.json
  - .planning/phases/06-tv-playback-experience-polish/06-UAT.md
autonomous: true
requirements:
  - TVUX-01
  - TVUX-02
  - TVUX-03
  - TVUX-04
  - TVUX-05
must_haves:
  truths:
    - "Automated TV tests cover idle, playing, loading/recovering, offline/error, conflict, notices, first-play prompt, and long text"
    - "A visual validation script captures TV screenshots at 1920x1080 and 1366x768 using local Chrome"
    - "UAT instructions tell the tester exactly what to verify on TV for Phase 6"
    - "Full TV Player test and typecheck commands pass before phase verification"
  artifacts:
    - path: "apps/tv-player/src/test/tv-screen-states.test.tsx"
      provides: "screen-state regression coverage"
      contains: "已有电视端在线"
    - path: "scripts/tv-visual-check.mjs"
      provides: "Chrome screenshot validation helper"
      contains: "--window-size=1920,1080"
    - path: "package.json"
      provides: "root visual check script entry"
      contains: "tv:visual-check"
    - path: ".planning/phases/06-tv-playback-experience-polish/06-UAT.md"
      provides: "manual UAT checklist for user verification"
      contains: "点击电视开始播放"
  key_links:
    - from: "package.json"
      to: "scripts/tv-visual-check.mjs"
      via: "pnpm tv:visual-check runs Chrome screenshot helper"
      pattern: "tv:visual-check"
    - from: "apps/tv-player/src/test/tv-screen-states.test.tsx"
      to: "apps/tv-player/src/screens/PlayingScreen.tsx"
      via: "long title and state rendering are tested against actual components"
      pattern: "PlayingScreen"
---

<objective>
Complete Phase 6 verification coverage with TV state tests, long-text regression tests, visual screenshot tooling, and a user-facing UAT checklist.

Purpose: Phase 6 is only done when TV behavior is test-covered and the user has a clear way to validate it visually in a real browser.
Output: Additional Vitest coverage, a Chrome screenshot helper, root script entry, and Phase 6 UAT instructions.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-tv-playback-experience-polish/06-CONTEXT.md
@.planning/phases/06-tv-playback-experience-polish/06-RESEARCH.md
@.planning/phases/06-tv-playback-experience-polish/06-UI-SPEC.md
@.planning/phases/06-tv-playback-experience-polish/06-02-SUMMARY.md
@apps/tv-player/src/App.tsx
@apps/tv-player/src/screens/IdleScreen.tsx
@apps/tv-player/src/screens/PlayingScreen.tsx
@apps/tv-player/src/screens/ConflictScreen.tsx
@apps/tv-player/src/components/PlaybackStatusBanner.tsx
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add screen-state and long-text regression tests</name>
  <files>apps/tv-player/src/test/tv-screen-states.test.tsx, apps/tv-player/src/test/playing-screen.test.tsx</files>
  <read_first>
    - apps/tv-player/src/test/playing-screen.test.tsx
    - apps/tv-player/src/screens/IdleScreen.tsx
    - apps/tv-player/src/screens/PlayingScreen.tsx
    - apps/tv-player/src/screens/ConflictScreen.tsx
    - apps/tv-player/src/screens/tv-display-model.ts
    - .planning/phases/06-tv-playback-experience-polish/06-UI-SPEC.md
  </read_first>
  <action>
    Create `apps/tv-player/src/test/tv-screen-states.test.tsx`.

    Cover these exact visible strings using Testing Library:
    - Idle screen renders `扫码点歌` and `手机扫码点歌`.
    - Conflict screen renders `已有电视端在线` and includes active device name `Living Room TV`.
    - Playing screen with `snapshot.state = "loading"` renders `准备中` or `正在准备播放`.
    - Playing screen with `snapshot.state = "recovering"` renders `恢复中` or `正在恢复播放`.
    - Playback failed notice renders `播放失败，已跳到下一首`.
    - Recovery fallback notice renders `已恢复播放，本首从头开始`.

    Extend `apps/tv-player/src/test/playing-screen.test.tsx` with a long-text case:
    - current song title `这是一个非常非常长的中文歌名用来验证电视端不会重叠也不会把时间挤出屏幕 Long English Title Segment`
    - next song title `下一首也是一个很长的歌名 Long Next Song`
    - Assert both long title text and `00:12 / 03:00` are present.
    - Assert the first-play prompt body contains `浏览器需要一次点击授权播放声音` when prompt is visible.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/tv-player test -- tv-screen-states playing-screen</automated>
  </verify>
  <acceptance_criteria>
    - `apps/tv-player/src/test/tv-screen-states.test.tsx` contains `扫码点歌`.
    - `apps/tv-player/src/test/tv-screen-states.test.tsx` contains `已有电视端在线`.
    - `apps/tv-player/src/test/tv-screen-states.test.tsx` contains `播放失败，已跳到下一首`.
    - `apps/tv-player/src/test/playing-screen.test.tsx` contains `非常非常长的中文歌名`.
    - `pnpm -F @home-ktv/tv-player test -- tv-screen-states playing-screen` exits 0.
  </acceptance_criteria>
  <done>TV state and long-text rendering are covered by automated component tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Chrome visual screenshot helper</name>
  <files>scripts/tv-visual-check.mjs, package.json</files>
  <read_first>
    - package.json
    - scripts/dev-local.mjs
    - apps/tv-player/vite.config.ts
    - .planning/phases/06-tv-playback-experience-polish/06-RESEARCH.md
  </read_first>
  <action>
    Create `scripts/tv-visual-check.mjs` as a Node ESM script that:
    - Reads `TV_VISUAL_URL` from env.
    - Defaults `TV_VISUAL_URL` to `http://127.0.0.1:5173/?apiBaseUrl=http://127.0.0.1:4000&roomSlug=living-room&deviceName=Living%20Room%20TV`.
    - Uses Chrome path from `CHROME_BIN` or `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`.
    - Creates `logs/visual` with `fs.mkdir(..., { recursive: true })`.
    - Captures two screenshots using Chrome headless:
      - `logs/visual/tv-player-1920x1080.png` with `--window-size=1920,1080`
      - `logs/visual/tv-player-1366x768.png` with `--window-size=1366,768`
    - Uses `child_process.spawn` with arguments, not shell string concatenation.
    - Exits non-zero if Chrome is missing, Chrome exits non-zero, or any screenshot file is missing/zero bytes.
    - Prints the final screenshot file paths.

    Add root script `"tv:visual-check": "node scripts/tv-visual-check.mjs"` to `package.json`.

    Do not add Playwright or any new package dependency in this plan. This script is the lightweight Chrome fallback identified by research.
  </action>
  <verify>
    <automated>node scripts/tv-visual-check.mjs --help</automated>
    <manual>Run `pnpm dev:local start`, open the TV URL once, then run `pnpm tv:visual-check`; verify both PNG files are created under `logs/visual/`.</manual>
  </verify>
  <acceptance_criteria>
    - `scripts/tv-visual-check.mjs` contains `--window-size=1920,1080`.
    - `scripts/tv-visual-check.mjs` contains `--window-size=1366,768`.
    - `scripts/tv-visual-check.mjs` contains `logs/visual/tv-player-1920x1080.png`.
    - `scripts/tv-visual-check.mjs` contains `spawn(`.
    - `package.json` contains `"tv:visual-check": "node scripts/tv-visual-check.mjs"`.
    - `node scripts/tv-visual-check.mjs --help` exits 0 and prints `TV_VISUAL_URL`.
  </acceptance_criteria>
  <done>A lightweight Chrome screenshot validation command exists without adding new dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Phase 6 UAT checklist and run final gates</name>
  <files>.planning/phases/06-tv-playback-experience-polish/06-UAT.md, apps/tv-player/src/test/app-runtime.test.tsx, package.json</files>
  <read_first>
    - .planning/phases/06-tv-playback-experience-polish/06-CONTEXT.md
    - .planning/phases/06-tv-playback-experience-polish/06-UI-SPEC.md
    - scripts/dev-local.mjs
    - package.json
  </read_first>
  <action>
    Create `.planning/phases/06-tv-playback-experience-polish/06-UAT.md` with Chinese manual verification steps:
    - Start services with `DATABASE_URL=postgres://ktv:ktv@127.0.0.1:5432/home_ktv MEDIA_ROOT=/Users/shaolongfei/OtherProjects/home-ktv-system/home-ktv-media PUBLIC_BASE_URL=http://10.91.130.150:4000 pnpm dev:local restart`.
    - Open TV URL from `pnpm dev:local restart` output.
    - Verify idle state shows `扫码点歌`, large QR, and no overlap.
    - On first queued song, if browser blocks playback, verify TV shows `点击电视开始播放`; click TV once; verify prompt disappears and playback starts.
    - Verify playing state shows current song, artist, vocal mode, state label, `mm:ss / mm:ss`, next song, and small QR.
    - Trigger 原唱/伴唱 switch from mobile; verify TV mode updates or shows `切换失败，已恢复到原模式` if rollback happens.
    - Trigger cut song; verify TV shows short feedback and does not hide core playing information.
    - Stop/restart API or refresh TV to verify offline/recovery/conflict copy as applicable.
    - Run `pnpm tv:visual-check`; verify `logs/visual/tv-player-1920x1080.png` and `logs/visual/tv-player-1366x768.png` exist and visually show no overlap.

    Update `apps/tv-player/src/test/app-runtime.test.tsx` if necessary so the first-play prompt retry test remains stable after all UI changes.

    Run final Phase 6 gates:
    - `pnpm -F @home-ktv/tv-player test`
    - `pnpm -F @home-ktv/tv-player typecheck`
    - `pnpm typecheck`
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/tv-player test</automated>
    <automated>pnpm -F @home-ktv/tv-player typecheck</automated>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/06-tv-playback-experience-polish/06-UAT.md` contains `点击电视开始播放`.
    - `.planning/phases/06-tv-playback-experience-polish/06-UAT.md` contains `pnpm tv:visual-check`.
    - `.planning/phases/06-tv-playback-experience-polish/06-UAT.md` contains `tv-player-1920x1080.png`.
    - `pnpm -F @home-ktv/tv-player test` exits 0.
    - `pnpm -F @home-ktv/tv-player typecheck` exits 0.
    - `pnpm typecheck` exits 0 or any unrelated pre-existing failures are documented in `06-03-SUMMARY.md`.
  </acceptance_criteria>
  <done>Phase 6 has automated TV coverage, visual screenshot validation, and concrete manual UAT instructions.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `pnpm -F @home-ktv/tv-player test`
- [ ] `pnpm -F @home-ktv/tv-player typecheck`
- [ ] `pnpm typecheck`
- [ ] `node scripts/tv-visual-check.mjs --help`
</verification>

<success_criteria>
- All Phase 6 TV UX requirements have automated or explicit manual verification.
- Real-browser screenshot validation is available for 1920x1080 and 1366x768.
- User-facing UAT instructions explain exactly how to validate first-play, playing state, switch, skip, recovery/offline/conflict, QR, and non-overlap.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tv-playback-experience-polish/06-03-SUMMARY.md`.
</output>
