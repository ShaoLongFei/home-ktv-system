---
phase: 06-tv-playback-experience-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/tv-player/src/screens/tv-display-model.ts
  - apps/tv-player/src/test/tv-display-model.test.ts
autonomous: true
requirements:
  - TVUX-01
  - TVUX-02
  - TVUX-03
  - TVUX-04
must_haves:
  truths:
    - "TV display state is derived from RoomSnapshot/useRoomSnapshot/runtime inputs instead of a separate business state machine"
    - "TV-facing state labels and notice copy are Chinese for touched Phase 6 states"
    - "First-play blocked state has explicit actionable copy: 点击电视开始播放"
    - "Time formatting supports mm:ss / mm:ss without showing a misleading 00:00 duration when duration is unknown"
  artifacts:
    - path: "apps/tv-player/src/screens/tv-display-model.ts"
      provides: "typed TV display state, notice copy, time formatting, and first-play prompt model"
      contains: "export function deriveTvDisplayState"
    - path: "apps/tv-player/src/test/tv-display-model.test.ts"
      provides: "regression tests for state derivation, copy, and time formatting"
      contains: "点击电视开始播放"
  key_links:
    - from: "packages/player-contracts/src/index.ts"
      to: "apps/tv-player/src/screens/tv-display-model.ts"
      via: "RoomSnapshot, PlaybackNotice, PlayerConflictState drive display model"
      pattern: "RoomSnapshot"
    - from: "apps/tv-player/src/screens/tv-display-model.ts"
      to: "apps/tv-player/src/App.tsx"
      via: "later plans consume deriveTvDisplayState in renderScreen"
      pattern: "deriveTvDisplayState"
---

<objective>
Create the TV display model and Chinese copy foundation that Phase 6 UI work will consume.

Purpose: TV polishing needs a small source of truth for state labels, notices, first-play prompt copy, and time formatting before component changes. This keeps UI work from spreading ad hoc strings and state checks through `App.tsx` and screen components.
Output: `tv-display-model.ts` with typed helpers and focused tests.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-tv-playback-experience-polish/06-CONTEXT.md
@.planning/phases/06-tv-playback-experience-polish/06-RESEARCH.md
@.planning/phases/06-tv-playback-experience-polish/06-UI-SPEC.md
@packages/player-contracts/src/index.ts
@apps/tv-player/src/App.tsx
@apps/tv-player/src/components/PlaybackStatusBanner.tsx
@apps/tv-player/src/screens/PlayingScreen.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add tests for TV display derivation and copy</name>
  <files>apps/tv-player/src/test/tv-display-model.test.ts</files>
  <read_first>
    - packages/player-contracts/src/index.ts
    - apps/tv-player/src/test/playing-screen.test.tsx
    - apps/tv-player/src/test/playback-status-banner.test.tsx
    - .planning/phases/06-tv-playback-experience-polish/06-UI-SPEC.md
  </read_first>
  <action>
    Create `apps/tv-player/src/test/tv-display-model.test.ts`. Import the future functions `deriveTvDisplayState`, `formatPlaybackClock`, `noticeCopyFor`, and `firstPlayPromptCopy` from `../screens/tv-display-model.js`.

    Add tests with these exact expectations:
    - `formatPlaybackClock(12_345, 180_000)` returns `00:12 / 03:00`.
    - `formatPlaybackClock(12_345, null)` returns `00:12 / --:--`.
    - `firstPlayPromptCopy.heading` equals `点击电视开始播放`.
    - `firstPlayPromptCopy.body` contains `浏览器需要一次点击授权播放声音`.
    - `noticeCopyFor({ kind: "playback_failed_skipped", message: "" })` returns `播放失败，已跳到下一首`.
    - `noticeCopyFor({ kind: "switch_failed_reverted", message: "" })` returns `切换失败，已恢复到原模式`.
    - `deriveTvDisplayState({ status: "booting", snapshot: null, errorMessage: null, firstPlayBlocked: false })` returns an object whose `kind` is `booting` and `heading` is `正在启动电视端`.
    - `deriveTvDisplayState({ status: "error", snapshot: null, errorMessage: "TV player request failed: 500", firstPlayBlocked: false })` returns `kind: "offline"` and `heading: "电视端离线"` without exposing the raw error as the heading.
    - A snapshot with `conflict` returns `kind: "conflict"`.
    - A `playing` snapshot plus `firstPlayBlocked: true` returns `firstPlayPrompt.visible === true`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/tv-player test -- tv-display-model</automated>
  </verify>
  <acceptance_criteria>
    - `apps/tv-player/src/test/tv-display-model.test.ts` contains `formatPlaybackClock(12_345, 180_000)`.
    - `apps/tv-player/src/test/tv-display-model.test.ts` contains `点击电视开始播放`.
    - `apps/tv-player/src/test/tv-display-model.test.ts` contains `播放失败，已跳到下一首`.
    - `pnpm -F @home-ktv/tv-player test -- tv-display-model` fails before implementation because `tv-display-model.ts` does not exist.
  </acceptance_criteria>
  <done>Failing tests specify the TV display model, time formatting, Chinese copy, and first-play prompt behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement TV display model helpers</name>
  <files>apps/tv-player/src/screens/tv-display-model.ts, apps/tv-player/src/test/tv-display-model.test.ts</files>
  <read_first>
    - apps/tv-player/src/App.tsx
    - apps/tv-player/src/components/PlaybackStatusBanner.tsx
    - packages/player-contracts/src/index.ts
    - .planning/phases/06-tv-playback-experience-polish/06-CONTEXT.md
    - .planning/phases/06-tv-playback-experience-polish/06-UI-SPEC.md
  </read_first>
  <action>
    Create `apps/tv-player/src/screens/tv-display-model.ts` with these concrete exports:
    - `export type TvDisplayKind = "booting" | "offline" | "waiting" | "idle" | "loading" | "playing" | "recovering" | "conflict";`
    - `export interface FirstPlayPromptModel { visible: boolean; heading: string; body: string; }`
    - `export interface TvDisplayState { kind: TvDisplayKind; heading: string; detail: string; stateLabel: string; tone: "neutral" | "ready" | "warning" | "danger"; firstPlayPrompt: FirstPlayPromptModel; }`
    - `export const firstPlayPromptCopy = { heading: "点击电视开始播放", body: "浏览器需要一次点击授权播放声音，点击后会继续当前歌曲。" } as const;`
    - `export function formatPlaybackClock(positionMs: number, durationMs: number | null): string`
    - `export function noticeCopyFor(notice: PlaybackNotice | null): string | null`
    - `export function deriveTvDisplayState(input: { status: "booting" | "ready" | "error"; snapshot: RoomSnapshot | null; errorMessage: string | null; firstPlayBlocked: boolean; }): TvDisplayState`

    Implement `formatPlaybackClock` so position is clamped to `>= 0`, finite duration is formatted as `mm:ss`, and null/non-finite duration renders `--:--`.

    Implement `noticeCopyFor` with these exact fallback strings:
    - `loading`: `正在准备播放`
    - `recovering`: `正在恢复播放`
    - `switch_failed_reverted`: `切换失败，已恢复到原模式`
    - `playback_failed_skipped`: `播放失败，已跳到下一首`
    - `recovery_fallback_start_over`: `已恢复播放，本首从头开始`

    If `notice.message` is non-empty and already Chinese, return it; otherwise use the fallback copy. Keep this simple by checking `/[\u4E00-\u9FFF]/u.test(notice.message)`.

    Implement `deriveTvDisplayState` so:
    - `status === "booting"` returns heading `正在启动电视端`, detail `正在准备客厅电视播放环境。`, label `启动中`, tone `neutral`.
    - `status === "error"` returns heading `电视端离线`, detail `请检查后台服务和电视网络，然后刷新电视页面。`, label `离线`, tone `danger`.
    - missing snapshot in ready status returns heading `等待房间状态`, detail `电视端正在等待服务端房间快照。`, label `等待中`, tone `warning`.
    - conflict snapshot returns heading `已有电视端在线`, detail `请先关闭当前在线的电视端，再刷新这个页面。`, label `冲突`, tone `danger`.
    - `snapshot.state === "idle"` returns heading `扫码点歌`, detail `用手机扫码进入点歌台，电视只负责播放和显示状态。`, label `待点歌`, tone `ready`.
    - `snapshot.state === "loading"` returns heading `正在准备播放`, detail `歌曲即将开始。`, label `准备中`, tone `warning`.
    - `snapshot.state === "recovering"` returns heading `正在恢复播放`, detail `正在恢复当前歌曲和播放进度。`, label `恢复中`, tone `warning`.
    - `snapshot.state === "playing"` returns heading `播放中`, detail `当前歌曲正在播放。`, label `播放中`, tone `ready`.
    - `firstPlayPrompt.visible` is true only when `firstPlayBlocked` is true and a current target exists.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/tv-player test -- tv-display-model</automated>
    <automated>pnpm -F @home-ktv/tv-player typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/tv-player/src/screens/tv-display-model.ts` contains `export function deriveTvDisplayState`.
    - `apps/tv-player/src/screens/tv-display-model.ts` contains `export function formatPlaybackClock`.
    - `apps/tv-player/src/screens/tv-display-model.ts` contains `点击电视开始播放`.
    - `apps/tv-player/src/screens/tv-display-model.ts` contains `--:--`.
    - `pnpm -F @home-ktv/tv-player test -- tv-display-model` exits 0.
    - `pnpm -F @home-ktv/tv-player typecheck` exits 0.
  </acceptance_criteria>
  <done>TV display model helpers exist and pass focused tests.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `pnpm -F @home-ktv/tv-player test -- tv-display-model`
- [ ] `pnpm -F @home-ktv/tv-player typecheck`
</verification>

<success_criteria>
- TV display state, copy, first-play prompt text, and time formatting are centralized.
- The display model is typed from existing player contracts.
- The helper tests prove Chinese copy and `mm:ss / --:--` behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/06-tv-playback-experience-polish/06-01-SUMMARY.md`.
</output>
