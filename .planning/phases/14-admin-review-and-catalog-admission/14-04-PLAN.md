---
phase: 14-admin-review-and-catalog-admission
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/src/modules/catalog/song-json.ts
  - apps/api/src/modules/catalog/song-json-consistency-validator.ts
  - apps/api/src/test/song-json-consistency-validator.test.ts
autonomous: true
requirements: [REVIEW-04]
must_haves:
  truths:
    - "Approved real-MV songs can write a durable `song.json` containing media path, cover path, one asset, track roles, codecs, and compatibility fields."
    - "The consistency validator accepts a single real-MV asset without requiring a legacy original/instrumental asset pair."
    - "The validator rejects unsafe or impossible real-MV media, cover, and track-role references."
  artifacts:
    - path: "apps/api/src/modules/catalog/song-json.ts"
      provides: "full durable real-MV song.json TypeScript shape"
      contains: "compatibilityStatus"
    - path: "apps/api/src/modules/catalog/song-json-consistency-validator.ts"
      provides: "single real-MV asset validation branch"
      contains: "validateSingleRealMvAsset"
    - path: "apps/api/src/test/song-json-consistency-validator.test.ts"
      provides: "validator coverage for single real-MV song.json"
      contains: "single real MV"
  key_links:
    - from: "apps/api/src/modules/catalog/song-json.ts"
      to: "apps/api/src/modules/catalog/admission-service.ts"
      via: "SongJsonDocument and SongJsonAsset fields used by writer"
      pattern: "trackRoles"
    - from: "apps/api/src/modules/catalog/song-json-consistency-validator.ts"
      to: "packages/domain/src/index.ts"
      via: "CompatibilityStatus, TrackRoles, MediaInfoSummary, PlaybackProfile"
      pattern: "single_file_audio_tracks"
---

<objective>
Teach formal `song.json` writing and validation about the single real-MV asset contract.

Purpose: Formal catalog restore/audit must not depend on DB-only real-MV fields, and the validator must not reject the Phase 12 one-file/one-asset model as a missing switch pair.
Output: `song-json.ts` supports full real-MV fields and `song-json-consistency-validator.ts` validates one real-MV asset plus cover path and track-role refs.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-admin-review-and-catalog-admission/14-CONTEXT.md
@.planning/phases/14-admin-review-and-catalog-admission/14-RESEARCH.md
@apps/api/src/modules/catalog/song-json.ts
@apps/api/src/modules/catalog/song-json-consistency-validator.ts
@apps/api/src/test/song-json-consistency-validator.test.ts
@packages/domain/src/index.ts

<interfaces>
Durable real-MV asset fields required by D-09 through D-11:
```ts
compatibilityStatus
compatibilityReasons
mediaInfoSummary
mediaInfoProvenance
trackRoles
playbackProfile
container
videoCodec
audioCodecs
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend SongJsonDocument and SongJsonAsset with real-MV fields</name>
  <files>apps/api/src/modules/catalog/song-json.ts</files>
  <read_first>
    - apps/api/src/modules/catalog/song-json.ts
    - packages/domain/src/index.ts
    - .planning/phases/14-admin-review-and-catalog-admission/14-CONTEXT.md
    - .planning/phases/14-admin-review-and-catalog-admission/14-RESEARCH.md
  </read_first>
  <behavior>
    - `SongJsonDocument` can carry an optional `coverPath`.
    - `SongJsonAsset` can carry the same durable real-MV fields already present on formal `Asset`.
    - Existing legacy `song.json` writers remain type-compatible.
  </behavior>
  <action>
    In `apps/api/src/modules/catalog/song-json.ts`, extend imports from `@home-ktv/domain` to include `CompatibilityReason`, `CompatibilityStatus`, `MediaInfoProvenance`, `MediaInfoSummary`, `PlaybackProfile`, and `TrackRoles`.

    Add these optional fields to `SongJsonAsset`:
    ```ts
    compatibilityStatus?: CompatibilityStatus;
    compatibilityReasons?: readonly CompatibilityReason[];
    mediaInfoSummary?: MediaInfoSummary | null;
    mediaInfoProvenance?: MediaInfoProvenance | null;
    trackRoles?: TrackRoles;
    playbackProfile?: PlaybackProfile;
    container?: string | null;
    videoCodec?: string | null;
    audioCodecs?: readonly string[];
    ```
    Add `coverPath?: string | null` to `SongJsonDocument`.

    Do not change `writeSongJson` atomic temp-file + rename behavior. Do not add a database dependency to `song-json.ts`.
  </action>
  <acceptance_criteria>
    - `rg -n "coverPath\\?: string \\| null|compatibilityStatus\\?: CompatibilityStatus|trackRoles\\?: TrackRoles|playbackProfile\\?: PlaybackProfile" apps/api/src/modules/catalog/song-json.ts` finds all fields.
    - `rg -n "writeFile\\(tempPath|rename\\(tempPath, targetPath\\)" apps/api/src/modules/catalog/song-json.ts` still finds the atomic write behavior.
    - `pnpm -F @home-ktv/api typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <done>`song-json.ts` exposes the full real-MV durable contract without breaking legacy callers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Validate single real-MV song.json assets and cover paths</name>
  <files>apps/api/src/modules/catalog/song-json-consistency-validator.ts, apps/api/src/test/song-json-consistency-validator.test.ts</files>
  <read_first>
    - apps/api/src/modules/catalog/song-json-consistency-validator.ts
    - apps/api/src/modules/catalog/song-json.ts
    - apps/api/src/test/song-json-consistency-validator.test.ts
    - packages/domain/src/index.ts
    - .planning/phases/14-admin-review-and-catalog-admission/14-CONTEXT.md
  </read_first>
  <behavior>
    - A formal song with exactly one `playbackProfile.kind === "single_file_audio_tracks"` asset is validated as a real-MV song, not as a legacy switch pair.
    - The validator checks `coverPath` for unsafe paths and missing files when present.
    - Real-MV `trackRoles` refs are checked against the asset `mediaInfoSummary.audioTracks` by both `id` and `index`.
    - Missing original or instrumental role is a warning, not a hard validation failure.
    - Legacy pair validation still runs for songs without a real-MV asset.
  </behavior>
  <action>
    In `song-json-consistency-validator.ts`, extend `FormalSongJsonAsset` with optional real-MV fields matching Task 1 and extend `FormalSongJsonDocument` with `coverPath?: string | null`.

    Add `compatibilityStatuses: CompatibilityStatus[] = ["unknown", "review_required", "playable", "unsupported"]` and use it in `isMalformedJsonAsset` so invalid `compatibilityStatus` values are malformed.

    Add `validateCoverPath(songsRoot, songDirectory, document.coverPath, issues)`:
    - Ignore `undefined`, `null`, and empty string.
    - Resolve with existing `safeResolve` behavior like media files.
    - Unsafe path pushes `{ code: "UNSAFE_COVER_PATH", severity: "error", message: "song.json cover path escapes the songs root", path: coverPath }`.
    - Missing file pushes `{ code: "MISSING_COVER_FILE", severity: "warning", message: "song.json references a missing cover file", path: coverPath }`.

    Replace unconditional `validateSwitchPair(input.assets, issues)` with:
    ```ts
    const realMvAssets = input.assets.filter((asset) => asset.playbackProfile?.kind === "single_file_audio_tracks");
    if (realMvAssets.length > 0) {
      validateSingleRealMvAsset(realMvAssets, issues);
    } else {
      validateSwitchPair(input.assets, issues);
    }
    ```

    Implement `validateSingleRealMvAsset(assets: Asset[], issues: SongJsonConsistencyIssue[]): void`:
    - If `assets.length !== 1`, push `REAL_MV_ASSET_COUNT_INVALID` error.
    - For the single asset, if `asset.assetKind !== "dual-track-video"` or `asset.vocalMode !== "dual"`, push `REAL_MV_ASSET_SHAPE_INVALID` error.
    - For `asset.trackRoles?.original` and `asset.trackRoles?.instrumental`, if a role is null/missing push `REAL_MV_TRACK_ROLES_MISSING` warning with `assetId`.
    - For each non-null role, require a matching `asset.mediaInfoSummary?.audioTracks` item by both `id` and `index`; otherwise push `REAL_MV_TRACK_ROLE_REF_INVALID` error with `assetId`.
    - Do not push `SWITCH_PAIR_NOT_VERIFIED` for real-MV assets.

    Add tests:
    - `"passes a single real MV song.json with cover, trackRoles, playbackProfile, and compatibility"`: create media and cover files, a `Song` with status `review_required`, and one `Asset` with `assetKind: "dual-track-video"`, `vocalMode: "dual"`, `status: "promoted"`, `switchQualityStatus: "review_required"`, `compatibilityStatus: "review_required"`, `mediaInfoSummary.audioTracks` with `0x1100` and `0x1101`, `trackRoles` pointing to both tracks, and `playbackProfile.kind: "single_file_audio_tracks"`. Assert status `passed`.
    - `"warns on missing real MV role refs without requiring a legacy switch pair"`: create one real-MV asset with `trackRoles.instrumental = null`; assert issue code `REAL_MV_TRACK_ROLES_MISSING`, result status `review_required`, and no issue code `SWITCH_PAIR_NOT_VERIFIED`.
    - `"fails invalid real MV track refs"`: set `trackRoles.instrumental.id = "missing"`; assert `REAL_MV_TRACK_ROLE_REF_INVALID`.
    - `"validates cover path safety"`: set `coverPath: "../cover.jpg"` and assert `UNSAFE_COVER_PATH`.
  </action>
  <acceptance_criteria>
    - `rg -n "validateSingleRealMvAsset|REAL_MV_TRACK_ROLE_REF_INVALID|REAL_MV_TRACK_ROLES_MISSING|UNSAFE_COVER_PATH|MISSING_COVER_FILE" apps/api/src/modules/catalog/song-json-consistency-validator.ts` finds all patterns.
    - `rg -n "single real MV|REAL_MV_TRACK_ROLES_MISSING|UNSAFE_COVER_PATH|0x1101" apps/api/src/test/song-json-consistency-validator.test.ts` finds test coverage.
    - `pnpm -F @home-ktv/api test -- src/test/song-json-consistency-validator.test.ts` exits 0.
    - `pnpm -F @home-ktv/api typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/song-json-consistency-validator.test.ts &amp;&amp; pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <done>The formal `song.json` validator supports one real-MV asset with durable fields and keeps legacy pair validation for legacy songs.</done>
</task>

</tasks>

<verification>
Run `pnpm -F @home-ktv/api test -- src/test/song-json-consistency-validator.test.ts` and `pnpm -F @home-ktv/api typecheck`.
</verification>

<success_criteria>
- Formal `song.json` can represent one real-MV asset with media path, cover path, track roles, codecs, MediaInfo, provenance, playback profile, and compatibility.
- The validator accepts valid single real-MV assets without demanding original/instrumental asset rows.
- Missing role confirmation is review-required, not a hard failure.
- Unsafe/missing media and cover references remain detectable.
</success_criteria>

<output>
After completion, create `.planning/phases/14-admin-review-and-catalog-admission/14-04-SUMMARY.md`.
</output>
