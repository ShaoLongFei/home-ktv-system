---
phase: 14-admin-review-and-catalog-admission
plan: 05
type: execute
wave: 3
depends_on: ["14-02", "14-03", "14-04"]
files_modified:
  - apps/api/src/test/real-mv-admission-regression.test.ts
  - apps/admin/src/test/real-mv-review-ui.test.tsx
autonomous: true
requirements: [REVIEW-01, REVIEW-02, REVIEW-03, REVIEW-04, REVIEW-05]
must_haves:
  truths:
    - "The complete reviewed real-MV path has regression coverage from Admin PATCH payload through formal one-asset admission."
    - "Unsupported and incomplete real-MV candidates remain visible with guidance and do not block other approvals."
    - "Phase 14 tests prove no mobile search, queue, TV playback, runtime switching, transcoding, or Android TV work is required."
  artifacts:
    - path: "apps/api/src/test/real-mv-admission-regression.test.ts"
      provides: "API/service regression coverage for reviewed trackRoles, readiness mapping, one-asset song.json, and unsupported candidates"
      contains: "real MV admission regression"
    - path: "apps/admin/src/test/real-mv-review-ui.test.tsx"
      provides: "Admin UI regression coverage for review facts, role correction payloads, and guidance"
      contains: "real MV review UI"
  key_links:
    - from: "apps/admin/src/test/real-mv-review-ui.test.tsx"
      to: "PATCH /admin/import-candidates/:candidateId"
      via: "fetch mock request body"
      pattern: "trackRoles"
    - from: "apps/api/src/test/real-mv-admission-regression.test.ts"
      to: "apps/api/src/modules/catalog/admission-service.ts"
      via: "CatalogAdmissionService approval harness"
      pattern: "asset-candidate-1-real-mv"
---

<objective>
Add final targeted Phase 14 regression tests across API and Admin.

Purpose: Earlier plans add focused tests while changing production code. This plan adds end-to-end-ish guardrails that prove the phase goal and locked decisions are covered without relying on Phase 15 runtime behavior.
Output: New API and Admin regression suites covering reviewed real-MV admission, single-asset `song.json`, UI guidance, and PATCH payloads.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/14-admin-review-and-catalog-admission/14-CONTEXT.md
@.planning/phases/14-admin-review-and-catalog-admission/14-RESEARCH.md
@.planning/phases/14-admin-review-and-catalog-admission/14-02-SUMMARY.md
@.planning/phases/14-admin-review-and-catalog-admission/14-03-SUMMARY.md
@.planning/phases/14-admin-review-and-catalog-admission/14-04-SUMMARY.md
@apps/api/src/test/catalog-admission.test.ts
@apps/api/src/test/admin-imports-routes.test.ts
@apps/api/src/test/song-json-consistency-validator.test.ts
@apps/admin/src/test/import-workbench.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add API real-MV admission regression suite</name>
  <files>apps/api/src/test/real-mv-admission-regression.test.ts</files>
  <read_first>
    - apps/api/src/test/catalog-admission.test.ts
    - apps/api/src/test/admin-imports-routes.test.ts
    - apps/api/src/test/song-json-consistency-validator.test.ts
    - apps/api/src/modules/catalog/admission-service.ts
    - apps/api/src/modules/catalog/song-json-consistency-validator.ts
    - .planning/phases/14-admin-review-and-catalog-admission/14-03-SUMMARY.md
    - .planning/phases/14-admin-review-and-catalog-admission/14-04-SUMMARY.md
  </read_first>
  <behavior>
    - Reviewed `trackRoles` flow into the single formal real-MV asset and written `song.json`.
    - Candidate cover sidecars flow into the formal song directory and written `song.json.coverPath`.
    - `playable`, `review_required`, and `unsupported` compatibility statuses produce the locked readiness outcomes.
    - The regression file contains no Mobile, queue, TV playback, runtime switching, transcoding, or Android TV assertions.
  </behavior>
  <action>
    Create `apps/api/src/test/real-mv-admission-regression.test.ts`. Reuse helper style from `catalog-admission.test.ts` but keep this file focused on real-MV cases only.

    Add tests:
    - `"real MV admission regression writes one song.json asset with reviewed trackRoles"`: construct one selected `dual-track-video` candidate file with `trackRoles.original.id = "0x1100"`, `trackRoles.instrumental.id = "0x1101"`, `mediaInfoSummary.audioTracks` containing both ids, `playbackProfile.kind = "single_file_audio_tracks"`, `compatibilityStatus = "review_required"`, and `probeSummary.realMv.sidecars.cover.relativePath = "artist/title/title.jpg"`. Write that cover fixture before approval. Approve it. Read the written `song.json` and assert:
      - `assets.length === 1`
      - `coverPath === "title.jpg"`
      - `assets[0].id === "asset-candidate-1-real-mv"`
      - `assets[0].assetKind === "dual-track-video"`
      - `assets[0].trackRoles.instrumental.id === "0x1101"`
      - formal `title.jpg` exists beside `song.json`
      - JSON string does not contain `asset-candidate-1-original`
      - JSON string does not contain `asset-candidate-1-instrumental`
    - `"real MV readiness mapping does not leak unsupported media into formal catalog"`: run approvals for `playable`, `review_required`, `unknown`, and `unsupported`; assert `playable -> ready/ready`, `review_required -> review_required/promoted`, `unknown -> review_required/promoted`, and `unsupported -> review_required result with reason real-mv-unsupported and no writer call`.
    - `"real MV validator accepts the admitted single asset contract"`: pass the admitted song and asset into `validateSongJsonConsistency`; assert status is `passed` or `review_required` only when a role is intentionally null, and assert there is no `SWITCH_PAIR_NOT_VERIFIED`.

    Add a final source guard assertion:
    ```ts
    const source = await readFile(new URL("./real-mv-admission-regression.test.ts", import.meta.url), "utf8");
    expect(source).not.toMatch(/queue|mobile|tv-player|transcod|Android TV/i);
    ```
    Keep the string literals for this guard split if needed so the guard does not match itself.
  </action>
  <acceptance_criteria>
    - `test -f apps/api/src/test/real-mv-admission-regression.test.ts` exits 0.
    - `rg -n "real MV admission regression|asset-candidate-1-real-mv|coverPath|title.jpg|real-mv-unsupported|SWITCH_PAIR_NOT_VERIFIED" apps/api/src/test/real-mv-admission-regression.test.ts` finds all patterns.
    - `pnpm -F @home-ktv/api test -- src/test/real-mv-admission-regression.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/real-mv-admission-regression.test.ts</automated>
  </verify>
  <done>API regression coverage proves reviewed real-MV admission is single-asset, compatibility-driven, and Phase-15-free.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Admin real-MV review UI regression suite</name>
  <files>apps/admin/src/test/real-mv-review-ui.test.tsx</files>
  <read_first>
    - apps/admin/src/test/import-workbench.test.tsx
    - apps/admin/src/imports/CandidateEditor.tsx
    - apps/admin/src/imports/types.ts
    - apps/admin/src/i18n.tsx
    - .planning/phases/14-admin-review-and-catalog-admission/14-02-SUMMARY.md
  </read_first>
  <behavior>
    - UI regression proves Admin can review title, artist, language, cover, MediaInfo, filename, sidecar, conflicts, and track roles.
    - UI regression proves role correction saves `trackRoles`.
    - UI regression proves missing cover or missing manual role confirmation does not disable approval when title and artist are filled.
    - UI regression proves unsupported candidates show preprocess/retry guidance and remain selectable.
  </behavior>
  <action>
    Create `apps/admin/src/test/real-mv-review-ui.test.tsx`. Reuse the fetch-mock and candidate fixture style from `import-workbench.test.tsx`, but keep this suite specific to real-MV review.

    Add tests:
    - `"real MV review UI shows metadata, raw audio facts, provenance, conflicts, and role controls"`: render Admin, select `real-mv-candidate`, assert visible Chinese strings `歌名`, `歌手`, `语言`, `MV 封面`, `媒体信息`, `原始音轨`, `Original vocal`, `Instrumental`, `filename`, `sidecar`, `元数据冲突`, `原唱声轨`, and `伴唱声轨`.
    - `"real MV review UI saves corrected trackRoles in PATCH payload"`: change `伴唱声轨` select to `0x1100`, save, and assert PATCH body `files[0].trackRoles.instrumental` equals `{ index: 0, id: "0x1100", label: "Original vocal" }`.
    - `"real MV review UI keeps approval light and shows repair guidance"`: fixture one review-required candidate with no cover and `trackRoles.instrumental = null`; assert approve button is enabled when title and artist are non-empty. Fixture one unsupported candidate; assert it remains in the candidate queue and shows `v1.2 不自动转码，请先在系统外预处理后再入库。` and `修复 song.json/封面或预处理媒体后，点击扫描导入目录重试。`.

    Do not add Playwright, visual screenshots, mobile controller tests, queue assertions, TV playback assertions, audio-track runtime switching assertions, transcoding, or Android TV setup.
  </action>
  <acceptance_criteria>
    - `test -f apps/admin/src/test/real-mv-review-ui.test.tsx` exits 0.
    - `rg -n "real MV review UI|原始音轨|元数据冲突|v1.2 不自动转码|trackRoles" apps/admin/src/test/real-mv-review-ui.test.tsx` finds all patterns.
    - `pnpm -F @home-ktv/admin test -- src/test/real-mv-review-ui.test.tsx` exits 0.
    - `pnpm -F @home-ktv/admin typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/admin test -- src/test/real-mv-review-ui.test.tsx &amp;&amp; pnpm -F @home-ktv/admin typecheck</automated>
  </verify>
  <done>Admin regression coverage proves the full Phase 14 review surface and preserves the Phase 15 boundary.</done>
</task>

</tasks>

<verification>
Run:
- `pnpm -F @home-ktv/api test -- src/test/real-mv-admission-regression.test.ts`
- `pnpm -F @home-ktv/admin test -- src/test/real-mv-review-ui.test.tsx`
- `pnpm -F @home-ktv/api typecheck`
- `pnpm -F @home-ktv/admin typecheck`
</verification>

<success_criteria>
- REVIEW-01 through REVIEW-05 each have direct regression evidence.
- API tests prove one formal Song plus one real-MV Asset with `trackRoles`.
- Admin tests prove raw facts, conflicts, guidance, and role correction payloads.
- Tests explicitly avoid Phase 15 and v1.2-excluded work.
</success_criteria>

<output>
After completion, create `.planning/phases/14-admin-review-and-catalog-admission/14-05-SUMMARY.md`.
</output>
