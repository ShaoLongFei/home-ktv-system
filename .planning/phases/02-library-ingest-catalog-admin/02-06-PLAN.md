---
phase: 02-library-ingest-catalog-admin
plan: 06
type: execute
wave: 5
depends_on:
  - "02-04"
  - "02-05"
files_modified:
  - apps/admin/src/App.tsx
  - apps/admin/src/App.css
  - apps/admin/src/api/client.ts
  - apps/admin/src/songs/types.ts
  - apps/admin/src/songs/SongCatalogView.tsx
  - apps/admin/src/songs/SongDetailEditor.tsx
  - apps/admin/src/songs/AssetPairEditor.tsx
  - apps/admin/src/songs/ConfirmDangerDialog.tsx
  - apps/admin/src/test/song-catalog.test.tsx
autonomous: true
requirements:
  - LIBR-05
  - ADMN-01
must_haves:
  truths:
    - "Admin can switch from import workbench to formal catalog maintenance inside the same admin app."
    - "Admin can browse songs/resources and see lyric mode, vocal mode, resource status, switch family, and switch quality."
    - "Admin can edit song metadata, default resource, and resource status with dangerous changes confirmed."
    - "UI tests use the DOM test setup created by Plan 02-04."
  artifacts:
    - path: "apps/admin/src/songs/SongCatalogView.tsx"
      provides: "formal song/resource browse UI"
    - path: "apps/admin/src/songs/SongDetailEditor.tsx"
      provides: "song metadata and default asset editor"
    - path: "apps/admin/src/songs/AssetPairEditor.tsx"
      provides: "asset status, vocal mode, switch family, and revalidation controls"
  key_links:
    - from: "apps/admin/src/songs/SongCatalogView.tsx"
      to: "apps/admin/src/api/client.ts"
      via: "admin catalog API client calls"
      pattern: "/admin/catalog/songs"
    - from: "apps/admin/src/songs/AssetPairEditor.tsx"
      to: "apps/api/src/routes/admin-catalog.ts"
      via: "revalidate and asset update endpoints from Plan 02-05"
      pattern: "revalidate"
---

<objective>
Add the formal catalog maintenance UI to the admin app created by Plan 02-04.

Purpose: Phase 2 also requires maintenance after approval. This plan keeps UI scope separate from the import workbench and consumes the formal catalog API from Plan 02-05.
Output: Songs/resources admin views, detail editor, asset pair editor, danger confirmations, and DOM-backed UI tests.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-library-ingest-catalog-admin/02-CONTEXT.md
@.planning/phases/02-library-ingest-catalog-admin/02-RESEARCH.md
@.planning/phases/02-library-ingest-catalog-admin/02-04-SUMMARY.md
@.planning/phases/02-library-ingest-catalog-admin/02-05-SUMMARY.md
@apps/admin/src/App.tsx
@apps/admin/src/api/client.ts
@apps/admin/vite.config.ts

<interfaces>
Formal catalog API expected from Plan 02-05:
```typescript
GET /admin/catalog/songs
GET /admin/catalog/songs/:songId
PATCH /admin/catalog/songs/:songId
PATCH /admin/catalog/songs/:songId/default-asset
PATCH /admin/catalog/assets/:assetId
POST /admin/catalog/songs/:songId/revalidate
GET /admin/catalog/songs/:songId/validate
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add formal catalog navigation and browse view</name>
  <files>apps/admin/src/App.tsx, apps/admin/src/App.css, apps/admin/src/api/client.ts, apps/admin/src/songs/types.ts, apps/admin/src/songs/SongCatalogView.tsx, apps/admin/src/test/song-catalog.test.tsx</files>
  <read_first>
- apps/admin/src/App.tsx
- apps/admin/src/api/client.ts
- apps/admin/vite.config.ts
- .planning/phases/02-library-ingest-catalog-admin/02-04-SUMMARY.md
- .planning/phases/02-library-ingest-catalog-admin/02-05-SUMMARY.md
  </read_first>
  <behavior>
- Test 1: admin app defaults to Imports but can switch to Songs without a page reload.
- Test 2: song list renders title, artist, language, song status, default asset, asset count, vocal modes, lyric modes, and switch quality.
- Test 3: filtering by status calls the correct `/admin/catalog/songs?status=...` URL.
  </behavior>
  <action>
Update `App.tsx` to add a compact tab/segmented control for Imports and Songs. Imports remains the default first screen per D-16, and Songs loads `SongCatalogView`.

Create `songs/types.ts` for formal catalog API response shapes. Include Song.status from Plan 02-01 and asset fields required by ADMN-01: lyricMode, vocalMode, status, switchFamily, and switchQualityStatus.

Create `SongCatalogView.tsx`:
- Fetch `/admin/catalog/songs`.
- Render a dense maintenance table/list for formal songs and asset summaries.
- Provide filters for song status and language.
- Select a song for detail editing in Task 2.

Extend `api/client.ts` with typed helper functions if helpful, but keep the underlying `fetchAdmin` as the shared transport from Plan 02-04.

Create `song-catalog.test.tsx` using `@testing-library/react` and the happy-dom environment from Plan 02-04.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/admin test -- song-catalog && pnpm -F @home-ktv/admin typecheck</automated>
  </verify>
  <acceptance_criteria>
- `rg "SongCatalogView" apps/admin/src/App.tsx apps/admin/src/songs/SongCatalogView.tsx` returns matches.
- `rg "/admin/catalog/songs" apps/admin/src/songs apps/admin/src/api/client.ts` returns a match.
- `rg "switchQualityStatus" apps/admin/src/songs` returns a match.
- `rg "screen\\.getBy" apps/admin/src/test/song-catalog.test.tsx` returns a match.
- `pnpm -F @home-ktv/admin test -- song-catalog` exits 0.
  </acceptance_criteria>
  <done>Admin app includes a formal catalog browse view using the DOM test setup from Plan 02-04.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add song detail and asset pair maintenance UI</name>
  <files>apps/admin/src/songs/SongDetailEditor.tsx, apps/admin/src/songs/AssetPairEditor.tsx, apps/admin/src/songs/ConfirmDangerDialog.tsx, apps/admin/src/songs/SongCatalogView.tsx, apps/admin/src/test/song-catalog.test.tsx, apps/admin/src/App.css</files>
  <read_first>
- apps/admin/src/songs/types.ts
- apps/admin/src/songs/SongCatalogView.tsx
- apps/admin/src/imports/ConfirmActionDialog.tsx
- .planning/phases/02-library-ingest-catalog-admin/02-CONTEXT.md
- .planning/phases/02-library-ingest-catalog-admin/02-05-SUMMARY.md
  </read_first>
  <behavior>
- Test 1: editing formal song metadata sends a PATCH request and updates the rendered detail after success.
- Test 2: changing default asset sends the default-asset endpoint.
- Test 3: disabling a ready asset, changing vocal mode, or changing switch family opens confirmation before the PATCH request.
- Test 4: revalidate and validate-song controls render review_required results and do not offer a manual verified override for `>300ms`.
  </behavior>
  <action>
Create `SongDetailEditor.tsx`:
- Edit title, artist, language, genre, tags, release year, aliases, searchHints, status, and default asset.
- Save normal metadata directly.
- Show validation warnings returned by `/validate`.

Create `AssetPairEditor.tsx`:
- Render each asset with file path, duration, lyric mode, vocal mode, status, switch family, and switch quality.
- Allow updates to default asset, asset status, vocal mode, lyric mode, and switch family through the API from Plan 02-05.
- Call revalidation after switch-sensitive changes or expose a revalidate button.
- Do not expose any "force verified" or manual override UI for duration deltas over 300ms.

Create or reuse a `ConfirmDangerDialog.tsx` for D-19 dangerous catalog changes: disabling `ready` assets, modifying pair relationship, or changing default asset if it affects readiness. Keep import reject-delete confirmation separate if reuse would create awkward props.

Extend `song-catalog.test.tsx` to cover metadata save, default asset change, dangerous confirmation, revalidation result display, and absence of manual override controls.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/admin test -- song-catalog && pnpm -F @home-ktv/admin build</automated>
  </verify>
  <acceptance_criteria>
- `rg "force verified|manual override|manualOverride" apps/admin/src/songs` returns no matches.
- `rg "ConfirmDangerDialog" apps/admin/src/songs` returns a match.
- `rg "revalidate" apps/admin/src/songs apps/admin/src/test/song-catalog.test.tsx` returns matches.
- `rg "switchFamily" apps/admin/src/songs` returns a match.
- `pnpm -F @home-ktv/admin build` exits 0.
  </acceptance_criteria>
  <done>Formal catalog UI supports safe song/resource maintenance without bypassing strict admission rules.</done>
</task>

</tasks>

<verification>
Run `pnpm -F @home-ktv/admin test -- song-catalog`, `pnpm -F @home-ktv/admin build`, and `pnpm -F @home-ktv/admin typecheck`.
</verification>

<success_criteria>
- Catalog admin UI uses the admin app and DOM test setup from Plan 02-04.
- Song/resource browse and maintenance cover ADMN-01 and LIBR-05.
- Dangerous edits require confirmation.
- No UI affordance exists for manually overriding duration delta `>300ms` into verified state.
- This plan modifies fewer than 15 files.
</success_criteria>

<output>
After completion, create `.planning/phases/02-library-ingest-catalog-admin/02-06-SUMMARY.md`.
</output>
