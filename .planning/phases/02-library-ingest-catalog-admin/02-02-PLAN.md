---
phase: 02-library-ingest-catalog-admin
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - apps/api/package.json
  - pnpm-lock.yaml
  - apps/api/src/config.ts
  - apps/api/src/server.ts
  - apps/api/src/modules/ingest/library-paths.ts
  - apps/api/src/modules/ingest/media-probe.ts
  - apps/api/src/modules/ingest/import-scanner.ts
  - apps/api/src/modules/ingest/candidate-builder.ts
  - apps/api/src/modules/ingest/scan-scheduler.ts
  - apps/api/src/modules/ingest/repositories/import-file-repository.ts
  - apps/api/src/modules/ingest/repositories/import-candidate-repository.ts
  - apps/api/src/modules/ingest/repositories/scan-run-repository.ts
  - apps/api/src/test/import-scanner.test.ts
  - apps/api/src/test/scan-scheduler-startup.test.ts
autonomous: true
requirements:
  - LIBR-01
must_haves:
  truths:
    - "Manual, watcher, and scheduled scan triggers all enqueue the same scanner intent."
    - "ImportScanner.scan() persists discovered files and then builds grouped import candidates."
    - "Unchanged files skip ffprobe by comparing root kind, relative path, size, mtime, and quick hash."
    - "The API process starts the scan scheduler and closes watcher/timer resources during Fastify shutdown."
  artifacts:
    - path: "apps/api/src/modules/ingest/import-scanner.ts"
      provides: "incremental file discovery and scanner-to-candidate-builder wiring"
    - path: "apps/api/src/modules/ingest/candidate-builder.ts"
      provides: "grouping import files into import candidates"
    - path: "apps/api/src/modules/ingest/scan-scheduler.ts"
      provides: "watcher, scheduled, and manual scan lifecycle"
    - path: "apps/api/src/server.ts"
      provides: "API bootstrap for scheduler startup and cleanup"
  key_links:
    - from: "apps/api/src/modules/ingest/import-scanner.ts"
      to: "apps/api/src/modules/ingest/candidate-builder.ts"
      via: "scan calls buildFromImportFiles after persisted file changes"
      pattern: "candidateBuilder\\.buildFromImportFiles"
    - from: "apps/api/src/modules/ingest/candidate-builder.ts"
      to: "apps/api/src/modules/ingest/repositories/import-candidate-repository.ts"
      via: "grouped candidate persistence"
      pattern: "upsertCandidateWithFiles"
    - from: "apps/api/src/server.ts"
      to: "apps/api/src/modules/ingest/scan-scheduler.ts"
      via: "createScanScheduler startup and onClose cleanup"
      pattern: "createScanScheduler"
---

<objective>
Implement the local ingest scanner, media probe cache, candidate generation link, and scheduler startup lifecycle.

Purpose: Scanning must create import candidates, not just import file rows, and automatic scan infrastructure must actually run with the API process while remaining lightweight.
Output: Chokidar-backed scheduler, incremental scanner/prober, candidate builder, repositories, startup wiring, and tests proving scan-to-candidate and lifecycle behavior.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-library-ingest-catalog-admin/02-CONTEXT.md
@.planning/phases/02-library-ingest-catalog-admin/02-RESEARCH.md
@.planning/phases/02-library-ingest-catalog-admin/02-01-SUMMARY.md
@apps/api/src/config.ts
@apps/api/src/server.ts
@apps/api/src/db/query-executor.ts
@apps/api/src/modules/ingest/repositories/import-candidate-repository.ts

<interfaces>
Contracts created by Plan 02-01 and consumed here:
```typescript
export type ImportFileRootKind = "imports_pending" | "imports_needs_review" | "songs";
export type ImportScanTrigger = "manual" | "scheduled" | "watcher";
export type ImportScanScope = "imports" | "songs" | "all";
export interface ImportCandidateFileDetail {
  rootKind: ImportFileRootKind;
  relativePath: string;
  probeStatus: ImportFileProbeStatus;
  durationMs: number | null;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build scanner, probe cache, and candidate generation</name>
  <files>apps/api/package.json, pnpm-lock.yaml, apps/api/src/config.ts, apps/api/src/modules/ingest/library-paths.ts, apps/api/src/modules/ingest/media-probe.ts, apps/api/src/modules/ingest/import-scanner.ts, apps/api/src/modules/ingest/candidate-builder.ts, apps/api/src/modules/ingest/repositories/import-file-repository.ts, apps/api/src/modules/ingest/repositories/import-candidate-repository.ts, apps/api/src/modules/ingest/repositories/scan-run-repository.ts, apps/api/src/test/import-scanner.test.ts</files>
  <read_first>
- apps/api/package.json
- apps/api/src/config.ts
- apps/api/src/db/schema.ts
- apps/api/src/db/query-executor.ts
- apps/api/src/modules/ingest/repositories/import-candidate-repository.ts
- .planning/phases/02-library-ingest-catalog-admin/02-CONTEXT.md
- .planning/phases/02-library-ingest-catalog-admin/02-RESEARCH.md
  </read_first>
  <behavior>
- Test 1: unchanged file identity `(rootKind, relativePath, sizeBytes, mtimeMs, quickHash)` returns `probeStatus: "skipped"` and does not call the injected probe function.
- Test 2: changed file identity calls the injected probe function once and persists `probeStatus: "probed"` with `durationMs`.
- Test 3: `ImportScanner.scan()` calls `candidateBuilder.buildFromImportFiles()` and the builder calls `upsertCandidateWithFiles()` with grouped candidate files.
  </behavior>
  <action>
Install `chokidar@5.0.0` in `@home-ktv/api`.

Update `apps/api/src/config.ts` to add `scanIntervalMinutes: number`, read from `SCAN_INTERVAL_MINUTES`, defaulting to `360`. Keep `MEDIA_ROOT` as the only configured library root.

Create `library-paths.ts` with `resolveLibraryPaths(mediaRoot)` returning `libraryRoot`, `songsRoot`, `importsPendingRoot`, and `importsNeedsReviewRoot`, using exactly `songs`, `imports/pending`, and `imports/needs-review` under `mediaRoot`. Add `toLibraryRelativePath(rootPath, absolutePath)` that returns POSIX-style relative paths and rejects paths escaping the root.

Create `media-probe.ts` using `execFile("ffprobe", ["-v","error","-print_format","json","-show_format","-show_streams", filePath], { timeout: 30000, maxBuffer: 2 * 1024 * 1024 })`. Parse duration from `format.duration` first, then stream duration, and return a typed probe summary with duration, format, codecs, dimensions, and raw payload.

Create `ImportFileRepository` and `ScanRunRepository` with Postgres implementations using `QueryExecutor`. `ImportFileRepository` must support `findByRootAndRelativePath`, `upsertDiscoveredFile`, and `markDeleted`.

Extend `apps/api/src/modules/ingest/repositories/import-candidate-repository.ts` in this plan, not a later one. The repository interface and `PgImportCandidateRepository` must include:
- `upsertCandidateWithFiles(input: { candidate: { title: string; artistName: string; language: Language; status?: ImportCandidateStatus; candidateMeta?: Record<string, unknown>; }; files: Array<{ importFileId: string; selected: boolean; proposedVocalMode: VocalMode; proposedAssetKind: AssetKind; roleConfidence: number; probeDurationMs: number | null; probeSummary: Record<string, unknown>; }>; }): Promise<ImportCandidate>`
- The implementation must write one `import_candidates` row and its linked `import_candidate_files` rows in a transaction or transaction-compatible helper, using `ON CONFLICT(candidate_id, import_file_id)` for file rows when appropriate.
- The method must be the exact persistence path used by `CandidateBuilder.buildFromImportFiles()`.

Create `candidate-builder.ts` with `CandidateBuilder.buildFromImportFiles(files)` that groups import files by inferred artist/title/version folder or filename stem, assigns proposed vocal roles, and persists groups via `importCandidateRepository.upsertCandidateWithFiles(...)`. This exact call is required to close the checker issue that scans may not generate import candidates.

Create `import-scanner.ts` with `ImportScanner.scan({ trigger, scope, pathHint? })`. It must:
- Discover supported media files `.mp4`, `.mkv`, `.mov`, `.webm` and `song.json`.
- Scan imports roots for candidate media and scan `/songs` lightly for consistency hints only.
- Compute `quickHash` as `${sizeBytes}:${sha1(first64KiB)}`.
- Compare previous `rootKind`, `relativePath`, `sizeBytes`, `mtimeMs`, and `quickHash` before probing.
- Persist changed import files, call `candidateBuilder.buildFromImportFiles(changedImportFiles)`, and finish the scan run with `candidateCount`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- import-scanner && pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
- `rg "\"chokidar\": \"5.0.0\"" apps/api/package.json` returns a match.
- `rg "SCAN_INTERVAL_MINUTES" apps/api/src/config.ts` returns a match.
- `rg "candidateBuilder\\.buildFromImportFiles" apps/api/src/modules/ingest/import-scanner.ts` returns a match.
- `rg "upsertCandidateWithFiles" apps/api/src/modules/ingest/candidate-builder.ts` returns a match.
- `rg "upsertCandidateWithFiles" apps/api/src/modules/ingest/repositories/import-candidate-repository.ts` returns a match.
- `rg "INSERT INTO import_candidates" apps/api/src/modules/ingest/repositories/import-candidate-repository.ts` returns a match.
- `rg "INSERT INTO import_candidate_files" apps/api/src/modules/ingest/repositories/import-candidate-repository.ts` returns a match.
- `rg "ffprobe" apps/api/src/modules/ingest/media-probe.ts` returns a match.
- `pnpm -F @home-ktv/api test -- import-scanner` exits 0.
  </acceptance_criteria>
  <done>Scanner output creates grouped import candidates and unchanged files do not repeat ffprobe work, satisfying D-04 and D-05.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Start scheduler from API bootstrap with cleanup</name>
  <files>apps/api/src/server.ts, apps/api/src/modules/ingest/scan-scheduler.ts, apps/api/src/test/scan-scheduler-startup.test.ts</files>
  <read_first>
- apps/api/src/server.ts
- apps/api/src/config.ts
- apps/api/src/modules/ingest/import-scanner.ts
- apps/api/src/modules/ingest/library-paths.ts
- apps/api/src/modules/ingest/candidate-builder.ts
  </read_first>
  <behavior>
- Test 1: `createScanScheduler` exposes `start()`, `close()`, and `enqueueManualScan(scope)` lifecycle methods.
- Test 2: watcher `add`, `change`, and `unlink` events enqueue `scanner.scan({ trigger: "watcher", scope: "all", pathHint })`.
- Test 3: `createServer()` calls scheduler `start()` when `DATABASE_URL` and `MEDIA_ROOT` are configured, and Fastify `onClose` calls scheduler `close()`.
  </behavior>
  <action>
Create `scan-scheduler.ts` with `createScanScheduler({ scanner, paths, scanIntervalMinutes, chokidarImpl? })`. The scheduler must:
- Watch `[paths.importsPendingRoot, paths.importsNeedsReviewRoot, paths.songsRoot]`.
- Use `ignoreInitial: true`, `awaitWriteFinish: { stabilityThreshold: 3000, pollInterval: 500 }`, `atomic: true`, and `depth: 5`.
- Handle `add`, `change`, and `unlink` by calling `scanner.scan({ trigger: "watcher", scope: "all", pathHint })`.
- Use `setInterval` for scheduled scans with `scanIntervalMinutes * 60_000`.
- Expose `enqueueManualScan(scope)` for later admin import API reuse.
- Expose `close()` that closes the watcher and clears the interval.

Update `apps/api/src/server.ts` to construct `resolveLibraryPaths(config.mediaRoot)`, repositories, `CandidateBuilder`, `ImportScanner`, and `createScanScheduler` for the Postgres-backed runtime. Start the scheduler during API bootstrap and add an `onClose` hook to call scheduler cleanup before ending the pool. In the in-memory/no-database runtime, do not start the scheduler.

Create `scan-scheduler-startup.test.ts` with injected fake scheduler/chokidar/timer dependencies so the test proves startup and cleanup without touching the real filesystem.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- scan-scheduler-startup && pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
- `rg "createScanScheduler" apps/api/src/server.ts` returns a match.
- `rg "server\\.addHook\\(\"onClose\".*scheduler\\.close" apps/api/src/server.ts` returns a match, or the onClose cleanup appears on adjacent lines.
- `rg "stabilityThreshold: 3000" apps/api/src/modules/ingest/scan-scheduler.ts` returns a match.
- `rg "trigger: \"watcher\"" apps/api/src/modules/ingest/scan-scheduler.ts` returns a match.
- `pnpm -F @home-ktv/api test -- scan-scheduler-startup` exits 0.
  </acceptance_criteria>
  <done>Watcher and scheduled scanning are actually started by API bootstrap and cleaned up during shutdown.</done>
</task>

</tasks>

<verification>
Run `pnpm -F @home-ktv/api test -- import-scanner scan-scheduler-startup` and `pnpm -F @home-ktv/api typecheck`.
</verification>

<success_criteria>
- Scanner writes import file rows and grouped import candidates in one scan flow.
- All scan triggers use the same scanner entry point.
- API startup wires scheduler lifecycle and cleanup.
- This plan modifies fewer than 15 files.
</success_criteria>

<output>
After completion, create `.planning/phases/02-library-ingest-catalog-admin/02-02-SUMMARY.md`.
</output>
