---
phase: 02-library-ingest-catalog-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/domain/src/index.ts
  - apps/api/src/db/schema.ts
  - apps/api/src/db/migrations/0002_library_ingest_admin.sql
  - apps/api/src/modules/catalog/repositories/song-repository.ts
  - apps/api/src/modules/ingest/repositories/import-candidate-repository.ts
  - apps/api/src/test/library-ingest-schema.test.ts
  - apps/api/src/test/catalog-contracts.test.ts
autonomous: true
requirements:
  - LIBR-01
  - LIBR-04
  - LIBR-06
must_haves:
  truths:
    - "The formal Song contract includes status so admin and playback-facing catalog code can distinguish ready, review_required, and unavailable songs."
    - "Import scan files, candidates, candidate files, scan runs, and source records are persisted outside songs/assets until admission approves them."
    - "Admin import APIs can request joined candidate file details with safe root-relative file metadata and probe evidence."
  artifacts:
    - path: "apps/api/src/db/migrations/0002_library_ingest_admin.sql"
      provides: "songs.status, import_* tables, source_records, and verified switch pair guard"
    - path: "packages/domain/src/index.ts"
      provides: "Song.status, import status enums, ImportCandidateFileDetail contract"
    - path: "apps/api/src/db/schema.ts"
      provides: "SongRow.status and import table row contracts"
    - path: "apps/api/src/modules/ingest/repositories/import-candidate-repository.ts"
      provides: "joined ImportCandidateFileDetail mapper and repository contract"
  key_links:
    - from: "apps/api/src/modules/catalog/repositories/song-repository.ts"
      to: "packages/domain/src/index.ts"
      via: "mapSongRow propagates SongRow.status into Song.status"
      pattern: "status: row\\.status as SongStatus"
    - from: "apps/api/src/modules/ingest/repositories/import-candidate-repository.ts"
      to: "apps/api/src/db/schema.ts"
      via: "joined ImportCandidateFileDetail row mapper"
      pattern: "ImportCandidateFileDetail"
---

<objective>
Create the Phase 2 database, domain, and repository contracts that all later scanner, import review, and catalog maintenance plans build on.

Purpose: The rest of Phase 2 must not infer missing fields or invent local response shapes. This plan makes Song.status, import staging tables, and joined candidate-file probe details available as explicit contracts.
Output: SQL migration, shared domain exports, schema row types, Song mapper propagation, and joined candidate-file detail repository contract.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-library-ingest-catalog-admin/02-CONTEXT.md
@.planning/phases/02-library-ingest-catalog-admin/02-RESEARCH.md
@.planning/phases/01-media-contract-tv-runtime/01-02-SUMMARY.md
@packages/domain/src/index.ts
@apps/api/src/db/schema.ts
@apps/api/src/db/migrations/0001_media_contract.sql
@apps/api/src/modules/catalog/repositories/song-repository.ts

<interfaces>
Existing Song contract before this plan:
```typescript
export interface Song {
  id: SongId;
  title: string;
  artistName: string;
  language: Language;
  defaultAssetId: AssetId | null;
  capabilities: SongCapabilities;
  createdAt: string;
  updatedAt: string;
}
```

Existing repository mapper pattern:
```typescript
export function mapSongRow(row: SongRow): Song {
  return {
    id: row.id,
    title: row.title,
    language: row.language as Language,
    defaultAssetId: row.default_asset_id,
    createdAt: row.created_at.toISOString(),
    updatedAt: row.updated_at.toISOString()
  };
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add schema and domain contracts</name>
  <files>packages/domain/src/index.ts, apps/api/src/db/schema.ts, apps/api/src/db/migrations/0002_library_ingest_admin.sql, apps/api/src/test/library-ingest-schema.test.ts</files>
  <read_first>
- packages/domain/src/index.ts
- apps/api/src/db/schema.ts
- apps/api/src/db/migrations/0001_media_contract.sql
- .planning/phases/02-library-ingest-catalog-admin/02-CONTEXT.md
- .planning/phases/02-library-ingest-catalog-admin/02-RESEARCH.md
  </read_first>
  <behavior>
- Test 1: migration SQL contains `ALTER TABLE songs ADD COLUMN IF NOT EXISTS status` with allowed values `ready`, `review_required`, and `unavailable`.
- Test 2: migration SQL contains `import_scan_runs`, `import_files`, `import_candidates`, `import_candidate_files`, `source_records`, and `assets_verified_switch_family_mode_uq`.
- Test 3: domain exports compile for `SongStatus`, `ImportCandidateStatus`, `ImportFileRootKind`, `ImportFileProbeStatus`, `ImportCandidate`, `ImportCandidateFile`, and `ImportCandidateFileDetail`.
  </behavior>
  <action>
Create `apps/api/src/db/migrations/0002_library_ingest_admin.sql` as the Phase 2 migration. It must:
- Add `songs.status text NOT NULL DEFAULT 'ready' CHECK (status IN ('ready','review_required','unavailable'))`.
- Create `import_scan_runs` with trigger `manual | scheduled | watcher`, status `queued | running | completed | failed`, scope `imports | songs | all`, counters, error_message, started_at, and finished_at.
- Create `import_files` with root_kind `imports_pending | imports_needs_review | songs`, relative_path, size_bytes, mtime_ms, quick_hash, probe_status `pending | probed | failed | skipped | deleted`, probe_payload jsonb, duration_ms, timestamps, and `UNIQUE(root_kind, relative_path)`.
- Create `import_candidates` with status `pending | held | review_required | conflict | approved | rejected_deleted | approval_failed`, editable metadata fields from D-07, `same_version_confirmed`, `conflict_song_id`, `review_notes`, `candidate_meta`, and timestamps.
- Create `import_candidate_files` with candidate_id, import_file_id, selected, proposed_vocal_mode, proposed_asset_kind, role_confidence, probe_duration_ms, probe_summary, timestamps, and `UNIQUE(candidate_id, import_file_id)`.
- Create `source_records` for approved assets with provider metadata and raw_meta jsonb.
- Add indexes for scan status, file root/path, candidate status, candidate files, source provider, and the partial unique `assets_verified_switch_family_mode_uq` on `(song_id, switch_family, vocal_mode)` where `switch_family IS NOT NULL`, `status = 'ready'`, and `switch_quality_status = 'verified'`.

Update `packages/domain/src/index.ts`:
- Add `export type SongStatus = "ready" | "review_required" | "unavailable";`.
- Add `status: SongStatus` to `Song`.
- Export import scan/file/candidate status, trigger, scope, and root-kind types.
- Export `ImportScanRun`, `ImportFile`, `ImportCandidate`, `ImportCandidateFile`, and joined `ImportCandidateFileDetail`. `ImportCandidateFileDetail` must include candidate-file fields plus `rootKind`, `relativePath`, `probeStatus`, `durationMs`, and probe summary/payload fields needed by admin/delete/approve.

Update `apps/api/src/db/schema.ts`:
- Add import table names to `tableNames`.
- Add enum arrays for song status, scan trigger/status/scope, file root kind, file probe status, and candidate status.
- Add `status: string` to `SongRow`.
- Add row interfaces for import scan runs, import files, import candidates, import candidate files, source records, and an `ImportCandidateFileDetailRow` that joins candidate-file rows to import-file rows.

Create `apps/api/src/test/library-ingest-schema.test.ts` that reads the migration file with `readFileSync(new URL("../db/migrations/0002_library_ingest_admin.sql", import.meta.url), "utf8")` and asserts the required table, column, enum, and index strings.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- library-ingest-schema && pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
- `rg "ADD COLUMN IF NOT EXISTS status" apps/api/src/db/migrations/0002_library_ingest_admin.sql` returns a match.
- `rg "CREATE TABLE IF NOT EXISTS import_candidate_files" apps/api/src/db/migrations/0002_library_ingest_admin.sql` returns a match.
- `rg "assets_verified_switch_family_mode_uq" apps/api/src/db/migrations/0002_library_ingest_admin.sql` returns a match.
- `rg "export type SongStatus" packages/domain/src/index.ts` returns a match.
- `rg "interface ImportCandidateFileDetail" packages/domain/src/index.ts` returns a match.
  </acceptance_criteria>
  <done>Phase 2 schema/domain contracts exist, and tests prove the required tables, Song.status, and verified switch pair guard are present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Propagate Song.status and candidate file detail mappers</name>
  <files>apps/api/src/modules/catalog/repositories/song-repository.ts, apps/api/src/modules/ingest/repositories/import-candidate-repository.ts, apps/api/src/test/catalog-contracts.test.ts</files>
  <read_first>
- apps/api/src/modules/catalog/repositories/song-repository.ts
- apps/api/src/db/schema.ts
- packages/domain/src/index.ts
- apps/api/src/db/query-executor.ts
- apps/api/src/modules/catalog/repositories/asset-repository.ts
  </read_first>
  <behavior>
- Test 1: `mapSongRow` maps `SongRow.status` to `Song.status`.
- Test 2: `PgSongRepository.findById` selects the `status` column from `songs`.
- Test 3: `mapImportCandidateFileDetailRow` returns `rootKind`, `relativePath`, `probeStatus`, `durationMs`, and candidate-file fields from a joined row.
  </behavior>
  <action>
Update `apps/api/src/modules/catalog/repositories/song-repository.ts` so every `SELECT` includes `status`, `mapSongRow` casts it to `SongStatus`, and returned `Song` objects include `status`. This fixes the checker issue where later admin catalog plans need song status but Plan 01 did not propagate it.

Create `apps/api/src/modules/ingest/repositories/import-candidate-repository.ts` with:
- `ImportCandidateRepository` interface exposing at least `listCandidateFileDetails(candidateId: string): Promise<ImportCandidateFileDetail[]>`.
- `mapImportCandidateFileDetailRow(row: ImportCandidateFileDetailRow): ImportCandidateFileDetail`.
- `PgImportCandidateRepository` using `QueryExecutor` and a join from `import_candidate_files` to `import_files`.
- The joined detail must expose safe logical fields only: `rootKind`, `relativePath`, `probeStatus`, `durationMs`, `probePayload`, `probeSummary`, selected/proposed fields, and timestamps. Do not expose unchecked absolute paths.

Create `apps/api/src/test/catalog-contracts.test.ts` with mapper-level tests for Song.status and `ImportCandidateFileDetail`. Use in-memory rows, not a live database.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- catalog-contracts && pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
- `rg "status: row\\.status as SongStatus" apps/api/src/modules/catalog/repositories/song-repository.ts` returns a match.
- `rg "status, created_at" apps/api/src/modules/catalog/repositories/song-repository.ts` returns a match or the SELECT includes `status` on its own line.
- `rg "mapImportCandidateFileDetailRow" apps/api/src/modules/ingest/repositories/import-candidate-repository.ts` returns a match.
- `rg "rootKind: row\\.root_kind" apps/api/src/modules/ingest/repositories/import-candidate-repository.ts` returns a match.
- `pnpm -F @home-ktv/api test -- catalog-contracts` exits 0.
  </acceptance_criteria>
  <done>Song.status and joined import candidate file details are available to downstream scanner, admission, and admin plans without local duplicate contracts.</done>
</task>

</tasks>

<verification>
Run `pnpm -F @home-ktv/api test -- library-ingest-schema catalog-contracts` and `pnpm -F @home-ktv/api typecheck`.
</verification>

<success_criteria>
- `Song.status`, `SongRow.status`, and `mapSongRow` propagation are implemented.
- Import staging tables exist in migration SQL and typed schema exports.
- `ImportCandidateFileDetail` includes joined file path/probe fields needed by admin delete, hold, and approve flows.
- This plan modifies fewer than 15 files.
</success_criteria>

<output>
After completion, create `.planning/phases/02-library-ingest-catalog-admin/02-01-SUMMARY.md`.
</output>
