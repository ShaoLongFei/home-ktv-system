---
phase: 02-library-ingest-catalog-admin
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/api/package.json
  - pnpm-lock.yaml
  - packages/domain/src/index.ts
  - apps/api/src/config.ts
  - apps/api/src/db/schema.ts
  - apps/api/src/db/migrations/0002_library_ingest_admin.sql
  - apps/api/src/modules/ingest/library-paths.ts
  - apps/api/src/modules/ingest/media-probe.ts
  - apps/api/src/modules/ingest/import-scanner.ts
  - apps/api/src/modules/ingest/candidate-builder.ts
  - apps/api/src/modules/ingest/scan-scheduler.ts
  - apps/api/src/modules/ingest/repositories/import-file-repository.ts
  - apps/api/src/modules/ingest/repositories/import-candidate-repository.ts
  - apps/api/src/modules/ingest/repositories/scan-run-repository.ts
  - apps/api/src/modules/catalog/admission-service.ts
  - apps/api/src/modules/catalog/song-json.ts
  - apps/api/src/test/library-ingest-schema.test.ts
  - apps/api/src/test/import-scanner.test.ts
  - apps/api/src/test/candidate-builder.test.ts
  - apps/api/src/test/catalog-admission.test.ts
autonomous: true
requirements:
  - LIBR-01
  - LIBR-04
  - LIBR-06
must_haves:
  truths:
    - "Admin-visible import candidates are created from configured local import directories without writing unreviewed rows into songs/assets."
    - "Watcher, scheduled, and manual scan triggers enqueue the same incremental scan intent."
    - "Unchanged media files skip ffprobe by comparing relative path, size, mtime_ms, and quick_hash."
    - "Formal admission rules reject single-resource candidates and only verify original/instrumental pairs with duration delta <=300ms."
  artifacts:
    - path: "apps/api/src/db/migrations/0002_library_ingest_admin.sql"
      provides: "import_scan_runs, import_files, import_candidates, import_candidate_files, source_records, and strict verified-pair indexes"
    - path: "apps/api/src/modules/ingest/import-scanner.ts"
      provides: "incremental local import/formal scan orchestration"
    - path: "apps/api/src/modules/ingest/media-probe.ts"
      provides: "ffprobe JSON wrapper with timeout and typed probe summary"
    - path: "apps/api/src/modules/catalog/admission-service.ts"
      provides: "strict switch-pair evaluation shared by approve and later maintenance flows"
  key_links:
    - from: "apps/api/src/modules/ingest/import-scanner.ts"
      to: "apps/api/src/modules/ingest/repositories/import-file-repository.ts"
      via: "scan file identity cache"
      pattern: "findByRootAndRelativePath"
    - from: "apps/api/src/modules/ingest/candidate-builder.ts"
      to: "apps/api/src/modules/ingest/repositories/import-candidate-repository.ts"
      via: "grouped candidate persistence"
      pattern: "upsertCandidateWithFiles"
    - from: "apps/api/src/modules/catalog/admission-service.ts"
      to: "packages/domain/src/index.ts"
      via: "VocalMode and SwitchQualityStatus values"
      pattern: "duration-delta-over-300ms"
---

<objective>
Build the Phase 2 catalog trust boundary: import candidates, scan/probe persistence, incremental scan triggers, and the strict admission rule evaluator that prevents uncertain media from becoming playback-visible catalog data.

Purpose: Phase 1 playback already trusts `ready` + `verified` + same `switchFamily` assets. This plan makes the import pipeline collect evidence without polluting formal `songs` and `assets`.
Output: Database/domain foundation, scanner/probe/candidate pipeline, chokidar watcher setup, and admission threshold tests.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-library-ingest-catalog-admin/02-CONTEXT.md
@.planning/phases/02-library-ingest-catalog-admin/02-RESEARCH.md
@.planning/phases/01-media-contract-tv-runtime/01-02-SUMMARY.md
@packages/domain/src/index.ts
@apps/api/src/db/schema.ts
@apps/api/src/db/migrations/0001_media_contract.sql
@apps/api/src/modules/catalog/repositories/song-repository.ts
@apps/api/src/modules/catalog/repositories/asset-repository.ts
@docs/KTV-ARCHITECTURE.md

<interfaces>
Existing shared catalog contracts:
```typescript
export type Language = "mandarin" | "cantonese" | "other";
export type VocalMode = "original" | "instrumental" | "dual" | "unknown";
export type AssetStatus = "ready" | "caching" | "failed" | "unavailable" | "stale" | "promoted";
export type SwitchQualityStatus = "verified" | "review_required" | "rejected" | "unknown";
export interface Song { id: SongId; title: string; artistName: string; language: Language; defaultAssetId: AssetId | null; capabilities: SongCapabilities; }
export interface Asset { id: AssetId; songId: SongId; filePath: string; durationMs: number; vocalMode: VocalMode; status: AssetStatus; switchFamily: SwitchFamily | null; switchQualityStatus: SwitchQualityStatus; }
```

Existing repository style:
```typescript
export interface SongRepository {
  findById(songId: SongId): Promise<Song | null>;
}

export interface AssetRepository {
  findById(assetId: AssetId): Promise<Asset | null>;
  findVerifiedSwitchCounterparts(asset: Asset): Promise<Asset[]>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add ingest domain contracts and SQL migration</name>
  <files>packages/domain/src/index.ts, apps/api/src/db/schema.ts, apps/api/src/db/migrations/0002_library_ingest_admin.sql, apps/api/src/test/library-ingest-schema.test.ts</files>
  <read_first>
- Every existing path listed in this task's <files>; if a listed path is new, verify it is absent before creating it.
- packages/domain/src/index.ts
- apps/api/src/db/schema.ts
- apps/api/src/db/migrations/0001_media_contract.sql
- apps/api/src/modules/catalog/repositories/song-repository.ts
- apps/api/src/modules/catalog/repositories/asset-repository.ts
- .planning/phases/02-library-ingest-catalog-admin/02-CONTEXT.md
- .planning/phases/02-library-ingest-catalog-admin/02-RESEARCH.md
  </read_first>
  <behavior>
- Test 1: migration SQL contains `CREATE TABLE IF NOT EXISTS import_scan_runs`, `import_files`, `import_candidates`, `import_candidate_files`, and `source_records`.
- Test 2: migration SQL contains `assets_verified_switch_family_mode_uq` partial unique index on `(song_id, switch_family, vocal_mode)` with `status = 'ready'` and `switch_quality_status = 'verified'`.
- Test 3: domain exports include `ImportCandidateStatus`, `ImportScanTrigger`, `ImportFileProbeStatus`, `ImportCandidate`, and `ImportCandidateFile`.
  </behavior>
  <action>
Create `apps/api/src/db/migrations/0002_library_ingest_admin.sql` with these exact structures:
- `ALTER TABLE songs ADD COLUMN IF NOT EXISTS status text NOT NULL DEFAULT 'ready' CHECK (status IN ('ready', 'review_required', 'unavailable'));`
- `CREATE TABLE IF NOT EXISTS import_scan_runs` with columns `id text PRIMARY KEY DEFAULT gen_random_uuid()::text`, `trigger text NOT NULL CHECK (trigger IN ('manual','scheduled','watcher'))`, `status text NOT NULL CHECK (status IN ('queued','running','completed','failed'))`, `scope text NOT NULL CHECK (scope IN ('imports','songs','all'))`, `changed_files integer NOT NULL DEFAULT 0`, `probed_files integer NOT NULL DEFAULT 0`, `candidate_count integer NOT NULL DEFAULT 0`, `error_message text`, `started_at timestamptz NOT NULL DEFAULT now()`, `finished_at timestamptz`.
- `CREATE TABLE IF NOT EXISTS import_files` with columns `id`, `root_kind text CHECK (root_kind IN ('imports_pending','imports_needs_review','songs'))`, `relative_path text NOT NULL`, `size_bytes bigint NOT NULL`, `mtime_ms bigint NOT NULL`, `quick_hash text NOT NULL`, `probe_status text NOT NULL CHECK (probe_status IN ('pending','probed','failed','skipped','deleted'))`, `probe_payload jsonb NOT NULL DEFAULT '{}'::jsonb`, `duration_ms integer`, `last_scanned_at timestamptz NOT NULL DEFAULT now()`, `created_at`, `updated_at`, plus `UNIQUE(root_kind, relative_path)`.
- `CREATE TABLE IF NOT EXISTS import_candidates` with columns `id`, `status text NOT NULL CHECK (status IN ('pending','held','review_required','conflict','approved','rejected_deleted'))`, `title text NOT NULL DEFAULT ''`, `artist_name text NOT NULL DEFAULT ''`, `language text NOT NULL DEFAULT 'mandarin'`, `genre text[] NOT NULL DEFAULT '{}'`, `tags text[] NOT NULL DEFAULT '{}'`, `aliases text[] NOT NULL DEFAULT '{}'`, `search_hints text[] NOT NULL DEFAULT '{}'`, `release_year integer`, `default_vocal_mode text NOT NULL DEFAULT 'instrumental' CHECK (default_vocal_mode IN ('original','instrumental','dual','unknown'))`, `same_version_confirmed boolean NOT NULL DEFAULT false`, `conflict_song_id text REFERENCES songs(id) ON DELETE SET NULL`, `review_notes text`, `candidate_meta jsonb NOT NULL DEFAULT '{}'::jsonb`, `created_at`, `updated_at`.
- `CREATE TABLE IF NOT EXISTS import_candidate_files` with columns `id`, `candidate_id text NOT NULL REFERENCES import_candidates(id) ON DELETE CASCADE`, `import_file_id text NOT NULL REFERENCES import_files(id) ON DELETE CASCADE`, `selected boolean NOT NULL DEFAULT true`, `proposed_vocal_mode text NOT NULL DEFAULT 'unknown' CHECK (proposed_vocal_mode IN ('original','instrumental','dual','unknown'))`, `proposed_asset_kind text NOT NULL DEFAULT 'video' CHECK (proposed_asset_kind IN ('video','audio+lyrics','dual-track-video'))`, `role_confidence numeric(5,4) NOT NULL DEFAULT 0`, `probe_duration_ms integer`, `probe_summary jsonb NOT NULL DEFAULT '{}'::jsonb`, `created_at`, `updated_at`, plus `UNIQUE(candidate_id, import_file_id)`.
- `CREATE TABLE IF NOT EXISTS source_records` with `id`, `asset_id text REFERENCES assets(id) ON DELETE CASCADE`, `provider text NOT NULL`, `provider_song_id text`, `original_path text`, `raw_meta jsonb NOT NULL DEFAULT '{}'::jsonb`, `created_at`, `updated_at`.
- Indexes: `import_scan_runs_status_started_idx`, `import_files_root_path_idx`, `import_candidates_status_updated_idx`, `import_candidate_files_candidate_idx`, `source_records_provider_idx`, and `assets_verified_switch_family_mode_uq`.

Update `packages/domain/src/index.ts` with these exact exported types:
- `export type SongStatus = "ready" | "review_required" | "unavailable";`
- `export type ImportCandidateStatus = "pending" | "held" | "review_required" | "conflict" | "approved" | "rejected_deleted";`
- `export type ImportScanTrigger = "manual" | "scheduled" | "watcher";`
- `export type ImportScanStatus = "queued" | "running" | "completed" | "failed";`
- `export type ImportScanScope = "imports" | "songs" | "all";`
- `export type ImportFileRootKind = "imports_pending" | "imports_needs_review" | "songs";`
- `export type ImportFileProbeStatus = "pending" | "probed" | "failed" | "skipped" | "deleted";`
- interfaces `ImportScanRun`, `ImportFile`, `ImportCandidate`, and `ImportCandidateFile` using camelCase fields matching the SQL columns.

Update `apps/api/src/db/schema.ts` to export table names for `importScanRuns`, `importFiles`, `importCandidates`, `importCandidateFiles`, `sourceRecords`; enum arrays for the status/trigger/scope/root values; and row interfaces matching the SQL column names. Keep existing table names and row interfaces intact.

Create `apps/api/src/test/library-ingest-schema.test.ts` that imports the migration SQL as text using `readFileSync(new URL("../db/migrations/0002_library_ingest_admin.sql", import.meta.url), "utf8")`, asserts the table/index strings above, and imports the new domain enum types in a compile-time sample object.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- library-ingest-schema && pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
- `rg "CREATE TABLE IF NOT EXISTS import_scan_runs" apps/api/src/db/migrations/0002_library_ingest_admin.sql` returns a match.
- `rg "assets_verified_switch_family_mode_uq" apps/api/src/db/migrations/0002_library_ingest_admin.sql` returns a match.
- `rg "export type ImportCandidateStatus" packages/domain/src/index.ts` returns a match.
- `rg "importCandidates" apps/api/src/db/schema.ts` returns a match.
- `pnpm -F @home-ktv/api test -- library-ingest-schema` exits 0.
  </acceptance_criteria>
  <done>Import/candidate/schema contracts exist and tests prove the strict import tables and verified switch-pair guard are present.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement incremental scanner, ffprobe wrapper, and scan triggers</name>
  <files>apps/api/package.json, pnpm-lock.yaml, apps/api/src/config.ts, apps/api/src/modules/ingest/library-paths.ts, apps/api/src/modules/ingest/media-probe.ts, apps/api/src/modules/ingest/import-scanner.ts, apps/api/src/modules/ingest/scan-scheduler.ts, apps/api/src/modules/ingest/repositories/import-file-repository.ts, apps/api/src/modules/ingest/repositories/scan-run-repository.ts, apps/api/src/test/import-scanner.test.ts</files>
  <read_first>
- Every existing path listed in this task's <files>; if a listed path is new, verify it is absent before creating it.
- apps/api/package.json
- pnpm-lock.yaml
- apps/api/src/config.ts
- apps/api/src/db/schema.ts
- apps/api/src/db/query-executor.ts
- apps/api/src/modules/assets/media-path-resolver.ts
- .planning/phases/02-library-ingest-catalog-admin/02-CONTEXT.md
- .planning/phases/02-library-ingest-catalog-admin/02-RESEARCH.md
  </read_first>
  <behavior>
- Test 1: unchanged file identity `(rootKind, relativePath, sizeBytes, mtimeMs, quickHash)` results in `probeStatus: "skipped"` and does not call the probe function.
- Test 2: changed file identity calls the injected probe function once and saves `probeStatus: "probed"` with `durationMs`.
- Test 3: scheduler exposes watcher, scheduled, and manual enqueue paths that all call `scanner.scan({ trigger, scope })`.
  </behavior>
  <action>
Install `chokidar@5.0.0` in `@home-ktv/api` with `pnpm add -F @home-ktv/api chokidar@5.0.0`.

Update `apps/api/src/config.ts` by adding `scanIntervalMinutes: number` to `ApiConfig` and reading `SCAN_INTERVAL_MINUTES`; default to `360`. Keep `MEDIA_ROOT` as the library root. Do not add `LIBRARY_ROOT`, `IMPORTS_ROOT`, or `SONGS_ROOT`.

Create `apps/api/src/modules/ingest/library-paths.ts` with:
- `resolveLibraryPaths(mediaRoot: string)` returning `{ libraryRoot, songsRoot, importsPendingRoot, importsNeedsReviewRoot }`.
- roots exactly `songs`, `imports/pending`, and `imports/needs-review` below `mediaRoot`.
- `toLibraryRelativePath(rootPath: string, absolutePath: string)` that returns POSIX-style relative paths and rejects paths escaping the root.

Create `apps/api/src/modules/ingest/media-probe.ts` with:
- `export interface MediaProbeResult { durationMs: number | null; formatName: string | null; videoCodec: string | null; audioCodec: string | null; width: number | null; height: number | null; raw: Record<string, unknown>; }`
- `probeMedia(filePath: string): Promise<MediaProbeResult>` using `execFile("ffprobe", ["-v","error","-print_format","json","-show_format","-show_streams",filePath], { timeout: 30000, maxBuffer: 2 * 1024 * 1024 })`.
- duration parsing from `format.duration` first, then first stream duration, converted to rounded milliseconds.

Create repository interfaces/classes:
- `ImportFileRepository` with `findByRootAndRelativePath(rootKind, relativePath)`, `upsertDiscoveredFile(input)`, `markDeleted(input)`.
- `ScanRunRepository` with `startRun({ trigger, scope })`, `finishRun({ runId, status, changedFiles, probedFiles, candidateCount, errorMessage })`.
- Postgres implementations using `QueryExecutor.query` and row mappers in the same style as existing catalog repositories.

Create `apps/api/src/modules/ingest/import-scanner.ts` with:
- `SUPPORTED_MEDIA_EXTENSIONS = [".mp4", ".mkv", ".mov", ".webm"]`.
- `SUPPORTED_METADATA_FILES = ["song.json"]`.
- `ImportScanner.scan({ trigger, scope, pathHint? })` where trigger is `manual | scheduled | watcher` and scope is `imports | songs | all`.
- Scan imports roots aggressively and songs root lightly: for `rootKind: "songs"`, read `song.json` and validate referenced files exist; run `ffprobe` only when file identity changed.
- Compute `quickHash` from the first 65536 bytes plus file size as `${sizeBytes}:${sha1(first64KiB)}`.
- Compare existing `import_files` row and skip probe when `sizeBytes`, `mtimeMs`, and `quickHash` are unchanged.

Create `apps/api/src/modules/ingest/scan-scheduler.ts` with:
- `createScanScheduler({ scanner, paths, scanIntervalMinutes })`.
- watcher via `chokidar.watch([paths.importsPendingRoot, paths.importsNeedsReviewRoot, paths.songsRoot], { ignoreInitial: true, awaitWriteFinish: { stabilityThreshold: 3000, pollInterval: 500 }, atomic: true, depth: 5 })`.
- `enqueueManualScan(scope: "imports" | "songs" | "all")`.
- low-frequency scheduled scan using `setInterval` with `scanIntervalMinutes * 60_000`.
- watcher event handlers for `add`, `change`, and `unlink` that call `scanner.scan({ trigger: "watcher", scope: "all", pathHint })`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- import-scanner && pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
- `rg "\"chokidar\": \"5.0.0\"" apps/api/package.json` returns a match.
- `rg "SCAN_INTERVAL_MINUTES" apps/api/src/config.ts` returns a match.
- `rg "SUPPORTED_MEDIA_EXTENSIONS = \\[\"\\.mp4\", \"\\.mkv\", \"\\.mov\", \"\\.webm\"\\]" apps/api/src/modules/ingest/import-scanner.ts` returns a match.
- `rg "stabilityThreshold: 3000" apps/api/src/modules/ingest/scan-scheduler.ts` returns a match.
- `rg "ffprobe" apps/api/src/modules/ingest/media-probe.ts` returns a match.
- `pnpm -F @home-ktv/api test -- import-scanner` exits 0.
  </acceptance_criteria>
  <done>All three scan triggers exist, scans are incremental by file identity, and unchanged files avoid repeated ffprobe work per D-04 and D-05.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Build candidate grouping and strict pair evaluation</name>
  <files>apps/api/src/modules/ingest/candidate-builder.ts, apps/api/src/modules/ingest/repositories/import-candidate-repository.ts, apps/api/src/modules/catalog/admission-service.ts, apps/api/src/modules/catalog/song-json.ts, apps/api/src/test/candidate-builder.test.ts, apps/api/src/test/catalog-admission.test.ts</files>
  <read_first>
- Every existing path listed in this task's <files>; if a listed path is new, verify it is absent before creating it.
- packages/domain/src/index.ts
- apps/api/src/db/schema.ts
- apps/api/src/modules/catalog/repositories/song-repository.ts
- apps/api/src/modules/catalog/repositories/asset-repository.ts
- apps/api/src/modules/playback/build-switch-target.ts
- .planning/phases/02-library-ingest-catalog-admin/02-CONTEXT.md
- .planning/phases/02-library-ingest-catalog-admin/02-RESEARCH.md
  </read_first>
  <behavior>
- Test 1: two files inferred as the same artist/title with `original` and `instrumental` roles create one `pending` candidate with two expandable file rows.
- Test 2: one playable resource remains a candidate and `evaluateSwitchPair` returns `{ status: "held", reason: "missing-counterpart" }`.
- Test 3: original/instrumental pair with confirmed same version and delta `300` returns `verified`; delta `301` returns `review_required`; delta `1001` returns `rejected`.
- Test 4: unconfirmed same version returns `review_required` even when duration delta is `<=300`.
  </behavior>
  <action>
Create `ImportCandidateRepository` with:
- `listByStatus(statuses: readonly ImportCandidateStatus[]): Promise<ImportCandidate[]>`.
- `getWithFiles(candidateId: string): Promise<{ candidate: ImportCandidate; files: ImportCandidateFile[] } | null>`.
- `upsertCandidateWithFiles(input: UpsertCandidateWithFilesInput): Promise<ImportCandidate>`.
- `saveMetadata(input: SaveCandidateMetadataInput): Promise<ImportCandidate>`.
- `markStatus(input: { candidateId: string; status: ImportCandidateStatus; reviewNotes?: string | null }): Promise<ImportCandidate>`.

Create `CandidateBuilder` with `buildFromImportFiles(files: ImportFile[]): Promise<ImportCandidate[]>`. Use this concrete inference:
- File names containing `伴奏`, `伴唱`, `instrumental`, or `karaoke` propose `vocalMode: "instrumental"`.
- File names containing `原唱`, `original`, or `vocal` propose `vocalMode: "original"`.
- Otherwise propose `vocalMode: "unknown"`.
- Initial `title` and `artistName` come from `artist - title` or `artist-title`; if not parseable, use empty strings and status `review_required`.
- Group key is `${normalizedArtistName}::${normalizedTitle}` when both exist; otherwise one candidate per file.
- Candidate statuses: `pending` when title, artist, and at least two selected files exist; `review_required` when metadata or role is ambiguous.

Create `apps/api/src/modules/catalog/admission-service.ts` with:
- `export type AdmissionDecisionStatus = "verified" | "review_required" | "rejected" | "held";`
- `evaluateSwitchPair(input: { originalDurationMs: number | null; instrumentalDurationMs: number | null; sameVersionConfirmed: boolean; selectedResourceCount: number; }): AdmissionDecision`.
- Return `{ status: "held", reason: "missing-counterpart" }` when `selectedResourceCount < 2` per D-10.
- Return `{ status: "review_required", reason: "missing-duration" }` when either duration is null.
- Return `{ status: "verified", durationDeltaMs }` only when `sameVersionConfirmed === true` and delta `<= 300`.
- Return `{ status: "review_required", reason: "duration-delta-over-300ms", durationDeltaMs }` when delta is `301` through `1000`.
- Return `{ status: "rejected", reason: "duration-delta-over-1000ms", durationDeltaMs }` when delta is `> 1000`.
- Return `{ status: "review_required", reason: "same-version-unconfirmed", durationDeltaMs }` when delta is `<=300` but `sameVersionConfirmed` is false.

Create `apps/api/src/modules/catalog/song-json.ts` with `buildSongJson(input)` returning an object containing exact keys `title`, `artist`, `language`, `genre`, `tags`, `releaseYear`, `aliases`, `searchHints`, `defaultAsset`, and `assets`. The default asset file must be `instrumental.mp4` when an instrumental selected file exists, per D-14.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- candidate-builder catalog-admission && pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
- `rg "missing-counterpart" apps/api/src/modules/catalog/admission-service.ts` returns a match.
- `rg "duration-delta-over-300ms" apps/api/src/modules/catalog/admission-service.ts` returns a match.
- `rg "duration-delta-over-1000ms" apps/api/src/modules/catalog/admission-service.ts` returns a match.
- `rg "upsertCandidateWithFiles" apps/api/src/modules/ingest/repositories/import-candidate-repository.ts` returns a match.
- `rg "instrumental.mp4" apps/api/src/modules/catalog/song-json.ts` returns a match.
- `pnpm -F @home-ktv/api test -- candidate-builder catalog-admission` exits 0.
  </acceptance_criteria>
  <done>Scanner output becomes grouped import candidates, and strict pair eligibility is represented by one tested admission evaluator used by later approval and maintenance flows.</done>
</task>

</tasks>

<verification>
Run `pnpm -F @home-ktv/api test -- library-ingest-schema import-scanner candidate-builder catalog-admission`, then `pnpm -F @home-ktv/api typecheck`.
</verification>

<success_criteria>
- `chokidar@5.0.0` is installed only for `@home-ktv/api`.
- Import tables exist separately from formal `songs` and `assets`.
- Watcher, scheduled, and manual triggers all call the same scanner path.
- Single-resource candidates cannot be verified for formal entry.
- `<=300ms` same-version pairs are the only `verified` admission outcome.
- `301ms-1000ms` pairs are `review_required`; `>1000ms` pairs are `rejected`.
</success_criteria>

<output>
After completion, create `.planning/phases/02-library-ingest-catalog-admin/02-01-SUMMARY.md`.
</output>
