---
phase: 02-library-ingest-catalog-admin
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - apps/api/src/server.ts
  - apps/api/src/routes/admin-catalog.ts
  - apps/api/src/modules/catalog/repositories/song-repository.ts
  - apps/api/src/modules/catalog/repositories/asset-repository.ts
  - apps/api/src/modules/catalog/catalog-maintenance-service.ts
  - apps/api/src/modules/catalog/admission-service.ts
  - apps/api/src/modules/catalog/song-json.ts
  - apps/api/src/modules/ingest/formal-library-validator.ts
  - apps/api/src/test/admin-catalog-routes.test.ts
  - apps/api/src/test/formal-library-validator.test.ts
  - apps/api/src/test/formal-admission-contract.test.ts
  - apps/admin/src/App.tsx
  - apps/admin/src/App.css
  - apps/admin/src/api/client.ts
  - apps/admin/src/songs/types.ts
  - apps/admin/src/songs/SongCatalog.tsx
  - apps/admin/src/songs/SongDetail.tsx
  - apps/admin/src/test/song-catalog.test.tsx
autonomous: true
requirements:
  - LIBR-04
  - LIBR-05
  - LIBR-06
  - ADMN-01
must_haves:
  truths:
    - "Admin can browse formal songs and inspect each resource's lyricMode, vocalMode, status, switchFamily, and switchQualityStatus."
    - "Admin can edit song metadata, default asset, and asset availability after import."
    - "Dangerous maintenance actions require confirmation before disabling ready assets or changing switch pairs."
    - "Formal /songs scan trusts song.json and performs lightweight consistency validation only."
    - "Formal catalog remains compatible with Phase 1 switch target construction: exactly one ready verified counterpart per switchFamily/vocalMode."
  artifacts:
    - path: "apps/api/src/routes/admin-catalog.ts"
      provides: "formal catalog list/detail/update/revalidate endpoints"
    - path: "apps/api/src/modules/catalog/catalog-maintenance-service.ts"
      provides: "safe metadata/default/status/switch-pair mutations"
    - path: "apps/api/src/modules/ingest/formal-library-validator.ts"
      provides: "song.json-based formal library consistency checks"
    - path: "apps/admin/src/songs/SongCatalog.tsx"
      provides: "formal song/resource maintenance UI"
  key_links:
    - from: "apps/api/src/modules/catalog/catalog-maintenance-service.ts"
      to: "apps/api/src/modules/catalog/admission-service.ts"
      via: "pair revalidation before dangerous asset updates"
      pattern: "evaluateSwitchPair"
    - from: "apps/api/src/modules/ingest/formal-library-validator.ts"
      to: "apps/api/src/modules/catalog/song-json.ts"
      via: "song.json parsing/validation"
      pattern: "song.json"
    - from: "apps/admin/src/songs/SongCatalog.tsx"
      to: "/admin/catalog/songs"
      via: "TanStack Query formal catalog list"
      pattern: "admin/catalog/songs"
---

<objective>
Complete Phase 2 by adding formal catalog maintenance, resource revalidation, and strict admission safeguards after import approval.

Purpose: Once songs are in `/songs`, administrators need a maintenance surface that cannot silently break Phase 1 switching guarantees.
Output: Formal catalog APIs, song/resource admin UI, song.json consistency validator, and regression tests proving strict switch eligibility remains true.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-library-ingest-catalog-admin/02-CONTEXT.md
@.planning/phases/02-library-ingest-catalog-admin/02-RESEARCH.md
@.planning/phases/02-library-ingest-catalog-admin/02-01-SUMMARY.md
@.planning/phases/02-library-ingest-catalog-admin/02-02-SUMMARY.md
@apps/api/src/modules/playback/build-switch-target.ts
@apps/api/src/test/build-switch-target.test.ts
@apps/api/src/routes/admin-imports.ts
@apps/admin/src/imports/ImportWorkbench.tsx

<interfaces>
Phase 1 switch contract to preserve:
```typescript
const verifiedCounterparts = counterparts.filter(
  (candidate) =>
    candidate.switchFamily === currentAsset.switchFamily &&
    candidate.vocalMode !== currentAsset.vocalMode &&
    candidate.status === "ready" &&
    candidate.switchQualityStatus === "verified"
);

if (verifiedCounterparts.length !== 1) {
  return null;
}
```

Phase 2 dangerous action rule:
```typescript
// Required request field for pair-affecting mutations:
confirmDangerous: true
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add formal catalog maintenance APIs with pair revalidation</name>
  <files>apps/api/src/server.ts, apps/api/src/routes/admin-catalog.ts, apps/api/src/modules/catalog/repositories/song-repository.ts, apps/api/src/modules/catalog/repositories/asset-repository.ts, apps/api/src/modules/catalog/catalog-maintenance-service.ts, apps/api/src/modules/catalog/admission-service.ts, apps/api/src/test/admin-catalog-routes.test.ts, apps/api/src/test/formal-admission-contract.test.ts</files>
  <read_first>
- Every existing path listed in this task's <files>; if a listed path is new, verify it is absent before creating it.
- apps/api/src/server.ts
- apps/api/src/routes/admin-imports.ts
- apps/api/src/modules/catalog/repositories/song-repository.ts
- apps/api/src/modules/catalog/repositories/asset-repository.ts
- apps/api/src/modules/catalog/admission-service.ts
- apps/api/src/modules/playback/build-switch-target.ts
- apps/api/src/test/build-switch-target.test.ts
- .planning/phases/02-library-ingest-catalog-admin/02-CONTEXT.md
- .planning/phases/02-library-ingest-catalog-admin/02-RESEARCH.md
  </read_first>
  <behavior>
- Test 1: `GET /admin/catalog/songs` returns songs with nested assets and includes `lyricMode`, `vocalMode`, `status`, `switchFamily`, and `switchQualityStatus`.
- Test 2: `PATCH /admin/catalog/songs/:songId` updates title, artistName, language, genre, tags, aliases, searchHints, and releaseYear.
- Test 3: `PATCH /admin/catalog/songs/:songId/default-asset` rejects an asset from another song and accepts the same-song instrumental asset.
- Test 4: disabling a `ready` asset in a verified pair returns 400 unless `{ "confirmDangerous": true }`.
- Test 5: changing `switchFamily` or `vocalMode` returns 400 unless `{ "confirmDangerous": true }` and re-runs `evaluateSwitchPair`.
- Test 6: after pair-breaking maintenance, `buildSwitchTarget` returns null instead of returning a stale switch target.
  </behavior>
  <action>
Create `apps/api/src/modules/catalog/catalog-maintenance-service.ts` with methods:
- `listSongs(input: { query?: string; status?: string; language?: string }): Promise<Array<{ song: Song; assets: Asset[] }>>`.
- `getSongDetail(songId: string): Promise<{ song: Song; assets: Asset[] } | null>`.
- `updateSongMetadata(input)` for `title`, `artistName`, `language`, `genre`, `tags`, `aliases`, `searchHints`, `releaseYear`.
- `setDefaultAsset(input: { songId: string; assetId: string }): Promise<Song>`.
- `updateAssetStatus(input: { assetId: string; status: AssetStatus; confirmDangerous?: boolean }): Promise<Asset>`.
- `updateAssetSwitchPair(input: { assetId: string; vocalMode: VocalMode; switchFamily: string | null; sameVersionConfirmed: boolean; confirmDangerous?: boolean }): Promise<Asset>`.

Rules:
- `setDefaultAsset` must verify the asset belongs to `songId`; otherwise return route error code `ASSET_NOT_IN_SONG`.
- `updateAssetStatus` must require `confirmDangerous === true` when changing a `ready` asset with `switchQualityStatus = 'verified'` to any non-ready status.
- `updateAssetSwitchPair` must require `confirmDangerous === true` for all changes to `vocalMode` or `switchFamily`.
- Pair-affecting mutations must re-run `evaluateSwitchPair`; if the pair no longer verifies, set affected assets' `switch_quality_status` to `review_required` and preserve `status` unless the requested status changed it.
- Do not allow direct setting of `switch_quality_status = 'verified'` from the UI or route body; verification only comes from `evaluateSwitchPair`.

Extend repositories with concrete methods:
- Song repository: `listForAdmin`, `findAdminDetailById`, `updateMetadata`, `setDefaultAsset`.
- Asset repository: `listBySongId`, `findBySongIdAndVocalMode`, `updateStatus`, `updateSwitchPair`, `markSwitchFamilyReviewRequired`.

Create `apps/api/src/routes/admin-catalog.ts` and register it in `createServer()`:
- `GET /admin/catalog/songs`
- `GET /admin/catalog/songs/:songId`
- `PATCH /admin/catalog/songs/:songId`
- `PATCH /admin/catalog/songs/:songId/default-asset`
- `PATCH /admin/catalog/assets/:assetId/status`
- `PATCH /admin/catalog/assets/:assetId/switch-pair`

Use route error codes exactly: `SONG_NOT_FOUND`, `ASSET_NOT_FOUND`, `ASSET_NOT_IN_SONG`, `CONFIRM_DANGEROUS_REQUIRED`, `PAIR_REVALIDATION_FAILED`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- admin-catalog-routes formal-admission-contract && pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
- `rg "registerAdminCatalogRoutes" apps/api/src/server.ts apps/api/src/routes/admin-catalog.ts` returns matches.
- `rg "CONFIRM_DANGEROUS_REQUIRED" apps/api/src/routes/admin-catalog.ts apps/api/src/modules/catalog/catalog-maintenance-service.ts` returns matches.
- `rg "markSwitchFamilyReviewRequired" apps/api/src/modules/catalog/repositories/asset-repository.ts apps/api/src/modules/catalog/catalog-maintenance-service.ts` returns matches.
- `rg "ASSET_NOT_IN_SONG" apps/api/src/routes/admin-catalog.ts apps/api/src/test/admin-catalog-routes.test.ts` returns matches.
- `pnpm -F @home-ktv/api test -- admin-catalog-routes formal-admission-contract` exits 0.
  </acceptance_criteria>
  <done>Formal catalog mutations are available through admin APIs, and every pair-breaking operation requires confirmation plus revalidation before persistence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add formal /songs song.json consistency validator</name>
  <files>apps/api/src/modules/ingest/formal-library-validator.ts, apps/api/src/modules/catalog/song-json.ts, apps/api/src/routes/admin-catalog.ts, apps/api/src/test/formal-library-validator.test.ts</files>
  <read_first>
- Every existing path listed in this task's <files>; if a listed path is new, verify it is absent before creating it.
- apps/api/src/modules/ingest/import-scanner.ts
- apps/api/src/modules/ingest/library-paths.ts
- apps/api/src/modules/ingest/media-probe.ts
- apps/api/src/modules/catalog/song-json.ts
- apps/api/src/routes/admin-catalog.ts
- docs/KTV-ARCHITECTURE.md
- .planning/phases/02-library-ingest-catalog-admin/02-CONTEXT.md
- .planning/phases/02-library-ingest-catalog-admin/02-RESEARCH.md
  </read_first>
  <behavior>
- Test 1: validator reads `/songs/<language>/<artist>/<title>/song.json` and does not infer business metadata from file names when song.json exists.
- Test 2: missing `song.json` returns a finding with code `SONG_JSON_MISSING` and severity `review_required`.
- Test 3: missing referenced asset file returns code `ASSET_FILE_MISSING`.
- Test 4: a single-resource song.json returns code `MISSING_SWITCH_COUNTERPART` and cannot be marked formal ready.
- Test 5: an original/instrumental pair with duration delta `>300ms` returns `DURATION_DELTA_OVER_300MS`.
  </behavior>
  <action>
Create `apps/api/src/modules/ingest/formal-library-validator.ts` with:
- `export interface FormalLibraryFinding { code: "SONG_JSON_MISSING" | "ASSET_FILE_MISSING" | "MISSING_SWITCH_COUNTERPART" | "DURATION_DELTA_OVER_300MS" | "SONG_JSON_DB_MISMATCH"; severity: "info" | "review_required" | "rejected"; relativePath: string; message: string; }`
- `validateFormalSongDirectory(input: { songDirectory: string; mediaRoot: string; probeMedia?: typeof probeMedia }): Promise<FormalLibraryFinding[]>`.
- `validateFormalLibrary(input: { songsRoot: string; mediaRoot: string }): Promise<FormalLibraryFinding[]>`.

Rules:
- Treat `song.json` as the business truth per D-03. Do not parse artist/title/language from directory names when `song.json` exists.
- Validate file existence for every `assets[].file` entry.
- Only call `probeMedia` when an asset file identity is new or changed; reuse existing import file cache when available.
- Require at least one `original` and one `instrumental` asset in the same `switchFamily`.
- Use `evaluateSwitchPair` and report `DURATION_DELTA_OVER_300MS` for any pair with delta `>300`; do not produce a manual override path.
- Add `POST /admin/catalog/revalidate` in `admin-catalog.ts` accepting `{ scope: "songs" }` and returning `{ findings }`.

Update `song-json.ts` with `parseSongJson(value: unknown)` that accepts keys produced by `buildSongJson`: `title`, `artist`, `language`, `genre`, `tags`, `releaseYear`, `aliases`, `searchHints`, `defaultAsset`, and `assets`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- formal-library-validator && pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
- `rg "SONG_JSON_MISSING" apps/api/src/modules/ingest/formal-library-validator.ts apps/api/src/test/formal-library-validator.test.ts` returns matches.
- `rg "MISSING_SWITCH_COUNTERPART" apps/api/src/modules/ingest/formal-library-validator.ts apps/api/src/test/formal-library-validator.test.ts` returns matches.
- `rg "DURATION_DELTA_OVER_300MS" apps/api/src/modules/ingest/formal-library-validator.ts apps/api/src/test/formal-library-validator.test.ts` returns matches.
- `rg "POST /admin/catalog/revalidate|/admin/catalog/revalidate" apps/api/src/routes/admin-catalog.ts` returns a match.
- `pnpm -F @home-ktv/api test -- formal-library-validator` exits 0.
  </acceptance_criteria>
  <done>Formal library rescans are lightweight song.json consistency checks, and they surface strict admission violations without silently changing catalog truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add song/resource maintenance UI</name>
  <files>apps/admin/src/App.tsx, apps/admin/src/App.css, apps/admin/src/api/client.ts, apps/admin/src/songs/types.ts, apps/admin/src/songs/SongCatalog.tsx, apps/admin/src/songs/SongDetail.tsx, apps/admin/src/test/song-catalog.test.tsx</files>
  <read_first>
- Every existing path listed in this task's <files>; if a listed path is new, verify it is absent before creating it.
- apps/admin/src/App.tsx
- apps/admin/src/App.css
- apps/admin/src/api/client.ts
- apps/admin/src/imports/ImportWorkbench.tsx
- apps/admin/src/imports/ConfirmActionDialog.tsx
- apps/api/src/routes/admin-catalog.ts
- .planning/phases/02-library-ingest-catalog-admin/02-CONTEXT.md
- .planning/phases/02-library-ingest-catalog-admin/02-RESEARCH.md
  </read_first>
  <behavior>
- Test 1: clicking `Songs` calls `/admin/catalog/songs` and renders song rows.
- Test 2: a song detail shows asset fields `lyricMode`, `vocalMode`, `status`, `switchFamily`, and `switchQualityStatus`.
- Test 3: disabling a ready verified resource opens a confirmation dialog containing `This may break vocal switching`.
- Test 4: changing a resource switchFamily or vocalMode sends `confirmDangerous: true` only after confirmation.
- Test 5: clicking `Revalidate /songs` calls `/admin/catalog/revalidate`.
  </behavior>
  <action>
Extend the admin app from Plan 02-02:
- Enable the `Songs` nav item in `App.tsx`. Keep `Imports` as the default homepage per D-16.
- Add client functions `listSongs`, `getSongDetail`, `updateSongMetadata`, `setDefaultAsset`, `updateAssetStatus`, `updateAssetSwitchPair`, and `revalidateSongs`.
- Create `apps/admin/src/songs/types.ts` mirroring the admin catalog response shape: `{ song, assets }`.

Create `SongCatalog.tsx`:
- Dense table columns: title, artistName, language, status, defaultAssetId, asset count, switch readiness (`ready verified pair` or `needs review`).
- Filters: `language`, `status`, and text `query`.
- Toolbar button text exactly `Revalidate /songs`.

Create `SongDetail.tsx`:
- Metadata form fields: `title`, `artistName`, `language`, `genre`, `tags`, `aliases`, `searchHints`, `releaseYear`.
- Asset table fields: `displayName`, `filePath`, `durationMs`, `lyricMode`, `vocalMode`, `status`, `switchFamily`, `switchQualityStatus`, and default marker.
- Default asset control must allow selecting an instrumental asset and saving through `/admin/catalog/songs/:songId/default-asset`.
- Status changes from `ready` to `unavailable`, `failed`, or `stale` require `ConfirmActionDialog` with exact message `This may break vocal switching`.
- Switch pair edits for `vocalMode` or `switchFamily` require the same confirmation and send `{ confirmDangerous: true }`.

Style constraints:
- Keep admin UI dense and operational with tables/forms.
- Do not add login, role management, hero sections, marketing copy, gradient backgrounds, or decorative cards.
- Use status chips with exact text values from the API: `ready`, `review_required`, `unavailable`, `verified`, `review_required`, `rejected`, `unknown`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/admin test -- song-catalog && pnpm -F @home-ktv/admin build</automated>
  </verify>
  <acceptance_criteria>
- `rg "Revalidate /songs" apps/admin/src/songs/SongCatalog.tsx apps/admin/src/test/song-catalog.test.tsx` returns matches.
- `rg "This may break vocal switching" apps/admin/src/songs/SongDetail.tsx apps/admin/src/test/song-catalog.test.tsx` returns matches.
- `rg "switchQualityStatus" apps/admin/src/songs/SongDetail.tsx apps/admin/src/songs/types.ts` returns matches.
- `rg "confirmDangerous: true" apps/admin/src/songs/SongDetail.tsx` returns a match.
- `pnpm -F @home-ktv/admin build` exits 0.
  </acceptance_criteria>
  <done>Admin can maintain formal songs/resources, inspect switch-critical fields, and must confirm all actions that can break ready verified switching.</done>
</task>

</tasks>

<verification>
Run `pnpm -F @home-ktv/api test -- admin-catalog-routes formal-library-validator formal-admission-contract`, `pnpm -F @home-ktv/admin test -- song-catalog`, `pnpm -F @home-ktv/admin build`, and `pnpm typecheck`.
</verification>

<success_criteria>
- Formal catalog browse/detail APIs and UI exist.
- Song metadata, default asset, and asset status are maintainable.
- Dangerous asset status and switch pair edits require confirmation.
- `/songs` scan is song.json-first and lightweight.
- Single-resource formal entries and `>300ms` pairs cannot become verified formal switch pairs.
- Existing Phase 1 `buildSwitchTarget` tests still pass.
</success_criteria>

<output>
After completion, create `.planning/phases/02-library-ingest-catalog-admin/02-03-SUMMARY.md`.
</output>
