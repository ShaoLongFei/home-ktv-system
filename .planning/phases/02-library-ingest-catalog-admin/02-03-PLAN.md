---
phase: 02-library-ingest-catalog-admin
plan: 03
type: execute
wave: 3
depends_on:
  - "02-02"
files_modified:
  - apps/api/src/server.ts
  - apps/api/src/routes/admin-imports.ts
  - apps/api/src/modules/catalog/admission-service.ts
  - apps/api/src/modules/catalog/song-json.ts
  - apps/api/src/modules/ingest/repositories/import-candidate-repository.ts
  - apps/api/src/modules/ingest/repositories/import-file-repository.ts
  - apps/api/src/test/admin-imports-routes.test.ts
  - apps/api/src/test/catalog-admission.test.ts
autonomous: true
requirements:
  - LIBR-02
  - LIBR-04
  - LIBR-06
must_haves:
  truths:
    - "Admin import review routes return candidates with joined ImportCandidateFileDetail records."
    - "Hold physically moves pending files into imports/needs-review and updates root_kind to imports_needs_review."
    - "Reject-delete requires explicit confirmation and deletes candidate files instead of moving them to imports/rejected."
    - "Approve is idempotent and can repair or report non-atomic file/song.json/DB promotion failures."
  artifacts:
    - path: "apps/api/src/routes/admin-imports.ts"
      provides: "manual scan, candidate detail, metadata save, hold, reject-delete, and approve endpoints"
    - path: "apps/api/src/modules/catalog/admission-service.ts"
      provides: "strict import candidate promotion and failure repair"
    - path: "apps/api/src/modules/catalog/song-json.ts"
      provides: "formal song.json writer used by approval"
  key_links:
    - from: "apps/api/src/routes/admin-imports.ts"
      to: "apps/api/src/modules/ingest/repositories/import-candidate-repository.ts"
      via: "ImportCandidateFileDetail list/detail response"
      pattern: "listCandidateFileDetails"
    - from: "apps/api/src/modules/catalog/admission-service.ts"
      to: "apps/api/src/modules/ingest/repositories/import-file-repository.ts"
      via: "hold move updates root_kind after safe physical move"
      pattern: "imports_needs_review"
    - from: "apps/api/src/modules/catalog/admission-service.ts"
      to: "apps/api/src/modules/catalog/song-json.ts"
      via: "approval writes formal song.json before or during DB promotion with repair state"
      pattern: "approval_failed"
---

<objective>
Build the import review/admission API that turns grouped candidates into explicit admin decisions while preserving the import/formal catalog boundary.

Purpose: Phase 2 needs a reliable API before the admin UI exists. Hold, reject-delete, and approve are dangerous enough that the route/service contract must encode safe path resolution, physical file moves, strict admission, and recovery from non-atomic file/database failures.
Output: Admin import Fastify routes, admission service, song.json writer, repository mutations, and tests for detail contracts, hold movement, reject confirmation, strict approve, and repair behavior.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-library-ingest-catalog-admin/02-CONTEXT.md
@.planning/phases/02-library-ingest-catalog-admin/02-RESEARCH.md
@.planning/phases/02-library-ingest-catalog-admin/02-01-SUMMARY.md
@.planning/phases/02-library-ingest-catalog-admin/02-02-SUMMARY.md
@apps/api/src/modules/ingest/repositories/import-candidate-repository.ts
@apps/api/src/modules/ingest/library-paths.ts
@apps/api/src/modules/catalog/repositories/song-repository.ts
@apps/api/src/modules/catalog/repositories/asset-repository.ts

<interfaces>
Required detail contract from Plan 02-01:
```typescript
export interface ImportCandidateFileDetail {
  candidateFileId: string;
  importFileId: string;
  rootKind: "imports_pending" | "imports_needs_review" | "songs";
  relativePath: string;
  probeStatus: ImportFileProbeStatus;
  durationMs: number | null;
  selected: boolean;
  proposedVocalMode: VocalMode;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add import review routes using joined details</name>
  <files>apps/api/src/server.ts, apps/api/src/routes/admin-imports.ts, apps/api/src/modules/ingest/repositories/import-candidate-repository.ts, apps/api/src/test/admin-imports-routes.test.ts</files>
  <read_first>
- apps/api/src/server.ts
- apps/api/src/modules/ingest/repositories/import-candidate-repository.ts
- apps/api/src/modules/ingest/scan-scheduler.ts
- apps/api/src/modules/ingest/library-paths.ts
- .planning/phases/02-library-ingest-catalog-admin/02-CONTEXT.md
  </read_first>
  <behavior>
- Test 1: `GET /admin/import-candidates` returns grouped candidates and each detail includes `rootKind`, `relativePath`, `probeStatus`, and `durationMs`.
- Test 2: `POST /admin/import-candidates/:id/metadata` saves D-07 metadata fields without changing file locations.
- Test 3: `POST /admin/imports/scan` calls scheduler `enqueueManualScan(scope)` and returns an accepted scan response.
  </behavior>
  <action>
Extend `ImportCandidateRepository` with read/update methods needed by the import review API:
- `listCandidates(filters)` returning grouped candidate summaries.
- `getCandidateWithFiles(candidateId)` returning candidate metadata plus `ImportCandidateFileDetail[]`.
- `updateCandidateMetadata(candidateId, metadata)` for D-07 editable fields.

Create `apps/api/src/routes/admin-imports.ts` and register it from `server.ts`. Routes must be under `/admin/imports` or `/admin/import-candidates` and require no auth per D-18. Implement:
- `GET /admin/import-candidates` with status filters for `pending`, `held`, `review_required`, and `conflict`.
- `GET /admin/import-candidates/:candidateId` returning the joined details from `ImportCandidateFileDetail`, not candidate-file-only fields.
- `PATCH /admin/import-candidates/:candidateId` or `POST /metadata` for title, artist, language, vocal roles, default vocal mode, same-version proof, genre, tags, year, aliases, and searchHints per D-07.
- `POST /admin/imports/scan` that calls scheduler `enqueueManualScan(scope)`.

Use zod or explicit validation for request bodies. Keep all absolute filesystem paths out of responses; route responses should contain logical root kind plus relative path only.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- admin-imports-routes && pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
- `rg "registerAdminImportRoutes" apps/api/src/routes/admin-imports.ts apps/api/src/server.ts` returns matches.
- `rg "listCandidateFileDetails|getCandidateWithFiles" apps/api/src/routes/admin-imports.ts` returns a match.
- `rg "rootKind" apps/api/src/routes/admin-imports.ts` returns a match.
- `rg "enqueueManualScan" apps/api/src/routes/admin-imports.ts` returns a match.
- `pnpm -F @home-ktv/api test -- admin-imports-routes` exits 0.
  </acceptance_criteria>
  <done>Import review APIs expose the joined file/probe detail contract required by approval, deletion, and the imports-first admin UI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement hold, reject-delete, and idempotent approval</name>
  <files>apps/api/src/routes/admin-imports.ts, apps/api/src/modules/catalog/admission-service.ts, apps/api/src/modules/catalog/song-json.ts, apps/api/src/modules/ingest/repositories/import-candidate-repository.ts, apps/api/src/modules/ingest/repositories/import-file-repository.ts, apps/api/src/test/catalog-admission.test.ts, apps/api/src/test/admin-imports-routes.test.ts</files>
  <read_first>
- apps/api/src/routes/admin-imports.ts
- apps/api/src/modules/ingest/library-paths.ts
- apps/api/src/modules/ingest/repositories/import-candidate-repository.ts
- apps/api/src/modules/ingest/repositories/import-file-repository.ts
- apps/api/src/modules/catalog/repositories/song-repository.ts
- apps/api/src/modules/catalog/repositories/asset-repository.ts
- .planning/phases/02-library-ingest-catalog-admin/02-CONTEXT.md
- .planning/phases/02-library-ingest-catalog-admin/02-RESEARCH.md
  </read_first>
  <behavior>
- Test 1: hold moves files from `imports/pending` to `imports/needs-review`, updates `import_files.root_kind = 'imports_needs_review'`, and sets candidate status `held` per D-08.
- Test 2: reject-delete fails unless the request includes explicit confirmation, then deletes candidate files and marks the candidate `rejected_deleted` per D-09.
- Test 3: approve rejects single-resource candidates, marks unproven or `>300ms` pairs `review_required`, and promotes only same-version original/instrumental pairs with duration delta `<=300ms`.
- Test 4: rerunning approve after partial file move/song.json/DB failure is idempotent and either repairs the formal state or leaves candidate status `approval_failed` with a repairable error.
  </behavior>
  <action>
Create `CatalogAdmissionService` in `apps/api/src/modules/catalog/admission-service.ts`. It must own the formal promotion workflow and expose:
- `evaluatePair(input)` returning `verified`, `review_required`, or `rejected` with a reason. A duration delta `>300ms` must never become `verified`, even with manual confirmation, per D-12.
- `holdCandidate(candidateId)` that resolves each `ImportCandidateFileDetail` under the configured media root, physically moves files from `imports/pending` into `imports/needs-review`, updates each `import_files.root_kind` to `imports_needs_review`, updates relative paths if the move changes them, and sets candidate status `held`.
- `rejectDeleteCandidate(candidateId, { confirmDelete: true })` that deletes physical files and marks rows deleted/rejected. Without confirmation, return a 400 route error.
- `approveCandidate(candidateId)` that verifies one original plus one instrumental, same-version confirmed, probeable durations, and duration delta `<=300ms`; then moves files to `/songs/<language>/<artist>/<title>/`, writes `song.json`, inserts/updates `songs` and `assets`, creates `source_records`, sets instrumental as default asset per D-14, and marks candidate approved.

Implement idempotent promotion and repair handling:
- Use deterministic target paths and switch family ids so reruns can identify already-moved files and already-created DB rows.
- If files moved but DB write fails, rerun must detect files in `/songs` and complete DB/song.json repair instead of failing because source files are gone.
- If DB rows exist but `song.json` is missing or invalid, rerun must rewrite `song.json`.
- On unrecoverable mismatch, set candidate status `approval_failed` and store a repair reason in `review_notes` or `candidate_meta`.

Create `song-json.ts` to write the formal `/songs` `song.json` with metadata, assets, default asset, vocal modes, durations, and switch family. Use atomic temp-file write plus rename inside the target directory.

Wire routes in `admin-imports.ts`:
- `POST /admin/import-candidates/:candidateId/hold`
- `POST /admin/import-candidates/:candidateId/reject-delete`
- `POST /admin/import-candidates/:candidateId/approve`
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- catalog-admission admin-imports-routes && pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
- `rg "imports_needs_review" apps/api/src/modules/catalog/admission-service.ts` returns a match.
- `rg "rename|copyFile" apps/api/src/modules/catalog/admission-service.ts` returns a match.
- `rg "approval_failed" apps/api/src/modules/catalog/admission-service.ts apps/api/src/db/migrations/0002_library_ingest_admin.sql` returns a match.
- `rg "duration-delta-over-300ms|> 300" apps/api/src/modules/catalog/admission-service.ts` returns a match.
- `rg "confirmDelete" apps/api/src/routes/admin-imports.ts` returns a match.
- `pnpm -F @home-ktv/api test -- catalog-admission` exits 0.
  </acceptance_criteria>
  <done>Import admission actions enforce D-08, D-09, D-10 through D-14, and approval can recover from non-atomic filesystem/database failures.</done>
</task>

</tasks>

<verification>
Run `pnpm -F @home-ktv/api test -- admin-imports-routes catalog-admission` and `pnpm -F @home-ktv/api typecheck`.
</verification>

<success_criteria>
- Import review API uses `ImportCandidateFileDetail` instead of candidate-file-only fields.
- Hold physically moves files into `imports/needs-review` and updates `root_kind`.
- Reject-delete is confirmed and destructive by explicit request only.
- Approval is strict, idempotent, and repair-aware.
- This plan modifies fewer than 15 files.
</success_criteria>

<output>
After completion, create `.planning/phases/02-library-ingest-catalog-admin/02-03-SUMMARY.md`.
</output>
