---
phase: 02-library-ingest-catalog-admin
plan: 05
type: execute
wave: 4
depends_on:
  - "02-03"
files_modified:
  - apps/api/src/server.ts
  - apps/api/src/routes/admin-catalog.ts
  - apps/api/src/modules/catalog/repositories/song-repository.ts
  - apps/api/src/modules/catalog/repositories/asset-repository.ts
  - apps/api/src/modules/catalog/admission-service.ts
  - apps/api/src/modules/catalog/song-json.ts
  - apps/api/src/modules/catalog/song-json-consistency-validator.ts
  - apps/api/src/test/admin-catalog-routes.test.ts
  - apps/api/src/test/song-json-consistency-validator.test.ts
autonomous: true
requirements:
  - LIBR-04
  - LIBR-05
  - LIBR-06
  - ADMN-01
must_haves:
  truths:
    - "Admin can browse formal songs and assets with song status, asset status, vocal mode, lyric mode, and switch quality."
    - "Admin can edit formal song metadata, default resource, and resource availability through revalidation-aware APIs."
    - "Formal /songs directories are validated against song.json and database rows."
    - "A pair with duration delta greater than 300ms cannot be manually overridden to verified."
  artifacts:
    - path: "apps/api/src/routes/admin-catalog.ts"
      provides: "formal catalog browse/edit/revalidate API"
    - path: "apps/api/src/modules/catalog/song-json-consistency-validator.ts"
      provides: "/songs song.json consistency checks"
    - path: "apps/api/src/modules/catalog/admission-service.ts"
      provides: "strict revalidation for catalog maintenance mutations"
  key_links:
    - from: "apps/api/src/routes/admin-catalog.ts"
      to: "apps/api/src/modules/catalog/admission-service.ts"
      via: "resource edits call revalidate before saving verified/ready pair state"
      pattern: "revalidate"
    - from: "apps/api/src/modules/catalog/song-json-consistency-validator.ts"
      to: "apps/api/src/modules/catalog/song-json.ts"
      via: "read/write shared song.json shape"
      pattern: "validateSongJsonConsistency"
    - from: "apps/api/src/modules/catalog/admission-service.ts"
      to: "apps/api/src/modules/catalog/repositories/asset-repository.ts"
      via: "duration delta over 300ms remains review_required/rejected"
      pattern: "duration-delta-over-300ms"
---

<objective>
Add formal catalog maintenance APIs and a `/songs` `song.json` consistency validator.

Purpose: Approved songs become maintainable formal catalog records, but maintenance must not bypass the same strict switch admission rules used during import approval.
Output: Admin catalog Fastify routes, repository write/list methods, strict pair revalidation, and validator tests for formal directory consistency.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/02-library-ingest-catalog-admin/02-CONTEXT.md
@.planning/phases/02-library-ingest-catalog-admin/02-RESEARCH.md
@.planning/phases/02-library-ingest-catalog-admin/02-01-SUMMARY.md
@.planning/phases/02-library-ingest-catalog-admin/02-03-SUMMARY.md
@apps/api/src/modules/catalog/repositories/song-repository.ts
@apps/api/src/modules/catalog/repositories/asset-repository.ts
@apps/api/src/modules/catalog/admission-service.ts
@apps/api/src/modules/catalog/song-json.ts

<interfaces>
Formal catalog contracts from Phase 1 and Plan 02-01:
```typescript
export type SongStatus = "ready" | "review_required" | "unavailable";
export type AssetStatus = "ready" | "caching" | "failed" | "unavailable" | "stale" | "promoted";
export type SwitchQualityStatus = "verified" | "review_required" | "rejected" | "unknown";
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add formal catalog browse and edit API</name>
  <files>apps/api/src/server.ts, apps/api/src/routes/admin-catalog.ts, apps/api/src/modules/catalog/repositories/song-repository.ts, apps/api/src/modules/catalog/repositories/asset-repository.ts, apps/api/src/modules/catalog/admission-service.ts, apps/api/src/test/admin-catalog-routes.test.ts</files>
  <read_first>
- apps/api/src/server.ts
- apps/api/src/modules/catalog/repositories/song-repository.ts
- apps/api/src/modules/catalog/repositories/asset-repository.ts
- apps/api/src/modules/catalog/admission-service.ts
- packages/domain/src/index.ts
- .planning/phases/02-library-ingest-catalog-admin/02-CONTEXT.md
  </read_first>
  <behavior>
- Test 1: `GET /admin/catalog/songs` returns formal songs with `status`, default asset, and asset summaries.
- Test 2: metadata edits update song fields but preserve strict status and switch eligibility.
- Test 3: changing default asset, asset status, vocal mode, or switch family calls strict revalidation and records `review_required` instead of verified when rules fail.
- Test 4: attempting to override a duration delta `>300ms` to verified returns 400 or a non-verified result.
  </behavior>
  <action>
Extend `SongRepository` and `AssetRepository` with formal admin methods:
- List songs with asset summaries and filters by `status`, language, and text query.
- Get one song with all assets.
- Update song metadata fields from D-17.
- Update default asset.
- Update asset status, vocal mode, lyric mode, switch family, and switch quality through a service, not raw route SQL.

Create `apps/api/src/routes/admin-catalog.ts` and register it from `server.ts`. Routes should include:
- `GET /admin/catalog/songs`
- `GET /admin/catalog/songs/:songId`
- `PATCH /admin/catalog/songs/:songId`
- `PATCH /admin/catalog/songs/:songId/default-asset`
- `PATCH /admin/catalog/assets/:assetId`
- `POST /admin/catalog/songs/:songId/revalidate`

Extend `CatalogAdmissionService` with `revalidateFormalSong(songId)` and `updateFormalAssetWithRevalidation(input)`. The service must re-evaluate original/instrumental pair eligibility after every dangerous mutation from D-19. It must never allow manual override to `verified` when duration delta is `>300ms`; set song or asset status to `review_required`/non-ready instead.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- admin-catalog-routes && pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
- `rg "registerAdminCatalogRoutes" apps/api/src/routes/admin-catalog.ts apps/api/src/server.ts` returns matches.
- `rg "revalidateFormalSong" apps/api/src/modules/catalog/admission-service.ts apps/api/src/routes/admin-catalog.ts` returns matches.
- `rg "duration-delta-over-300ms|> 300" apps/api/src/modules/catalog/admission-service.ts` returns a match.
- `rg "status" apps/api/src/routes/admin-catalog.ts` returns a match.
- `pnpm -F @home-ktv/api test -- admin-catalog-routes` exits 0.
  </acceptance_criteria>
  <done>Formal catalog API supports browse/edit workflows while routing switch-sensitive mutations through strict revalidation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add /songs song.json consistency validator</name>
  <files>apps/api/src/modules/catalog/song-json.ts, apps/api/src/modules/catalog/song-json-consistency-validator.ts, apps/api/src/routes/admin-catalog.ts, apps/api/src/test/song-json-consistency-validator.test.ts</files>
  <read_first>
- apps/api/src/modules/catalog/song-json.ts
- apps/api/src/modules/catalog/admission-service.ts
- apps/api/src/modules/ingest/library-paths.ts
- docs/KTV-ARCHITECTURE.md
- .planning/phases/02-library-ingest-catalog-admin/02-CONTEXT.md
  </read_first>
  <behavior>
- Test 1: validator reports missing `song.json`, missing referenced media files, malformed metadata, and DB/song.json status mismatch.
- Test 2: validator reports original/instrumental duration delta `>300ms` as not verified with no manual override path.
- Test 3: admin route exposes validation results for one song and for the songs root scan.
  </behavior>
  <action>
Define the formal song.json shape in `song-json.ts` if not already complete from Plan 02-03. Include title, artist, language, status, default asset id/path, assets with vocalMode/lyricMode/status/switchFamily/durationMs, genre, tags, aliases, searchHints, and source metadata.

Create `song-json-consistency-validator.ts` with:
- `validateSongJsonConsistency({ songsRoot, song, assets })`.
- Safe path resolution under `/songs`; no absolute path from JSON may escape the songs root.
- Checks for required fields, referenced file existence, DB row consistency, missing/default asset mismatch, status mismatch, and pair eligibility.
- Strict revalidation using `CatalogAdmissionService.evaluatePair` rules. Duration delta `>300ms` must return a failure/review result with no `manualOverride` option.

Expose validation from `admin-catalog.ts`:
- `GET /admin/catalog/songs/:songId/validate`
- `POST /admin/catalog/validate-songs-root`

Tests should use temporary directories and in-memory song/asset fixtures, not a live database.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- song-json-consistency-validator admin-catalog-routes && pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
- `rg "validateSongJsonConsistency" apps/api/src/modules/catalog/song-json-consistency-validator.ts apps/api/src/routes/admin-catalog.ts` returns matches.
- `rg "manualOverride" apps/api/src/modules/catalog/song-json-consistency-validator.ts apps/api/src/modules/catalog/admission-service.ts` returns no matches.
- `rg "duration-delta-over-300ms|> 300" apps/api/src/modules/catalog/song-json-consistency-validator.ts apps/api/src/test/song-json-consistency-validator.test.ts` returns matches.
- `pnpm -F @home-ktv/api test -- song-json-consistency-validator` exits 0.
  </acceptance_criteria>
  <done>Formal `/songs` consistency validation is available and cannot bypass the strict `<=300ms` admission rule.</done>
</task>

</tasks>

<verification>
Run `pnpm -F @home-ktv/api test -- admin-catalog-routes song-json-consistency-validator` and `pnpm -F @home-ktv/api typecheck`.
</verification>

<success_criteria>
- Formal catalog admin API covers song/resource browsing and maintenance.
- Mutations that affect switching revalidate pair eligibility.
- `/songs` `song.json` consistency validator exists and is exposed through admin routes.
- There is no manual override path for duration delta `>300ms`.
- This plan modifies fewer than 15 files.
</success_criteria>

<output>
After completion, create `.planning/phases/02-library-ingest-catalog-admin/02-05-SUMMARY.md`.
</output>
