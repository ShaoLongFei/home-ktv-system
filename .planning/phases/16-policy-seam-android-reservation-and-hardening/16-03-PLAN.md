---
phase: 16-policy-seam-android-reservation-and-hardening
plan: 03
type: execute
wave: 2
depends_on: []
files_modified:
  - scripts/real-mv-playback-risk-spike.mjs
  - scripts/real-mv-playback-risk-spike.test.mjs
autonomous: true
requirements: [HARD-02]
must_haves:
  truths:
    - "The hardening report can be driven by the user's local `MEDIA_ROOT` and the two representative sample files."
    - "The script stays portable when no database/index context is available."
    - "Existing controlled spike behavior remains intact."
  artifacts:
    - path: "scripts/real-mv-playback-risk-spike.mjs"
      provides: "local hardening report CLI"
      contains: "index cross-check skipped"
    - path: "scripts/real-mv-playback-risk-spike.test.mjs"
      provides: "portable CLI/report regression"
      contains: "songs-sample/关喆-想你的夜(MTV)-国语-流行.mkv"
  key_links:
    - from: "local sample mode"
      to: "MEDIA_ROOT and sample files"
      via: "default sample path resolution"
      pattern: "media-root"
    - from: "report generation"
      to: "optional index cross-check"
      via: "DATABASE_URL"
      pattern: "index cross-check skipped"
---

<objective>
Turn the existing real-MV spike script into a Phase 16 local hardening report.

Purpose: The phase needs meaningful real-world evidence from the user’s own media library and index, but the repo must remain portable. This plan keeps the controlled spike mode and adds local sample / optional index cross-check support.
Output: one CLI can produce a markdown report from the representative local MKV/MPG samples and, when available, compare them with the existing index.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/16-policy-seam-android-reservation-and-hardening/16-CONTEXT.md
@.planning/phases/16-policy-seam-android-reservation-and-hardening/16-RESEARCH.md
@scripts/real-mv-playback-risk-spike.mjs
@scripts/real-mv-playback-risk-spike.test.mjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend the real-MV spike script with local sample and index cross-check modes</name>
  <files>scripts/real-mv-playback-risk-spike.mjs, scripts/real-mv-playback-risk-spike.test.mjs</files>
  <read_first>
    - scripts/real-mv-playback-risk-spike.mjs
    - scripts/real-mv-playback-risk-spike.test.mjs
    - .planning/phases/16-policy-seam-android-reservation-and-hardening/16-CONTEXT.md
    - .planning/phases/16-policy-seam-android-reservation-and-hardening/16-RESEARCH.md
  </read_first>
  <behavior>
    - Controlled fixture mode still works exactly as before.
    - Local sample mode accepts `MEDIA_ROOT` / `--media-root` plus explicit sample paths.
    - Optional index cross-check is informational and never blocks report generation.
  </behavior>
  <action>
    Update `scripts/real-mv-playback-risk-spike.mjs` so the CLI accepts:
    - `--media-root <path>`
    - `--sample-mkv <path>`
    - `--sample-mpg <path>`
    - `--database-url <postgres-url>`

    Resolve sample defaults in this order:
    1. explicit `--sample-mkv` / `--sample-mpg`;
    2. `MEDIA_ROOT` or `--media-root`, with default filenames:
       - `songs-sample/关喆-想你的夜(MTV)-国语-流行.mkv`
       - `songs-sample/蔡依林-BECAUSE OF YOU(演唱会)-国语-流行.mpg`
    3. the existing phase 12 controlled fixtures.

    Keep the existing browser capability probe, `--controlled-only`, and report layout, but add a local-hardening section that includes:
    - the resolved sample paths;
    - the sample filenames;
    - a line saying `index cross-check skipped` when no database URL is provided;
    - an optional index cross-check summary when `--database-url` is provided.

    If `--database-url` is supplied, query the app database for a short index summary from the existing catalog tables (`songs`, `assets`, `source_records`) and include a markdown `Index cross-check` section. The report should stay markdown-only and should not depend on a successful DB write.

    Update `scripts/real-mv-playback-risk-spike.test.mjs` so it verifies:
    - `--help` documents the new flags;
    - controlled-only mode still writes a report with the existing `canPlayType`, `hasAudioTracksApi`, and `compatibilityStatus` evidence;
    - local-sample mode with empty temp files and explicit sample paths still writes a report mentioning the two representative sample filenames and `index cross-check skipped`.
  </action>
  <acceptance_criteria>
    - `rg -n -- "--media-root|--sample-mkv|--sample-mpg|--database-url|index cross-check skipped" scripts/real-mv-playback-risk-spike.mjs scripts/real-mv-playback-risk-spike.test.mjs` exits 0.
    - `node scripts/real-mv-playback-risk-spike.mjs --help` shows the new local-sample flags.
    - `pnpm real-mv:risk-spike -- --controlled-only --output /tmp/real-mv-phase16-controlled.md` exits 0.
    - `node --test scripts/real-mv-playback-risk-spike.test.mjs` exits 0 and covers local-sample mode with temp sample files.
  </acceptance_criteria>
  <verify>
    <automated>pnpm real-mv:risk-spike -- --controlled-only --output /tmp/real-mv-phase16-controlled.md</automated>
    <automated>node --test scripts/real-mv-playback-risk-spike.test.mjs</automated>
  </verify>
  <done>The existing spike script can now produce a portable Phase 16 hardening report from local samples and optional index data.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Make the report point at the user's real sample library by default</name>
  <files>scripts/real-mv-playback-risk-spike.mjs, scripts/real-mv-playback-risk-spike.test.mjs</files>
  <read_first>
    - scripts/real-mv-playback-risk-spike.mjs
    - scripts/real-mv-playback-risk-spike.test.mjs
    - .planning/phases/16-policy-seam-android-reservation-and-hardening/16-CONTEXT.md
  </read_first>
  <behavior>
    - If `MEDIA_ROOT` is defined and explicit sample paths are omitted, the script should discover the two representative files under `songs-sample/`.
    - The report should show the real relative filenames from the sample library, not only temp-path placeholders.
  </behavior>
  <action>
    Add a helper that resolves the default sample paths from `MEDIA_ROOT` or `--media-root`:
    ```ts
    songs-sample/关喆-想你的夜(MTV)-国语-流行.mkv
    songs-sample/蔡依林-BECAUSE OF YOU(演唱会)-国语-流行.mpg
    ```

    When both files exist, use them in local-sample mode and write the report path chosen by the caller. When either file is missing, print a clear error that names the missing default file and exit non-zero.

    Extend the test file with one case that sets `MEDIA_ROOT` to a temp directory containing the two sample filenames and verifies the report includes both names and the `MEDIA_ROOT`-based resolution path.
  </action>
  <acceptance_criteria>
    - `rg -n "songs-sample/关喆-想你的夜\\(MTV\\)-国语-流行\\.mkv|songs-sample/蔡依林-BECAUSE OF YOU\\(演唱会\\)-国语-流行\\.mpg" scripts/real-mv-playback-risk-spike.mjs scripts/real-mv-playback-risk-spike.test.mjs` exits 0.
    - `node --test scripts/real-mv-playback-risk-spike.test.mjs` exits 0 and covers `--media-root` defaults with temp sample files.
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/real-mv-playback-risk-spike.test.mjs</automated>
  </verify>
  <done>The report defaults to the user's real sample library when local sample paths are not passed explicitly.</done>
</task>

</tasks>

<verification>
Run:
- `pnpm real-mv:risk-spike -- --help`
- `pnpm real-mv:risk-spike -- --controlled-only --output /tmp/real-mv-phase16-controlled.md`
- `node --test scripts/real-mv-playback-risk-spike.test.mjs`
</verification>

<success_criteria>
- HARD-02 has portable automated coverage for representative two-track and unsupported media checks.
- The real sample library can be used without committing binary fixtures.
- The script remains usable when the optional database/index cross-check is unavailable.
</success_criteria>

<output>
After completion, create `.planning/phases/16-policy-seam-android-reservation-and-hardening/16-03-SUMMARY.md`.
</output>
