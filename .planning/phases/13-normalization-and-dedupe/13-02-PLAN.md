---
phase: 13-normalization-and-dedupe
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - packages/hot-songs/src/normalize/candidates.ts
  - packages/hot-songs/src/test/candidates.test.ts
autonomous: true
requirements:
  - NORM-01
  - NORM-02
  - NORM-03
  - NORM-04
requirements_addressed: [NORM-01, NORM-02, NORM-03, NORM-04]
must_haves:
  truths:
    - "Rows from multiple sources merge only when normalized title, normalized artist key set, and variant signature all match"
    - "Same-title different-artist rows remain separate candidates"
    - "Candidate evidence preserves all raw source rows"
    - "Candidate IDs and song keys are deterministic and stable across input row order"
  artifacts:
    - path: "packages/hot-songs/src/normalize/candidates.ts"
      provides: "Conservative source row grouping and candidate snapshot builder"
      contains: "buildCandidateSnapshot"
    - path: "packages/hot-songs/src/test/candidates.test.ts"
      provides: "Dedupe and stability tests"
      contains: "same title different artist"
---

<objective>
Build conservative candidate grouping and stable identity generation from Phase 12 source rows.

Purpose: Transform raw source rows into candidate identities while preserving every source evidence row and avoiding false merges.
Output: Candidate snapshot builder, stable song keys/IDs, evidence aggregation, and grouping tests.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-normalization-and-dedupe/13-RESEARCH.md
@.planning/phases/13-normalization-and-dedupe/13-01-PLAN.md
@packages/hot-songs/src/contracts.ts
@packages/hot-songs/src/normalize/contracts.ts
@packages/hot-songs/src/normalize/text.ts
@packages/hot-songs/src/normalize/variants.ts
@.planning/reports/hot-songs/phase-12-fixture-all/source-rows.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Group source rows into conservative candidates</name>
  <files>packages/hot-songs/src/normalize/candidates.ts, packages/hot-songs/src/test/candidates.test.ts</files>
  <read_first>
    - packages/hot-songs/src/contracts.ts
    - packages/hot-songs/src/normalize/contracts.ts
    - packages/hot-songs/src/normalize/text.ts
    - .planning/reports/hot-songs/phase-12-fixture-all/source-rows.json
  </read_first>
  <action>
    Create `packages/hot-songs/src/normalize/candidates.ts` exporting:
    - `buildCandidateSnapshot(input)`
    - `buildCandidateIdentity(rows)`
    - `buildSongKey(identityParts)`
    - `buildCandidateId(songKey)`

    Group rows by:
    - `canonicalTitleKey`
    - sorted `canonicalArtistKeys`
    - `variantSignature`

    Use `crypto.createHash("sha256")` to produce stable IDs formatted as `song_<16 lowercase hex chars>`.

    Candidate fields must include all fields required by `CandidateIdentitySchema`, including raw evidence rows, `sourceIds`, `sourceTypes`, and merged warnings.

    Display fields:
    - choose `displayTitle` from the highest-priority evidence row after sorting by KTV-first before support, lower rank first, then source id
    - choose `displayArtists` from the same evidence row

    Add tests proving the Phase 12 fixture rows build six candidates and `后来` merges evidence from multiple sources.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/hot-songs test -- src/test/candidates.test.ts</automated>
    <automated>pnpm -F @home-ktv/hot-songs typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/hot-songs/src/normalize/candidates.ts` contains `buildCandidateSnapshot`.
    - `packages/hot-songs/src/normalize/candidates.ts` contains `createHash`.
    - `packages/hot-songs/src/normalize/candidates.ts` contains `variantSignature`.
    - `packages/hot-songs/src/test/candidates.test.ts` contains `后来`.
    - `packages/hot-songs/src/test/candidates.test.ts` contains `candidateCount`.
    - `pnpm -F @home-ktv/hot-songs test -- src/test/candidates.test.ts` exits 0.
  </acceptance_criteria>
  <done>Phase 12 source rows can be grouped into stable, evidence-preserving candidate identities.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove conservative dedupe edge cases</name>
  <files>packages/hot-songs/src/test/candidates.test.ts, packages/hot-songs/src/normalize/candidates.ts</files>
  <read_first>
    - packages/hot-songs/src/normalize/candidates.ts
    - packages/hot-songs/src/normalize/variants.ts
    - .planning/REQUIREMENTS.md
  </read_first>
  <action>
    Extend `candidates.test.ts` with edge cases proving:
    - same title with different artist keys creates separate candidates
    - `同手同脚 (Live)` carries `variant-live`
    - row-level warnings remain on evidence and lift to candidate warnings
    - candidate IDs and song keys are stable when input rows are reversed

    Adjust implementation only if needed to satisfy those tests. Do not add fuzzy matching or scoring.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/hot-songs test -- src/test/candidates.test.ts</automated>
    <automated>pnpm -F @home-ktv/hot-songs typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/hot-songs/src/test/candidates.test.ts` contains `same title different artist`.
    - `packages/hot-songs/src/test/candidates.test.ts` contains `variant-live`.
    - `packages/hot-songs/src/test/candidates.test.ts` contains `reversed`.
    - `packages/hot-songs/src/normalize/candidates.ts` does not contain `score`.
    - `pnpm -F @home-ktv/hot-songs test -- src/test/candidates.test.ts` exits 0.
  </acceptance_criteria>
  <done>Dedupe behavior is conservative, stable, and preserves variant/evidence warnings.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `pnpm -F @home-ktv/hot-songs test -- src/test/candidates.test.ts`
- [ ] `pnpm -F @home-ktv/hot-songs typecheck`
</verification>

<success_criteria>
- Same-song rows merge conservatively.
- Same-title different-artist rows remain separate.
- Variant markers remain visible through candidate warnings/signatures.
- Candidate IDs and song keys are stable across input order.
- No scoring, ranking, candidate tiers, Markdown/CSV final export, OpenList, download, scheduler, DB, Admin UI, or runtime mutation enters this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/13-normalization-and-dedupe/13-02-SUMMARY.md`.
</output>
