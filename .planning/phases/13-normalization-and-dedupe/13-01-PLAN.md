---
phase: 13-normalization-and-dedupe
plan: 01
type: execute
wave: 1
depends_on: ["12-05"]
files_modified:
  - packages/hot-songs/src/normalize/contracts.ts
  - packages/hot-songs/src/normalize/text.ts
  - packages/hot-songs/src/normalize/variants.ts
  - packages/hot-songs/src/test/normalize.test.ts
autonomous: true
requirements:
  - NORM-01
  - NORM-03
  - NORM-04
requirements_addressed: [NORM-01, NORM-03, NORM-04]
must_haves:
  truths:
    - "Candidate identity contracts preserve raw source evidence while adding canonical keys and stable IDs"
    - "Title and artist normalization produce deterministic keys without translating, fuzzy matching, or dropping raw values"
    - "Variant markers such as Live, DJ, Remix, 伴奏, 翻唱, 片段, 女声版, 男声版 are detected as warnings"
    - "Variant signatures are explicit identity components so variant rows are not silently merged with non-variant rows"
  artifacts:
    - path: "packages/hot-songs/src/normalize/contracts.ts"
      provides: "Candidate evidence, candidate, and snapshot contracts"
      contains: "hot-songs.candidate-snapshot.v1"
    - path: "packages/hot-songs/src/normalize/text.ts"
      provides: "Deterministic title and artist key helpers"
      contains: "normalizeTitleIdentity"
    - path: "packages/hot-songs/src/normalize/variants.ts"
      provides: "Variant/noise marker detection"
      contains: "variant-live"
---

<objective>
Create Phase 13 candidate identity contracts, deterministic text normalization helpers, and variant detection.

Purpose: Establish the identity vocabulary before grouping rows so Phase 13 preserves raw evidence and surfaces noisy variants instead of losing them.
Output: Candidate snapshot schemas, title/artist key helpers, variant detection helpers, and focused tests.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-normalization-and-dedupe/13-RESEARCH.md
@.planning/phases/12-source-contracts-and-fetch-harness/12-05-SUMMARY.md
@packages/hot-songs/src/contracts.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add candidate identity contracts</name>
  <files>packages/hot-songs/src/normalize/contracts.ts, packages/hot-songs/src/test/normalize.test.ts</files>
  <read_first>
    - packages/hot-songs/src/contracts.ts
    - .planning/REQUIREMENTS.md
    - .planning/phases/13-normalization-and-dedupe/13-RESEARCH.md
  </read_first>
  <action>
    Create `packages/hot-songs/src/normalize/contracts.ts` with Zod schemas and inferred types:
    - `CandidateEvidenceSchema`
    - `CandidateIdentitySchema`
    - `CandidateSnapshotSchema`

    `CandidateEvidenceSchema` must preserve Phase 12 row fields: `sourceId`, `sourceType`, `provider`, `rank`, `rawTitle`, `rawArtists`, `sourceUrl`, `sourcePublishedAt`, `collectedAt`, and `warnings`.

    `CandidateIdentitySchema` must include:
    - `candidateId`
    - `songKey`
    - `canonicalTitleKey`
    - `baseTitleKey`
    - `canonicalArtistKeys`
    - `variantSignature`
    - `displayTitle`
    - `displayArtists`
    - `sourceIds`
    - `sourceTypes`
    - `warnings`
    - `evidence`

    `CandidateSnapshotSchema` must include literal `schemaVersion: "hot-songs.candidate-snapshot.v1"`, `generatedAt`, `sourceRowCount`, `candidateCount`, optional `sourceStatuses`, and `candidates`.

    Add tests proving the snapshot schema accepts one candidate with raw evidence and rejects an empty `candidateId`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/hot-songs test -- src/test/normalize.test.ts</automated>
    <automated>pnpm -F @home-ktv/hot-songs typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/hot-songs/src/normalize/contracts.ts` contains `CandidateSnapshotSchema`.
    - `packages/hot-songs/src/normalize/contracts.ts` contains `hot-songs.candidate-snapshot.v1`.
    - `packages/hot-songs/src/normalize/contracts.ts` contains `CandidateEvidenceSchema`.
    - `packages/hot-songs/src/test/normalize.test.ts` contains `rawTitle`.
    - `pnpm -F @home-ktv/hot-songs test -- src/test/normalize.test.ts` exits 0.
  </acceptance_criteria>
  <done>Candidate snapshot output has runtime contracts that preserve raw source evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add text normalization and variant detection</name>
  <files>packages/hot-songs/src/normalize/text.ts, packages/hot-songs/src/normalize/variants.ts, packages/hot-songs/src/test/normalize.test.ts</files>
  <read_first>
    - packages/hot-songs/src/normalize/contracts.ts
    - .planning/phases/13-normalization-and-dedupe/13-RESEARCH.md
  </read_first>
  <action>
    Create `packages/hot-songs/src/normalize/text.ts` exporting:
    - `normalizeSearchText(value: string): string`
    - `normalizeArtistKey(value: string): string`
    - `normalizeArtistKeys(values: string[]): string[]`
    - `normalizeTitleIdentity(rawTitle: string): { canonicalTitleKey: string; baseTitleKey: string; variantSignature: string; warnings: string[] }`

    Create `packages/hot-songs/src/normalize/variants.ts` exporting:
    - `detectVariantWarnings(rawTitle: string): string[]`
    - `stripVariantMarkers(rawTitle: string): string`

    Required behavior:
    - Unicode normalize with `NFKC`
    - lowercase ASCII
    - collapse whitespace
    - convert common punctuation/separators to spaces for keys
    - keep Chinese characters unchanged
    - do not translate, romanize, or fuzzy match
    - detect stable warnings including `variant-live`, `variant-dj`, `variant-remix`, `variant-accompaniment`, `variant-cover`, `variant-clip`, `variant-gender-version`, and `variant-version`
    - produce a deterministic `variantSignature`, using `original` when no variant warnings exist

    Extend tests proving:
    - `Run Wild（向风而野）` creates a stable lowercase key while preserving Chinese words
    - `同手同脚 (Live)` returns warning `variant-live`
    - `后来` has variant signature `original`
    - `周深` artist key is stable
    - multi-artist keys sort deterministically
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/hot-songs test -- src/test/normalize.test.ts</automated>
    <automated>pnpm -F @home-ktv/hot-songs typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/hot-songs/src/normalize/text.ts` contains `normalizeTitleIdentity`.
    - `packages/hot-songs/src/normalize/text.ts` contains `NFKC`.
    - `packages/hot-songs/src/normalize/variants.ts` contains `variant-live`.
    - `packages/hot-songs/src/normalize/variants.ts` contains `variant-accompaniment`.
    - `packages/hot-songs/src/test/normalize.test.ts` contains `同手同脚 (Live)`.
    - `packages/hot-songs/src/test/normalize.test.ts` contains `Run Wild（向风而野）`.
    - `pnpm -F @home-ktv/hot-songs test -- src/test/normalize.test.ts` exits 0.
  </acceptance_criteria>
  <done>Raw song text can be converted into deterministic keys while variant/noise markers remain visible.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `pnpm -F @home-ktv/hot-songs test -- src/test/normalize.test.ts`
- [ ] `pnpm -F @home-ktv/hot-songs typecheck`
</verification>

<success_criteria>
- Candidate output contracts preserve raw source evidence.
- Title/artist key helpers are deterministic and conservative.
- Variant markers surface as warnings/signatures instead of being silently discarded.
- No candidate grouping, scoring, ranking, CSV/Markdown export, OpenList, download, scheduler, DB, Admin UI, or runtime mutation enters this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/13-normalization-and-dedupe/13-01-SUMMARY.md`.
</output>
