---
phase: 03-room-sessions-queue-control
plan: 03
type: execute
wave: 3
depends_on:
  - "03-01"
  - "03-02"
files_modified:
  - apps/api/src/db/migrations/0004_queue_commands.sql
  - apps/api/src/db/schema.ts
  - packages/domain/src/index.ts
  - packages/protocol/src/index.ts
  - packages/session-engine/src/index.ts
  - apps/api/src/modules/playback/repositories/queue-entry-repository.ts
  - apps/api/src/modules/playback/repositories/playback-session-repository.ts
  - apps/api/src/modules/playback/repositories/room-session-command-repository.ts
  - apps/api/src/test/session-engine.test.ts
autonomous: true
requirements:
  - QUEU-01
  - QUEU-03
  - QUEU-04
  - QUEU-05
  - QUEU-06
  - PLAY-04
must_haves:
  truths:
    - "Queue removal is represented explicitly and can support a short undo window."
    - "Controller command names and room snapshot event names live in the shared protocol package."
    - "Session Engine exposes deterministic helper behavior for effective queue ordering, promote, and stale-version conflicts."
    - "Repositories expose the methods needed by the command service without route-level SQL."
  artifacts:
    - path: "packages/session-engine/src/index.ts"
      provides: "typed queue command surface and pure queue helpers"
    - path: "apps/api/src/modules/playback/repositories/queue-entry-repository.ts"
      provides: "queue append/list/remove/undo/reorder and undoable-removed lookup methods"
    - path: "apps/api/src/modules/playback/repositories/playback-session-repository.ts"
      provides: "session start/idle/switch-intent methods"
  key_links:
    - from: "packages/session-engine/src/index.ts"
      to: "apps/api/src/modules/playback/repositories/queue-entry-repository.ts"
      via: "pure queue ordering helpers drive repository reorder behavior"
      pattern: "promoteAfterCurrent"
    - from: "apps/api/src/db/migrations/0004_queue_commands.sql"
      to: "apps/api/src/modules/playback/repositories/queue-entry-repository.ts"
      via: "removed queue state and undo expiry columns are mapped by repository methods"
      pattern: "listUndoableRemoved"
    - from: "packages/protocol/src/index.ts"
      to: "apps/api/src/modules/playback/repositories/room-session-command-repository.ts"
      via: "shared command names match persisted command_type values"
      pattern: "controller.command"
---

<objective>
Add queue command contracts and repository foundations for the authoritative Session Engine.

Purpose: command routes in the next plan need a typed queue/session model and repository layer before they can safely mutate room state.
Output: queue status migration, shared protocol command names, session-engine helpers, repository methods, and tests.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-room-sessions-queue-control/03-CONTEXT.md
@.planning/phases/03-room-sessions-queue-control/03-RESEARCH.md
@.planning/phases/03-room-sessions-queue-control/03-02-SUMMARY.md
@packages/domain/src/index.ts
@packages/protocol/src/index.ts
@packages/session-engine/src/index.ts
@apps/api/src/modules/playback/repositories/queue-entry-repository.ts
@apps/api/src/modules/playback/repositories/playback-session-repository.ts

<interfaces>
Command types:
```typescript
"add-queue-entry" | "delete-queue-entry" | "undo-delete-queue-entry" | "promote-queue-entry" | "skip-current" | "switch-vocal-mode" | "player-ended"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add removed queue state and command protocol names</name>
  <files>apps/api/src/db/migrations/0004_queue_commands.sql, apps/api/src/db/schema.ts, packages/domain/src/index.ts, packages/protocol/src/index.ts, apps/api/src/test/session-engine.test.ts</files>
  <read_first>
- apps/api/src/db/schema.ts
- apps/api/src/db/migrations/0003_room_sessions_control.sql
- packages/domain/src/index.ts
- packages/protocol/src/index.ts
- .planning/phases/03-room-sessions-queue-control/03-CONTEXT.md
  </read_first>
  <behavior>
- Test 1: `QueueEntryStatus` includes `removed`.
- Test 2: queue entries include `removedAt`, `removedByControlSessionId`, and `undoExpiresAt`.
- Test 3: protocol exports six controller command names and `room.control.snapshot.updated`.
  </behavior>
  <action>
Create `apps/api/src/db/migrations/0004_queue_commands.sql`:
```sql
ALTER TABLE queue_entries
  DROP CONSTRAINT IF EXISTS queue_entries_status_check;

ALTER TABLE queue_entries
  ADD CONSTRAINT queue_entries_status_check
  CHECK (status IN ('queued', 'preparing', 'loading', 'playing', 'played', 'skipped', 'failed', 'removed'));

ALTER TABLE queue_entries
  ADD COLUMN IF NOT EXISTS removed_at timestamptz,
  ADD COLUMN IF NOT EXISTS removed_by_control_session_id text REFERENCES control_sessions(id) ON DELETE SET NULL,
  ADD COLUMN IF NOT EXISTS undo_expires_at timestamptz;

CREATE INDEX IF NOT EXISTS queue_entries_room_effective_position_idx
  ON queue_entries(room_id, status, queue_position)
  WHERE status IN ('queued', 'preparing', 'loading', 'playing');
```

Update schema/domain mapping to include `removed`, `removed_at`, `removed_by_control_session_id`, and `undo_expires_at`.

Update `packages/protocol/src/index.ts`:
- Add `controllerCommandNames.addQueueEntry = "controller.command.add_queue_entry"`.
- Add `controllerCommandNames.deleteQueueEntry = "controller.command.delete_queue_entry"`.
- Add `controllerCommandNames.undoDeleteQueueEntry = "controller.command.undo_delete_queue_entry"`.
- Add `controllerCommandNames.promoteQueueEntry = "controller.command.promote_queue_entry"`.
- Add `controllerCommandNames.skipCurrent = "controller.command.skip_current"`.
- Add `controllerCommandNames.switchVocalMode = "controller.command.switch_vocal_mode"`.
- Add `roomEventNames.controlSnapshotUpdated = "room.control.snapshot.updated"`.
- Include all names in `protocolMessageNames`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- session-engine</automated>
  </verify>
  <acceptance_criteria>
- `rg "\"removed\"" packages/domain/src/index.ts apps/api/src/db/schema.ts apps/api/src/db/migrations/0004_queue_commands.sql` returns matches.
- `rg "undo_expires_at" apps/api/src/db/migrations/0004_queue_commands.sql apps/api/src/db/schema.ts` returns matches.
- `rg "controller.command.add_queue_entry" packages/protocol/src/index.ts` returns a match.
- `rg "room.control.snapshot.updated" packages/protocol/src/index.ts` returns a match.
- `pnpm -F @home-ktv/api test -- session-engine` exits 0.
  </acceptance_criteria>
  <done>Shared schema and protocol can represent controller commands, removed entries, undo windows, and realtime snapshot events.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement session engine helpers and repository methods</name>
  <files>packages/session-engine/src/index.ts, apps/api/src/modules/playback/repositories/queue-entry-repository.ts, apps/api/src/modules/playback/repositories/playback-session-repository.ts, apps/api/src/modules/playback/repositories/room-session-command-repository.ts, apps/api/src/test/session-engine.test.ts</files>
  <read_first>
- packages/session-engine/src/index.ts
- apps/api/src/modules/playback/repositories/queue-entry-repository.ts
- apps/api/src/modules/playback/repositories/playback-session-repository.ts
- apps/api/src/modules/playback/repositories/playback-event-repository.ts
- apps/api/src/db/schema.ts
  </read_first>
  <behavior>
- Test 1: `promoteAfterCurrent(queue, id, currentId)` places the selected queued entry immediately after current and does not change current.
- Test 2: `effectiveQueue(queue)` excludes `removed`, `played`, `skipped`, and `failed`.
- Test 3: stale command results use `SESSION_VERSION_CONFLICT`.
- Test 4: `listUndoableRemoved(roomId, now)` returns removed entries whose `undo_expires_at > now`, ordered by `removed_at DESC`.
  </behavior>
  <action>
Rewrite `packages/session-engine/src/index.ts` to export:
- `RoomSessionCommandType`.
- `RoomSessionCommand` with `commandId`, `roomId`, `controlSessionId`, `sessionVersion`, `type`, and `payload`.
- `RoomSessionCommandResult` with `status: "accepted" | "duplicate" | "conflict" | "rejected"`, `sessionVersion`, optional `code`, and optional `message`.
- `SESSION_VERSION_CONFLICT = "SESSION_VERSION_CONFLICT"`.
- `effectiveQueue(queue)`.
- `promoteAfterCurrent(queue, queueEntryId, currentQueueEntryId)`.

Extend `QueueEntryRepository`:
- `listEffectiveQueue(roomId)`.
- `listUndoableRemoved(roomId, now)` returning removed entries with `undo_expires_at > now`, ordered by `removed_at DESC`.
- `findCurrentForRoom(roomId)`.
- `append(input)`.
- `markRemoved(input)`.
- `undoRemoved(input)`.
- `renumberQueue(roomId, orderedQueueEntryIds)`.
- `markCompleted(input)`.

Extend `PlaybackSessionRepository`:
- `startQueueEntry(input)`.
- `setIdle(roomId)`.
- `requestSwitchTarget(input)`.

Create `room-session-command-repository.ts`:
- `findCommand(commandId)`.
- `insertCommandAttempt(input)`.
- `updateCommandResult(input)`.
- PostgreSQL implementation over `control_commands`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- session-engine && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
- `rg "SESSION_VERSION_CONFLICT" packages/session-engine/src/index.ts apps/api/src/test/session-engine.test.ts` returns matches.
- `rg "promoteAfterCurrent" packages/session-engine/src/index.ts apps/api/src/test/session-engine.test.ts` returns matches.
- `rg "listEffectiveQueue" apps/api/src/modules/playback/repositories/queue-entry-repository.ts` returns a match.
- `rg "listUndoableRemoved" apps/api/src/modules/playback/repositories/queue-entry-repository.ts apps/api/src/test/session-engine.test.ts` returns matches.
- `rg "startQueueEntry" apps/api/src/modules/playback/repositories/playback-session-repository.ts` returns a match.
- `rg "findCommand" apps/api/src/modules/playback/repositories/room-session-command-repository.ts` returns a match.
- `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>Command services can use typed helpers and repositories instead of route-level SQL or local-client queue logic.</done>
</task>

</tasks>

<verification>
Run `pnpm -F @home-ktv/api test -- session-engine` and `pnpm typecheck`.
</verification>

<success_criteria>
- Queue delete/undo has explicit state and metadata.
- Controller command and realtime event names live in `@home-ktv/protocol`.
- Session engine helpers encode promote and effective-queue semantics.
- Repository methods needed by command routes exist.
- This plan modifies fewer than 15 files.
</success_criteria>

<output>
After completion, create `.planning/phases/03-room-sessions-queue-control/03-03-SUMMARY.md`.
</output>
