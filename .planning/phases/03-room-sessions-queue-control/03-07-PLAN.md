---
phase: 03-room-sessions-queue-control
plan: 07
type: execute
wave: 6
depends_on:
  - "03-02"
  - "03-04"
files_modified:
  - apps/api/src/routes/admin-rooms.ts
  - apps/api/src/server.ts
  - apps/api/src/test/admin-room-status.test.ts
  - apps/admin/src/App.tsx
  - apps/admin/src/App.css
  - apps/admin/src/api/client.ts
  - apps/admin/src/rooms/types.ts
  - apps/admin/src/rooms/RoomStatusView.tsx
  - apps/admin/src/test/room-status.test.tsx
autonomous: true
requirements:
  - PAIR-02
  - ADMN-03
must_haves:
  truths:
    - "Admin Rooms view shows token expiry, online controller count, TV online state, session version, and queue summary."
    - "Admin can refresh the pairing token from the room status view."
    - "Admin room status does not expand into full ADMN-02 event history or device management."
  artifacts:
    - path: "apps/api/src/routes/admin-rooms.ts"
      provides: "admin room status and pairing refresh API"
    - path: "apps/admin/src/rooms/RoomStatusView.tsx"
      provides: "admin room/token status UI"
  key_links:
    - from: "apps/admin/src/rooms/RoomStatusView.tsx"
      to: "apps/admin/src/api/client.ts"
      via: "Rooms tab uses typed admin room status and refresh helpers"
      pattern: "refreshPairingToken"
    - from: "apps/admin/src/api/client.ts"
      to: "apps/api/src/routes/admin-rooms.ts"
      via: "admin UI consumes room status and token refresh endpoints"
      pattern: "/admin/rooms/"
    - from: "apps/api/src/routes/admin-rooms.ts"
      to: "apps/api/src/modules/rooms/build-control-snapshot.ts"
      via: "admin status summarizes server-authored room snapshot state"
      pattern: "buildRoomControlSnapshot"
---

<objective>
Add the admin room status and pairing token management view.

Purpose: administrators need to see pairing/token state and refresh room entry tokens without disrupting active control sessions.
Output: admin room status API, Rooms tab, room status UI, refresh action, and tests.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-room-sessions-queue-control/03-CONTEXT.md
@.planning/phases/03-room-sessions-queue-control/03-RESEARCH.md
@.planning/phases/03-room-sessions-queue-control/03-02-SUMMARY.md
@.planning/phases/03-room-sessions-queue-control/03-04-SUMMARY.md
@apps/api/src/routes/admin-rooms.ts
@apps/api/src/modules/rooms/build-control-snapshot.ts
@apps/admin/src/App.tsx
@apps/admin/src/api/client.ts
@apps/admin/src/test/song-catalog.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add admin room status API</name>
  <files>apps/api/src/routes/admin-rooms.ts, apps/api/src/server.ts, apps/api/src/test/admin-room-status.test.ts</files>
  <read_first>
- apps/api/src/routes/admin-rooms.ts
- apps/api/src/modules/rooms/build-control-snapshot.ts
- apps/api/src/routes/admin-catalog.ts
- apps/api/src/server.ts
  </read_first>
  <behavior>
- Test 1: `GET /admin/rooms/living-room` returns token expiry, TV online state, controller count, session version, current song, and queue summary.
- Test 2: `POST /admin/rooms/living-room/pairing-token/refresh` still returns updated pairing and does not revoke sessions.
  </behavior>
  <action>
Extend `routes/admin-rooms.ts`:
- Keep `POST /admin/rooms/:roomSlug/pairing-token/refresh`.
- Add `GET /admin/rooms/:roomSlug`.
- Return:
```typescript
{
  room: { roomId, roomSlug, status },
  pairing: { tokenExpiresAt, controllerUrl, qrPayload },
  tvPresence: { online, deviceName, lastSeenAt, conflict },
  controllers: { onlineCount },
  sessionVersion,
  current: { queueEntryId, songTitle, artistName, vocalMode } | null,
  queue: RoomQueueEntryPreview[]
}
```
- Do not include playback event history, full device list, online task status, or RBAC.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- admin-room-status</automated>
  </verify>
  <acceptance_criteria>
- `rg "GET /admin/rooms/:roomSlug" apps/api/src/routes/admin-rooms.ts apps/api/src/test/admin-room-status.test.ts` returns a match.
- `rg "onlineCount|tvPresence|tokenExpiresAt|sessionVersion" apps/api/src/routes/admin-rooms.ts apps/api/src/test/admin-room-status.test.ts` returns matches.
- `rg "playback event|event history|device list|RBAC" apps/api/src/routes/admin-rooms.ts` returns no matches.
- `pnpm -F @home-ktv/api test -- admin-room-status` exits 0.
  </acceptance_criteria>
  <done>Admin API exposes the Phase 3 room status surface without expanding into Phase 5 admin scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Rooms tab and room status UI</name>
  <files>apps/admin/src/App.tsx, apps/admin/src/App.css, apps/admin/src/api/client.ts, apps/admin/src/rooms/types.ts, apps/admin/src/rooms/RoomStatusView.tsx, apps/admin/src/test/room-status.test.tsx</files>
  <read_first>
- apps/admin/src/App.tsx
- apps/admin/src/api/client.ts
- apps/admin/src/test/song-catalog.test.tsx
- apps/admin/src/App.css
  </read_first>
  <behavior>
- Test 1: admin app defaults to Imports but can switch to Rooms.
- Test 2: Rooms view renders `Token expires`, `Online controllers`, `TV status`, session version, current song, and queue summary.
- Test 3: clicking `Refresh pairing token` calls the refresh endpoint and updates token expiry.
  </behavior>
  <action>
Update `App.tsx`:
- Add third tab labeled `Rooms`.
- Keep Imports as default.

Extend `api/client.ts`:
- `fetchRoomStatus(roomSlug)`.
- `refreshPairingToken(roomSlug)`.
- Use `/admin/rooms/${roomSlug}` and `/admin/rooms/${roomSlug}/pairing-token/refresh`.

Create `rooms/types.ts` for the API response.

Create `rooms/RoomStatusView.tsx`:
- Use room slug `living-room`.
- Render labels `Token expires`, `Online controllers`, `TV status`, `Session version`, `Refresh pairing token`.
- Show current song and first five queue entries.

Add compact CSS in `App.css`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/admin test -- room-status && pnpm -F @home-ktv/admin build</automated>
  </verify>
  <acceptance_criteria>
- `rg "Rooms" apps/admin/src/App.tsx apps/admin/src/test/room-status.test.tsx` returns matches.
- `rg "Refresh pairing token" apps/admin/src/rooms/RoomStatusView.tsx apps/admin/src/test/room-status.test.tsx` returns matches.
- `rg "Online controllers|TV status|Token expires|Session version" apps/admin/src/rooms/RoomStatusView.tsx apps/admin/src/test/room-status.test.tsx` returns matches.
- `rg "/admin/rooms/\\$\\{roomSlug\\}" apps/admin/src/api/client.ts` returns a match.
- `pnpm -F @home-ktv/admin test -- room-status` exits 0.
  </acceptance_criteria>
  <done>Admin users can inspect and refresh room entry token state from the existing admin app.</done>
</task>

</tasks>

<verification>
Run `pnpm -F @home-ktv/api test -- admin-room-status`, `pnpm -F @home-ktv/admin test -- room-status`, `pnpm -F @home-ktv/admin build`, and `pnpm typecheck`.
</verification>

<success_criteria>
- Admin room status API and UI satisfy ADMN-03.
- Token refresh remains available and does not kill active control sessions.
- Admin scope stays limited to Phase 3 token/status summary.
- This plan modifies fewer than 15 files.
</success_criteria>

<output>
After completion, create `.planning/phases/03-room-sessions-queue-control/03-07-SUMMARY.md`.
</output>
