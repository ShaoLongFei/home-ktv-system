---
phase: 03-room-sessions-queue-control
plan: 04
type: execute
wave: 4
depends_on:
  - "03-02"
  - "03-03"
files_modified:
  - apps/api/src/modules/playback/session-command-service.ts
  - apps/api/src/modules/rooms/build-control-snapshot.ts
  - apps/api/src/routes/control-commands.ts
  - apps/api/src/routes/available-songs.ts
  - apps/api/src/routes/player.ts
  - apps/api/src/modules/player/telemetry-service.ts
  - apps/api/src/server.ts
  - apps/api/src/test/room-queue-commands.test.ts
autonomous: true
requirements:
  - QUEU-01
  - QUEU-03
  - QUEU-04
  - QUEU-05
  - QUEU-06
  - PLAY-04
must_haves:
  truths:
    - "All queue and playback mutations go through one backend command service."
    - "Commands touch the control session and refresh the 2-hour cookie activity window."
    - "Stale sessionVersion requests return a conflict with the latest server snapshot."
    - "TV ended and phone skip share one advancement helper."
    - "Phase 3 exposes a minimal ready-song picker without Phase 4 search."
  artifacts:
    - path: "apps/api/src/modules/playback/session-command-service.ts"
      provides: "authoritative command execution and advancement"
    - path: "apps/api/src/routes/control-commands.ts"
      provides: "mobile command HTTP API"
    - path: "apps/api/src/routes/available-songs.ts"
      provides: "minimal ready-song picker"
  key_links:
    - from: "apps/api/src/routes/control-commands.ts"
      to: "apps/api/src/modules/playback/session-command-service.ts"
      via: "all controller mutations call one authoritative service"
      pattern: "executeRoomCommand"
    - from: "apps/api/src/modules/playback/session-command-service.ts"
      to: "apps/api/src/modules/playback/repositories/queue-entry-repository.ts"
      via: "delete command writes removed state and snapshot reads undoable removed entries"
      pattern: "listUndoableRemoved"
    - from: "apps/api/src/routes/player.ts"
      to: "apps/api/src/modules/playback/session-command-service.ts"
      via: "TV ended facts advance through the same helper as skip-current"
      pattern: "handlePlayerEnded"
---

<objective>
Implement authoritative controller command routes, ready-song listing, and TV-ended advancement.

Purpose: phones can request queue changes, but the backend must remain the only writer of room state.
Output: command service, command routes, ready-song route, TV ended integration, control snapshot queue filling, and tests.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-room-sessions-queue-control/03-CONTEXT.md
@.planning/phases/03-room-sessions-queue-control/03-RESEARCH.md
@.planning/phases/03-room-sessions-queue-control/03-02-SUMMARY.md
@.planning/phases/03-room-sessions-queue-control/03-03-SUMMARY.md
@apps/api/src/modules/controller/control-session-service.ts
@apps/api/src/modules/playback/repositories/queue-entry-repository.ts
@apps/api/src/modules/playback/repositories/playback-session-repository.ts
@apps/api/src/modules/playback/build-switch-target.ts
@apps/api/src/routes/player.ts

<interfaces>
Command routes:
```typescript
GET /rooms/:roomSlug/available-songs
POST /rooms/:roomSlug/commands/add-queue-entry
POST /rooms/:roomSlug/commands/delete-queue-entry
POST /rooms/:roomSlug/commands/undo-delete-queue-entry
POST /rooms/:roomSlug/commands/promote-queue-entry
POST /rooms/:roomSlug/commands/skip-current
POST /rooms/:roomSlug/commands/switch-vocal-mode
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement authoritative command service</name>
  <files>apps/api/src/modules/playback/session-command-service.ts, apps/api/src/modules/rooms/build-control-snapshot.ts, apps/api/src/test/room-queue-commands.test.ts</files>
  <read_first>
- apps/api/src/modules/controller/control-session-service.ts
- apps/api/src/modules/playback/repositories/queue-entry-repository.ts
- apps/api/src/modules/playback/repositories/playback-session-repository.ts
- apps/api/src/modules/playback/repositories/room-session-command-repository.ts
- apps/api/src/modules/playback/build-switch-target.ts
- apps/api/src/modules/rooms/build-control-snapshot.ts
- packages/session-engine/src/index.ts
  </read_first>
  <behavior>
- Test 1: add queue entry accepts a ready song with ready default asset and increments session version.
- Test 2: delete queued entry marks `removed`, sets `undo_expires_at = now + 10000ms`, hides it from the effective queue, and returns `undo: { queueEntryId, undoExpiresAt }`.
- Test 3: undo within 10 seconds restores the entry to `queued`.
- Test 4: promote moves a queued item to first position after current and does not interrupt current.
- Test 5: skip-current requires `confirmSkip: true`.
- Test 6: duplicate `commandId` is idempotent.
- Test 7: stale `sessionVersion` returns `SESSION_VERSION_CONFLICT` with latest snapshot.
- Test 8: every accepted command calls `touchControlSession` and refreshes session activity.
- Test 9: snapshots include undoable removed entries only from `listUndoableRemoved(roomId, now)`, with `status: "removed"`, `canDelete: false`, `canPromote: false`, and `undoExpiresAt`.
  </behavior>
  <action>
Create `session-command-service.ts`:
- Export `QUEUE_DELETE_UNDO_TTL_MS = 10 * 1000`.
- Export `executeRoomCommand(input)`.
- Export `advanceToNext(input)`.
- Export `handlePlayerEnded(input)`.
- Validate control session, `commandId`, and `sessionVersion` before mutation.
- On stale version, return `{ status: "conflict", code: "SESSION_VERSION_CONFLICT", latestSessionVersion, snapshot }`.
- Store accepted, duplicate, conflict, and rejected results in `control_commands`.
- Call `touchControlSession` for accepted controller commands and expose the refreshed cookie value to routes.
- `add-queue-entry` validates ready song, ready default asset, same-song default asset, and verified switch counterpart; otherwise return `SONG_NOT_QUEUEABLE`.
- `delete-queue-entry` rejects current/playing/loading/preparing/played/skipped/failed/removed entries with `QUEUE_ENTRY_NOT_DELETABLE`.
- `delete-queue-entry` returns server-authored `undo: { queueEntryId, undoExpiresAt }` on accepted deletion.
- `skip-current` requires `confirmSkip: true`; otherwise return `SKIP_CONFIRMATION_REQUIRED`.
- `switch-vocal-mode` calls `buildSwitchTarget`; unavailable returns `SWITCH_TARGET_NOT_AVAILABLE`.

Extend `build-control-snapshot.ts`:
- Fill active rows from `listEffectiveQueue(roomId)` plus song previews.
- Fill undo rows from `listUndoableRemoved(roomId, now)` plus song previews.
- Removed undo rows must use `RoomQueueEntryPreview` with `status: "removed"`, `canDelete: false`, `canPromote: false`, and `undoExpiresAt`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- room-queue-commands</automated>
  </verify>
  <acceptance_criteria>
- `rg "QUEUE_DELETE_UNDO_TTL_MS = 10 \\* 1000" apps/api/src/modules/playback/session-command-service.ts` returns a match.
- `rg "executeRoomCommand" apps/api/src/modules/playback/session-command-service.ts apps/api/src/test/room-queue-commands.test.ts` returns matches.
- `rg "touchControlSession" apps/api/src/modules/playback/session-command-service.ts apps/api/src/test/room-queue-commands.test.ts` returns matches.
- `rg "SESSION_VERSION_CONFLICT" apps/api/src/modules/playback/session-command-service.ts apps/api/src/test/room-queue-commands.test.ts` returns matches.
- `rg "SKIP_CONFIRMATION_REQUIRED" apps/api/src/modules/playback/session-command-service.ts apps/api/src/test/room-queue-commands.test.ts` returns a match.
- `rg "listUndoableRemoved" apps/api/src/modules/playback/session-command-service.ts apps/api/src/modules/rooms/build-control-snapshot.ts apps/api/src/test/room-queue-commands.test.ts` returns matches.
- `rg "undo: \\{ queueEntryId, undoExpiresAt \\}" apps/api/src/modules/playback/session-command-service.ts apps/api/src/test/room-queue-commands.test.ts` returns matches.
- `pnpm -F @home-ktv/api test -- room-queue-commands` exits 0.
  </acceptance_criteria>
  <done>The backend command service is the only authority for queue mutations, advancement, conflicts, idempotency, and session activity renewal.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add command routes and minimal ready-song picker</name>
  <files>apps/api/src/routes/control-commands.ts, apps/api/src/routes/available-songs.ts, apps/api/src/server.ts, apps/api/src/test/room-queue-commands.test.ts</files>
  <read_first>
- apps/api/src/modules/playback/session-command-service.ts
- apps/api/src/modules/controller/control-session-service.ts
- apps/api/src/modules/catalog/repositories/song-repository.ts
- apps/api/src/modules/catalog/repositories/asset-repository.ts
- apps/api/src/server.ts
  </read_first>
  <behavior>
- Test 1: all command routes require valid `ktv_control_session` cookie plus matching `deviceId`.
- Test 2: accepted command responses return `{ status, commandId, sessionVersion, snapshot }`, include `undo` when the command service returns undo metadata, and refresh `Set-Cookie`.
- Test 3: stale version returns HTTP 409 with `SESSION_VERSION_CONFLICT`.
- Test 4: `GET /rooms/living-room/available-songs` returns only ready songs with ready default assets and verified switch counterparts.
  </behavior>
  <action>
Create `routes/control-commands.ts`:
- Implement the six POST command routes listed in the interfaces block.
- Every route accepts `{ commandId, sessionVersion, deviceId, ... }`.
- Every route restores the control session from cookie + device id before executing.
- Accepted responses set refreshed `ktv_control_session` cookie when the service returns one.
- `delete-queue-entry` responses pass through the service-authored `undo: { queueEntryId, undoExpiresAt }` object.
- Conflict responses use HTTP 409 and include `{ code: "SESSION_VERSION_CONFLICT", latestSessionVersion, snapshot }`.

Create `routes/available-songs.ts`:
- `GET /rooms/:roomSlug/available-songs` returns `{ songs }`.
- Each item has `songId`, `title`, `artistName`, `language`, `defaultAssetId`, and `durationMs`.
- Include only songs where `status = 'ready'`, `default_asset_id IS NOT NULL`, default asset `status = 'ready'`, and a verified counterpart exists.
- Do not include Phase 4 search fields: no `searchHints`, `titlePinyin`, or `titleInitials` in this route response.

Register both routes from `server.ts`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- room-queue-commands && pnpm -F @home-ktv/api build</automated>
  </verify>
  <acceptance_criteria>
- `rg "POST /rooms/:roomSlug/commands/add-queue-entry" apps/api/src/routes/control-commands.ts apps/api/src/test/room-queue-commands.test.ts` returns a match.
- `rg "POST /rooms/:roomSlug/commands/switch-vocal-mode" apps/api/src/routes/control-commands.ts apps/api/src/test/room-queue-commands.test.ts` returns a match.
- `rg "GET /rooms/:roomSlug/available-songs" apps/api/src/routes/available-songs.ts apps/api/src/test/room-queue-commands.test.ts` returns a match.
- `rg "searchHints|titlePinyin|titleInitials" apps/api/src/routes/available-songs.ts` returns no matches.
- `pnpm -F @home-ktv/api test -- room-queue-commands` exits 0.
  </acceptance_criteria>
  <done>Mobile HTTP commands and the minimal Phase 3 song picker are available without adding Phase 4 search scope.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Route TV ended telemetry through shared advancement</name>
  <files>apps/api/src/routes/player.ts, apps/api/src/modules/player/telemetry-service.ts, apps/api/src/modules/playback/session-command-service.ts, apps/api/src/test/room-queue-commands.test.ts</files>
  <read_first>
- apps/api/src/routes/player.ts
- apps/api/src/modules/player/telemetry-service.ts
- apps/api/src/modules/playback/session-command-service.ts
- apps/api/src/test/player-runtime-contract.test.ts
- .planning/phases/03-room-sessions-queue-control/03-CONTEXT.md
  </read_first>
  <behavior>
- Test 1: TV telemetry `ended` marks current `played`, advances next queued entry, and increments session version.
- Test 2: TV telemetry `ended` with no next entry clears current target and returns idle.
- Test 3: failed telemetry does not implement alternate/backup resource fallback in Phase 3.
  </behavior>
  <action>
Update `routes/player.ts`:
- When `eventType === "ended"`, call `handlePlayerEnded`.
- Return the server snapshot produced after advancement.
- Keep non-ended telemetry on the existing persistence path.

Update `telemetry-service.ts`:
- Keep `failed` mapping to `playerState = "error"`.
- Do not select alternate, backup, or fallback assets for `failed`; PLAY-05 is Phase 5.

Update `session-command-service.ts`:
- `handlePlayerEnded` calls `advanceToNext({ completionStatus: "played" })`.
- `skip-current` calls the same helper with `completionStatus: "skipped"`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- room-queue-commands player-runtime-contract && pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
- `rg "handlePlayerEnded" apps/api/src/routes/player.ts apps/api/src/modules/playback/session-command-service.ts` returns matches.
- `rg "completionStatus: \"played\"" apps/api/src/modules/playback/session-command-service.ts apps/api/src/test/room-queue-commands.test.ts` returns a match.
- `rg "completionStatus: \"skipped\"" apps/api/src/modules/playback/session-command-service.ts apps/api/src/test/room-queue-commands.test.ts` returns a match.
- `rg "alternate|backup" apps/api/src/modules/player/telemetry-service.ts apps/api/src/modules/playback/session-command-service.ts` returns no matches.
- `pnpm -F @home-ktv/api test -- room-queue-commands player-runtime-contract` exits 0.
  </acceptance_criteria>
  <done>TV ended facts and phone skip commands share one advancement path, preserving service-side truth.</done>
</task>

</tasks>

<verification>
Run `pnpm -F @home-ktv/api test -- room-queue-commands player-runtime-contract`, `pnpm -F @home-ktv/api build`, and `pnpm typecheck`.
</verification>

<success_criteria>
- Queue add/delete/undo/promote/skip/switch commands are accepted only through server command routes.
- `sessionVersion` and `commandId` protect concurrent commands.
- Commands renew control session idle timeout.
- TV ended and phone skip share one server advancement helper.
- Minimal ready-song listing exists without Phase 4 search or multi-version selection.
- This plan modifies fewer than 15 files.
</success_criteria>

<output>
After completion, create `.planning/phases/03-room-sessions-queue-control/03-04-SUMMARY.md`.
</output>
