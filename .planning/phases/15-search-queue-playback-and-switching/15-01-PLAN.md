---
phase: 15-search-queue-playback-and-switching
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/domain/src/index.ts
  - apps/api/src/modules/catalog/repositories/song-repository.ts
  - apps/api/src/routes/song-search.ts
  - apps/api/src/test/song-search-routes.test.ts
autonomous: true
requirements: [PLAY-01, PLAY-05]
must_haves:
  truths:
    - "Approved real-MV songs are searchable from Mobile even when their current asset is not queueable."
    - "Search version options explicitly say whether the selected asset can be queued."
    - "Mobile-facing unavailable labels stay Chinese and hide low-level codec/probe details."
  artifacts:
    - path: "packages/domain/src/index.ts"
      provides: "Search result queueability fields"
      contains: "SongSearchVersionQueueState"
    - path: "apps/api/src/modules/catalog/repositories/song-repository.ts"
      provides: "Formal search eligibility for legacy switch pairs and single-file real MV assets"
      contains: "single_file_audio_tracks"
    - path: "apps/api/src/test/song-search-routes.test.ts"
      provides: "Route regression coverage for queueable and disabled real-MV search results"
      contains: "需预处理"
  key_links:
    - from: "apps/api/src/modules/catalog/repositories/song-repository.ts"
      to: "packages/domain/src/index.ts"
      via: "SongSearchVersionOption"
      pattern: "queueState|disabledLabel"
    - from: "apps/api/src/routes/song-search.ts"
      to: "apps/api/src/modules/catalog/repositories/song-repository.ts"
      via: "searchFormalSongs"
      pattern: "versions"
---

<objective>
Broaden Mobile search so real-MV catalog entries are visible and version options carry queueability.

Purpose: Phase 14 can admit one real-MV Asset with `playbackProfile.kind = "single_file_audio_tracks"`, but existing search only returns verified counterpart-asset families. Phase 15 needs Mobile search to find approved real MV songs and to show unsupported/review-required songs as disabled instead of hiding them.
Output: `GET /rooms/:roomSlug/songs/search` returns legacy queueable options plus real-MV options with `queueState: "queueable" | "needs_preprocess" | "temporarily_unavailable" | "missing_track_role"` and Chinese disabled labels.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-search-queue-playback-and-switching/15-CONTEXT.md
@.planning/phases/15-search-queue-playback-and-switching/15-RESEARCH.md
@packages/domain/src/index.ts
@apps/api/src/modules/catalog/repositories/song-repository.ts
@apps/api/src/routes/song-search.ts
@apps/api/src/test/song-search-routes.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add queueability metadata to search version options</name>
  <files>packages/domain/src/index.ts, apps/api/src/test/song-search-routes.test.ts</files>
  <read_first>
    - packages/domain/src/index.ts
    - apps/api/src/test/song-search-routes.test.ts
    - .planning/phases/15-search-queue-playback-and-switching/15-CONTEXT.md
    - .planning/phases/15-search-queue-playback-and-switching/15-RESEARCH.md
  </read_first>
  <behavior>
    - `SongSearchVersionOption` keeps existing fields for backward compatibility.
    - Every version option can declare whether it is queueable.
    - Disabled real-MV options expose simple Chinese copy only.
  </behavior>
  <action>
    In `packages/domain/src/index.ts`, add:
    ```ts
    export type SongSearchVersionQueueState =
      | "queueable"
      | "needs_preprocess"
      | "temporarily_unavailable"
      | "missing_track_role";
    ```

    Extend `SongSearchVersionOption` with:
    ```ts
    queueState: SongSearchVersionQueueState;
    canQueue: boolean;
    disabledLabel: string | null;
    ```

    Preserve all existing fields: `assetId`, `displayName`, `sourceType`, `sourceLabel`, `durationMs`, `qualityLabel`, and `isRecommended`. Do not add codec messages, MediaInfo detail, Admin diagnostic text, or provider/acquisition fields to the Mobile search contract.

    In `apps/api/src/test/song-search-routes.test.ts`, update existing expected version option objects to include:
    ```ts
    queueState: "queueable",
    canQueue: true,
    disabledLabel: null
    ```
  </action>
  <acceptance_criteria>
    - `rg -n "SongSearchVersionQueueState" packages/domain/src/index.ts` exits 0.
    - `rg -n "canQueue: boolean" packages/domain/src/index.ts` exits 0.
    - `rg -n "disabledLabel: string \\| null" packages/domain/src/index.ts` exits 0.
    - `rg -n "queueState: \"queueable\"" apps/api/src/test/song-search-routes.test.ts` exits 0.
    - `pnpm -F @home-ktv/api test -- src/test/song-search-routes.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/song-search-routes.test.ts</automated>
  </verify>
  <done>Search results can express queueable and disabled version options without changing command semantics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Include single-file real-MV assets in formal search results</name>
  <files>apps/api/src/modules/catalog/repositories/song-repository.ts, apps/api/src/test/song-search-routes.test.ts</files>
  <read_first>
    - apps/api/src/modules/catalog/repositories/song-repository.ts
    - apps/api/src/test/song-search-routes.test.ts
    - apps/api/src/modules/catalog/repositories/asset-repository.ts
    - packages/domain/src/index.ts
    - .planning/phases/14-admin-review-and-catalog-admission/14-VERIFICATION.md
  </read_first>
  <behavior>
    - Legacy verified switch-pair search behavior remains unchanged.
    - Real-MV assets are included when `asset_kind = 'dual-track-video'` or `playback_profile.kind = 'single_file_audio_tracks'`.
    - Queueable real-MV options require `status = 'ready'`, `source_type <> 'online_ephemeral'`, `compatibility_status = 'playable'`, and a reviewed `track_roles.instrumental`.
    - Nonqueueable real-MV options remain visible with `canQueue: false`.
  </behavior>
  <action>
    In `apps/api/src/modules/catalog/repositories/song-repository.ts`, replace the current `eligible_assets` CTE with a broader candidate CTE that includes:
    - legacy assets that satisfy the existing verified counterpart criteria;
    - real-MV assets where `a.playback_profile->>'kind' = 'single_file_audio_tracks'` OR `a.asset_kind = 'dual-track-video'`.

    Add selected columns needed for option construction:
    - `asset_status`
    - `asset_compatibility_status`
    - `asset_track_roles`
    - `asset_playback_profile`
    - `asset_switch_quality_status`

    Implement helper functions in the same file:
    ```ts
    function isRealMvSearchRow(row: SearchFormalSongRow): boolean
    function queueStateForSearchRow(row: SearchFormalSongRow): SongSearchVersionQueueState
    function disabledLabelForQueueState(state: SongSearchVersionQueueState): string | null
    ```

    Use these exact labels:
    - `"needs_preprocess"` -> `"需预处理"`
    - `"temporarily_unavailable"` -> `"暂不可播放"`
    - `"missing_track_role"` -> `"缺少伴唱声轨"`
    - `"queueable"` -> `null`

    For real-MV rows:
    - return `"queueable"` only when `status = 'ready'`, `source_type <> 'online_ephemeral'`, `compatibility_status = 'playable'`, and `track_roles.instrumental` is present;
    - return `"needs_preprocess"` when `compatibility_status = 'unsupported'`;
    - return `"missing_track_role"` when the real-MV asset lacks `track_roles.instrumental`;
    - otherwise return `"temporarily_unavailable"`.

    In `buildVersionOptions`, set `queueState`, `canQueue: queueState === "queueable"`, and `disabledLabel`.

    Add route tests:
    - a ready playable real-MV song with instrumental track is returned with `canQueue: true`, `queueState: "queueable"`, `qualityLabel` containing `dual-track-video`, and `disabledLabel: null`;
    - an unsupported real-MV song is returned with `canQueue: false`, `queueState: "needs_preprocess"`, and `disabledLabel: "需预处理"`;
    - a review-required real-MV song with missing instrumental role is returned with `queueState: "missing_track_role"` and `disabledLabel: "缺少伴唱声轨"`.
  </action>
  <acceptance_criteria>
    - `rg -n "function isRealMvSearchRow|function queueStateForSearchRow|function disabledLabelForQueueState" apps/api/src/modules/catalog/repositories/song-repository.ts` exits 0.
    - `rg -n "single_file_audio_tracks|dual-track-video" apps/api/src/modules/catalog/repositories/song-repository.ts` exits 0.
    - `rg -n "需预处理|暂不可播放|缺少伴唱声轨" apps/api/src/modules/catalog/repositories/song-repository.ts apps/api/src/test/song-search-routes.test.ts` exits 0.
    - `pnpm -F @home-ktv/api test -- src/test/song-search-routes.test.ts` exits 0.
    - `pnpm -F @home-ktv/api typecheck` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/song-search-routes.test.ts &amp;&amp; pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <done>Mobile search can see real-MV formal songs and distinguish queueable versus disabled versions.</done>
</task>

</tasks>

<verification>
Run:
- `pnpm -F @home-ktv/api test -- src/test/song-search-routes.test.ts`
- `pnpm -F @home-ktv/api typecheck`
</verification>

<success_criteria>
- PLAY-01 search visibility is covered for approved real-MV songs.
- PLAY-05 disabled/preprocess state is exposed in Mobile-safe contract fields.
- Legacy verified switch-pair results still return queueable options.
- No queue command, TV runtime, transcoding, or Android TV implementation enters this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/15-search-queue-playback-and-switching/15-01-SUMMARY.md`.
</output>
