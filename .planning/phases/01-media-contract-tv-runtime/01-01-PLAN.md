---
phase: 01-media-contract-tv-runtime
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - pnpm-workspace.yaml
  - turbo.json
  - tsconfig.base.json
  - .env.example
  - apps/api/package.json
  - apps/api/tsconfig.json
  - apps/api/src/server.ts
  - apps/api/src/config.ts
  - apps/api/src/routes/health.ts
  - apps/tv-player/package.json
  - apps/tv-player/tsconfig.json
  - apps/tv-player/vite.config.ts
  - apps/tv-player/src/main.tsx
  - apps/tv-player/src/App.tsx
  - packages/domain/package.json
  - packages/domain/tsconfig.json
  - packages/domain/src/index.ts
  - packages/protocol/package.json
  - packages/protocol/tsconfig.json
  - packages/protocol/src/index.ts
  - packages/session-engine/package.json
  - packages/session-engine/tsconfig.json
  - packages/session-engine/src/index.ts
  - packages/player-contracts/package.json
  - packages/player-contracts/tsconfig.json
  - packages/player-contracts/src/index.ts
autonomous: true
requirements:
  - LIBR-03
user_setup: []
must_haves:
  truths:
    - "Repo becomes a runnable pnpm workspace with `apps/api`, `apps/tv-player`, and shared packages under `packages/`."
    - "Canonical domain and protocol packages export Phase 1 entities and enums for `Song`, `Asset`, `Room`, `QueueEntry`, `PlaybackSession`, `DeviceSession`, `PlaybackEvent`, `VocalMode`, `LyricMode`, and `switchFamily`."
    - "API bootstrap and TV app bootstrap both compile against the same shared TypeScript contracts."
  artifacts:
    - "Workspace root config exists (`package.json`, `pnpm-workspace.yaml`, `turbo.json`, `tsconfig.base.json`)."
    - "`apps/api` and `apps/tv-player` exist with build/typecheck scripts."
    - "`packages/domain`, `packages/protocol`, `packages/session-engine`, and `packages/player-contracts` exist and export index files."
  key_links:
    - "`apps/api/src/server.ts` imports shared protocol/domain package exports instead of redefining request/response types locally."
    - "`apps/tv-player/src/App.tsx` imports the player contract package instead of hard-coding room playback payload shapes."
    - "Root workspace scripts invoke package-level build/typecheck commands across both apps and all shared packages."
---

<objective>
建立 Phase 1 的 monorepo、共享领域/协议包和最小 API/TV app 骨架。

Purpose: 让后续 schema、播放目标和 TV runtime 都建立在一个不会漂移的共享契约上，而不是在空目录里各写各的。
Output: 一个可安装、可 typecheck 的 pnpm workspace，包含 `api`、`tv-player` 和 4 个共享包。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-media-contract-tv-runtime/01-CONTEXT.md
@.planning/phases/01-media-contract-tv-runtime/01-RESEARCH.md
@docs/KTV-ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold the pnpm workspace and root toolchain</name>
  <files>package.json, pnpm-workspace.yaml, turbo.json, tsconfig.base.json, .env.example</files>
  <read_first>CLAUDE.md, .planning/phases/01-media-contract-tv-runtime/01-CONTEXT.md, .planning/phases/01-media-contract-tv-runtime/01-RESEARCH.md</read_first>
  <action>Create a root `package.json` with private workspace metadata and exact scripts `dev`, `build`, `typecheck`, `lint`, and `test`; create `pnpm-workspace.yaml` with `apps/*` and `packages/*`; create `turbo.json` pipelines for `build`, `typecheck`, `lint`, and `test`; create `tsconfig.base.json` with shared strict TypeScript options for Node + browser packages; create `.env.example` with exact keys `DATABASE_URL=`, `MEDIA_ROOT=`, `PUBLIC_BASE_URL=`, and `TV_ROOM_SLUG=living-room`.</action>
  <verify>`pnpm install`</verify>
  <acceptance_criteria>
    - `package.json` contains scripts named `dev`, `build`, `typecheck`, `lint`, and `test`
    - `pnpm-workspace.yaml` contains both `apps/*` and `packages/*`
    - `.env.example` contains `DATABASE_URL=`, `MEDIA_ROOT=`, `PUBLIC_BASE_URL=`, and `TV_ROOM_SLUG=living-room`
  </acceptance_criteria>
  <done>Workspace root config exists and `pnpm typecheck` can traverse the repo without missing-workspace errors.</done>
</task>

<task type="auto">
  <name>Task 2: Create shared domain, protocol, session-engine, and player-contract packages</name>
  <files>packages/domain/package.json, packages/domain/tsconfig.json, packages/domain/src/index.ts, packages/protocol/package.json, packages/protocol/tsconfig.json, packages/protocol/src/index.ts, packages/session-engine/package.json, packages/session-engine/tsconfig.json, packages/session-engine/src/index.ts, packages/player-contracts/package.json, packages/player-contracts/tsconfig.json, packages/player-contracts/src/index.ts</files>
  <read_first>package.json, tsconfig.base.json, .planning/phases/01-media-contract-tv-runtime/01-CONTEXT.md, docs/KTV-ARCHITECTURE.md</read_first>
  <action>Create four packages with exact package names `@home-ktv/domain`, `@home-ktv/protocol`, `@home-ktv/session-engine`, and `@home-ktv/player-contracts`. In `@home-ktv/domain`, export exact entity/types for `Song`, `Asset`, `Room`, `QueueEntry`, `PlaybackSession`, `DeviceSession`, and `PlaybackEvent`, plus enums/unions for `VocalMode`, `LyricMode`, `AssetStatus`, `PlayerState`, `DeviceType`, and `SwitchQualityStatus`. In `@home-ktv/player-contracts`, export `PlaybackTarget`, `SwitchTarget`, and `PlayerTelemetryEvent` types with fields `roomId`, `sessionVersion`, `queueEntryId`, `assetId`, `switchFamily`, `vocalMode`, `resumePositionMs`, and `rollbackAssetId`. In `@home-ktv/protocol`, export exact command/event names for `player.bootstrap`, `player.heartbeat`, `player.telemetry.loading`, `player.telemetry.playing`, `player.telemetry.ended`, `player.telemetry.failed`, and `room.snapshot.updated`. Leave `@home-ktv/session-engine` as a typed entry point package with placeholder exports for the reducer and handlers that later plans will fill in.</action>
  <verify>`pnpm --filter @home-ktv/domain build && pnpm --filter @home-ktv/protocol build && pnpm --filter @home-ktv/player-contracts build`</verify>
  <acceptance_criteria>
    - `packages/domain/src/index.ts` exports `Song`, `Asset`, `PlaybackSession`, and `SwitchQualityStatus`
    - `packages/player-contracts/src/index.ts` exports `PlaybackTarget`, `SwitchTarget`, and `PlayerTelemetryEvent`
    - `packages/protocol/src/index.ts` contains the string `room.snapshot.updated`
  </acceptance_criteria>
  <done>All shared packages build and expose the exact Phase 1 contract vocabulary needed by backend and TV runtime.</done>
</task>

<task type="auto">
  <name>Task 3: Bootstrap API and TV app shells against the shared contracts</name>
  <files>apps/api/package.json, apps/api/tsconfig.json, apps/api/src/server.ts, apps/api/src/config.ts, apps/api/src/routes/health.ts, apps/tv-player/package.json, apps/tv-player/tsconfig.json, apps/tv-player/vite.config.ts, apps/tv-player/src/main.tsx, apps/tv-player/src/App.tsx</files>
  <read_first>package.json, pnpm-workspace.yaml, packages/domain/src/index.ts, packages/player-contracts/src/index.ts, .planning/phases/01-media-contract-tv-runtime/01-RESEARCH.md</read_first>
  <action>Create `@home-ktv/api` as a Fastify app with a `GET /health` route returning room-aware config metadata including `roomSlug: "living-room"` and `mediaRootConfigured: boolean`. Create `@home-ktv/tv-player` as a Vite React app whose initial `App.tsx` renders a placeholder shell with explicit states `booting`, `idle`, `playing`, and `conflict`, and imports `PlaybackTarget` types from `@home-ktv/player-contracts`. Add package scripts so `pnpm --filter @home-ktv/api build` and `pnpm --filter @home-ktv/tv-player build` are valid commands.</action>
  <verify>`pnpm --filter @home-ktv/api build && pnpm --filter @home-ktv/tv-player build`</verify>
  <acceptance_criteria>
    - `apps/api/src/routes/health.ts` contains `roomSlug: "living-room"`
    - `apps/tv-player/src/App.tsx` contains the strings `booting`, `idle`, `playing`, and `conflict`
    - `apps/tv-player/src/App.tsx` imports from `@home-ktv/player-contracts`
  </acceptance_criteria>
  <done>Both runtime shells compile against the shared contracts and give later plans stable entry points instead of blank apps.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm install`
- [ ] `pnpm typecheck`
- [ ] `pnpm --filter @home-ktv/api build`
- [ ] `pnpm --filter @home-ktv/tv-player build`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo has a single shared contract surface for backend and TV runtime work
- Later plans can import domain/protocol/player types without inventing new shapes
</success_criteria>

<output>
After completion, create `.planning/phases/01-media-contract-tv-runtime/01-01-SUMMARY.md`
</output>
