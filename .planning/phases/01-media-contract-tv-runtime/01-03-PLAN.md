---
phase: 01-media-contract-tv-runtime
plan: 03
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - apps/api/src/routes/player.ts
  - apps/api/src/routes/room-snapshots.ts
  - apps/api/src/modules/player/register-player.ts
  - apps/api/src/modules/player/heartbeat-service.ts
  - apps/api/src/modules/player/telemetry-service.ts
  - apps/api/src/modules/player/conflict-service.ts
  - apps/api/src/modules/playback/apply-switch-transition.ts
  - apps/api/src/modules/playback/apply-reconnect-recovery.ts
  - apps/api/src/test/player-runtime-contract.test.ts
  - apps/tv-player/src/App.tsx
  - apps/tv-player/src/runtime/player-client.ts
  - apps/tv-player/src/runtime/use-room-snapshot.ts
  - apps/tv-player/src/runtime/video-pool.ts
  - apps/tv-player/src/runtime/switch-controller.ts
  - apps/tv-player/src/runtime/recovery-controller.ts
  - apps/tv-player/src/components/PairingQr.tsx
  - apps/tv-player/src/components/PlaybackStatusBanner.tsx
  - apps/tv-player/src/screens/IdleScreen.tsx
  - apps/tv-player/src/screens/PlayingScreen.tsx
  - apps/tv-player/src/screens/ConflictScreen.tsx
  - apps/tv-player/src/test/switch-runtime.test.tsx
  - apps/tv-player/src/test/reconnect-recovery.test.tsx
autonomous: false
requirements:
  - PLAY-01
  - PLAY-02
  - PLAY-03
  - PLAY-06
  - PLAY-07
user_setup:
  - "Provide `DATABASE_URL`, `MEDIA_ROOT`, and `PUBLIC_BASE_URL` for a local Phase 1 environment."
  - "Place at least one manually curated verified original/instrumental pair under `MEDIA_ROOT` so runtime switching can be exercised on a real screen."
  - "Open desktop Chrome/Chromium on the mini PC connected to the TV for the human verification checkpoint."
must_haves:
  truths:
    - "A single `living-room` TV player can bootstrap, receive room snapshots, and report loading/playing/ended/failed facts back to the backend."
    - "TV UI shows large QR in `idle`, small corner QR in `playing`, current song, next song, current vocal mode, and productized loading/recovery/conflict messaging."
    - "In-song switching uses a standby player path with progress-preserving re-entry and rolls back to the previous asset when the target fails."
    - "A second TV player for the same room sees an explicit conflict state instead of taking over."
    - "Reload/reconnect attempts resume near the prior position; if not possible, they restart the same song from 0 with a clear user-facing notice."
  artifacts:
    - "API routes and services exist for player bootstrap, heartbeat, telemetry, room snapshots, switch application, and reconnect recovery."
    - "TV runtime files exist for snapshot subscription, dual-video switching, recovery logic, and QR/status UI."
    - "Automated tests exist for switch rollback and reconnect behavior."
  key_links:
    - "`apps/tv-player/src/runtime/player-client.ts` consumes backend snapshot payloads from `@home-ktv/player-contracts`."
    - "`switch-controller.ts` calls backend switch/recovery semantics rather than guessing asset relationships locally."
    - "`conflict-service.ts` and `ConflictScreen.tsx` both use the same explicit conflict state instead of ad-hoc UI strings."
---

<objective>
落地 TV Player 绑定、播放中切换、心跳/事实状态上报、重连恢复和冲突处理的完整运行时循环。

Purpose: 这是 Phase 1 的真实交付中心，决定系统能否在客厅里稳定“播对、切对、断了能回来、冲突时不乱套”。
Output: 后端 player/session 路由与服务、前端 TV runtime、切换/回退/恢复逻辑、自动化测试和一个实机人类验证检查点。
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
@$HOME/.codex/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-media-contract-tv-runtime/01-CONTEXT.md
@.planning/phases/01-media-contract-tv-runtime/01-RESEARCH.md
@.planning/phases/01-media-contract-tv-runtime/01-01-PLAN.md
@.planning/phases/01-media-contract-tv-runtime/01-02-PLAN.md
@docs/KTV-ARCHITECTURE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement the backend player session, snapshot, heartbeat, and conflict contract</name>
  <files>apps/api/src/routes/player.ts, apps/api/src/routes/room-snapshots.ts, apps/api/src/modules/player/register-player.ts, apps/api/src/modules/player/heartbeat-service.ts, apps/api/src/modules/player/telemetry-service.ts, apps/api/src/modules/player/conflict-service.ts, apps/api/src/modules/playback/apply-switch-transition.ts, apps/api/src/modules/playback/apply-reconnect-recovery.ts, apps/api/src/test/player-runtime-contract.test.ts</files>
  <read_first>apps/api/src/db/schema.ts, apps/api/src/modules/playback/build-playback-target.ts, packages/player-contracts/src/index.ts, .planning/phases/01-media-contract-tv-runtime/01-RESEARCH.md</read_first>
  <action>Implement backend flows for a single active TV player per room. Add exact capabilities for `player.bootstrap`, `player.heartbeat`, and `player.telemetry.*` ingestion; expose room snapshot delivery through HTTP and/or WebSocket in `room-snapshots.ts`; on bootstrap, register the player against `living-room` and return either a normal snapshot or an explicit conflict payload when another active player exists; generate a `controllerUrl`/QR payload string from `PUBLIC_BASE_URL`, `roomSlug`, and a short-lived token format but do not implement the mobile controller yet; persist telemetry events for `loading`, `playing`, `ended`, `failed`, `switch_failed`, and `recovery_fallback_start_over`; in reconnect recovery, prefer the stored `player_position_ms` and only fall back to `0` when resume cannot be honored.</action>
  <verify>`pnpm --filter @home-ktv/api test -- player-runtime-contract && pnpm --filter @home-ktv/api build`</verify>
  <acceptance_criteria>
    - `apps/api/src/modules/player/conflict-service.ts` contains logic for exactly one active TV player per room
    - `apps/api/src/modules/playback/apply-reconnect-recovery.ts` contains both a near-position resume path and a fallback-to-0 path
    - `apps/api/src/test/player-runtime-contract.test.ts` contains `switch_failed` and `recovery_fallback_start_over`
  </acceptance_criteria>
  <done>Backend player/session semantics exist as stable APIs and persist enough truth for TV runtime, rollback, and reconnect behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Build the TV runtime with dual-video switching, QR states, and productized playback messaging</name>
  <files>apps/tv-player/src/App.tsx, apps/tv-player/src/runtime/player-client.ts, apps/tv-player/src/runtime/use-room-snapshot.ts, apps/tv-player/src/runtime/video-pool.ts, apps/tv-player/src/runtime/switch-controller.ts, apps/tv-player/src/runtime/recovery-controller.ts, apps/tv-player/src/components/PairingQr.tsx, apps/tv-player/src/components/PlaybackStatusBanner.tsx, apps/tv-player/src/screens/IdleScreen.tsx, apps/tv-player/src/screens/PlayingScreen.tsx, apps/tv-player/src/screens/ConflictScreen.tsx</files>
  <read_first>apps/tv-player/src/App.tsx, packages/player-contracts/src/index.ts, .planning/phases/01-media-contract-tv-runtime/01-CONTEXT.md, .planning/phases/01-media-contract-tv-runtime/01-RESEARCH.md, docs/KTV-ARCHITECTURE.md</read_first>
  <action>Replace the placeholder TV app with a real runtime shell. Implement a snapshot client that subscribes to room state and updates local runtime mode. Build `video-pool.ts` around two HTMLVideoElements named `activeVideo` and `standbyVideo`; `switch-controller.ts` must preload the target asset on standby, seek to `resumePositionMs`, attempt `play()`, and only commit the visual swap when the new video is confirmed ready via `requestVideoFrameCallback()` or a `playing` fallback. `IdleScreen.tsx` must render a large QR; `PlayingScreen.tsx` must render a small corner QR plus current song, next song, current vocal mode, and productized status banners; `ConflictScreen.tsx` must tell the user the room already has an active player and must not offer takeover. `PlaybackStatusBanner.tsx` must cover exact user-visible states for `loading`, `recovering`, `switch failed and reverted`, and `resumed from start`.</action>
  <verify>`pnpm --filter @home-ktv/tv-player build`</verify>
  <acceptance_criteria>
    - `apps/tv-player/src/runtime/video-pool.ts` contains both `activeVideo` and `standbyVideo`
    - `apps/tv-player/src/screens/IdleScreen.tsx` contains a large QR render path and `apps/tv-player/src/screens/PlayingScreen.tsx` contains a small/corner QR render path
    - `apps/tv-player/src/components/PlaybackStatusBanner.tsx` contains productized states for switch failure rollback and resume fallback
  </acceptance_criteria>
  <done>The TV app becomes a real room-bound runtime instead of a passive page shell, and its UI matches the agreed idle/playing/conflict behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Add automated coverage for rollback and reconnect edge cases</name>
  <files>apps/tv-player/src/test/switch-runtime.test.tsx, apps/tv-player/src/test/reconnect-recovery.test.tsx</files>
  <read_first>apps/tv-player/src/runtime/switch-controller.ts, apps/tv-player/src/runtime/recovery-controller.ts, .planning/phases/01-media-contract-tv-runtime/01-CONTEXT.md</read_first>
  <action>Create automated tests that simulate: successful original-to-instrumental switch within the same queue entry; switch target play failure causing immediate rollback to the prior asset; reconnect with resumable progress; reconnect that cannot resume and therefore restarts the current song from 0 with a visible fallback state; and receipt of a conflict snapshot that keeps playback disabled. Use deterministic fake snapshot payloads and mocked `play()` Promise resolution/rejection so the tests prove the runtime contract instead of only rendering components.</action>
  <verify>`pnpm --filter @home-ktv/tv-player test -- switch-runtime reconnect-recovery`</verify>
  <acceptance_criteria>
    - `apps/tv-player/src/test/switch-runtime.test.tsx` contains a rollback-on-failure case
    - `apps/tv-player/src/test/reconnect-recovery.test.tsx` contains both a resume-near-position case and a restart-from-0 case
    - test command exits 0
  </acceptance_criteria>
  <done>The riskiest Phase 1 runtime behaviors are covered by repeatable tests instead of only by manual clicking.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>TV Player runtime, backend player/session contract, in-song switch rollback, and reconnect recovery path - launch the API and TV player in the Phase 1 environment and open the TV player in desktop Chrome on the mini PC connected to the TV.</what-built>
  <how-to-verify>Confirm four things on the real TV path: 1) `idle` shows a large QR, 2) `playing` shows a small corner QR plus current/next/vocal mode, 3) switching between a verified original/instrumental pair stays near the prior progress and visibly falls back to the prior mode if forced to fail, 4) refreshing/reconnecting either resumes near the prior point or clearly says it restarted the same song from the beginning.</how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm typecheck`
- [ ] `pnpm --filter @home-ktv/api test -- player-runtime-contract`
- [ ] `pnpm --filter @home-ktv/tv-player build`
- [ ] `pnpm --filter @home-ktv/tv-player test -- switch-runtime reconnect-recovery`
- [ ] Human verification on desktop Chrome / mini PC connected to TV
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- TV runtime can bind to `living-room`, report facts, show the agreed QR/status UI, and handle conflict/recovery states
- Playback-time original/instrumental switching is implemented as a rollback-safe runtime contract rather than a best-effort UI toggle
</success_criteria>

<output>
After completion, create `.planning/phases/01-media-contract-tv-runtime/01-03-SUMMARY.md`
</output>
