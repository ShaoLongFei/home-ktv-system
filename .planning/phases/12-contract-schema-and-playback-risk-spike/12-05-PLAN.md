---
phase: 12-contract-schema-and-playback-risk-spike
plan: 05
type: execute
wave: 4
depends_on: [12-04]
files_modified:
  - apps/tv-player/src/runtime/playback-capability.ts
  - apps/tv-player/src/test/playback-capability.test.ts
  - scripts/real-mv-playback-risk-spike.mjs
  - scripts/real-mv-playback-risk-spike.test.mjs
  - package.json
autonomous: true
requirements: [MEDIA-02, MEDIA-04]
must_haves:
  truths:
    - "Current web playback risk can be inspected without assuming browser audioTracks support."
    - "A failed audio-track capability check keeps current playback unchanged and reports a clear unsupported switching message."
    - "Controlled fixture playback-risk evidence can be generated without adding a browser automation dependency."
    - "Spike reports include compatibilityStatus and structured reasons using the same rules as the Phase 12 compatibility evaluator."
  artifacts:
    - path: "apps/tv-player/src/runtime/playback-capability.ts"
      provides: "Platform-neutral browser capability inspection helper"
      contains: "inspectCurrentWebPlaybackProfile"
    - path: "scripts/real-mv-playback-risk-spike.mjs"
      provides: "Controlled and real sample playback-risk evidence runner"
      contains: "current device does not support audio-track switching"
    - path: "scripts/real-mv-playback-risk-spike.test.mjs"
      provides: "Node tests for script argument/output behavior"
      contains: "controlled"
  key_links:
    - from: "apps/tv-player/src/runtime/playback-capability.ts"
      to: "packages/player-contracts/src/index.ts"
      via: "PlaybackProfile-compatible runtime inspection"
      pattern: "canPlayType|audioTracks"
    - from: "scripts/real-mv-playback-risk-spike.mjs"
      to: "apps/tv-player/src/runtime/playback-capability.ts"
      via: "same result shape and unsupported message"
      pattern: "hasAudioTracksApi"
    - from: "scripts/real-mv-playback-risk-spike.mjs"
      to: "apps/api/src/modules/media/real-mv-compatibility.ts"
      via: "same compatibilityStatus and structured reason rules"
      pattern: "compatibilityStatus|compatibilityReasons"
---

<objective>
Add a playback-risk spike harness that inspects browser container support and audio-track switching capability without wiring full switching into the TV app.

Purpose: make Phase 12 playback risk evidence reproducible before Phase 15 implements runtime-gated switching.
Output: TV capability helper, tests, and a dependency-light CLI script for controlled and real sample checks.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-contract-schema-and-playback-risk-spike/12-CONTEXT.md
@.planning/phases/12-contract-schema-and-playback-risk-spike/12-RESEARCH.md
@packages/player-contracts/src/index.ts
@apps/tv-player/src/runtime/switch-controller.ts
@apps/tv-player/src/runtime/video-pool.ts
@apps/tv-player/src/test/switch-runtime.test.tsx
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add non-invasive TV playback capability helper</name>
  <files>apps/tv-player/src/runtime/playback-capability.ts, apps/tv-player/src/test/playback-capability.test.ts</files>
  <read_first>
    - apps/tv-player/src/runtime/switch-controller.ts
    - apps/tv-player/src/runtime/video-pool.ts
    - apps/tv-player/src/test/switch-runtime.test.tsx
    - packages/player-contracts/src/index.ts
    - .planning/phases/12-contract-schema-and-playback-risk-spike/12-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: helper reports canPlayType, mediaCapabilities support, and whether `audioTracks` exists on a video element.
    - Test 2: helper returns the exact message `current device does not support audio-track switching` when `audioTracks` is unavailable.
    - Test 3: helper does not mutate any active video element or call load/play/pause.
  </behavior>
  <action>
    Create `apps/tv-player/src/runtime/playback-capability.ts` exporting:
    - `export const AUDIO_TRACK_SWITCH_UNSUPPORTED_MESSAGE = "current device does not support audio-track switching";`
    - `export interface CurrentWebPlaybackProfileInspection { canPlayType: "" | "maybe" | "probably"; mediaCapabilitiesSupported: boolean | "unavailable"; hasAudioTracksApi: boolean; audioTrackSwitchMessage: string | null; }`
    - `export async function inspectCurrentWebPlaybackProfile(input: { contentType: string; createVideoElement?: () => HTMLVideoElement; mediaCapabilities?: MediaCapabilities | null; decodingConfig?: MediaDecodingConfiguration; }): Promise<CurrentWebPlaybackProfileInspection>`

    Implementation rules:
    - Create a detached video with `input.createVideoElement?.() ?? document.createElement("video")`.
    - Read `video.canPlayType(input.contentType)` exactly once.
    - Set `hasAudioTracksApi` using `"audioTracks" in video`.
    - If `input.mediaCapabilities` and `input.decodingConfig` exist, call `decodingInfo(input.decodingConfig)` and use its `.supported`; otherwise set `"unavailable"`.
    - Set `audioTrackSwitchMessage` to the exported unsupported message when `hasAudioTracksApi` is false, otherwise null.
    - Do not integrate this helper into `SwitchController` or `DualVideoPool` in Phase 12.
  </action>
  <acceptance_criteria>
    - `test -f apps/tv-player/src/runtime/playback-capability.ts` exits 0.
    - `rg -n 'AUDIO_TRACK_SWITCH_UNSUPPORTED_MESSAGE = "current device does not support audio-track switching"' apps/tv-player/src/runtime/playback-capability.ts` exits 0.
    - `rg -n 'inspectCurrentWebPlaybackProfile' apps/tv-player/src/runtime/playback-capability.ts apps/tv-player/src/test/playback-capability.test.ts` exits 0.
    - `rg -n '"audioTracks" in video' apps/tv-player/src/runtime/playback-capability.ts` exits 0.
    - `rg -n 'new SwitchController|DualVideoPool|\\.play\\(|\\.load\\(|\\.pause\\(' apps/tv-player/src/runtime/playback-capability.ts` exits 1.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/tv-player test -- src/test/playback-capability.test.ts && pnpm -F @home-ktv/tv-player typecheck</automated>
  </verify>
  <done>TV runtime has a tested, non-invasive capability helper that can report audio-track switching risk without changing playback behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add dependency-light playback risk spike script</name>
  <files>scripts/real-mv-playback-risk-spike.mjs, scripts/real-mv-playback-risk-spike.test.mjs, package.json</files>
  <read_first>
    - scripts/tv-visual-check.mjs
    - scripts/tv-visual-check.test.mjs
    - package.json
    - apps/api/src/modules/ingest/media-probe.ts
    - apps/api/src/modules/media/real-mv-compatibility.ts
    - apps/tv-player/src/runtime/playback-capability.ts
    - .planning/phases/12-contract-schema-and-playback-risk-spike/12-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: script `--help` documents `--controlled-only`, `--mkv`, `--mpeg`, and `--output`.
    - Test 2: script fails with a clear message when real sample mode is requested without both sample paths.
    - Test 3: script can write a Markdown report containing `Controlled fixture`, `MKV sample`, `MPEG sample`, `canPlayType`, `hasAudioTracksApi`, `compatibilityStatus`, `compatibilityReasons`, and the unsupported switching message.
  </behavior>
  <action>
    Create `scripts/real-mv-playback-risk-spike.mjs` as a Node built-in script, with no new npm dependencies.

    CLI behavior:
    - `--help` prints usage.
    - `--controlled-only` runs without user sample paths.
    - `--mkv <absolute-or-relative-path>` supplies the real MKV sample.
    - `--mpeg <absolute-or-relative-path>` supplies the real MPG/MPEG sample.
    - `--output <path>` writes a Markdown report.
    - If not `--controlled-only`, require both `--mkv` and `--mpeg`; if either path is missing or not a file, exit nonzero and print `Phase 12 real sample spike requires both --mkv and --mpeg sample files`.

    Evidence behavior:
    - Use `ffprobe -v error -print_format json -show_format -show_streams` for supplied files and controlled fixtures when ffprobe exists.
    - Generate controlled fixture files under a temporary directory when `ffmpeg` exists: one `.mkv` and one `.mpg`. If ffmpeg is unavailable, record `controlled fixture generation skipped: ffmpeg unavailable` and keep script exit 0 in controlled-only mode.
    - Locate Chrome using `$CHROME_BIN` first, then `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`. If unavailable, record `Chrome capability check skipped: Chrome unavailable`.
    - For browser checks, run a temporary HTML file in headless Chrome that reports `canPlayType` for `video/x-matroska` and `video/mpeg`, and `hasAudioTracksApi` using `"audioTracks" in document.createElement("video")`.
    - For each controlled or real sample, evaluate compatibility with the same rules as `apps/api/src/modules/media/real-mv-compatibility.ts`: missing video codec -> `missing-video-codec` error, no audio tracks -> `missing-audio-tracks` error, missing instrumental mapping -> `instrumental-track-unmapped` warning, empty browser `canPlayType` -> `browser-cannot-play-type` error, any error -> `unsupported`, any warning or non-`probably` browser result -> `review_required`, otherwise `playable`.
    - The Markdown report must include `compatibilityStatus` and `compatibilityReasons` for controlled fixtures and for each supplied real sample.
    - Always include the exact message `current device does not support audio-track switching` in the report when `hasAudioTracksApi` is false or unknown.

    Add root package script:
    - `"real-mv:risk-spike": "node scripts/real-mv-playback-risk-spike.mjs"`

    Add `scripts/real-mv-playback-risk-spike.test.mjs` using `node:test` and `node:assert/strict`, mirroring the import-safe/test style used by existing script tests.
  </action>
  <acceptance_criteria>
    - `test -f scripts/real-mv-playback-risk-spike.mjs` exits 0.
    - `test -f scripts/real-mv-playback-risk-spike.test.mjs` exits 0.
    - `rg -n '"real-mv:risk-spike": "node scripts/real-mv-playback-risk-spike.mjs"' package.json` exits 0.
    - `node scripts/real-mv-playback-risk-spike.mjs --help | rg -- '--controlled-only|--mkv|--mpeg|--output'` exits 0.
    - `rg -n 'Phase 12 real sample spike requires both --mkv and --mpeg sample files' scripts/real-mv-playback-risk-spike.mjs scripts/real-mv-playback-risk-spike.test.mjs` exits 0.
    - `rg -n 'compatibilityStatus|compatibilityReasons|browser-cannot-play-type|instrumental-track-unmapped' scripts/real-mv-playback-risk-spike.mjs scripts/real-mv-playback-risk-spike.test.mjs` exits 0.
    - `rg -n 'current device does not support audio-track switching' scripts/real-mv-playback-risk-spike.mjs` exits 0.
    - `rg -n 'playwright|puppeteer' scripts/real-mv-playback-risk-spike.mjs package.json` exits 1.
  </acceptance_criteria>
  <verify>
    <automated>node --test scripts/real-mv-playback-risk-spike.test.mjs && pnpm real-mv:risk-spike -- --controlled-only --output .planning/phases/12-contract-schema-and-playback-risk-spike/12-PLAYBACK-RISK-CONTROLLED.md && rg -n 'Controlled fixture|canPlayType|hasAudioTracksApi|compatibilityStatus|compatibilityReasons' .planning/phases/12-contract-schema-and-playback-risk-spike/12-PLAYBACK-RISK-CONTROLLED.md</automated>
  </verify>
  <done>A reproducible playback-risk spike command exists, creates controlled evidence, and can later run against user-provided MKV and MPG/MPEG samples.</done>
</task>

</tasks>

<verification>
Run `pnpm -F @home-ktv/tv-player test -- src/test/playback-capability.test.ts`, `node --test scripts/real-mv-playback-risk-spike.test.mjs`, and `pnpm real-mv:risk-spike -- --controlled-only --output .planning/phases/12-contract-schema-and-playback-risk-spike/12-PLAYBACK-RISK-CONTROLLED.md`.
</verification>

<success_criteria>
- Browser playback risk is inspected with `canPlayType`, optional MediaCapabilities, and audioTracks feature detection.
- Spike reports include `compatibilityStatus` and structured reasons that match the Phase 12 evaluator rules.
- Switch failure guidance uses the exact message `current device does not support audio-track switching`.
- Full TV switching integration is not implemented in Phase 12.
- The spike harness can produce controlled evidence without new dependencies.
</success_criteria>

<output>
After completion, create `.planning/phases/12-contract-schema-and-playback-risk-spike/12-05-SUMMARY.md`.
</output>
