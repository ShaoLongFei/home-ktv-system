---
phase: 12-contract-schema-and-playback-risk-spike
plan: 02
type: execute
wave: 2
depends_on: [12-01]
files_modified:
  - apps/api/src/db/migrations/0007_real_mv_contracts.sql
  - apps/api/src/db/schema.ts
  - apps/api/src/modules/catalog/repositories/asset-repository.ts
  - apps/api/src/modules/ingest/repositories/import-candidate-repository.ts
  - apps/api/src/test/real-mv-schema.test.ts
  - apps/api/src/test/catalog-contracts.test.ts
autonomous: true
requirements: [MEDIA-01, MEDIA-02, MEDIA-03]
must_haves:
  truths:
    - "Durable catalog rows can store compatibilityStatus, structured reasons, media summary/provenance, trackRoles, and playbackProfile."
    - "Import candidate file rows can carry the same normalized real-MV facts before admission."
    - "Existing ready and verified assets remain playable after the additive migration."
  artifacts:
    - path: "apps/api/src/db/migrations/0007_real_mv_contracts.sql"
      provides: "Additive PostgreSQL migration for real-MV contract fields"
      contains: "compatibility_status"
    - path: "apps/api/src/db/schema.ts"
      provides: "Row interfaces, enum values, and schemaSql mirror"
      contains: "compatibilityStatus"
    - path: "apps/api/src/modules/catalog/repositories/asset-repository.ts"
      provides: "Asset row mapping for new real-MV fields"
      contains: "trackRoles"
    - path: "apps/api/src/modules/ingest/repositories/import-candidate-repository.ts"
      provides: "Candidate file row mapping for new real-MV fields"
      contains: "mediaInfoSummary"
  key_links:
    - from: "apps/api/src/db/migrations/0007_real_mv_contracts.sql"
      to: "apps/api/src/db/schema.ts"
      via: "matching column names and enumValues.compatibilityStatus"
      pattern: "compatibility_status"
    - from: "apps/api/src/db/schema.ts"
      to: "apps/api/src/modules/catalog/repositories/asset-repository.ts"
      via: "AssetRow fields mapped by mapAssetRow"
      pattern: "track_roles"
---

<objective>
Persist the Phase 12 real-MV contract fields in PostgreSQL and expose them through existing repository mappers.

Purpose: make the shared contract durable without building full scanner sidecar behavior or Admin review UI.
Output: additive migration, schema mirror, repository row mapping, and schema/mapping tests.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-contract-schema-and-playback-risk-spike/12-CONTEXT.md
@.planning/phases/12-contract-schema-and-playback-risk-spike/12-RESEARCH.md
@packages/domain/src/index.ts
@apps/api/src/db/schema.ts
@apps/api/src/db/migrations/0002_library_ingest_admin.sql
@apps/api/src/modules/catalog/repositories/asset-repository.ts
@apps/api/src/modules/ingest/repositories/import-candidate-repository.ts
</context>

<interfaces>
New domain contracts from Plan 12-01 that this plan consumes:

```typescript
export type CompatibilityStatus = "unknown" | "review_required" | "playable" | "unsupported";
export interface TrackRef { index: number; id: string; label: string; }
export interface TrackRoles { original: TrackRef | null; instrumental: TrackRef | null; }
export interface MediaInfoSummary {
  container: string | null;
  durationMs: number | null;
  videoCodec: string | null;
  resolution: { width: number; height: number } | null;
  fileSizeBytes: number;
  audioTracks: readonly AudioTrackSummary[];
}
export interface PlaybackProfile {
  kind: "separate_asset_pair" | "single_file_audio_tracks";
  container: string | null;
  videoCodec: string | null;
  audioCodecs: readonly string[];
  requiresAudioTrackSelection: boolean;
}
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add additive SQL migration for real-MV contract fields</name>
  <files>apps/api/src/db/migrations/0007_real_mv_contracts.sql, apps/api/src/test/real-mv-schema.test.ts</files>
  <read_first>
    - apps/api/src/db/migrations/0002_library_ingest_admin.sql
    - apps/api/src/db/migrations/0006_online_candidates.sql
    - apps/api/src/db/schema.ts
    - apps/api/src/test/library-ingest-schema.test.ts
    - .planning/phases/12-contract-schema-and-playback-risk-spike/12-CONTEXT.md
  </read_first>
  <behavior>
    - Test 1: migration contains the exact compatibility status check values.
    - Test 2: migration adds JSONB fields for reasons, summary, provenance, trackRoles, and playback profile to `assets`.
    - Test 3: migration adds the same candidate-facing fields to `import_candidate_files`.
    - Test 4: migration backfills existing ready and verified assets to `playable`.
  </behavior>
  <action>
    Create `apps/api/src/db/migrations/0007_real_mv_contracts.sql` with only additive SQL:
    - `ALTER TABLE assets ADD COLUMN IF NOT EXISTS compatibility_status text NOT NULL DEFAULT 'unknown' CHECK (compatibility_status IN ('unknown', 'review_required', 'playable', 'unsupported'));`
    - `ALTER TABLE assets ADD COLUMN IF NOT EXISTS compatibility_reasons jsonb NOT NULL DEFAULT '[]'::jsonb;`
    - `ALTER TABLE assets ADD COLUMN IF NOT EXISTS media_info_summary jsonb NOT NULL DEFAULT '{"container":null,"durationMs":null,"videoCodec":null,"resolution":null,"fileSizeBytes":0,"audioTracks":[]}'::jsonb;`
    - `ALTER TABLE assets ADD COLUMN IF NOT EXISTS media_info_provenance jsonb NOT NULL DEFAULT '{"source":"unknown","sourceVersion":null,"probedAt":null,"importedFrom":null}'::jsonb;`
    - `ALTER TABLE assets ADD COLUMN IF NOT EXISTS track_roles jsonb NOT NULL DEFAULT '{"original":null,"instrumental":null}'::jsonb;`
    - `ALTER TABLE assets ADD COLUMN IF NOT EXISTS playback_profile jsonb NOT NULL DEFAULT '{"kind":"separate_asset_pair","container":null,"videoCodec":null,"audioCodecs":[],"requiresAudioTrackSelection":false}'::jsonb;`
    - Add the same six columns to `import_candidate_files`, with identical shaped JSON defaults and the same compatibility check.
    - Add `CREATE INDEX IF NOT EXISTS assets_compatibility_status_idx ON assets(compatibility_status, status);`
    - Add `CREATE INDEX IF NOT EXISTS import_candidate_files_compatibility_status_idx ON import_candidate_files(compatibility_status, selected);`
    - Add a backfill:
      `UPDATE assets SET compatibility_status = 'playable', compatibility_reasons = '[]'::jsonb WHERE status = 'ready' AND switch_quality_status = 'verified' AND compatibility_status = 'unknown';`

    Add `apps/api/src/test/real-mv-schema.test.ts` assertions that read this migration and check for exact column names, exact status values, both index names, and the backfill predicate above. Do not add Android-specific columns.
  </action>
  <acceptance_criteria>
    - `test -f apps/api/src/db/migrations/0007_real_mv_contracts.sql` exits 0.
    - `rg -n "compatibility_status IN \\('unknown', 'review_required', 'playable', 'unsupported'\\)" apps/api/src/db/migrations/0007_real_mv_contracts.sql` exits 0.
    - `rg -n 'media_info_summary jsonb NOT NULL DEFAULT .*"audioTracks":\\[\\]' apps/api/src/db/migrations/0007_real_mv_contracts.sql` exits 0.
    - `rg -n 'media_info_provenance jsonb NOT NULL DEFAULT .*"source":"unknown"' apps/api/src/db/migrations/0007_real_mv_contracts.sql` exits 0.
    - `rg -n 'track_roles jsonb NOT NULL DEFAULT .*"original":null.*"instrumental":null' apps/api/src/db/migrations/0007_real_mv_contracts.sql` exits 0.
    - `rg -n 'playback_profile jsonb NOT NULL DEFAULT' apps/api/src/db/migrations/0007_real_mv_contracts.sql` exits 0.
    - `rg -n 'UPDATE assets SET compatibility_status = '\\''playable'\\''' apps/api/src/db/migrations/0007_real_mv_contracts.sql` exits 0.
    - `rg -n 'android|media3|exo' apps/api/src/db/migrations/0007_real_mv_contracts.sql apps/api/src/test/real-mv-schema.test.ts` exits 1.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/real-mv-schema.test.ts</automated>
  </verify>
  <done>Migration 0007 adds durable real-MV fields to formal assets and import candidate files, with exact status values and legacy playable backfill.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Mirror schema fields and enum values in TypeScript rows</name>
  <files>apps/api/src/db/schema.ts, apps/api/src/test/real-mv-schema.test.ts</files>
  <read_first>
    - apps/api/src/db/schema.ts
    - apps/api/src/db/migrations/0007_real_mv_contracts.sql
    - apps/api/src/test/control-session-schema.test.ts
    - apps/api/src/test/catalog-admission.test.ts
  </read_first>
  <behavior>
    - Test 1: `enumValues.compatibilityStatus` equals the four locked values.
    - Test 2: `AssetRow`, `ImportCandidateFileRow`, and `ImportCandidateFileDetailRow` expose the six new real-MV columns.
    - Test 3: `schemaSql` includes the new `assets` columns and the `assets_compatibility_status_idx` index.
  </behavior>
  <action>
    Update `apps/api/src/db/schema.ts`:
    - Add `compatibilityStatus: ["unknown", "review_required", "playable", "unsupported"]` to `enumValues`.
    - Add these fields to `AssetRow`: `compatibility_status: string; compatibility_reasons: Record<string, unknown>[]; media_info_summary: MediaInfoSummary; media_info_provenance: MediaInfoProvenance; track_roles: TrackRoles; playback_profile: PlaybackProfile;`
    - Add the same six fields to `ImportCandidateFileRow`.
    - Add the same six fields to `ImportCandidateFileDetailRow`.
    - Mirror the new `assets` columns inside the `CREATE TABLE IF NOT EXISTS assets` block in `schemaSql`.
    - Add `CREATE INDEX IF NOT EXISTS assets_compatibility_status_idx ON assets(compatibility_status, status);` to `schemaSql`.

    Do not attempt to retrofit the whole import staging schema into `schemaSql` in this task; only mirror the real-MV `assets` columns and row interface fields needed by current in-memory/schema tests.
  </action>
  <acceptance_criteria>
    - `rg -n 'compatibilityStatus: \\["unknown", "review_required", "playable", "unsupported"\\]' apps/api/src/db/schema.ts` exits 0.
    - `rg -n 'compatibility_status: string' apps/api/src/db/schema.ts` exits 0.
    - `rg -n 'compatibility_reasons: Record<string, unknown>\\[\\]' apps/api/src/db/schema.ts` exits 0.
    - `rg -n 'media_info_summary: MediaInfoSummary' apps/api/src/db/schema.ts` exits 0.
    - `rg -n 'media_info_provenance: MediaInfoProvenance' apps/api/src/db/schema.ts` exits 0.
    - `rg -n 'track_roles: TrackRoles' apps/api/src/db/schema.ts` exits 0.
    - `rg -n 'playback_profile: PlaybackProfile' apps/api/src/db/schema.ts` exits 0.
    - `rg -n 'assets_compatibility_status_idx' apps/api/src/db/schema.ts apps/api/src/test/real-mv-schema.test.ts` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/real-mv-schema.test.ts && pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <done>Schema row interfaces and schemaSql expose the same real-MV asset fields that migration 0007 adds.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Map real-MV fields through asset and candidate repositories</name>
  <files>apps/api/src/modules/catalog/repositories/asset-repository.ts, apps/api/src/modules/ingest/repositories/import-candidate-repository.ts, apps/api/src/test/catalog-contracts.test.ts</files>
  <read_first>
    - apps/api/src/modules/catalog/repositories/asset-repository.ts
    - apps/api/src/modules/ingest/repositories/import-candidate-repository.ts
    - apps/api/src/test/catalog-contracts.test.ts
    - packages/domain/src/index.ts
  </read_first>
  <behavior>
    - Test 1: `mapAssetRow` returns `compatibilityStatus`, `compatibilityReasons`, `mediaInfoSummary`, `mediaInfoProvenance`, `trackRoles`, and `playbackProfile`.
    - Test 2: asset repository SELECT statements include the six new columns.
    - Test 3: `mapImportCandidateFileDetailRow` returns candidate file real-MV fields without exposing raw probe payload as the normalized summary.
    - Test 4: repository mapping helpers never expose `{}` as `mediaInfoSummary`, `mediaInfoProvenance`, or `trackRoles`; empty or malformed objects are normalized to the same shaped defaults used by migration 0007.
  </behavior>
  <action>
    Update `apps/api/src/modules/catalog/repositories/asset-repository.ts`:
    - Import `CompatibilityReason`, `CompatibilityStatus`, `MediaInfoProvenance`, `MediaInfoSummary`, `PlaybackProfile`, and `TrackRoles`.
    - In `mapAssetRow`, map snake_case row fields to camelCase fields on `Asset`.
    - Add small normalization helpers so `{}` or malformed JSONB values become valid shaped contracts:
      - `normalizeMediaInfoSummary(value)` returns `{ container: null, durationMs: null, videoCodec: null, resolution: null, fileSizeBytes: 0, audioTracks: [] }` when required fields are absent.
      - `normalizeMediaInfoProvenance(value)` returns `{ source: "unknown", sourceVersion: null, probedAt: null, importedFrom: null }` when required fields are absent.
      - `normalizeTrackRoles(value)` returns `{ original: null, instrumental: null }` when required fields are absent.
      - `normalizePlaybackProfile(value)` returns `{ kind: "separate_asset_pair", container: null, videoCodec: null, audioCodecs: [], requiresAudioTrackSelection: false }` when required fields are absent.
    - Extend all `SELECT id, song_id, ... FROM assets` statements to include `compatibility_status, compatibility_reasons, media_info_summary, media_info_provenance, track_roles, playback_profile`.
    - Extend `UpdateFormalAssetInput` and `updateFormalAsset` to support the same six fields using JSONB casts for object/array fields.

    Update `apps/api/src/modules/ingest/repositories/import-candidate-repository.ts`:
    - Extend `UpsertImportCandidateWithFilesInput.files[]` with optional `compatibilityStatus`, `compatibilityReasons`, `mediaInfoSummary`, `mediaInfoProvenance`, `trackRoles`, and `playbackProfile`.
    - Map those fields in `mapImportCandidateFileDetailRow`.
    - Extend the insert/upsert SQL for `import_candidate_files` with the six columns and defaults: status defaults to `"unknown"`, reasons to `[]`, summary to `{ container: null, durationMs: null, videoCodec: null, resolution: null, fileSizeBytes: 0, audioTracks: [] }`, provenance to `{ source: "unknown", sourceVersion: null, probedAt: null, importedFrom: null }`, trackRoles to `{ original: null, instrumental: null }`, and playbackProfile to `{ kind: "separate_asset_pair", container: null, videoCodec: null, audioCodecs: [], requiresAudioTrackSelection: false }` when omitted.

    Update `apps/api/src/test/catalog-contracts.test.ts` with mapping tests that use a single real-MV asset and a single candidate file containing `trackRoles: { original: { index: 0, id: "0x1100", label: "Original vocal" }, instrumental: { index: 1, id: "0x1101", label: "Instrumental" } }`.
  </action>
  <acceptance_criteria>
    - `rg -n 'compatibilityStatus: row.compatibility_status as CompatibilityStatus' apps/api/src/modules/catalog/repositories/asset-repository.ts` exits 0.
    - `rg -n 'trackRoles: normalizeTrackRoles\\(row.track_roles\\)' apps/api/src/modules/catalog/repositories/asset-repository.ts` exits 0.
    - `rg -n 'compatibility_status, compatibility_reasons, media_info_summary, media_info_provenance, track_roles, playback_profile' apps/api/src/modules/catalog/repositories/asset-repository.ts` exits 0.
    - `rg -n 'compatibilityStatus\\?: CompatibilityStatus' apps/api/src/modules/ingest/repositories/import-candidate-repository.ts` exits 0.
    - `rg -n 'trackRoles: normalizeTrackRoles\\(row.track_roles\\)' apps/api/src/modules/ingest/repositories/import-candidate-repository.ts` exits 0.
    - `rg -n 'normalizeMediaInfoSummary\\(\\{\\}\\)|normalizeTrackRoles\\(\\{\\}\\)' apps/api/src/test/catalog-contracts.test.ts` exits 0.
    - `rg -n 'single real-MV asset' apps/api/src/test/catalog-contracts.test.ts` exits 0.
    - `rg -n 'originalAssetId|instrumentalAssetId|androidProfile' apps/api/src/modules/catalog/repositories/asset-repository.ts apps/api/src/modules/ingest/repositories/import-candidate-repository.ts apps/api/src/test/catalog-contracts.test.ts` exits 1.
  </acceptance_criteria>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/catalog-contracts.test.ts src/test/real-mv-schema.test.ts && pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <done>Existing repositories can persist and return normalized real-MV compatibility/media/track fields without introducing duplicate asset identities.</done>
</task>

</tasks>

<verification>
Run `pnpm -F @home-ktv/api test -- src/test/real-mv-schema.test.ts src/test/catalog-contracts.test.ts` and `pnpm -F @home-ktv/api typecheck`.
</verification>

<success_criteria>
- Migration 0007 is additive and contains the exact Phase 12 compatibility status values.
- Formal assets and import candidate files have durable fields for reasons, summary, provenance, trackRoles, and playbackProfile.
- Repository mapping uses one asset row with `trackRoles`; no two-logical-asset fields are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/12-contract-schema-and-playback-risk-spike/12-02-SUMMARY.md`.
</output>
