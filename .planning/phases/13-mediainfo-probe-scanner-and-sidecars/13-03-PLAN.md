---
phase: 13-mediainfo-probe-scanner-and-sidecars
plan: 03
type: execute
wave: 3
depends_on: ["13-02"]
files_modified:
  - apps/api/src/modules/ingest/import-scanner.ts
  - apps/api/src/modules/ingest/candidate-builder.ts
  - apps/api/src/modules/media/real-mv-compatibility.ts
  - apps/api/src/test/import-scanner.test.ts
  - apps/api/src/test/real-mv-media-contracts.test.ts
autonomous: true
requirements:
  - SCAN-01
  - SCAN-03
  - SCAN-04
  - SCAN-05
requirements_addressed: [SCAN-01, SCAN-03, SCAN-04, SCAN-05]
must_haves:
  truths:
    - "CandidateBuilder creates one candidate for one real MV file."
    - "Discovered song.json content is parsed into probePayload.realMv.sidecarMetadata or scannerReasons before CandidateBuilder runs."
    - "Real MV candidate metadata is prefilled from the merged metadata draft and preserves provenance/conflicts in candidateMeta."
    - "Real MV candidate file details are marked dual-track-video with single_file_audio_tracks playback profile."
    - "Uncertain track roles and runtime support produce review_required compatibility rather than false playable claims."
  key_links:
    - "ImportScanner reads realMv.sidecars.songJson, calls parseRealMvSidecarJson, and stores sidecarMetadata plus scannerReasons in probePayload.realMv."
    - "CandidateBuilder calls buildRealMvMetadataDraft with mediaInfoSummary, parseRealMvFilename output, sidecarMetadata, and scannerReasons."
    - "CandidateBuilder writes candidateMeta.realMv.metadataSources, metadataConflicts, sidecarMetadata, scannerReasons, sidecars, trackRoles, and playbackProfile into upsertCandidateWithFiles."
    - "real-mv-compatibility keeps scanner-origin runtime uncertainty at review_required unless later TV runtime evidence proves playability."
  artifacts:
    - path: "apps/api/src/modules/ingest/candidate-builder.ts"
      provides: "Single-file real MV candidate construction"
      contains: "single_file_real_mv"
---

<objective>
Integrate real MV scanner facts into import candidates so the Admin receives stable, reviewable single-file candidates.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-mediainfo-probe-scanner-and-sidecars/13-CONTEXT.md
@.planning/phases/13-mediainfo-probe-scanner-and-sidecars/13-RESEARCH.md
@apps/api/src/modules/ingest/import-scanner.ts
@apps/api/src/modules/ingest/candidate-builder.ts
@apps/api/src/modules/ingest/real-mv-sidecars.ts
@apps/api/src/modules/ingest/real-mv-metadata.ts
@apps/api/src/modules/media/real-mv-compatibility.ts
@apps/api/src/test/import-scanner.test.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire discovered song.json into real MV probe payloads</name>
  <files>apps/api/src/modules/ingest/import-scanner.ts, apps/api/src/test/import-scanner.test.ts</files>
  <read_first>
    - apps/api/src/modules/ingest/import-scanner.ts
    - apps/api/src/modules/ingest/real-mv-sidecars.ts
    - apps/api/src/modules/ingest/real-mv-metadata.ts
    - apps/api/src/test/import-scanner.test.ts
  </read_first>
  <action>
    Add failing scanner integration tests proving:
    - for `relativePath: "关喆-想你的夜.mkv"` with adjacent `关喆-想你的夜.song.json`, `upsertDiscoveredFile` receives `probePayload.realMv.sidecarMetadata.title === "想你的夜"` and `probePayload.realMv.sidecarMetadata.artistName === "关喆"`;
    - for invalid adjacent `关喆-想你的夜.song.json`, scan does not throw, `probePayload.realMv.scannerReasons` includes `code: "sidecar-json-invalid"`, and `candidateBuilder.buildFromImportFiles` is still called with the changed real MV import file;
    - the persisted `probePayload.realMv.sidecars.songJson.relativePath` is root-relative and no absolute filesystem path is serialized.

    Modify `ImportScanner` so real MV media files:
    - import `parseRealMvSidecarJson` from `apps/api/src/modules/ingest/real-mv-metadata.ts`;
    - when `findRealMvSidecars` returns `songJson`, read the sidecar using `fs.readFile` with UTF-8;
    - on parse ok, attach `sidecarMetadata` under `probePayload.realMv`;
    - on parse invalid or read failure, append scanner reasons under `probePayload.realMv.scannerReasons`;
    - preserve `probePayload.realMv.sidecars` from Plan 13-01 and merge it with the existing `probeMediaFile` summary payload.

    The final scanner payload shape for a valid sidecar must include:
    ```ts
    {
      realMv: {
        mediaKind: "single_file_real_mv",
        sidecars: { songJson: { relativePath: "关喆-想你的夜.song.json" } },
        sidecarMetadata: { title: "想你的夜", artistName: "关喆" },
        scannerReasons: []
      }
    }
    ```
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/import-scanner.test.ts</automated>
    <automated>pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/modules/ingest/import-scanner.ts` contains `parseRealMvSidecarJson`.
    - `apps/api/src/modules/ingest/import-scanner.ts` contains `sidecarMetadata`.
    - `apps/api/src/modules/ingest/import-scanner.ts` contains `sidecar-json-invalid`.
    - `apps/api/src/test/import-scanner.test.ts` contains `关喆-想你的夜.song.json`.
    - `apps/api/src/test/import-scanner.test.ts` contains `sidecarMetadata`.
    - `pnpm -F @home-ktv/api test -- src/test/import-scanner.test.ts` exits 0.
  </acceptance_criteria>
  <done>Scanner output carries parsed sidecar song metadata into the candidate-building boundary.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build one candidate per real MV media file</name>
  <files>apps/api/src/modules/ingest/candidate-builder.ts, apps/api/src/test/import-scanner.test.ts</files>
  <read_first>
    - apps/api/src/modules/ingest/candidate-builder.ts
    - apps/api/src/modules/ingest/real-mv-metadata.ts
    - apps/api/src/test/import-scanner.test.ts
  </read_first>
  <action>
    Add failing tests for `CandidateBuilder` using one import file:
    - `relativePath: "关喆-想你的夜(MTV)-国语-流行.mkv"`;
    - `probePayload.realMv.mediaKind: "single_file_real_mv"`;
    - `probePayload.realMv.sidecarMetadata` containing at least one supplemental field, e.g. `{ "searchHints": ["xiang ni de ye"] }`;
    - `probePayload.mediaInfoSummary.audioTracks` with two tracks.

    Expected upsert:
    - exactly one candidate;
    - candidate title is `想你的夜`;
    - candidate artistName is `关喆`;
    - `candidate.searchHints` contains `"xiang ni de ye"` when sidecar metadata provides it;
    - `candidate.candidateMeta.realMv.groupKey` contains the media relative path;
    - `candidate.candidateMeta.realMv.sidecarMetadata.searchHints` contains `"xiang ni de ye"`;
    - file has `proposedAssetKind: "dual-track-video"`;
    - file has `proposedVocalMode: "dual"`;
    - file has `selected: true`.

    Modify `CandidateBuilder` so real MV media files branch away from existing multi-file original/instrumental grouping. For real MV media, call `buildRealMvMetadataDraft` with `mediaInfoSummary`, `parseRealMvFilename(relativePath)`, `probePayload.realMv.sidecarMetadata`, and `probePayload.realMv.scannerReasons`. Keep existing grouping behavior for `.mp4` original/instrumental demo files.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/import-scanner.test.ts</automated>
    <automated>pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/modules/ingest/candidate-builder.ts` contains `single_file_real_mv`.
    - `apps/api/src/modules/ingest/candidate-builder.ts` contains `buildRealMvMetadataDraft`.
    - `apps/api/src/modules/ingest/candidate-builder.ts` contains `sidecarMetadata`.
    - `apps/api/src/modules/ingest/candidate-builder.ts` contains `dual-track-video`.
    - `apps/api/src/test/import-scanner.test.ts` contains `关喆-想你的夜`.
    - `apps/api/src/test/import-scanner.test.ts` contains `xiang ni de ye`.
    - `apps/api/src/test/import-scanner.test.ts` contains `proposedVocalMode: \"dual\"` or `proposedVocalMode: "dual"`.
    - `pnpm -F @home-ktv/api test -- src/test/import-scanner.test.ts` exits 0.
  </acceptance_criteria>
  <done>One real MV file becomes one import candidate.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Populate playback profile, track roles, and compatibility</name>
  <files>apps/api/src/modules/ingest/candidate-builder.ts, apps/api/src/modules/media/real-mv-compatibility.ts, apps/api/src/test/import-scanner.test.ts, apps/api/src/test/real-mv-media-contracts.test.ts</files>
  <read_first>
    - apps/api/src/modules/media/real-mv-compatibility.ts
    - apps/api/src/test/real-mv-media-contracts.test.ts
    - apps/api/src/modules/ingest/candidate-builder.ts
    - packages/domain/src/index.ts
  </read_first>
  <action>
    Add failing tests proving:
    - two audio tracks with labels `Original vocal` and `Instrumental` infer `trackRoles.original.index === 0` and `trackRoles.instrumental.index === 1`;
    - two unlabeled audio tracks leave roles null and set `compatibilityStatus: "review_required"`;
    - `playbackProfile` for real MV uses `kind: "single_file_audio_tracks"`, `requiresAudioTrackSelection: true`, and `audioCodecs` from `MediaInfoSummary.audioTracks`;
    - scanner does not call `currentWebCanPlayType: "probably"`; unknown runtime support stays review-required.

    Add or extend helper(s):
    - `inferTrackRolesFromRealMv(input: { mediaInfoSummary; sidecarTrackRoles? }): TrackRoles`;
    - `buildSingleFileAudioTrackPlaybackProfile(mediaInfoSummary): PlaybackProfile`;
    - compatibility input for scanner should use `currentWebCanPlayType: "unknown"` unless a later phase supplies runtime evidence.

    Feed these values into `upsertCandidateWithFiles`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/import-scanner.test.ts src/test/real-mv-media-contracts.test.ts</automated>
    <automated>pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/modules/ingest/candidate-builder.ts` contains `single_file_audio_tracks`.
    - `apps/api/src/modules/ingest/candidate-builder.ts` contains `requiresAudioTrackSelection`.
    - `apps/api/src/modules/ingest/candidate-builder.ts` contains `Instrumental`.
    - `apps/api/src/test/real-mv-media-contracts.test.ts` contains `currentWebCanPlayType: \"unknown\"` or `currentWebCanPlayType: "unknown"`.
    - `pnpm -F @home-ktv/api test -- src/test/import-scanner.test.ts src/test/real-mv-media-contracts.test.ts` exits 0.
  </acceptance_criteria>
  <done>Real MV candidates carry platform-neutral playback intent without overclaiming browser support.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Preserve provenance, conflicts, sidecar data, and retry reasons in candidateMeta</name>
  <files>apps/api/src/modules/ingest/candidate-builder.ts, apps/api/src/test/import-scanner.test.ts</files>
  <read_first>
    - apps/api/src/modules/ingest/candidate-builder.ts
    - apps/api/src/modules/ingest/real-mv-metadata.ts
    - apps/api/src/modules/ingest/repositories/import-candidate-repository.ts
  </read_first>
  <action>
    Add failing tests proving `upsertCandidateWithFiles` receives:
    - `candidate.candidateMeta.realMv.metadataSources`;
    - `candidate.candidateMeta.realMv.metadataConflicts`;
    - `candidate.candidateMeta.realMv.sidecarMetadata`;
    - `candidate.candidateMeta.realMv.scannerReasons`;
    - `candidate.candidateMeta.realMv.sidecars.cover.relativePath` when a cover exists.

    Ensure unstable or probe-failed real MV files still create/update a visible candidate with:
    - `status: "review_required"` when scanner reasons exist;
    - file compatibility reason copied from scanner reasons;
    - no thrown error for invalid sidecar JSON;
    - `sidecar-json-invalid` visible in `candidate.candidateMeta.realMv.scannerReasons`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/import-scanner.test.ts</automated>
    <automated>pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/modules/ingest/candidate-builder.ts` contains `metadataSources`.
    - `apps/api/src/modules/ingest/candidate-builder.ts` contains `metadataConflicts`.
    - `apps/api/src/modules/ingest/candidate-builder.ts` contains `sidecarMetadata`.
    - `apps/api/src/modules/ingest/candidate-builder.ts` contains `scannerReasons`.
    - `apps/api/src/test/import-scanner.test.ts` contains `sidecar-json-invalid`.
    - `apps/api/src/test/import-scanner.test.ts` contains `sidecars`.
    - `pnpm -F @home-ktv/api test -- src/test/import-scanner.test.ts` exits 0.
  </acceptance_criteria>
  <done>Admin-facing candidates retain the evidence needed for review and retry.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `pnpm -F @home-ktv/api test -- src/test/import-scanner.test.ts src/test/real-mv-media-contracts.test.ts`
- [ ] `pnpm -F @home-ktv/api typecheck`
</verification>

<success_criteria>
- SCAN-01 produces one review candidate per real MV media file.
- SCAN-03 and SCAN-04 metadata is visible in candidate state.
- SCAN-05 retry reasons do not prevent candidate reconciliation.
- No formal catalog admission or playback integration enters this plan.
</success_criteria>

<output>
After completion, create `.planning/phases/13-mediainfo-probe-scanner-and-sidecars/13-03-SUMMARY.md`.
</output>
