---
phase: 13-mediainfo-probe-scanner-and-sidecars
plan: 02
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - apps/api/src/modules/ingest/real-mv-metadata.ts
  - apps/api/src/test/real-mv-metadata.test.ts
autonomous: true
requirements:
  - SCAN-03
  - SCAN-04
  - SCAN-05
requirements_addressed: [SCAN-03, SCAN-04, SCAN-05]
must_haves:
  truths:
    - "Same-stem song.json is parsed with a narrow schema and invalid JSON becomes scanner warnings rather than scan failure."
    - "Filename metadata can prefill common real MV names without aggressive guessing."
    - "Candidate metadata preserves per-field source provenance and conflicts for Admin review."
  key_links:
    - "parseRealMvSidecarJson returns sidecarMetadata or scanner CompatibilityReason values that the scanner will place under probePayload.realMv."
    - "parseRealMvFilename produces conservative filename metadata with fallback confidence so sidecar song.json can fill weak filename gaps."
    - "buildRealMvMetadataDraft consumes MediaInfo tags, filename metadata, sidecarMetadata, and sidecar parse reasons before CandidateBuilder persists candidateMeta.realMv."
  artifacts:
    - path: "apps/api/src/modules/ingest/real-mv-metadata.ts"
      provides: "Sidecar schema, filename parser, metadata merge, provenance, and conflict helpers"
      contains: "buildRealMvMetadataDraft"
---

<objective>
Create pure metadata helpers for sidecar `song.json`, filename fallback, provenance, and conflict preservation.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-mediainfo-probe-scanner-and-sidecars/13-CONTEXT.md
@.planning/phases/13-mediainfo-probe-scanner-and-sidecars/13-RESEARCH.md
@apps/api/src/modules/ingest/real-mv-sidecars.ts
@apps/api/src/modules/ingest/candidate-builder.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add sidecar song.json schema and parser</name>
  <files>apps/api/src/modules/ingest/real-mv-metadata.ts, apps/api/src/test/real-mv-metadata.test.ts</files>
  <read_first>
    - packages/domain/src/index.ts
    - apps/api/src/modules/ingest/real-mv-sidecars.ts
    - .planning/phases/13-mediainfo-probe-scanner-and-sidecars/13-RESEARCH.md
  </read_first>
  <action>
    Create `apps/api/src/test/real-mv-metadata.test.ts` with failing tests for `parseRealMvSidecarJson`.

    Required accepted shape:
    ```json
    {
      "title": "想你的夜",
      "artistName": "关喆",
      "language": "mandarin",
      "genre": ["pop"],
      "tags": ["mv"],
      "aliases": ["miss you tonight"],
      "searchHints": ["xiang ni de ye"],
      "releaseYear": 2012,
      "trackRoles": {
        "original": 0,
        "instrumental": 1
      }
    }
    ```

    Create `apps/api/src/modules/ingest/real-mv-metadata.ts` exporting:
    - `RealMvSidecarMetadataSchema`;
    - `parseRealMvSidecarJson(raw: string): { status: "ok"; metadata: RealMvSidecarMetadata } | { status: "invalid"; reasons: CompatibilityReason[] }`.

    Invalid JSON or invalid field types must return `status: "invalid"` with a reason:
    - `code: "sidecar-json-invalid"` for JSON parse failure;
    - `code: "sidecar-schema-invalid"` for schema failure;
    - `severity: "warning"`;
    - `source: "scanner"`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/real-mv-metadata.test.ts</automated>
    <automated>pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/modules/ingest/real-mv-metadata.ts` contains `RealMvSidecarMetadataSchema`.
    - `apps/api/src/modules/ingest/real-mv-metadata.ts` contains `sidecar-json-invalid`.
    - `apps/api/src/modules/ingest/real-mv-metadata.ts` contains `sidecar-schema-invalid`.
    - `apps/api/src/test/real-mv-metadata.test.ts` contains `想你的夜`.
    - `pnpm -F @home-ktv/api test -- src/test/real-mv-metadata.test.ts` exits 0.
  </acceptance_criteria>
  <done>Sidecar metadata can be parsed safely without failing the scan.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add conservative filename parser</name>
  <files>apps/api/src/modules/ingest/real-mv-metadata.ts, apps/api/src/test/real-mv-metadata.test.ts</files>
  <read_first>
    - apps/api/src/modules/ingest/candidate-builder.ts
    - apps/api/src/modules/catalog/search-normalization.ts
    - apps/api/src/test/real-mv-metadata.test.ts
  </read_first>
  <action>
    Add failing tests for `parseRealMvFilename`.

    Required cases:
    - `关喆-想你的夜(MTV)-国语-流行.mkv` returns `artistName: "关喆"`, `title: "想你的夜"`, `language: "mandarin"`, `tags` or `genre` containing `"流行"`;
    - `蔡依林-BECAUSE OF YOU(演唱会)-国语-流行.mpg` returns artist `蔡依林` and title `BECAUSE OF YOU`;
    - `unknown-file-name.mkv` returns only a conservative title fallback and does not invent artist/language.

    Implement `parseRealMvFilename(relativePath: string): FilenameMetadataDraft`.

    Parsing rules:
    - strip extension;
    - remove parenthesized display/version markers from title only when they appear after the title;
    - split on `-` into at most artist/title/language/genre style parts;
    - map `国语` and `普通话` to `mandarin`, `粤语` to `cantonese`;
    - do not romanize, translate, or infer aliases.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/real-mv-metadata.test.ts</automated>
    <automated>pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/modules/ingest/real-mv-metadata.ts` contains `parseRealMvFilename`.
    - `apps/api/src/modules/ingest/real-mv-metadata.ts` contains `国语`.
    - `apps/api/src/modules/ingest/real-mv-metadata.ts` contains `cantonese`.
    - `apps/api/src/test/real-mv-metadata.test.ts` contains `BECAUSE OF YOU`.
    - `pnpm -F @home-ktv/api test -- src/test/real-mv-metadata.test.ts` exits 0.
  </acceptance_criteria>
  <done>Common local MV filenames can prefill review metadata conservatively.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Merge metadata with provenance and conflicts</name>
  <files>apps/api/src/modules/ingest/real-mv-metadata.ts, apps/api/src/test/real-mv-metadata.test.ts</files>
  <read_first>
    - apps/api/src/modules/ingest/media-probe.ts
    - apps/api/src/modules/ingest/real-mv-metadata.ts
    - packages/domain/src/index.ts
  </read_first>
  <action>
    Add failing tests for `buildRealMvMetadataDraft`.

    Required behavior:
    - technical fields from `MediaInfoSummary` are always represented as source `mediainfo`;
    - title/artist use MediaInfo tags when present, then filename for missing fields, then sidecar for missing fields;
    - when filename title and sidecar title disagree, preserve `metadataConflicts` with `field: "title"`;
    - returned `metadataSources` includes exact source labels: `"mediainfo"`, `"filename"`, and `"sidecar"`;
    - invalid sidecar reasons are copied into `scannerReasons`.

    Export:
    - `buildRealMvMetadataDraft(input): RealMvMetadataDraft`.

    Include fields:
    - `title`, `artistName`, `language`, `genre`, `tags`, `aliases`, `searchHints`, `releaseYear`;
    - `metadataSources: Array<{ field: string; source: "mediainfo" | "filename" | "sidecar" }>`;
    - `metadataConflicts: Array<{ field: string; values: Array<{ source: string; value: unknown }> }>`;
    - `scannerReasons: CompatibilityReason[]`;
    - `sidecarMetadata`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/real-mv-metadata.test.ts</automated>
    <automated>pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/modules/ingest/real-mv-metadata.ts` contains `buildRealMvMetadataDraft`.
    - `apps/api/src/modules/ingest/real-mv-metadata.ts` contains `metadataSources`.
    - `apps/api/src/modules/ingest/real-mv-metadata.ts` contains `metadataConflicts`.
    - `apps/api/src/test/real-mv-metadata.test.ts` contains `metadataConflicts`.
    - `pnpm -F @home-ktv/api test -- src/test/real-mv-metadata.test.ts` exits 0.
  </acceptance_criteria>
  <done>Scanner metadata decisions are explicit enough for Admin review.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `pnpm -F @home-ktv/api test -- src/test/real-mv-metadata.test.ts`
- [ ] `pnpm -F @home-ktv/api typecheck`
</verification>

<success_criteria>
- SCAN-03 sidecar metadata is parsed safely.
- SCAN-04 MediaInfo, filename, and sidecar precedence is implemented as explicit provenance.
- SCAN-05 invalid sidecars stay retryable and visible.
</success_criteria>

<output>
After completion, create `.planning/phases/13-mediainfo-probe-scanner-and-sidecars/13-02-SUMMARY.md`.
</output>
