---
phase: 13-mediainfo-probe-scanner-and-sidecars
plan: 04
type: execute
wave: 4
depends_on: ["13-03"]
files_modified:
  - apps/api/src/routes/admin-imports.ts
  - apps/api/src/test/admin-imports-routes.test.ts
  - apps/admin/src/imports/types.ts
  - apps/admin/src/imports/CandidateEditor.tsx
  - apps/admin/src/test/import-workbench.test.tsx
autonomous: true
requirements:
  - SCAN-02
  - SCAN-03
  - SCAN-04
  - SCAN-05
requirements_addressed: [SCAN-02, SCAN-03, SCAN-04, SCAN-05]
must_haves:
  truths:
    - "Admin import API serializes real-MV preview fields without leaking absolute filesystem paths."
    - "Admin can see cover preview, MediaInfo facts, metadata provenance, conflicts, and scanner retry reasons."
    - "Preview UI stays inside existing Import Workbench and does not become Phase 14 review UI."
  key_links:
    - "serializeCandidateFileDetail maps candidate file detail realMv data into Admin API response fields without absolute paths."
    - "coverPreviewUrl points to the new /admin/import-candidates/:candidateId/files/:candidateFileId/cover route and is derived only from stored sidecar metadata."
    - "CandidateEditor renders mediaInfoSummary, realMv.metadataSources, realMv.metadataConflicts, realMv.scannerReasons, and coverPreviewUrl from apps/admin/src/imports/types.ts."
  artifacts:
    - path: "apps/api/src/routes/admin-imports.ts"
      provides: "Admin import serialization and cover preview route"
      contains: "coverPreviewUrl"
    - path: "apps/admin/src/imports/CandidateEditor.tsx"
      provides: "Minimal real-MV candidate preview"
      contains: "mediaInfoSummary"
---

<objective>
Expose scanner-enriched real MV candidate data through the Admin API and render a minimal reviewable preview in the existing Import Workbench.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/13-mediainfo-probe-scanner-and-sidecars/13-CONTEXT.md
@.planning/phases/13-mediainfo-probe-scanner-and-sidecars/13-RESEARCH.md
@.planning/phases/13-mediainfo-probe-scanner-and-sidecars/13-UI-SPEC.md
@apps/api/src/routes/admin-imports.ts
@apps/admin/src/imports/CandidateEditor.tsx
@apps/admin/src/imports/types.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Serialize real MV file detail fields</name>
  <files>apps/api/src/routes/admin-imports.ts, apps/api/src/test/admin-imports-routes.test.ts, apps/admin/src/imports/types.ts</files>
  <read_first>
    - apps/api/src/routes/admin-imports.ts
    - apps/api/src/test/admin-imports-routes.test.ts
    - apps/admin/src/imports/types.ts
    - packages/domain/src/index.ts
  </read_first>
  <action>
    Add failing API tests where an import candidate file detail includes:
    - `compatibilityStatus: "review_required"`;
    - `compatibilityReasons` with code `instrumental-track-unmapped`;
    - `mediaInfoSummary.container: "matroska,webm"`;
    - `mediaInfoProvenance.source: "ffprobe"`;
    - `trackRoles`;
    - `playbackProfile.kind: "single_file_audio_tracks"`;
    - `probeSummary.realMv.metadataSources`;
    - `probeSummary.realMv.metadataConflicts`;
    - `probeSummary.realMv.scannerReasons`;
    - `probeSummary.realMv.sidecars.cover.relativePath`.

    Modify `serializeCandidateFileDetail` to include:
    - `compatibilityStatus`;
    - `compatibilityReasons`;
    - `mediaInfoSummary`;
    - `mediaInfoProvenance`;
    - `trackRoles`;
    - `playbackProfile`;
    - `realMv`;
    - `coverPreviewUrl` when `realMv.sidecars.cover.relativePath` exists.

    Update `apps/admin/src/imports/types.ts` with matching TypeScript interfaces. Do not include absolute filesystem paths.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/admin-imports-routes.test.ts</automated>
    <automated>pnpm -F @home-ktv/admin typecheck</automated>
    <automated>pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/routes/admin-imports.ts` contains `compatibilityStatus`.
    - `apps/api/src/routes/admin-imports.ts` contains `coverPreviewUrl`.
    - `apps/admin/src/imports/types.ts` contains `mediaInfoSummary`.
    - `apps/admin/src/imports/types.ts` contains `playbackProfile`.
    - `pnpm -F @home-ktv/api test -- src/test/admin-imports-routes.test.ts` exits 0.
  </acceptance_criteria>
  <done>Admin receives scanner-enriched real MV file details.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add safe cover preview route</name>
  <files>apps/api/src/routes/admin-imports.ts, apps/api/src/test/admin-imports-routes.test.ts</files>
  <read_first>
    - apps/api/src/routes/admin-imports.ts
    - apps/api/src/routes/media.ts
    - apps/api/src/modules/ingest/library-paths.ts
    - apps/api/src/test/admin-imports-routes.test.ts
  </read_first>
  <action>
    Add a route:
    - `GET /admin/import-candidates/:candidateId/files/:candidateFileId/cover`

    Test requirements:
    - route returns `404` when the candidate or file does not exist;
    - route returns `404` when no cover sidecar exists;
    - route rejects path traversal and any path outside the file's scan root;
    - route serves `image/jpeg`, `image/png`, or `image/webp` based on stored cover content type;
    - serialized `coverPreviewUrl` points to this route.

    The route may resolve the file detail through `getCandidateWithFiles(candidateId)`. It must derive the cover path from stored `realMv.sidecars.cover.relativePath` and root kind, not from a client-supplied path.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- src/test/admin-imports-routes.test.ts</automated>
    <automated>pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/routes/admin-imports.ts` contains `/cover`.
    - `apps/api/src/routes/admin-imports.ts` contains `image/jpeg`.
    - `apps/api/src/routes/admin-imports.ts` contains `Path is outside` or `outside`.
    - `apps/api/src/test/admin-imports-routes.test.ts` contains `coverPreviewUrl`.
    - `pnpm -F @home-ktv/api test -- src/test/admin-imports-routes.test.ts` exits 0.
  </acceptance_criteria>
  <done>Cover thumbnails can be previewed safely from Admin.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Render minimal real MV preview in CandidateEditor</name>
  <files>apps/admin/src/imports/CandidateEditor.tsx, apps/admin/src/test/import-workbench.test.tsx</files>
  <read_first>
    - .planning/phases/13-mediainfo-probe-scanner-and-sidecars/13-UI-SPEC.md
    - apps/admin/src/imports/CandidateEditor.tsx
    - apps/admin/src/imports/types.ts
    - apps/admin/src/test/import-workbench.test.tsx
  </read_first>
  <action>
    Add failing Admin tests that render a candidate with a real MV file and assert visible Chinese labels for:
    - cover preview alt text `MV 封面`;
    - MediaInfo summary label `媒体信息`;
    - provenance label `来源`;
    - conflict or warning label `需要确认`.

    Modify `CandidateEditor`:
    - render a compact preview section above the metadata form when any selected file has `mediaInfoSummary` or `realMv`;
    - show cover image using `coverPreviewUrl` when present;
    - show neutral fallback text `暂无封面` when no cover exists;
    - show container, duration, video codec, resolution, and audio track count;
    - show metadata sources from `realMv.metadataSources`;
    - show scanner warnings/conflicts from `realMv.metadataConflicts` and `realMv.scannerReasons`;
    - keep existing form fields and action buttons unchanged.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/admin test -- src/test/import-workbench.test.tsx</automated>
    <automated>pnpm -F @home-ktv/admin typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/admin/src/imports/CandidateEditor.tsx` contains `MV 封面`.
    - `apps/admin/src/imports/CandidateEditor.tsx` contains `媒体信息`.
    - `apps/admin/src/imports/CandidateEditor.tsx` contains `暂无封面`.
    - `apps/admin/src/test/import-workbench.test.tsx` contains `需要确认`.
    - `pnpm -F @home-ktv/admin test -- src/test/import-workbench.test.tsx` exits 0.
  </acceptance_criteria>
  <done>Admin can see real-MV scanner evidence before Phase 14 admission work.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `pnpm -F @home-ktv/api test -- src/test/admin-imports-routes.test.ts`
- [ ] `pnpm -F @home-ktv/admin test -- src/test/import-workbench.test.tsx`
- [ ] `pnpm -F @home-ktv/api typecheck`
- [ ] `pnpm -F @home-ktv/admin typecheck`
</verification>

<success_criteria>
- SCAN-02 cover preview is visible in Admin.
- SCAN-03 sidecar metadata and SCAN-04 provenance/conflicts are visible enough for review.
- SCAN-05 retry/probe warnings are visible.
- No Phase 14 approval/admission redesign is included.
</success_criteria>

<output>
After completion, create `.planning/phases/13-mediainfo-probe-scanner-and-sidecars/13-04-SUMMARY.md`.
</output>
