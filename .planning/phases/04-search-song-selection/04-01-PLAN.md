---
phase: 04-search-song-selection
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/domain/src/index.ts
  - apps/api/package.json
  - pnpm-lock.yaml
  - apps/api/src/modules/catalog/search-normalization.ts
  - apps/api/src/modules/catalog/search-ranking.ts
  - apps/api/src/db/migrations/0005_catalog_search.sql
  - apps/api/src/db/schema.ts
  - apps/api/src/modules/catalog/admission-service.ts
  - apps/api/src/modules/catalog/repositories/song-repository.ts
  - apps/api/src/routes/song-search.ts
  - apps/api/src/server.ts
  - apps/api/src/test/catalog-admission.test.ts
  - apps/api/src/test/catalog-search-normalization.test.ts
  - apps/api/src/test/catalog-search-repository.test.ts
  - apps/api/src/test/song-search-routes.test.ts
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
must_haves:
  truths:
    - "User can search ready queueable local songs by Chinese title"
    - "User can search ready queueable local songs by artist name"
    - "Existing and newly admitted songs have populated artist pinyin and initials for artist search"
    - "User can search ready queueable local songs by pinyin, initials, aliases, and searchHints"
    - "Search response separates local queueable results from a disabled online placeholder"
    - "Import candidates, review_required songs, unready assets, and assets without verified switch counterparts are excluded"
  artifacts:
    - path: "packages/domain/src/index.ts"
      provides: "shared SongSearchResponse, SongSearchLocalResult, SongSearchVersionOption contracts"
      contains: "export interface SongSearchResponse"
    - path: "apps/api/src/modules/catalog/search-normalization.ts"
      provides: "NFKC/OpenCC/pinyin normalization helpers"
      contains: "export function normalizeSearchText"
    - path: "apps/api/src/modules/catalog/search-ranking.ts"
      provides: "explicit D-02 match score buckets"
      contains: "title_exact: 1000"
    - path: "apps/api/src/db/migrations/0005_catalog_search.sql"
      provides: "pg_trgm extension and search indexes"
      contains: "CREATE EXTENSION IF NOT EXISTS pg_trgm"
    - path: "apps/api/src/modules/catalog/admission-service.ts"
      provides: "formal-catalog admission write path for artist pinyin and initials"
      contains: "buildPinyinSearchKeys"
    - path: "apps/api/src/routes/song-search.ts"
      provides: "GET /rooms/:roomSlug/songs/search route"
      contains: "/rooms/:roomSlug/songs/search"
  key_links:
    - from: "apps/api/src/routes/song-search.ts"
      to: "apps/api/src/modules/catalog/repositories/song-repository.ts"
      via: "dependencies.songs.searchFormalSongs"
      pattern: "searchFormalSongs"
    - from: "apps/api/src/modules/catalog/repositories/song-repository.ts"
      to: "assets"
      via: "verified switch-family queueability SQL"
      pattern: "switch_quality_status = 'verified'"
    - from: "apps/api/src/modules/catalog/repositories/song-repository.ts"
      to: "apps/api/src/modules/catalog/search-normalization.ts"
      via: "buildPinyinSearchKeys for artist_pinyin/artist_initials backfill and metadata updates"
      pattern: "buildPinyinSearchKeys"
    - from: "apps/api/src/server.ts"
      to: "apps/api/src/routes/song-search.ts"
      via: "registerSongSearchRoutes"
      pattern: "registerSongSearchRoutes"
---

<objective>
Build the Chinese-first formal catalog search backend.

Purpose: Phase 4 needs one typed, server-authoritative search read path that respects the formal catalog boundary before the mobile UI can replace the Phase 3 ready-song list.
Output: Shared search contracts, normalization/ranking helpers, PostgreSQL search migration/indexes, a repository search method, and `GET /rooms/:roomSlug/songs/search?q=&limit=`.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-search-song-selection/04-CONTEXT.md
@.planning/phases/04-search-song-selection/04-RESEARCH.md
@.planning/phases/02-library-ingest-catalog-admin/02-05-SUMMARY.md
@.planning/phases/03-room-sessions-queue-control/03-04-SUMMARY.md
@packages/domain/src/index.ts
@apps/api/src/db/migrations/0001_media_contract.sql
@apps/api/src/db/schema.ts
@apps/api/src/modules/catalog/repositories/song-repository.ts
@apps/api/src/modules/catalog/repositories/asset-repository.ts
@apps/api/src/routes/available-songs.ts
@apps/api/src/server.ts

<interfaces>
Existing contracts the executor must preserve:

```typescript
export interface Song {
  id: SongId;
  title: string;
  normalizedTitle: string;
  titlePinyin: string;
  titleInitials: string;
  artistId: EntityId;
  artistName: string;
  status: SongStatus;
  aliases: readonly string[];
  searchHints: readonly string[];
  searchWeight: number;
  defaultAssetId: AssetId | null;
}

export interface Asset {
  id: AssetId;
  songId: SongId;
  sourceType: AssetSourceType;
  assetKind: AssetKind;
  displayName: string;
  durationMs: number;
  vocalMode: VocalMode;
  status: AssetStatus;
  switchFamily: SwitchFamily | null;
  switchQualityStatus: SwitchQualityStatus;
}

export interface AssetRepository {
  findById(assetId: AssetId): Promise<Asset | null>;
  findVerifiedSwitchCounterparts(asset: Asset): Promise<Asset[]>;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add shared search contracts and normalization helpers</name>
  <files>packages/domain/src/index.ts, apps/api/package.json, pnpm-lock.yaml, apps/api/src/modules/catalog/search-normalization.ts, apps/api/src/modules/catalog/search-ranking.ts, apps/api/src/test/catalog-search-normalization.test.ts</files>
  <read_first>
    - packages/domain/src/index.ts
    - apps/api/package.json
    - pnpm-lock.yaml
    - apps/api/src/test/catalog-contracts.test.ts
    - .planning/phases/04-search-song-selection/04-CONTEXT.md
    - .planning/phases/04-search-song-selection/04-RESEARCH.md
  </read_first>
  <behavior>
    - `normalizeSearchText(" Ｑｉ Li-Xiang ")` returns `qilixiang`.
    - `normalizeSearchText("後來")` returns the simplified normalized form `后来`.
    - `buildPinyinSearchKeys("七里香")` returns `{ pinyin: "qilixiang", initials: "qlx" }`.
    - `searchMatchScores.title_exact` is greater than `artist_exact`, which is greater than `normalized_title`, `alias`, `pinyin`, `initials`, and `search_hint`.
  </behavior>
  <action>
    Install the API-only dependencies with `pnpm add -F @home-ktv/api pinyin-pro@3.28.1 opencc-js@1.3.0`.

    In `packages/domain/src/index.ts`, export these exact shared contracts:
    - `export type SongSearchMatchReason = "title" | "artist" | "normalized_title" | "alias" | "pinyin" | "initials" | "search_hint" | "default";`
    - `export type SongSearchQueueState = "not_queued" | "queued";`
    - `export interface SongSearchVersionOption { assetId: AssetId; displayName: string; sourceType: AssetSourceType; sourceLabel: string; durationMs: number; qualityLabel: string; isRecommended: boolean; }`
    - `export interface SongSearchLocalResult { songId: SongId; title: string; artistName: string; language: Language; matchReason: SongSearchMatchReason; queueState: SongSearchQueueState; versions: SongSearchVersionOption[]; }`
    - `export interface SongSearchOnlinePlaceholder { status: "disabled"; message: string; candidates: []; }`
    - `export interface SongSearchResponse { query: string; local: SongSearchLocalResult[]; online: SongSearchOnlinePlaceholder; }`

    Create `apps/api/src/modules/catalog/search-normalization.ts` with `normalizeSearchText(value: string): string` and `buildPinyinSearchKeys(value: string): { pinyin: string; initials: string }`. `normalizeSearchText` must use OpenCC simplified conversion, `String.prototype.normalize("NFKC")`, lowercase, and remove non-letter/non-number separators with `/[^\p{Letter}\p{Number}]+/gu`. `buildPinyinSearchKeys` must use `pinyin(value, { toneType: "none", type: "array" })`, join the full pinyin without spaces, and derive initials from the first character of each pinyin syllable.

    Create `apps/api/src/modules/catalog/search-ranking.ts` with the exact exported constant `searchMatchScores = { title_exact: 1000, artist_exact: 900, normalized_title: 800, alias: 700, pinyin: 600, initials: 500, search_hint: 400, default: 100 } as const;` and `export type SearchScoreBucket = keyof typeof searchMatchScores;`. This implements D-01 through D-04 with explicit low weight for initials and `searchHints`.

    Add `apps/api/src/test/catalog-search-normalization.test.ts` covering the four behavior cases above.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- catalog-search-normalization</automated>
    <automated>pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `packages/domain/src/index.ts` contains `export interface SongSearchResponse`.
    - `packages/domain/src/index.ts` contains `export interface SongSearchVersionOption`.
    - `apps/api/package.json` contains `"pinyin-pro": "3.28.1"` or `"pinyin-pro": "^3.28.1"`.
    - `apps/api/package.json` contains `"opencc-js": "1.3.0"` or `"opencc-js": "^1.3.0"`.
    - `apps/api/src/modules/catalog/search-normalization.ts` contains `export function normalizeSearchText`.
    - `apps/api/src/modules/catalog/search-normalization.ts` contains `normalize("NFKC")`.
    - `apps/api/src/modules/catalog/search-ranking.ts` contains `title_exact: 1000`.
    - `apps/api/src/modules/catalog/search-ranking.ts` contains `search_hint: 400`.
    - `pnpm -F @home-ktv/api test -- catalog-search-normalization` exits 0.
  </acceptance_criteria>
  <done>Shared search response types and deterministic Chinese normalization/ranking helpers exist and are covered by API tests.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add formal search indexes and repository read model</name>
  <files>apps/api/src/db/migrations/0005_catalog_search.sql, apps/api/src/db/schema.ts, apps/api/src/modules/catalog/admission-service.ts, apps/api/src/modules/catalog/repositories/song-repository.ts, apps/api/src/test/catalog-admission.test.ts, apps/api/src/test/catalog-search-repository.test.ts</files>
  <read_first>
    - apps/api/src/db/migrations/0001_media_contract.sql
    - apps/api/src/db/migrations/0004_queue_commands.sql
    - apps/api/src/db/schema.ts
    - apps/api/src/modules/catalog/admission-service.ts
    - apps/api/src/modules/catalog/repositories/song-repository.ts
    - apps/api/src/modules/catalog/repositories/asset-repository.ts
    - apps/api/src/test/catalog-admission.test.ts
    - apps/api/src/test/catalog-contracts.test.ts
    - apps/api/src/test/control-session-schema.test.ts
    - docs/KTV-ARCHITECTURE.md
  </read_first>
  <behavior>
    - Empty query returns ready formal songs ordered by `search_weight DESC, updated_at DESC, title ASC`.
    - Non-empty query searches `normalized_title`, `artist_name`, `artist_pinyin`, `artist_initials`, `title_pinyin`, `title_initials`, `aliases`, and `search_hints`.
    - Existing rows with empty `artist_pinyin` or `artist_initials` are backfilled from `buildPinyinSearchKeys(artist_name)` before artist pinyin/initial search runs.
    - `updateSongMetadata(..., { artistName })` and `PgCatalogAdmissionWriter.promoteApprovedCandidate` both persist `artist_pinyin` and `artist_initials` from `buildPinyinSearchKeys`.
    - Artist pinyin query `zhoujielun` and artist initials query `zjl` return the song whose `artist_name` is `周杰伦`.
    - SQL excludes `songs.status <> 'ready'`, `assets.status <> 'ready'`, `assets.source_type = 'online_ephemeral'`, and assets without a ready verified counterpart in the same `switch_family`.
    - Multi-asset songs produce one `SongSearchVersionOption` per verified `switch_family`, not one option per vocal mode.
    - Version options are sorted for UI recommendation by highest quality first, then newest resource, then stable display name; the formal `song.defaultAssetId` family is not allowed to override that recommendation sort per D-12.
  </behavior>
  <action>
    Create `apps/api/src/db/migrations/0005_catalog_search.sql` with:
    - `CREATE EXTENSION IF NOT EXISTS pg_trgm;`
    - `ALTER TABLE songs ADD COLUMN IF NOT EXISTS artist_pinyin text NOT NULL DEFAULT '';`
    - `ALTER TABLE songs ADD COLUMN IF NOT EXISTS artist_initials text NOT NULL DEFAULT '';`
    - `CREATE INDEX IF NOT EXISTS songs_normalized_title_trgm_idx ON songs USING gin (normalized_title gin_trgm_ops);`
    - `CREATE INDEX IF NOT EXISTS songs_artist_name_trgm_idx ON songs USING gin (lower(artist_name) gin_trgm_ops);`
    - `CREATE INDEX IF NOT EXISTS songs_title_pinyin_trgm_idx ON songs USING gin (title_pinyin gin_trgm_ops);`
    - `CREATE INDEX IF NOT EXISTS songs_title_initials_idx ON songs (title_initials);`
    - `CREATE INDEX IF NOT EXISTS songs_artist_pinyin_trgm_idx ON songs USING gin (artist_pinyin gin_trgm_ops);`
    - `CREATE INDEX IF NOT EXISTS songs_artist_initials_idx ON songs (artist_initials);`
    - `CREATE INDEX IF NOT EXISTS songs_aliases_gin_idx ON songs USING gin (aliases);`
    - `CREATE INDEX IF NOT EXISTS songs_search_hints_gin_idx ON songs USING gin (search_hints);`
    - `CREATE INDEX IF NOT EXISTS assets_queueable_search_idx ON assets(song_id, switch_family, source_type, status, switch_quality_status) WHERE status = 'ready' AND switch_quality_status = 'verified';`

    Mirror the same schema changes in `apps/api/src/db/schema.ts`: add `artist_pinyin` and `artist_initials` to `SongRow`, add both columns to `schemaSql`, and add the same index strings to `schemaSql`.

    Add artist search-key population and maintenance using `buildPinyinSearchKeys`:
    - In `apps/api/src/modules/catalog/repositories/song-repository.ts`, import `buildPinyinSearchKeys` from `../search-normalization.js`.
    - Update every songs `SELECT` projection used by `mapSongRow`, `findById`, `listFormalSongs`, `getFormalSongWithAssets`, and `searchFormalSongs` to include `artist_pinyin` and `artist_initials` once those columns exist.
    - Extend `UpdateSongMetadataInput` handling so when `input.artistName !== undefined`, it computes `const artistKeys = buildPinyinSearchKeys(input.artistName)` and updates `artist_pinyin = artistKeys.pinyin` and `artist_initials = artistKeys.initials` in the same `UPDATE songs` statement as `artist_name` and `artist_id`.
    - Add `backfillArtistSearchKeys(limit = 1000): Promise<number>` to `PgSongRepository`. It must select songs where `artist_pinyin = '' OR artist_initials = ''`, compute keys with `buildPinyinSearchKeys(row.artist_name)`, update each row, and return the number of rows updated. Call this method at the start of `searchFormalSongs` before building the search SQL so existing catalog rows can be found by artist pinyin/initials without a manual data script.
    - In `apps/api/src/modules/catalog/admission-service.ts`, update `PgCatalogAdmissionWriter.promoteApprovedCandidate` to compute `const artistKeys = buildPinyinSearchKeys(input.artistName)` and write `artist_pinyin`, `artist_initials`, `title_pinyin`, and `title_initials` in the `INSERT INTO songs` columns and `ON CONFLICT DO UPDATE` assignments. Title keys must use `buildPinyinSearchKeys(input.title)` instead of leaving title pinyin/defaults empty.
    - Do not rely on the migration defaults as the real population mechanism; the defaults only keep old deploys non-breaking while repository/admission write paths fill usable search values.

    Extend `apps/api/src/modules/catalog/repositories/song-repository.ts`:
    - Import `SongSearchMatchReason`, `SongSearchQueueState`, and `SongSearchVersionOption` from `@home-ktv/domain`.
    - Add `export interface SearchFormalSongsInput { query: string; limit?: number; queuedSongIds?: readonly SongId[]; }`.
    - Add `export interface SearchFormalSongRecord { song: Song; matchReason: SongSearchMatchReason; score: number; queueState: SongSearchQueueState; versions: SongSearchVersionOption[]; }`.
    - Add `searchFormalSongs(input: SearchFormalSongsInput): Promise<SearchFormalSongRecord[]>` to `AdminCatalogSongRepository`.
    - Implement `PgSongRepository.searchFormalSongs` using one SQL read model that filters to `s.status = 'ready'` and queueable verified asset families only. A queueable family is a ready asset with `source_type <> 'online_ephemeral'`, `switch_quality_status = 'verified'`, non-null `switch_family`, and an opposite-vocal-mode ready verified counterpart in the same `song_id` and `switch_family`.
    - Use `normalizeSearchText(input.query)` for the normalized query, `%${normalizedQuery}%` for LIKE patterns, and explicit score buckets matching `searchMatchScores`: exact `normalized_title` 1000, exact normalized `artist_name` 900, title LIKE/similarity 800, alias 700, pinyin 600, initials 500, search hint 400, empty query/default 100. Add `s.search_weight` only as a tie-breaker inside the bucket.
    - Return `queueState: "queued"` when `song.id` is in `input.queuedSongIds`, otherwise `"not_queued"`.
    - Build version options from verified switch families. `displayName` must use the selected representative asset `displayName` when non-empty, otherwise `本地版本`. `sourceLabel` must be `本地` for `local`, `已缓存在线` for `online_cached`, and must never be generated for `online_ephemeral`. `qualityLabel` must use the representative asset `assetKind` plus duration seconds, for example `video / 180s`.
    - Implement D-12 recommendation/sorting rules for version options: group by `switch_family`; for each family compute `familyQualityRank = max(assetKind === "video" ? 2 : 1)` and `familyNewestAt = max(updated_at)` across ready verified assets in that family; choose the version option representative asset by `assetKind video before audio`, `updated_at DESC`, `duration_ms DESC`, then `id ASC`; sort version options by `familyQualityRank DESC`, `familyNewestAt DESC`, `displayName ASC`; set `isRecommended: true` only on the first sorted option.
    - Preserve formal default asset behavior separately from UI recommendation: do not boost the family containing `song.defaultAssetId` in search result ordering and do not mark it recommended unless it wins the D-12 sort. The existing add-queue fallback to `song.defaultAssetId` remains only for payloads that omit `assetId`; selected version playback still switches original/伴唱 through `switchFamily`, not through separate version rows.

    Add `apps/api/src/test/catalog-search-repository.test.ts` with a fake `QueryExecutor` and focused assertions that the generated SQL contains `switch_quality_status = 'verified'`, `source_type <> 'online_ephemeral'`, `artist_pinyin`, `artist_initials`, `title_initials`, `search_hints`, and `ORDER BY score DESC`. Include tests proving `searchFormalSongs({ query: "zhoujielun" })` and `searchFormalSongs({ query: "zjl" })` return a fixture song with `artist_name = "周杰伦"` after `backfillArtistSearchKeys` has populated `artist_pinyin = "zhoujielun"` and `artist_initials = "zjl"`. Include a write-path test proving `updateSongMetadata(songId, { artistName: "周杰伦" })` sends `zhoujielun` and `zjl` in the SQL parameters. Include a mapping test where two vocal assets in one `switch_family` produce one version option. Include a D-12 mapping test with an older default-asset family and a newer/higher-quality non-default family; assert the newer/higher-quality option sorts first and is the only `isRecommended: true`, while the default-asset family remains selectable but is not recommended.

    Extend `apps/api/src/test/catalog-admission.test.ts` so `PgCatalogAdmissionWriter.promoteApprovedCandidate` is covered for search-key writes: promoting title `七里香` and artist `周杰伦` must insert or update `title_pinyin = "qilixiang"`, `title_initials = "qlx"`, `artist_pinyin = "zhoujielun"`, and `artist_initials = "zjl"` via parameters generated from `buildPinyinSearchKeys`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- catalog-search-repository catalog-search-normalization catalog-admission</automated>
    <automated>pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/db/migrations/0005_catalog_search.sql` contains `CREATE EXTENSION IF NOT EXISTS pg_trgm`.
    - `apps/api/src/db/migrations/0005_catalog_search.sql` contains `artist_pinyin`.
    - `apps/api/src/db/migrations/0005_catalog_search.sql` contains `assets_queueable_search_idx`.
    - `apps/api/src/db/schema.ts` contains `artist_initials: string;`.
    - `apps/api/src/db/schema.ts` contains `songs_normalized_title_trgm_idx`.
    - `apps/api/src/modules/catalog/admission-service.ts` contains `buildPinyinSearchKeys(input.artistName)`.
    - `apps/api/src/modules/catalog/admission-service.ts` contains `artist_pinyin`.
    - `apps/api/src/modules/catalog/repositories/song-repository.ts` contains `backfillArtistSearchKeys`.
    - `apps/api/src/modules/catalog/repositories/song-repository.ts` contains `buildPinyinSearchKeys(input.artistName)`.
    - `apps/api/src/modules/catalog/repositories/song-repository.ts` contains `searchFormalSongs(input: SearchFormalSongsInput)`.
    - `apps/api/src/modules/catalog/repositories/song-repository.ts` contains `source_type <> 'online_ephemeral'`.
    - `apps/api/src/modules/catalog/repositories/song-repository.ts` contains `switch_quality_status = 'verified'`.
    - `apps/api/src/modules/catalog/repositories/song-repository.ts` contains `familyQualityRank`.
    - `apps/api/src/modules/catalog/repositories/song-repository.ts` contains `isRecommended`.
    - `apps/api/src/test/catalog-search-repository.test.ts` contains `zhoujielun`.
    - `apps/api/src/test/catalog-search-repository.test.ts` contains `zjl`.
    - `apps/api/src/test/catalog-search-repository.test.ts` contains `defaultAssetId`.
    - `apps/api/src/test/catalog-search-repository.test.ts` contains `isRecommended`.
    - `apps/api/src/test/catalog-admission.test.ts` contains `artist_initials`.
    - `pnpm -F @home-ktv/api test -- catalog-search-repository catalog-search-normalization catalog-admission` exits 0.
  </acceptance_criteria>
  <done>PostgreSQL search schema, artist search-key population/backfill, and `PgSongRepository.searchFormalSongs` return ranked, queueable, D-12-recommended, version-grouped formal catalog results only.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose room-scoped song search route</name>
  <files>apps/api/src/routes/song-search.ts, apps/api/src/server.ts, apps/api/src/test/song-search-routes.test.ts</files>
  <read_first>
    - apps/api/src/routes/available-songs.ts
    - apps/api/src/routes/control-commands.ts
    - apps/api/src/routes/control-sessions.ts
    - apps/api/src/server.ts
    - apps/api/src/modules/catalog/repositories/song-repository.ts
    - apps/api/src/modules/playback/repositories/queue-entry-repository.ts
    - apps/api/src/test/admin-catalog-routes.test.ts
    - apps/api/src/test/realtime-room-sync.test.ts
  </read_first>
  <behavior>
    - `GET /rooms/living-room/songs/search?q=七里香&limit=20` returns `{ query: "七里香", local: [...], online: { status: "disabled", message: "本地未入库，补歌功能后续可用", candidates: [] } }`.
    - Missing room returns `404` with `{ code: "ROOM_NOT_FOUND" }`.
    - Empty `q` is accepted and calls `searchFormalSongs({ query: "", limit: 30, queuedSongIds })`.
    - Local results always appear in the `local` array; online candidates are never queueable in Phase 4.
  </behavior>
  <action>
    Create `apps/api/src/routes/song-search.ts` with:
    - `export interface SongSearchRouteDependencies { rooms: RoomRepository; songs: AdminCatalogSongRepository; queueEntries: QueueEntryRepository; }`
    - `export async function registerSongSearchRoutes(server: FastifyInstance, dependencies: SongSearchRouteDependencies): Promise<void>`.
    - A Fastify `GET /rooms/:roomSlug/songs/search` route accepting query params `{ q?: string; limit?: string | number }`.
    - Resolve the room through `dependencies.rooms.findBySlug(request.params.roomSlug)` and return `404 { code: "ROOM_NOT_FOUND" }` if absent.
    - Parse `limit` as an integer between 1 and 50; default to 30.
    - Compute `queuedSongIds` from `await dependencies.queueEntries.listEffectiveQueue(room.id)` by taking each entry `songId`.
    - Call `dependencies.songs.searchFormalSongs({ query: String(request.query.q ?? ""), limit, queuedSongIds })`.
    - Send a `SongSearchResponse` with `query`, `local`, and `online: { status: "disabled", message: "本地未入库，补歌功能后续可用", candidates: [] }`.

    Wire `registerSongSearchRoutes` in `apps/api/src/server.ts` immediately after `registerAvailableSongsRoutes`, using `rooms: repositories.rooms`, `songs: repositories.songs`, and `queueEntries: repositories.queueEntries`. Keep `GET /rooms/:roomSlug/available-songs` for compatibility, but the new search route is the Phase 4 primary read path.

    Add `apps/api/src/test/song-search-routes.test.ts` using a Fastify instance and fake repositories. Cover room not found, empty query default limit 30, non-empty query forwarding, queued song state forwarding, and disabled online placeholder response.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- song-search-routes catalog-search-repository</automated>
    <automated>pnpm -F @home-ktv/api build</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/routes/song-search.ts` contains `/rooms/:roomSlug/songs/search`.
    - `apps/api/src/routes/song-search.ts` contains `message: "本地未入库，补歌功能后续可用"`.
    - `apps/api/src/routes/song-search.ts` contains `limit = Math.min(50`.
    - `apps/api/src/routes/song-search.ts` contains `listEffectiveQueue`.
    - `apps/api/src/server.ts` contains `registerSongSearchRoutes`.
    - `apps/api/src/test/song-search-routes.test.ts` contains `ROOM_NOT_FOUND`.
    - `apps/api/src/test/song-search-routes.test.ts` contains `本地未入库，补歌功能后续可用`.
    - `pnpm -F @home-ktv/api test -- song-search-routes catalog-search-repository` exits 0.
  </acceptance_criteria>
  <done>Room-scoped song search route is registered, returns typed local results plus a disabled online placeholder, and marks queued songs from server state.</done>
</task>

</tasks>

<verification>
Run:
- `pnpm -F @home-ktv/api test -- catalog-search-normalization catalog-search-repository song-search-routes`
- `pnpm -F @home-ktv/api build`
- `pnpm typecheck`
</verification>

<success_criteria>
- `GET /rooms/:roomSlug/songs/search` exists and is wired into the API server.
- Search uses explicit D-02 ranking buckets across title, artist, normalized title, alias, pinyin, initials, and search hints.
- Search returns only formal ready songs with at least one queueable verified switch family.
- Search response includes a disabled online placeholder and does not implement Phase 5 online request/cache workflows.
</success_criteria>

<output>
After completion, create `.planning/phases/04-search-song-selection/04-01-SUMMARY.md`.
</output>
