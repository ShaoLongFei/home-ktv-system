---
phase: 04-search-song-selection
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - apps/api/src/routes/control-commands.ts
  - apps/api/src/modules/playback/session-command-service.ts
  - apps/api/src/test/room-queue-commands.test.ts
  - apps/mobile-controller/package.json
  - pnpm-lock.yaml
  - apps/mobile-controller/src/api/client.ts
  - apps/mobile-controller/src/runtime/use-room-controller.ts
  - apps/mobile-controller/src/App.tsx
  - apps/mobile-controller/src/App.css
  - apps/mobile-controller/src/test/controller.test.tsx
autonomous: true
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - QUEU-02
must_haves:
  truths:
    - "User can type in the mobile search input and receive debounced search results"
    - "User can press the keyboard search key or search icon to trigger an immediate search"
    - "Empty query shows ready local queueable songs instead of a blank page"
    - "Each result row shows title, artist, source/status, version hint, and an add button"
    - "Queued songs show queued status and require confirmation before adding a duplicate"
    - "Multi-version songs show expanded version rows and queue the selected assetId"
    - "Online placeholder is disabled and says the song is not locally imported yet"
  artifacts:
    - path: "apps/api/src/modules/playback/session-command-service.ts"
      provides: "assetId-aware add-queue-entry server validation"
      contains: "requestedAssetId"
    - path: "apps/mobile-controller/src/api/client.ts"
      provides: "searchSongs and assetId-aware addQueueEntry client helpers"
      contains: "searchSongs"
    - path: "apps/mobile-controller/src/runtime/use-room-controller.ts"
      provides: "search state, debounce, immediate submit, duplicate confirmation state"
      contains: "submitSongSearch"
    - path: "apps/mobile-controller/src/App.tsx"
      provides: "mobile search and version selection UI"
      contains: "搜索歌曲"
  key_links:
    - from: "apps/mobile-controller/src/api/client.ts"
      to: "/rooms/:roomSlug/songs/search"
      via: "searchSongs fetch"
      pattern: "songs/search"
    - from: "apps/mobile-controller/src/api/client.ts"
      to: "/rooms/:roomSlug/commands/add-queue-entry"
      via: "assetId in command payload"
      pattern: "assetId"
    - from: "apps/api/src/modules/playback/session-command-service.ts"
      to: "apps/api/src/modules/catalog/repositories/asset-repository.ts"
      via: "findVerifiedSwitchCounterparts selected asset validation"
      pattern: "findVerifiedSwitchCounterparts"
---

<objective>
Build the version-aware mobile song selection flow on top of the Phase 4 search backend.

Purpose: Users need a phone-first flow that finds songs quickly, distinguishes local from disabled online placeholders, and queues the selected resource version while keeping original/伴唱 switching as playback-time control.
Output: Asset-version-aware enqueue validation, mobile search client/runtime state, and a tested mobile search/selection UI.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-search-song-selection/04-CONTEXT.md
@.planning/phases/04-search-song-selection/04-RESEARCH.md
@.planning/phases/04-search-song-selection/04-01-SUMMARY.md
@.planning/phases/03-room-sessions-queue-control/03-04-SUMMARY.md
@.planning/phases/03-room-sessions-queue-control/03-06-SUMMARY.md
@apps/api/src/routes/control-commands.ts
@apps/api/src/modules/playback/session-command-service.ts
@apps/mobile-controller/src/api/client.ts
@apps/mobile-controller/src/runtime/use-room-controller.ts
@apps/mobile-controller/src/App.tsx
@apps/mobile-controller/src/App.css
@apps/mobile-controller/src/test/controller.test.tsx

<interfaces>
Contracts created by 04-01 and consumed here:

```typescript
export interface SongSearchResponse {
  query: string;
  local: SongSearchLocalResult[];
  online: SongSearchOnlinePlaceholder;
}

export interface SongSearchLocalResult {
  songId: SongId;
  title: string;
  artistName: string;
  language: Language;
  matchReason: SongSearchMatchReason;
  queueState: "not_queued" | "queued";
  versions: SongSearchVersionOption[];
}

export interface SongSearchVersionOption {
  assetId: AssetId;
  displayName: string;
  sourceType: AssetSourceType;
  sourceLabel: string;
  durationMs: number;
  qualityLabel: string;
  isRecommended: boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make add-queue-entry validate selected assetId</name>
  <files>apps/api/src/routes/control-commands.ts, apps/api/src/modules/playback/session-command-service.ts, apps/api/src/test/room-queue-commands.test.ts</files>
  <read_first>
    - apps/api/src/routes/control-commands.ts
    - apps/api/src/modules/playback/session-command-service.ts
    - apps/api/src/modules/catalog/repositories/asset-repository.ts
    - apps/api/src/test/room-queue-commands.test.ts
    - .planning/phases/04-search-song-selection/04-CONTEXT.md
    - .planning/phases/03-room-sessions-queue-control/03-04-SUMMARY.md
  </read_first>
  <behavior>
    - `add-queue-entry` with `{ songId: "song-ready", assetId: "asset-ready-alt-instrumental" }` appends a queue entry whose `assetId` is `asset-ready-alt-instrumental`.
    - Missing `assetId` still falls back to `song.defaultAssetId` for single-version compatibility.
    - A selected asset is rejected with `SONG_NOT_QUEUEABLE` when it belongs to another song, is not `ready`, has `sourceType === "online_ephemeral"`, has `switchQualityStatus !== "verified"`, or lacks a ready verified opposite-vocal counterpart.
  </behavior>
  <action>
    In `apps/api/src/routes/control-commands.ts`, extend `AddQueueEntryBody` to include `assetId?: string`, and pass both `songId: request.body.songId` and `assetId: request.body.assetId` into `handleCommand` for `"add-queue-entry"`.

    In `apps/api/src/modules/playback/session-command-service.ts`, update `addQueueEntry` as follows:
    - Keep reading `songId` from `input.payload.songId`.
    - After loading and validating the ready song, set `const requestedAssetId = typeof input.payload.assetId === "string" ? input.payload.assetId : song.defaultAssetId;`.
    - Reject with `SONG_NOT_QUEUEABLE` if `requestedAssetId` is falsy.
    - Load `const selectedAsset = await input.repositories.assets.findById(requestedAssetId);`.
    - Reject with `SONG_NOT_QUEUEABLE` unless `selectedAsset.songId === song.id`, `selectedAsset.status === "ready"`, `selectedAsset.sourceType !== "online_ephemeral"`, and `selectedAsset.switchQualityStatus === "verified"`.
    - Call `findVerifiedSwitchCounterparts(selectedAsset)` and reject when it returns an empty array.
    - Append the queue entry with `assetId: selectedAsset.id`.
    - Do not treat `selectedAsset.vocalMode` as a permanent original/伴唱 choice; playback-time switching remains handled by `switch-vocal-mode` through `switchFamily` per D-12.

    Extend `apps/api/src/test/room-queue-commands.test.ts` fake fixtures with an alternate verified switch family for `song-ready`: `asset-ready-alt-instrumental` and `asset-ready-alt-original`. Add tests for selected asset acceptance, default fallback, wrong-song rejection, unready rejection, `online_ephemeral` rejection, and missing-counterpart rejection.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/api test -- room-queue-commands</automated>
    <automated>pnpm -F @home-ktv/api typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/api/src/routes/control-commands.ts` contains `assetId?: string`.
    - `apps/api/src/routes/control-commands.ts` contains `assetId: request.body.assetId`.
    - `apps/api/src/modules/playback/session-command-service.ts` contains `requestedAssetId`.
    - `apps/api/src/modules/playback/session-command-service.ts` contains `sourceType !== "online_ephemeral"`.
    - `apps/api/src/modules/playback/session-command-service.ts` contains `switchQualityStatus !== "verified"`.
    - `apps/api/src/modules/playback/session-command-service.ts` contains `assetId: selectedAsset.id`.
    - `apps/api/src/test/room-queue-commands.test.ts` contains `asset-ready-alt-instrumental`.
    - `pnpm -F @home-ktv/api test -- room-queue-commands` exits 0.
  </acceptance_criteria>
  <done>Server command validation accepts a concrete selected version asset and rejects stale or non-queueable asset selections.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add mobile search client and controller state</name>
  <files>apps/mobile-controller/package.json, pnpm-lock.yaml, apps/mobile-controller/src/api/client.ts, apps/mobile-controller/src/runtime/use-room-controller.ts, apps/mobile-controller/src/test/controller.test.tsx</files>
  <read_first>
    - apps/mobile-controller/package.json
    - pnpm-lock.yaml
    - apps/mobile-controller/src/api/client.ts
    - apps/mobile-controller/src/runtime/use-room-controller.ts
    - apps/mobile-controller/src/test/controller.test.tsx
    - packages/domain/src/index.ts
    - .planning/phases/04-search-song-selection/04-01-SUMMARY.md
  </read_first>
  <behavior>
    - After control-session restore succeeds, the controller fetches `/rooms/living-room/songs/search?q=&limit=30` instead of `/available-songs`.
    - Updating the search query schedules a debounced search after 250ms.
    - Calling `submitSongSearch()` cancels the debounce and immediately fetches the latest query.
    - `addSongVersion(songId, assetId)` sends `songId` and `assetId` in the add-queue-entry command payload.
  </behavior>
  <action>
    Add `"@home-ktv/domain": "workspace:*"` to `apps/mobile-controller/package.json` dependencies if it is not already present, and run `pnpm install --lockfile-only` if needed to update `pnpm-lock.yaml`.

    In `apps/mobile-controller/src/api/client.ts`:
    - Import `type SongSearchResponse` from `@home-ktv/domain`.
    - Replace the primary Phase 3 list helper with `export async function searchSongs(input: { roomSlug: string; query: string; limit?: number; signal?: AbortSignal }): Promise<SongSearchResponse>`.
    - Build the URL as `/rooms/${encodeURIComponent(input.roomSlug)}/songs/search?q=${encodeURIComponent(input.query)}&limit=${input.limit ?? 30}`.
    - Pass `signal: input.signal` to `fetchController`.
    - Keep `fetchAvailableSongs` only if existing tests or compatibility need it; the runtime must use `searchSongs`.
    - Change `addQueueEntry(input: CommandBaseInput & { songId: string; assetId?: string })` to call `sendCommand(input, "add-queue-entry", { songId: input.songId, ...(input.assetId ? { assetId: input.assetId } : {}) })`.

    In `apps/mobile-controller/src/runtime/use-room-controller.ts`:
    - Replace `availableSongs: AvailableSong[]` with `songSearch: SongSearchResponse | null`, `songSearchQuery: string`, `songSearchStatus: "idle" | "loading" | "success" | "error"`, and `duplicateConfirm: { songId: string; assetId: string; title: string } | null`.
    - Export controller methods `setSongSearchQuery(query: string): void`, `submitSongSearch(): void`, `requestAddSongVersion(songId: string, assetId: string, title: string, queueState: "not_queued" | "queued"): void`, `confirmDuplicateAdd(): Promise<void>`, `cancelDuplicateAdd(): void`, and `addSongVersion(songId: string, assetId: string): Promise<void>`.
    - On successful session restore or create, call `searchSongs({ roomSlug: initial.roomSlug, query: "", limit: 30 })` so empty query shows ready songs per D-06.
    - Add a 250ms debounce for `songSearchQuery` changes with `AbortController`; before setting results, verify the response belongs to the latest query to avoid stale overwrite per research pitfall 5.
    - `submitSongSearch()` must clear any pending debounce and immediately fetch the current query.
    - `requestAddSongVersion` must open duplicate confirmation when `queueState === "queued"`; otherwise it must call `addSongVersion(songId, assetId)`.
    - `confirmDuplicateAdd()` must call `addSongVersion` with the stored duplicate selection and then clear `duplicateConfirm`.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/mobile-controller test -- controller</automated>
    <automated>pnpm -F @home-ktv/mobile-controller typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile-controller/package.json` contains `"@home-ktv/domain": "workspace:*"`.
    - `apps/mobile-controller/src/api/client.ts` contains `export async function searchSongs`.
    - `apps/mobile-controller/src/api/client.ts` contains `/songs/search?q=`.
    - `apps/mobile-controller/src/api/client.ts` contains `assetId: input.assetId`.
    - `apps/mobile-controller/src/runtime/use-room-controller.ts` contains `songSearchQuery`.
    - `apps/mobile-controller/src/runtime/use-room-controller.ts` contains `submitSongSearch`.
    - `apps/mobile-controller/src/runtime/use-room-controller.ts` contains `duplicateConfirm`.
    - `apps/mobile-controller/src/runtime/use-room-controller.ts` contains `250`.
    - `apps/mobile-controller/src/test/controller.test.tsx` contains `/rooms/living-room/songs/search?q=&limit=30`.
    - `pnpm -F @home-ktv/mobile-controller test -- controller` exits 0.
  </acceptance_criteria>
  <done>Mobile controller runtime uses the new search endpoint, supports debounced and immediate search, and can enqueue a selected version assetId.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace ready-song list with version-aware search UI</name>
  <files>apps/mobile-controller/src/App.tsx, apps/mobile-controller/src/App.css, apps/mobile-controller/src/test/controller.test.tsx</files>
  <read_first>
    - apps/mobile-controller/src/App.tsx
    - apps/mobile-controller/src/App.css
    - apps/mobile-controller/src/runtime/use-room-controller.ts
    - apps/mobile-controller/src/api/client.ts
    - apps/mobile-controller/src/test/controller.test.tsx
    - .planning/phases/04-search-song-selection/04-CONTEXT.md
  </read_first>
  <behavior>
    - The phone UI contains a search input labelled `搜索歌曲`; typing `qlx` triggers search and pressing Enter triggers immediate search.
    - Empty query renders local ready songs from `songSearch.local`.
    - Every result row displays title, artist, `本地可播` or `已点 / 队列中`, version count, and an add button.
    - One-version results show one add button and do not require extra version choice per D-09.
    - Multi-version results render all version rows inline with version name, source label, duration/quality label, recommended marker, and a button for each version per D-10 and D-11.
    - Queued result add opens a confirmation dialog titled `重复点歌`; confirming still sends the selected assetId per D-08.
    - Disabled online placeholder text says `本地未入库，补歌功能后续可用` and has no enabled queue button per D-13 through D-16.
  </behavior>
  <action>
    In `apps/mobile-controller/src/App.tsx`, replace the `可点歌曲` section that maps `controller.availableSongs` with a search panel:
    - Use a `<form role="search" onSubmit={...}>` that calls `controller.submitSongSearch()` and prevents default.
    - Add `<input aria-label="搜索歌曲" value={controller.songSearchQuery} onChange={(event) => controller.setSongSearchQuery(event.currentTarget.value)} placeholder="歌名 / 歌手 / 拼音 / 首字母" />`.
    - Add a submit button labelled `搜索`.
    - Show loading text `搜索中` only when `controller.songSearchStatus === "loading"`.
    - Render `controller.songSearch?.local` in `.song-list`.
    - For each result row, show `<strong>{result.title}</strong>`, artist name, a status text: `已点 / 队列中` when `result.queueState === "queued"` else `本地可播`, and a version hint text `${result.versions.length} 个版本`.
    - If `result.versions.length === 1`, render one button labelled `点歌` or `加点` that calls `controller.requestAddSongVersion(result.songId, result.versions[0].assetId, result.title, result.queueState)`.
    - If `result.versions.length > 1`, render all versions inline in a `.version-list`; each `.version-row` must show `version.displayName`, `version.sourceLabel`, `version.qualityLabel`, `推荐` when `version.isRecommended`, and a button labelled `点这个版本`.
    - Render `controller.songSearch?.online.message` in a disabled online block with text `在线补歌` and `本地未入库，补歌功能后续可用`; do not render a button that can add online candidates.
    - Add a duplicate confirmation modal when `controller.duplicateConfirm` is present with title `重复点歌`, body `${controller.duplicateConfirm.title} 已在队列中，仍要再点一次吗？`, and buttons `取消` and `确认加点`.

    In `apps/mobile-controller/src/App.css`, add stable mobile styles for `.search-form`, `.search-input`, `.result-meta`, `.version-list`, `.version-row`, `.online-placeholder`, and `.queued-badge`. Keep button min-height at 44px and ensure rows use `grid-template-columns: minmax(0, 1fr) auto` or a single column on narrow widths so text does not overlap.

    Extend `apps/mobile-controller/src/test/controller.test.tsx` fetch mock to return a `SongSearchResponse` from `/songs/search`, including:
    - One single-version result `晴天` with `assetId: "asset-sunny-main"` and `queueState: "not_queued"`.
    - One multi-version queued result `七里香` with `assetId: "asset-qlx-hd"` and `assetId: "asset-qlx-live"`, `queueState: "queued"`, and one `isRecommended: true`.
    - `online: { status: "disabled", message: "本地未入库，补歌功能后续可用", candidates: [] }`.
    Add DOM tests for input-triggered search URL, Enter-submit immediate URL, single-version click body includes `assetId: "asset-sunny-main"`, multi-version row click body includes `assetId: "asset-qlx-live"`, duplicate confirmation before queued add, and disabled online placeholder copy.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/mobile-controller test -- controller</automated>
    <automated>pnpm -F @home-ktv/mobile-controller build</automated>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile-controller/src/App.tsx` contains `aria-label="搜索歌曲"`.
    - `apps/mobile-controller/src/App.tsx` contains `placeholder="歌名 / 歌手 / 拼音 / 首字母"`.
    - `apps/mobile-controller/src/App.tsx` contains `已点 / 队列中`.
    - `apps/mobile-controller/src/App.tsx` contains `点这个版本`.
    - `apps/mobile-controller/src/App.tsx` contains `重复点歌`.
    - `apps/mobile-controller/src/App.tsx` contains `本地未入库，补歌功能后续可用`.
    - `apps/mobile-controller/src/App.css` contains `.version-row`.
    - `apps/mobile-controller/src/App.css` contains `.online-placeholder`.
    - `apps/mobile-controller/src/test/controller.test.tsx` contains `asset-qlx-live`.
    - `apps/mobile-controller/src/test/controller.test.tsx` contains `确认加点`.
    - `pnpm -F @home-ktv/mobile-controller test -- controller` exits 0.
  </acceptance_criteria>
  <done>Mobile controller shows the complete Chinese-first, version-aware song selection UI and preserves duplicate confirmation plus disabled online boundaries.</done>
</task>

</tasks>

<verification>
Run:
- `pnpm -F @home-ktv/api test -- room-queue-commands`
- `pnpm -F @home-ktv/mobile-controller test -- controller`
- `pnpm -F @home-ktv/mobile-controller build`
- `pnpm typecheck`
</verification>

<success_criteria>
- Mobile search uses input-while-typing plus immediate submit behavior.
- Empty query shows queueable local songs through the search endpoint.
- Version rows are shown inline for multi-version songs and enqueue the selected `assetId`.
- Re-adding a queued song requires confirmation but remains allowed.
- Online placeholder text is visible and disabled, with no queue/cache request path.
</success_criteria>

<output>
After completion, create `.planning/phases/04-search-song-selection/04-02-SUMMARY.md`.
</output>
