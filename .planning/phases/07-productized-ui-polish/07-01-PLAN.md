---
phase: 07-productized-ui-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/admin/src/i18n.tsx
  - apps/admin/src/rooms/RoomStatusView.tsx
  - apps/admin/src/songs/SongCatalogView.tsx
  - apps/admin/src/songs/AssetPairEditor.tsx
  - apps/admin/src/songs/SongDetailEditor.tsx
  - apps/admin/src/imports/ImportWorkbench.tsx
  - apps/mobile-controller/src/i18n.tsx
  - apps/mobile-controller/src/App.tsx
  - apps/mobile-controller/src/test/controller.test.tsx
  - apps/admin/src/test/room-status.test.tsx
  - apps/admin/src/test/song-catalog.test.tsx
  - apps/admin/src/test/import-workbench.test.tsx
autonomous: true
requirements:
  - PROD-01
  - PROD-02
  - PROD-04
must_haves:
  truths:
    - "Admin and Mobile default Chinese paths do not expose raw primary labels: unknown, original, instrumental, idle, playing, loading, review_required"
    - "Admin and Mobile keep language switching to English and back"
    - "Shared product terms use the Phase 7 glossary: 电视在线, 电视离线, 点歌控制台, 播放队列, 当前播放, 原唱, 伴唱, 在线补歌, 请求补歌, 已准备, 入库"
    - "Errors and empty states lead with Chinese product copy instead of raw backend codes"
  artifacts:
    - path: "apps/admin/src/i18n.tsx"
      provides: "Admin status, room, playback, event, language, and vocal copy helpers"
      contains: "export function playbackStateText"
    - path: "apps/mobile-controller/src/i18n.tsx"
      provides: "Mobile playback, online task, candidate metadata, and vocal copy helpers"
      contains: "export function playbackStateName"
    - path: "apps/mobile-controller/src/App.tsx"
      provides: "Chinese current playback and online supplement labels"
      contains: "playbackStateName"
    - path: "apps/admin/src/rooms/RoomStatusView.tsx"
      provides: "Productized room, TV, task, current song, and recent event labels"
      contains: "eventTypeText"
  key_links:
    - from: "apps/admin/src/i18n.tsx"
      to: "apps/admin/src/rooms/RoomStatusView.tsx"
      via: "statusText, playbackStateText, eventTypeText, vocalModeName"
      pattern: "eventTypeText"
    - from: "apps/mobile-controller/src/i18n.tsx"
      to: "apps/mobile-controller/src/App.tsx"
      via: "playbackStateName, onlineTaskStateName, candidateTypeName, reliabilityName, riskName"
      pattern: "onlineTaskStateName"
---

<objective>
Create the Phase 7 copy and status foundation for Admin and Mobile.

Purpose: Product polish needs a controlled Chinese default path before layout and visual work. This plan removes visible raw enum/debug copy from key Admin/Mobile surfaces while preserving the existing language switch behavior.
Output: localized helper functions, updated Admin/Mobile rendering, and regression tests for Chinese copy and language switching.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-productized-ui-polish/07-CONTEXT.md
@.planning/phases/07-productized-ui-polish/07-RESEARCH.md
@.planning/phases/07-productized-ui-polish/07-UI-SPEC.md
@apps/admin/src/i18n.tsx
@apps/admin/src/rooms/RoomStatusView.tsx
@apps/admin/src/songs/SongCatalogView.tsx
@apps/mobile-controller/src/i18n.tsx
@apps/mobile-controller/src/App.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing copy regression tests for Admin and Mobile</name>
  <files>apps/mobile-controller/src/test/controller.test.tsx, apps/admin/src/test/room-status.test.tsx, apps/admin/src/test/song-catalog.test.tsx, apps/admin/src/test/import-workbench.test.tsx</files>
  <read_first>
    - apps/mobile-controller/src/test/controller.test.tsx
    - apps/admin/src/test/room-status.test.tsx
    - apps/admin/src/test/song-catalog.test.tsx
    - apps/admin/src/test/import-workbench.test.tsx
    - .planning/phases/07-productized-ui-polish/07-UI-SPEC.md
  </read_first>
  <action>
    Update Mobile controller tests:
    - In the language-switch test, assert the Chinese default page has accessible labels `点歌控制台`, `当前播放`, `播放队列`, `搜索歌曲`, and `在线补歌`.
    - Add a test named `does not expose raw playback or vocal enum labels in the Chinese controller`.
    - In that test, render the default `roomSnapshot()` and assert `screen.getByText("播放中")`, `screen.getByText("伴唱")`, and `screen.getByText("当前模式")` are present.
    - In that test, assert the app shell text does not contain these exact primary raw labels: `unknown`, `original`, `instrumental`, `playing`.
    - Update online supplement tests to expect localized metadata labels: `MV`, `高可靠`, `普通风险`, and `已发现` instead of raw `mv`, `high`, `normal`, `discovered`.

    Update Admin room status tests:
    - Assert room summary renders `启用中` for active room status.
    - Assert current song renders `伴唱` instead of `instrumental`.
    - Assert recent event row has product label `播放失败` while raw `player.failed` can remain visible as secondary diagnostics.
    - Assert online task counts render localized statuses such as `已准备 1 / 失败 1`.

    Update Admin song catalog tests:
    - Assert song rows render `国语` and `已准备` instead of raw `mandarin` and `ready`.
    - Assert asset summaries render `原唱` or `伴唱` for visible vocal mode values.

    Update Admin import workbench tests only where existing visible assertions expect raw statuses; keep diagnostics such as file paths and codec strings unchanged.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/mobile-controller test -- controller</automated>
    <automated>pnpm -F @home-ktv/admin test -- room-status song-catalog import-workbench</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile-controller/src/test/controller.test.tsx` contains `does not expose raw playback or vocal enum labels`.
    - `apps/mobile-controller/src/test/controller.test.tsx` contains `高可靠`.
    - `apps/admin/src/test/room-status.test.tsx` contains `播放失败`.
    - `apps/admin/src/test/song-catalog.test.tsx` contains `国语`.
    - `pnpm -F @home-ktv/mobile-controller test -- controller` fails before implementation because raw labels are still visible.
  </acceptance_criteria>
  <done>Regression tests define the Chinese-first copy contract for key Admin and Mobile flows.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add controlled i18n helpers and glossary entries</name>
  <files>apps/admin/src/i18n.tsx, apps/mobile-controller/src/i18n.tsx</files>
  <read_first>
    - apps/admin/src/i18n.tsx
    - apps/mobile-controller/src/i18n.tsx
    - .planning/phases/07-productized-ui-polish/07-CONTEXT.md
    - .planning/phases/07-productized-ui-polish/07-UI-SPEC.md
  </read_first>
  <action>
    In `apps/admin/src/i18n.tsx`, add dictionary keys for both `en` and `zh`:
    - `roomState.active`, `roomState.inactive`, `roomState.maintenance`
    - `playbackState.idle`, `playbackState.preparing`, `playbackState.loading`, `playbackState.playing`, `playbackState.paused`, `playbackState.recovering`, `playbackState.error`, `playbackState.conflict`, `playbackState.unknown`
    - `event.player.failed`, `event.player.ended`, `event.player.loading`, `event.player.playing`, `event.controller.add_queue_entry`, `event.controller.delete_queue_entry`, `event.controller.promote_queue_entry`, `event.controller.skip_current`, `event.system.notice`, `event.unknown`
    - `task.discovered`, `task.selected`, `task.review_required`, `task.fetching`, `task.fetched`, `task.ready`, `task.failed`, `task.stale`, `task.promoted`, `task.purged`, `task.total`

    Use these exact Chinese values where applicable:
    - `roomState.active`: `启用中`
    - `playbackState.playing`: `播放中`
    - `playbackState.idle`: `待点歌`
    - `playbackState.loading`: `加载中`
    - `playbackState.recovering`: `恢复中`
    - `playbackState.unknown`: `未知`
    - `event.player.failed`: `播放失败`
    - `task.discovered`: `已发现`
    - `task.selected`: `已选择`
    - `task.review_required`: `需复核`
    - `task.fetching`: `获取中`
    - `task.fetched`: `已获取`
    - `task.ready`: `已准备`
    - `task.failed`: `失败`
    - `task.stale`: `过期`
    - `task.promoted`: `已入库`
    - `task.purged`: `已清理`
    - `task.total`: `总计`

    Export these helpers:
    - `export function roomStateText(status: string, t: I18nContextValue["t"]): string`
    - `export function playbackStateText(status: string, t: I18nContextValue["t"]): string`
    - `export function eventTypeText(eventType: string, t: I18nContextValue["t"]): string`
    - `export function taskStateText(status: string, t: I18nContextValue["t"]): string`

    In `apps/mobile-controller/src/i18n.tsx`, update Chinese aria dictionary values:
    - `current.aria`: `当前播放`
    - `queue.aria`: `播放队列`
    - `search.aria`: `搜索歌曲`
    - `online.aria`: `在线补歌`
    - `vocal.unknown`: `未知`

    Add Mobile dictionary keys for both `en` and `zh`:
    - `playbackState.idle`, `playbackState.preparing`, `playbackState.loading`, `playbackState.playing`, `playbackState.paused`, `playbackState.recovering`, `playbackState.error`, `playbackState.conflict`, `playbackState.unknown`
    - `onlineTask.discovered`, `onlineTask.selected`, `onlineTask.review_required`, `onlineTask.fetching`, `onlineTask.fetched`, `onlineTask.ready`, `onlineTask.failed`, `onlineTask.stale`, `onlineTask.promoted`, `onlineTask.purged`
    - `candidateType.mv`, `candidateType.karaoke`, `candidateType.audio`, `candidateType.unknown`
    - `reliability.high`, `reliability.medium`, `reliability.low`, `reliability.unknown`
    - `risk.normal`, `risk.review`, `risk.high`, `risk.unknown`

    Use these exact Chinese values where applicable:
    - `playbackState.playing`: `播放中`
    - `onlineTask.discovered`: `已发现`
    - `onlineTask.ready`: `已准备`
    - `candidateType.mv`: `MV`
    - `candidateType.karaoke`: `KTV`
    - `reliability.high`: `高可靠`
    - `reliability.medium`: `中等可靠`
    - `risk.normal`: `普通风险`
    - `risk.review`: `需复核`

    Export these helpers:
    - `export function playbackStateName(state: string | null | undefined, t: I18nContextValue["t"]): string`
    - `export function onlineTaskStateName(state: string | null | undefined, t: I18nContextValue["t"]): string`
    - `export function candidateTypeName(type: string | null | undefined, t: I18nContextValue["t"]): string`
    - `export function reliabilityName(value: string | null | undefined, t: I18nContextValue["t"]): string`
    - `export function riskName(value: string | null | undefined, t: I18nContextValue["t"]): string`
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/admin typecheck</automated>
    <automated>pnpm -F @home-ktv/mobile-controller typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/admin/src/i18n.tsx` contains `export function playbackStateText`.
    - `apps/admin/src/i18n.tsx` contains `export function eventTypeText`.
    - `apps/admin/src/i18n.tsx` contains `"event.player.failed": "播放失败"`.
    - `apps/mobile-controller/src/i18n.tsx` contains `export function playbackStateName`.
    - `apps/mobile-controller/src/i18n.tsx` contains `"current.aria": "当前播放"`.
    - `apps/mobile-controller/src/i18n.tsx` contains `"vocal.unknown": "未知"`.
    - `pnpm -F @home-ktv/admin typecheck` exits 0.
    - `pnpm -F @home-ktv/mobile-controller typecheck` exits 0.
  </acceptance_criteria>
  <done>Admin and Mobile have controlled copy helpers for Phase 7 visible states.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace raw visible labels in Admin and Mobile</name>
  <files>apps/mobile-controller/src/App.tsx, apps/admin/src/rooms/RoomStatusView.tsx, apps/admin/src/songs/SongCatalogView.tsx, apps/admin/src/songs/AssetPairEditor.tsx, apps/admin/src/songs/SongDetailEditor.tsx, apps/admin/src/imports/ImportWorkbench.tsx</files>
  <read_first>
    - apps/mobile-controller/src/App.tsx
    - apps/mobile-controller/src/i18n.tsx
    - apps/admin/src/rooms/RoomStatusView.tsx
    - apps/admin/src/songs/SongCatalogView.tsx
    - apps/admin/src/songs/AssetPairEditor.tsx
    - apps/admin/src/songs/SongDetailEditor.tsx
    - apps/admin/src/imports/ImportWorkbench.tsx
    - apps/admin/src/i18n.tsx
  </read_first>
  <action>
    Update `apps/mobile-controller/src/App.tsx`:
    - Import `playbackStateName`, `onlineTaskStateName`, `candidateTypeName`, `reliabilityName`, and `riskName`.
    - Replace `snapshot?.state ?? t("current.connecting")` with `playbackStateName(snapshot?.state, t)`.
    - Replace online candidate metadata rendering of `candidate.candidateType`, `candidate.reliabilityLabel`, `candidate.riskLabel`, and `candidate.taskState` with the new helper outputs.
    - Keep provider/source label visible as source diagnostics.

    Update `apps/admin/src/rooms/RoomStatusView.tsx`:
    - Import `eventTypeText`, `playbackStateText`, `roomStateText`, `taskStateText`, and `vocalModeName`.
    - Render room status via `roomStateText(roomStatus.room.status, t)`.
    - Render current song vocal mode via `vocalModeName(roomStatus.current.vocalMode, t)`.
    - Use `taskStateText()` inside `formatTaskCounts()`.
    - In recent events, render `<strong>{eventTypeText(event.eventType, t)}</strong>` and include raw event type as secondary text with queue/date details.

    Update `apps/admin/src/songs/SongCatalogView.tsx` and related song/asset detail components:
    - Use `languageName(song.language, t)` instead of `song.language`.
    - Use `statusText(song.status, t)` instead of raw `song.status`.
    - Use `vocalModeName(asset.vocalMode, t)` for visible asset vocal mode.
    - Keep asset IDs, switch family, switch quality, and technical fields visible as diagnostics.

    Update `apps/admin/src/imports/ImportWorkbench.tsx` only where raw candidate status or probe status is a primary label. Preserve file probe details as secondary compact metadata.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/mobile-controller test -- controller</automated>
    <automated>pnpm -F @home-ktv/admin test -- room-status song-catalog import-workbench</automated>
    <automated>pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile-controller/src/App.tsx` contains `playbackStateName(snapshot?.state, t)`.
    - `apps/mobile-controller/src/App.tsx` contains `onlineTaskStateName(candidate.taskState, t)`.
    - `apps/admin/src/rooms/RoomStatusView.tsx` contains `eventTypeText(event.eventType, t)`.
    - `apps/admin/src/rooms/RoomStatusView.tsx` contains `roomStateText(roomStatus.room.status, t)`.
    - `apps/admin/src/songs/SongCatalogView.tsx` contains `languageName(song.language, t)`.
    - `apps/admin/src/songs/SongCatalogView.tsx` contains `statusText(song.status, t)`.
    - `pnpm -F @home-ktv/mobile-controller test -- controller` exits 0.
    - `pnpm -F @home-ktv/admin test -- room-status song-catalog import-workbench` exits 0.
    - `pnpm typecheck` exits 0.
  </acceptance_criteria>
  <done>Admin and Mobile visible labels use controlled Chinese-first product copy.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `pnpm -F @home-ktv/mobile-controller test -- controller`
- [ ] `pnpm -F @home-ktv/admin test -- room-status song-catalog import-workbench`
- [ ] `pnpm -F @home-ktv/mobile-controller typecheck`
- [ ] `pnpm -F @home-ktv/admin typecheck`
- [ ] `pnpm typecheck`
</verification>

<success_criteria>
- PROD-01: Admin and Mobile default Chinese paths are complete and language switch behavior remains.
- PROD-02: Key visible empty/loading/error/status labels use controlled product copy.
- PROD-04: Admin room/catalog/import surfaces no longer lead with raw enum labels where a product label exists.
</success_criteria>

<output>
After completion, create `.planning/phases/07-productized-ui-polish/07-01-SUMMARY.md`.
</output>
