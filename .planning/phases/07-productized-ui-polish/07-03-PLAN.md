---
phase: 07-productized-ui-polish
plan: 03
type: execute
wave: 3
depends_on: ["07-02"]
files_modified:
  - scripts/ui-visual-check.mjs
  - package.json
  - .planning/phases/07-productized-ui-polish/07-UAT.md
  - apps/mobile-controller/src/test/controller.test.tsx
  - apps/admin/src/test/room-status.test.tsx
  - apps/admin/src/test/import-workbench.test.tsx
  - apps/admin/src/test/song-catalog.test.tsx
  - apps/tv-player/src/test/tv-screen-states.test.tsx
autonomous: true
requirements:
  - PROD-01
  - PROD-02
  - PROD-03
  - PROD-04
  - PROD-05
must_haves:
  truths:
    - "UI regression tests cover Chinese copy, language switching, button clickability, action states, and empty/error states"
    - "Visual screenshot script captures Mobile and Admin viewports without adding new dependencies"
    - "TV Phase 6 regression and visual checks remain part of final verification"
    - "Phase 7 UAT tells the user exactly what to open, click, and verify"
  artifacts:
    - path: "scripts/ui-visual-check.mjs"
      provides: "Chrome screenshot helper for Admin and Mobile UI"
      contains: "mobile-controller-390x844.png"
    - path: "package.json"
      provides: "root UI visual check command"
      contains: "ui:visual-check"
    - path: ".planning/phases/07-productized-ui-polish/07-UAT.md"
      provides: "Chinese manual validation checklist"
      contains: "默认中文"
    - path: "apps/mobile-controller/src/test/controller.test.tsx"
      provides: "Mobile Chinese product flow regression coverage"
      contains: "请求补歌"
  key_links:
    - from: "package.json"
      to: "scripts/ui-visual-check.mjs"
      via: "pnpm ui:visual-check runs Chrome screenshots"
      pattern: "ui:visual-check"
    - from: ".planning/phases/07-productized-ui-polish/07-UAT.md"
      to: "scripts/dev-local.sh"
      via: "UAT uses existing local dev launcher and URLs"
      pattern: "pnpm dev:local restart"
---

<objective>
Close Phase 7 with UI regression coverage, a lightweight Admin/Mobile visual check, and Chinese manual UAT instructions.

Purpose: Productized UI polish is not complete until regressions are covered and the user has clear steps to verify the visible behavior that previously caused confusion.
Output: visual screenshot helper, root script entry, final test coverage, and Phase 7 UAT document.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-productized-ui-polish/07-CONTEXT.md
@.planning/phases/07-productized-ui-polish/07-RESEARCH.md
@.planning/phases/07-productized-ui-polish/07-UI-SPEC.md
@.planning/phases/07-productized-ui-polish/07-02-SUMMARY.md
@scripts/tv-visual-check.mjs
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fill remaining UI regression gaps</name>
  <files>apps/mobile-controller/src/test/controller.test.tsx, apps/admin/src/test/room-status.test.tsx, apps/admin/src/test/import-workbench.test.tsx, apps/admin/src/test/song-catalog.test.tsx, apps/tv-player/src/test/tv-screen-states.test.tsx</files>
  <read_first>
    - apps/mobile-controller/src/test/controller.test.tsx
    - apps/admin/src/test/room-status.test.tsx
    - apps/admin/src/test/import-workbench.test.tsx
    - apps/admin/src/test/song-catalog.test.tsx
    - apps/tv-player/src/test/tv-screen-states.test.tsx
    - .planning/phases/07-productized-ui-polish/07-UI-SPEC.md
  </read_first>
  <action>
    Review tests changed in Plans 07-01 and 07-02, then add any missing assertions needed to satisfy the UI-SPEC verification contract:
    - Admin defaults to Chinese and can switch to English and back.
    - Mobile defaults to Chinese and can switch to English and back.
    - Mobile current playback does not expose raw `unknown`, `idle`, `playing`, `original`, or `instrumental` in Chinese mode.
    - Mobile online supplement empty, candidate, pending, and ready states use localized/productized copy.
    - Mobile key controls are clickable/enabled when allowed: `点歌`, `加点`, `确认加点`, `顶歌`, `删除`, `撤销`, `切歌`, `切到原唱`, `切到伴唱`, `请求补歌`.
    - Admin import scan, catalog actions, room refresh, pairing token refresh, retry, clean, and promote buttons remain clickable and show disabled/busy or updated state.
    - Admin Rooms task promote/retry/clean results become visible without browser reload.
    - TV existing Phase 6 Chinese state tests continue to assert `扫码点歌`, `点击电视开始播放`, `播放失败，已跳到下一首`, and `已有电视端在线`.

    Avoid adding brittle pixel assertions in unit tests. Use visible text, roles, enabled/disabled state, request records, and DOM absence/presence.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/mobile-controller test</automated>
    <automated>pnpm -F @home-ktv/admin test</automated>
    <automated>pnpm -F @home-ktv/tv-player test</automated>
  </verify>
  <acceptance_criteria>
    - `apps/mobile-controller/src/test/controller.test.tsx` contains `请求补歌`.
    - `apps/mobile-controller/src/test/controller.test.tsx` contains `提交中`.
    - `apps/admin/src/test/room-status.test.tsx` contains `刷新配对 token`.
    - `apps/admin/src/test/import-workbench.test.tsx` contains `扫描导入目录`.
    - `apps/admin/src/test/song-catalog.test.tsx` contains `校验 song.json`.
    - `apps/tv-player/src/test/tv-screen-states.test.tsx` contains `扫码点歌`.
    - `pnpm -F @home-ktv/mobile-controller test` exits 0.
    - `pnpm -F @home-ktv/admin test` exits 0.
    - `pnpm -F @home-ktv/tv-player test` exits 0.
  </acceptance_criteria>
  <done>UI regression tests cover the Phase 7 copy, interaction, and state requirements.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Admin and Mobile visual screenshot helper</name>
  <files>scripts/ui-visual-check.mjs, package.json</files>
  <read_first>
    - scripts/tv-visual-check.mjs
    - package.json
    - scripts/dev-local.sh
    - .planning/phases/07-productized-ui-polish/07-UI-SPEC.md
  </read_first>
  <action>
    Create `scripts/ui-visual-check.mjs` as a Node ESM script modeled after `scripts/tv-visual-check.mjs`.

    Requirements:
    - Reads `ADMIN_VISUAL_URL` from env, default `http://127.0.0.1:5174/`.
    - Reads `MOBILE_VISUAL_URL` from env, default `http://127.0.0.1:5176/controller?room=living-room`.
    - Uses Chrome path from `CHROME_BIN` or `/Applications/Google Chrome.app/Contents/MacOS/Google Chrome`.
    - Creates `logs/visual` recursively.
    - Captures these four screenshots:
      - `logs/visual/mobile-controller-390x844.png` with `--window-size=390,844`
      - `logs/visual/mobile-controller-375x667.png` with `--window-size=375,667`
      - `logs/visual/admin-1440x900.png` with `--window-size=1440,900`
      - `logs/visual/admin-768x900.png` with `--window-size=768,900`
    - Uses `child_process.spawn` with argument arrays, not shell string concatenation.
    - Uses a temporary Chrome user data dir under `logs/visual/.chrome-ui-check-*` and removes it after each capture when possible.
    - Exits non-zero if Chrome is missing, Chrome exits non-zero, or any screenshot file is missing/zero bytes.
    - Supports `--help` and prints `ADMIN_VISUAL_URL`, `MOBILE_VISUAL_URL`, `CHROME_BIN`, and the screenshot file names.

    Add root script:
    - `"ui:visual-check": "node scripts/ui-visual-check.mjs"`

    Do not add Playwright or any package dependency.
  </action>
  <verify>
    <automated>node scripts/ui-visual-check.mjs --help</automated>
    <manual>With `pnpm dev:local restart` running, run `pnpm ui:visual-check` and verify the four PNG files are created under `logs/visual/`.</manual>
  </verify>
  <acceptance_criteria>
    - `scripts/ui-visual-check.mjs` contains `mobile-controller-390x844.png`.
    - `scripts/ui-visual-check.mjs` contains `mobile-controller-375x667.png`.
    - `scripts/ui-visual-check.mjs` contains `admin-1440x900.png`.
    - `scripts/ui-visual-check.mjs` contains `admin-768x900.png`.
    - `scripts/ui-visual-check.mjs` contains `spawn(`.
    - `package.json` contains `"ui:visual-check": "node scripts/ui-visual-check.mjs"`.
    - `node scripts/ui-visual-check.mjs --help` exits 0 and prints `MOBILE_VISUAL_URL`.
  </acceptance_criteria>
  <done>Admin/Mobile visual screenshot validation exists without adding dependencies.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Chinese Phase 7 UAT and run final gates</name>
  <files>.planning/phases/07-productized-ui-polish/07-UAT.md, package.json</files>
  <read_first>
    - .planning/phases/07-productized-ui-polish/07-CONTEXT.md
    - .planning/phases/07-productized-ui-polish/07-UI-SPEC.md
    - scripts/dev-local.sh
    - package.json
  </read_first>
  <action>
    Create `.planning/phases/07-productized-ui-polish/07-UAT.md` in Chinese.

    Include these exact sections:
    - `## 1. 启动`
    - `## 2. 默认中文`
    - `## 3. 手机点歌控制台`
    - `## 4. 后台控制台`
    - `## 5. TV 页面`
    - `## 6. 视觉截图检查`
    - `## 7. 通过标准`

    Include startup command:
    `DATABASE_URL=postgres://ktv:ktv@127.0.0.1:5432/home_ktv MEDIA_ROOT=/Users/shaolongfei/OtherProjects/home-ktv-system/home-ktv-media PUBLIC_BASE_URL=http://10.91.130.150:4000 pnpm dev:local restart`

    Include verification URLs from dev-local output pattern:
    - Admin: `http://10.91.130.150:5174/`
    - Mobile: `http://10.91.130.150:5176/controller?room=living-room`
    - TV: `http://10.91.130.150:5173/?apiBaseUrl=http://10.91.130.150:4000&roomSlug=living-room&deviceName=Living%20Room%20TV`

    Include concrete user checks:
    - Admin/Mobile/TV default Chinese.
    - Language switch works on Admin and Mobile.
    - Mobile shows `电视在线` when TV is online before any song is queued.
    - Mobile can search, 点歌, 顶歌, 删除, 撤销, 切歌, and request online supplement if a candidate is available.
    - Mobile buttons stay centered on phone width.
    - Admin Rooms can refresh room state, refresh pairing token, and promote/retry/clean online tasks without manual browser reload.
    - Admin Imports scan button shows scanning/busy feedback.
    - Admin Songs page shows localized status/language/vocal labels.
    - TV still shows Phase 6 first-play prompt and playback time behavior.
    - Run `pnpm ui:visual-check` and `pnpm tv:visual-check`.

    Run final gates:
    - `pnpm -F @home-ktv/mobile-controller test`
    - `pnpm -F @home-ktv/admin test`
    - `pnpm -F @home-ktv/tv-player test`
    - `pnpm typecheck`
    - `node scripts/ui-visual-check.mjs --help`
    - `node scripts/tv-visual-check.mjs --help`
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/mobile-controller test</automated>
    <automated>pnpm -F @home-ktv/admin test</automated>
    <automated>pnpm -F @home-ktv/tv-player test</automated>
    <automated>pnpm typecheck</automated>
    <automated>node scripts/ui-visual-check.mjs --help</automated>
    <automated>node scripts/tv-visual-check.mjs --help</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/07-productized-ui-polish/07-UAT.md` contains `## 2. 默认中文`.
    - `.planning/phases/07-productized-ui-polish/07-UAT.md` contains `pnpm ui:visual-check`.
    - `.planning/phases/07-productized-ui-polish/07-UAT.md` contains `pnpm tv:visual-check`.
    - `.planning/phases/07-productized-ui-polish/07-UAT.md` contains `电视在线`.
    - `pnpm -F @home-ktv/mobile-controller test` exits 0.
    - `pnpm -F @home-ktv/admin test` exits 0.
    - `pnpm -F @home-ktv/tv-player test` exits 0.
    - `pnpm typecheck` exits 0.
    - `node scripts/ui-visual-check.mjs --help` exits 0.
    - `node scripts/tv-visual-check.mjs --help` exits 0.
  </acceptance_criteria>
  <done>Phase 7 has automated regression coverage, visual screenshot tooling, and concrete Chinese UAT instructions.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `pnpm -F @home-ktv/mobile-controller test`
- [ ] `pnpm -F @home-ktv/admin test`
- [ ] `pnpm -F @home-ktv/tv-player test`
- [ ] `pnpm typecheck`
- [ ] `node scripts/ui-visual-check.mjs --help`
- [ ] `node scripts/tv-visual-check.mjs --help`
</verification>

<success_criteria>
- PROD-05: UI regression tests cover key Chinese copy, button clickability, state changes, and empty/error states.
- Phase 7 visual checks exist for Mobile/Admin and keep Phase 6 TV visual check in the final gate.
- User-facing UAT explains exactly how to validate Phase 7.
</success_criteria>

<output>
After completion, create `.planning/phases/07-productized-ui-polish/07-03-SUMMARY.md`.
</output>
