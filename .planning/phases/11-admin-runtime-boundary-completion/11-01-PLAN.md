---
phase: 11-admin-runtime-boundary-completion
plan: 01
type: execute
wave: 1
depends_on: ["08-code-structure-hardening"]
gap_closure: true
files_modified:
  - apps/admin/src/imports/use-import-workbench-runtime.ts
  - apps/admin/src/imports/ImportWorkbench.tsx
  - apps/admin/src/test/import-workbench-runtime.test.tsx
  - apps/admin/src/test/import-workbench.test.tsx
autonomous: true
must_haves:
  truths:
    - "Import Workbench query and mutation orchestration lives in useImportWorkbenchRuntime()"
    - "ImportWorkbench.tsx renders state and callbacks instead of owning TanStack Query orchestration"
    - "CandidateEditor visible behavior, Chinese copy, buttons, dialogs, busy states, and load-error behavior remain unchanged"
    - "Import runtime/helper tests prove the extracted boundary owns selection, cache updates, and mutation side effects"
  artifacts:
    - path: "apps/admin/src/imports/use-import-workbench-runtime.ts"
      provides: "Feature-local runtime hook for Import Workbench"
      contains: "useImportWorkbenchRuntime"
    - path: "apps/admin/src/imports/ImportWorkbench.tsx"
      provides: "Render-focused Import Workbench shell"
      contains: "useImportWorkbenchRuntime"
    - path: "apps/admin/src/test/import-workbench-runtime.test.tsx"
      provides: "Runtime boundary regression tests for Import Workbench"
      contains: "renderHook"
  key_links:
    - from: "apps/admin/src/imports/ImportWorkbench.tsx"
      to: "apps/admin/src/imports/use-import-workbench-runtime.ts"
      via: "Import Workbench uses feature-local runtime hook"
      pattern: "useImportWorkbenchRuntime"
    - from: "apps/admin/src/test/import-workbench-runtime.test.tsx"
      to: "apps/admin/src/imports/use-import-workbench-runtime.ts"
      via: "Runtime tests import the extracted hook"
      pattern: "useImportWorkbenchRuntime"
---

<objective>
Extract Import Workbench runtime orchestration into a focused app-local hook while preserving the existing Admin import review UI behavior.

Purpose: Close the Import half of the `QUAL-01` Admin runtime boundary gap by moving candidate loading, selection repair, scan/save/action mutations, cache updates, and busy state out of `ImportWorkbench.tsx`.
Output: `useImportWorkbenchRuntime()`, a thinner `ImportWorkbench.tsx`, and focused runtime tests.
</objective>

<execution_context>
@/Users/shaolongfei/.codex/get-shit-done/workflows/execute-plan.md
@/Users/shaolongfei/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-admin-runtime-boundary-completion/11-CONTEXT.md
@.planning/phases/11-admin-runtime-boundary-completion/11-RESEARCH.md
@.planning/phases/08-code-structure-hardening/08-CONTEXT.md
@apps/admin/src/imports/ImportWorkbench.tsx
@apps/admin/src/imports/CandidateEditor.tsx
@apps/admin/src/imports/types.ts
@apps/admin/src/rooms/use-room-status.ts
@apps/admin/src/api/client.ts
@apps/admin/src/test/import-workbench.test.tsx
@apps/admin/package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Import Workbench runtime tests</name>
  <files>apps/admin/src/test/import-workbench-runtime.test.tsx</files>
  <read_first>
    - apps/admin/src/imports/ImportWorkbench.tsx
    - apps/admin/src/imports/types.ts
    - apps/admin/src/test/import-workbench.test.tsx
    - apps/admin/src/rooms/use-room-status.ts
    - apps/admin/package.json
  </read_first>
  <action>
    Create `apps/admin/src/test/import-workbench-runtime.test.tsx` using Vitest, `renderHook`, `waitFor`, and a local `QueryClientProvider` wrapper.

    The tests should target the planned `useImportWorkbenchRuntime()` hook and may initially fail until Task 2 implements the hook.

    Include at least these behaviors:
    - The hook loads candidate queues for `pending`, `held`, `review_required`, and `conflict`, exposes `candidatesByStatus`, sets `queueReady` to true after the queries settle, and selects the first candidate for the active status when no candidate is selected.
    - `scanImports()` posts to `/admin/imports/scan` with `{ scope: "imports" }`, exposes `isScanning` while pending, and invalidates or refreshes candidate queues after success.
    - `saveMetadata(candidateId, input)` PATCHes `/admin/import-candidates/:candidateId`, updates the detail cache and the matching list row through runtime cache logic, and keeps the visible selected candidate available.

    Use a local fetch mock shaped like `import-workbench.test.tsx`. Keep fixtures small but include one `pending` candidate with two files so the runtime can prove selection and cache update behavior.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/admin test -- src/test/import-workbench-runtime.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `apps/admin/src/test/import-workbench-runtime.test.tsx` contains `renderHook`.
    - `apps/admin/src/test/import-workbench-runtime.test.tsx` contains `QueryClientProvider`.
    - `apps/admin/src/test/import-workbench-runtime.test.tsx` contains `useImportWorkbenchRuntime`.
    - `apps/admin/src/test/import-workbench-runtime.test.tsx` contains `/admin/imports/scan`.
    - `apps/admin/src/test/import-workbench-runtime.test.tsx` contains `/admin/import-candidates/candidate-1`.
    - `pnpm -F @home-ktv/admin test -- src/test/import-workbench-runtime.test.tsx` exits 0 after Task 2.
  </acceptance_criteria>
  <done>Import Workbench runtime boundary behavior is specified before extraction.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract useImportWorkbenchRuntime</name>
  <files>apps/admin/src/imports/use-import-workbench-runtime.ts, apps/admin/src/imports/ImportWorkbench.tsx</files>
  <read_first>
    - apps/admin/src/imports/ImportWorkbench.tsx
    - apps/admin/src/imports/CandidateEditor.tsx
    - apps/admin/src/imports/types.ts
    - apps/admin/src/rooms/use-room-status.ts
    - apps/admin/src/api/client.ts
    - apps/admin/src/test/import-workbench-runtime.test.tsx
  </read_first>
  <action>
    Create `apps/admin/src/imports/use-import-workbench-runtime.ts`.

    Export:
    - `importCandidateStatusFilters: Array<{ status: ImportCandidateStatus }>` with statuses `pending`, `held`, `review_required`, `conflict`.
    - `useImportWorkbenchRuntime()`.

    `useImportWorkbenchRuntime()` must own the runtime orchestration currently in `ImportWorkbench.tsx`:
    - `activeStatus` and `setActiveStatus`.
    - `selectedCandidateId` and `setSelectedCandidateId`.
    - Candidate queue `useQueries` for all four statuses.
    - `candidatesByStatus`.
    - `queueReady`.
    - `queueHasError`.
    - `selectedCandidate`.
    - Detail `useQuery` for the selected candidate.
    - `selectedDetail`.
    - `scanMutation` exposed as `isScanning` and `scanImports()`.
    - Save metadata mutation exposed as `saveMetadata(candidateId, input)`.
    - Hold mutation exposed as `holdCandidate(candidateId)`.
    - Approve mutation exposed as `approveCandidate(candidateId)`.
    - Reject-delete mutation exposed as `rejectDeleteCandidate(candidateId)`.
    - Resolve-conflict mutation exposed as `resolveConflict(candidateId, input)`.
    - Busy aggregation exposed as `isBusy`.
    - Cache helper equivalent to the current `cacheCandidate()` behavior.

    Refactor `ImportWorkbench.tsx` so it imports `useImportWorkbenchRuntime` and `importCandidateStatusFilters`, calls the hook once, and uses returned state/callbacks for rendering.

    Keep these visible behaviors unchanged:
    - Header title/description and scan button text.
    - Status filter labels/counts and active status behavior.
    - Queue loading/error/empty states.
    - Candidate selection behavior.
    - `CandidateEditor` props and callback behavior.
    - Busy/disabled behavior for editor actions.

    If you discover a small stale-selection/cache bug during extraction, fix it only if a test covers it and document it in the eventual summary as a small bug fix.
  </action>
  <verify>
    <automated>pnpm -F @home-ktv/admin test -- src/test/import-workbench-runtime.test.tsx</automated>
    <automated>pnpm -F @home-ktv/admin test -- src/test/import-workbench.test.tsx</automated>
    <automated>pnpm -F @home-ktv/admin typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `apps/admin/src/imports/use-import-workbench-runtime.ts` contains `export function useImportWorkbenchRuntime`.
    - `apps/admin/src/imports/use-import-workbench-runtime.ts` contains `importCandidateStatusFilters`.
    - `apps/admin/src/imports/use-import-workbench-runtime.ts` contains `useQueries`.
    - `apps/admin/src/imports/use-import-workbench-runtime.ts` contains `useMutation`.
    - `apps/admin/src/imports/use-import-workbench-runtime.ts` contains `cacheCandidate`.
    - `apps/admin/src/imports/ImportWorkbench.tsx` contains `useImportWorkbenchRuntime`.
    - `apps/admin/src/imports/ImportWorkbench.tsx` does not contain `useMutation(`.
    - `apps/admin/src/imports/ImportWorkbench.tsx` does not contain `useQueries(`.
    - `apps/admin/src/imports/ImportWorkbench.tsx` does not contain `fetchAdmin<`.
    - `pnpm -F @home-ktv/admin test -- src/test/import-workbench-runtime.test.tsx` exits 0.
    - `pnpm -F @home-ktv/admin test -- src/test/import-workbench.test.tsx` exits 0.
    - `pnpm -F @home-ktv/admin typecheck` exits 0.
  </acceptance_criteria>
  <done>Import Workbench is render-focused and import runtime orchestration lives behind a feature-local hook.</done>
</task>

</tasks>

<verification>
Before declaring this plan complete:
- [ ] `pnpm -F @home-ktv/admin test -- src/test/import-workbench-runtime.test.tsx`
- [ ] `pnpm -F @home-ktv/admin test -- src/test/import-workbench.test.tsx`
- [ ] `pnpm -F @home-ktv/admin typecheck`
</verification>

<success_criteria>
- Import Workbench candidate loading, selection, detail loading, scan, metadata save, hold, approve, reject-delete, resolve-conflict, cache updates, and busy state are behind `useImportWorkbenchRuntime()`.
- Existing Import Workbench UI behavior remains unchanged.
- Focused runtime tests prove the new boundary.
</success_criteria>

<output>
After completion, create `.planning/phases/11-admin-runtime-boundary-completion/11-01-SUMMARY.md`.
</output>

